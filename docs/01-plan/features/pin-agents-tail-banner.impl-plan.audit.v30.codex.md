AUDIT-pin-agents-tail-banner-impl-plan-v30-BEGIN
## Summary
The plan remains blocked: the prescribed matcher code does not execute as claimed, and the v29 wiring correction leaves one invalid WIRE-PIN plus another wire whose RED cannot isolate the connection. Cross-document matcher instructions and several reported counts also contradict the implementation contract, so dispatch could either halt at RED or recreate the wrong-pane defect.

## Must-fix
- The `_agent_tail_re` shell block is not executable as the measured classifier — its codex/agy arms use `printf '%s\\\\n'` and doubled `\\\\(` / `\\\\)` escapes inside single-quoted shell strings. Executing the block verbatim makes `printf` append the literal bytes `\\n`, and `grep -E` rejects the resulting positive-control regex with `repetition-operator operand invalid` (rc 2); use shell-source escapes (`%s\\n`, `\\(`, `\\)`) and re-anchor the JSON mutations to that corrected source before dispatch.
- T4's declared WIRE-PIN is backwards and its RED split is unsatisfiable — disconnecting `rival_tail_re="$(_agent_tail_re "$rival")"` makes rival prose *not* suppress the wanted pane, which is exactly what `test_tail_pass_rival_prose_does_not_suppress` expects, so that node is green with the wire removed. `test_tail_pass_rejects_rival_signature` is the caller-observable disconnect pin; AC-4.6 should be RED: PASS with `rival-re-prose-unsafe` as its reject-direction proof, changing the stated T4 and aggregate fail/pass counts.
- T3's wanted-matcher WIRE still cannot meet the plan's own caller-observable RED rule — `_agent_tail_re` and its call site are both introduced in T3, so the declared pin's task-RED failure cannot demonstrate a missing connection with the callee intact, and no separate mutation forces this matcher connection in the opposite direction. Land the callee in an earlier task (or split the task), then provide remove-connection and force-connection checks that each leave the callee intact.
- The authoritative documents still prescribe mutually exclusive rival/wanted matchers — T4's description says the shared `$rival_re` is "reused unchanged", while its code and AC-4.6 explicitly forbid that and call `_agent_tail_re`; the design Executive Summary and the spec Executive Summary/FR-1 likewise say the tail pass reuses `_agent_pv_re` while their later safety rule requires the stricter tail-only grammar. Following the earlier prose recreates the measured prose false-positive/false-negative path, so every normative surface must name `_agent_tail_re` for both checks.
- Reported corpus/task counts were not re-derived — AC-3.17 enumerates only 11 positive controls while the impl-plan, spec, design, comments, and histories claim 12/12, and AC-4.2 says "T4 has FOUR nodes" although AC-4.1/4.3/4.4/4.5/4.6 and the RED table contain five. This breaches the count-reporting invariant and leaves the classifier evidence unreproducible; enumerate the missing control or correct the count, execute the exact corpus, and regenerate all node/count surfaces after fixing AC-4.6's RED classification.

## Should-fix
- Task 6 still opens with "Every guard this feature introduces gets a mutation", the source plan says "Each guard is mutation-tested", and the design compliance section says "Every guard is mutation-tested", although the impl-plan later admits `--limit 4000`, `--json`, and the missing-file exit have no mutations. Use the already-accurate narrower claim that every enumerated mutation target is stubbed; RED-failing tests discriminate the other controls.
- The source citations are stale: the impl-plan header calls the design v1.23 and spec v1.9, while the paired files now contain v1.25 and v1.10 history entries. Update the cited audited versions so dispatch is tied to the documents whose matcher rules it actually implements.

## Nit
- AC-3.17 still calls the 24-row corpus "all seven", then labels nonterminal groups as "the last five" and "the last seven". Name the four groups by row range or shape instead of relative position so future corpus additions do not make the rationale false again.
AUDIT-pin-agents-tail-banner-impl-plan-v30-END
