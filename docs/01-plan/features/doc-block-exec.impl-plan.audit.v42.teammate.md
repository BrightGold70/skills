## Summary
I re-derived every figure v1.44 publishes at the freeze sha `06ef40f` (HEAD == the shipping commit; `4e4a00c` re-read out of the commit where the document stamps it). Both self-corrections the dispatch names are REAL and reproduce exactly: the folded-paragraph count moved 19 → 20 while the occurrence count held at 49 and the pin population is byte-identical (both lists sorted and diffed, empty), and `test_noise_floor_on_documents_that_survived_eighty_cycles` is green with `tests/test_h_mad_precheck_doc.py` at exactly `24 passed`, the eight `PLACEHOLDER` hits unchanged and the entry recording the 8 → 16 incident. The four individually-derived addends reproduce at Task 1 **25**, Task 2 **5**, Task 3 **24**, Task 4 **28**, union **81**, sum **82**, sole duplicate `empty-key-accepted-by-api`, **0** design rows in no block; the second live recurrence-ledger site reproduces (whole-file **2** lines / **3** occurrences against body **0** / **0**, the two lines being the v1.41 entry carrying two and the v1.43 entry carrying one). **Decision Q result: of every property claim this document ships, exactly ONE is unexecuted — the marker screen's published reading of 5, which the screen's own published command returns as 6.** Every other screen, control, sweep and probe I ran reproduced its published figure verbatim: the LINEPIN fold probe (flat 1 / wrapped 0 / unedited 0), the three AC-6.1 clause controls, the fence-scan disjunct split (space 2 / tab 0 in `h-mad/references/inline-protocols.md`), the `^`-branch fixtures in both positions under `bash` and `zsh`, the 2 × 3 grid, the `SKILL` screens across six shas (1 at `74e126f`, 0 at the other five), the four `.py:` screens at 49 / 49 / 0 / 0, all 39 locator readings, the 22- and 7-body AST sweeps, the module-level glob sweep at 7-in-2-files / 21-in-body, the GNU/BSD six-token sweep at 3 lines, `_SCANNED` at 160 nodes / 58 each / 37 scripts, the `30 0` closing-hash tab arm, and roughly thirty tree line pins opened one at a time.
Evidence: 26 files opened, 118 greps/commands run.

## Must-fix
- The marker screen's published reading is **5** and the screen's own published command returns **6** on the tree this revision ships — and the sixth hit is precisely the failure the sentence claims is prevented: a *reference* to the enumeration written in the bolded member form. Run verbatim at `06ef40f`, `grep -noE '\*\*\([0-9]\)\*\*' docs/01-plan/features/doc-block-exec.impl-plan.md` prints six hits — `:386` `**(1)**`, `:388` `**(2)**`, `:389` `**(3)**`, `:389` `**(4)**`, `:391` `**(5)**` (the five enumeration members) and `:757` `**(4)**`, which is a *pointer into* the list, not a member. `sed -n '757p'` reads: `  expires exactly as fast as "the spec carries X" — member **(4)** of the list above, the 5f note's`, and the very next line of the same sentence uses the corrected unbolded form for its sibling reference (`(as distinct from member (3), its clearance)`), so the repair was applied to one reference in the sentence and not the other. The reading is 6 at `4e4a00c` as well, so this is a claim v1.44 introduced about a document state that did not hold when it was written. It breaks the invariant the whole bullet rests on — decision Q (a screen's stated property is executed, never asserted) and decision H (a count carries its unit *and* its scope): this is the only screen in the bullet shipped without an `awk`/fence block or a published scope, which is exactly how the wrong reading survived, since every neighbouring screen carries both and every one of those reproduced. **Instance of the class**: *a numeric marker used as a reference to the enumeration but written in the member form*. **The rule over the class**: every `**(N)**` in this document is a member of the enumeration and nothing else; a reference is `member (N)`, unbolded. **Its residual**: the screen detects only the over-count direction — a genuine member accidentally written unbolded would drop the reading to 4 and read as a *pass* against a target of 5, so the screen must be published with its target derived as `len(list)` and not as the literal 5. **Prescription**: unbold `**(4)**` → `(4)` at `:757` (which brings the published command to 5), and publish the screen with a scope block and both readings, in the form the `.py:` and `SKILL` screens beside it already use.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `as a member by the marker screen `grep -oE '\*\*\([0-9]\)\*\*'`, which reads **5** over the`
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `expires exactly as fast as "the spec carries X" — member **(4)** of the list above, the 5f note's`

## Should-fix
- The one-sentence statement of the mutation-payload axis misclassifies two of `docsections.json`'s eight rows, and only the enumeration beside it saves them. The axis is written as whether the row's anchor file exists at HEAD; `fence-tracking-removed` and `section-no-longer-owns-its-subsections` both carry `"file": "tests/docsections.py"` in the shipped `h-mad/tests/mutation-specs/docsections.json` today (I read all four shipped rows out of it), so that file *does* exist at HEAD and the rule as worded puts them on the payload-now side — where the document then, correctly, forbids them, because Task 1 re-anchors them into the helper. The substance is right and I verified it: their current `find` values are `        if stripped.startswith("```"):` and `        elif not in_fence and re.match(rf"^#{{1,{level}}} ", line):`, both inside `_fence_aware_end` (`h-mad/tests/docsections.py:31`, the `re.match` at `:39`), both deleted by Task 1. What is wrong is only the axis sentence: the operative file is the anchor file **after the task that lands the payload**, not at HEAD, and today a 5e implementer who applies the rule rather than the list gets two refusals. The stated residual does not cover this — it names `offset-anchored-bound-runs-to-end-of-file` and `missing-heading-returns-empty-instead-of-failing`, which are the anchor-*text*-rewritten case, not the anchor-*file*-moved case.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `**The axis is not which spec a row belongs to — it is whether the row's anchor file exists at`
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `is `h-mad/scripts/h_mad_doc_block_exec.py` carries its mechanism and its `test` key`
- `2075` is the only one of AC-6.1's control integers that is not a function of the commit it is stamped at, and it carries no caveat saying so. Every other figure in that paragraph (30 / 30 / 35 / 0) is derived from tracked state or from a `.pytest_cache` set the AC names explicitly; `2075` is `len(R.glob('*/**/*.md'))` with the root clause deleted, which counts **untracked** files. Re-run at `06ef40f` it is **2076**, and the one extra is `docs/02-design/features/doc-block-exec.design.audit.v91.p1.md`, untracked and written into the tree by a concurrent agent after the shipping commit — so the 2075 was honest when taken and is unreproducible at the sha it is stamped at. The control's conclusion (the root clause is load-bearing) is unaffected and both members do contribute — I measured 27 `h-mad` + 3 `handoff` = 30 — so this is precision, not correctness. It matters because the paragraph's own discipline is that a figure names the commit it can be re-derived at, and the AC already carries the right caveat two sentences later for its other integers.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `**controlled**, 30 with the clause against 2075 without it, and both members contribute (27 `h-mad` + 3 `handoff` = 30)`
- The AC-6.4 `2748` / `2486` floors are now labelled inherited-unverified at the site, which is the right fix, and I did not re-run them either — a `--collect-only` pass over the suite was outside what this pass took. Recording it so the register does not launder into verified on a fourth round: this is now **four** consecutive rounds with no surface re-running them, and the same is true of the plan's 263/76/0 and 268/76/0 differentials and their markdown-it-py oracles. Marked `unverified` — I am not asserting the numbers are wrong, only that no leg has checked them.

## Nit
- "the eight `--allow` substrings" mixes two units. The precheck emits **8** `PLACEHOLDER` findings but they span only **6** distinct spans — `os_error: "<text>"` accounts for three of the eight (L1900, L2060, L2072). I reproduced `[H-MAD] precheck doc-block-exec.impl-plan.md PASS issues=0` passing six `--allow` values, against `FAIL issues=8` with none. By the document's own decision H the phrase should read "eight hits under six `--allow` substrings".
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `the precheck is `PRECHECK: PASS issues=0` under the eight `--allow` substrings and the file is **24 passed**`
- The sentence at `:757`–`:758` separates its subject from its verb by a 26-word parenthetical spanning the fold — the reference at the end of `:757` reaches its verb only after the bracketed aside that opens `:758` — the same subject–verb separation v1.40's nit (5) fixed one paragraph over. Splitting the aside into its own sentence would also make the bolded marker visible to the next reviser.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `**debt** (as distinct from member (3), its clearance), *was* a debt, not a description, so a rule scoped to the present tense would have let its own`
