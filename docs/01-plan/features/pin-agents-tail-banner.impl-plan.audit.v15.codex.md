## Summary
The executable node table is internally derivable as 38 nodes (27 RED failures and 11 RED passes), and the wiring gate recognizes Task 3, but the implementation plan is not yet consistent with its source plan and design. Three blocking gaps remain: stale dispatch counts, upstream live checks that can pass without exercising the new pass, and an unreconciled helper contract. Audit-side note: running the wire-pin validator refreshed only the registration timestamp in `.h-mad/wires.jsonl`; per the read-only instruction, I did not revert that repository change.

## Must-fix
- RED cardinality is stale on current, load-bearing surfaces — the authoritative test-name table re-derives to 38/27/11 and T5 has four nodes, but the impl-plan still says “all 37 nodes” in the live Task 3 discussion, “T5's three included” in the selector discussion, and “all 37” beside the per-task derivation; the source plan and paired design still state 26/37. These values govern `--expect-fail`/`--expect-pass`, so leaving contradictory carried counts violates the Counts-a-dispatch-reports invariant and can halt a correct 5d dispatch.
- The source plan and paired design still carry vacuous live-verification bodies — the plan's Success Criteria requires only that `env` resolve Codex and that a created pane be closed, while the design says pins are cleared but never re-reads the pin file that `pin-agents --clear` was supposed to mutate. An ambient file pin can therefore short-circuit `_orca_find` and satisfy those source checks with zero `terminal read` calls, despite both version histories claiming the stronger fix landed; back-propagate the impl-plan's pin-file re-read, earlier-pass blindness checks, tail-evidence marker, and cleanup re-list to both documents. This is a Mutation-verification breach, not merely stale prose.
- The paired design's exact helper contract conflicts with the implementation code it is supposed to authorize — the design prescribes a subprocess call to `hmad-dispatch run` and extraction of `.result.terminal.tail` without recording its type, while T2 requires an in-process `_cmd_run` call and an array-aware `join("\n")`, explicitly because the measured live value is a JSON array. These are load-bearing call and data-shape decisions; back-propagate the observed array output and the sanctioned in-process call into the design (and reconcile the source plan's exact-command wording) so an implementer following the cited source cannot produce the alternate code path the impl-plan rejects.

## Should-fix
- Task 5 names `test_skill_md_codex_banner_claim_qualified` and requires exact qualified wording, but its Code structure provides neither that test's body nor the replacement sentence; “pin ... with the same doc-rule test” also conflicts with the separate node named by AC-5.5. Add the exact whitespace-collapsed old/new phrases and assertion so this fourth T5 node is as executable as the other three.
- `_orca_read_dir` promises that a handle absent from `envelopes` is unreadable, but `mkdir(exist_ok=True)` reuses `tmp_path/reads` and never removes previously written handle files. Either create a fresh directory per call or clear stale `*.json` files so the helper's documented fixture semantics remain true if a test reconfigures responses in one `tmp_path`.

## Nit
- The withdrawn AC-4.2 paragraph retains the dangling sentence “this pass: no handle, no stderr marker, fall through to the OS-evidence pass,” which reads as an active criterion after the withdrawal explanation.
