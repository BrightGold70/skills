## Summary
The authoritative RED table still derives to 45 nodes (32 FAIL, 13 PASS), the embedded mutation spec parses with 37 mutations, and the current test module collects 290 nodes. The plan remains blocked by two mutation-discrimination defects and by an authoritative-design ownership gap that its version history incorrectly claims was fixed.

## Must-fix
- `entry-gated-on-n-eq-0` does not implement the mechanism its own rationale describes — the JSON inserts `[ "$n" -eq 0 ] || return 1`, which aborts `_orca_find` and prevents Pass 4 from running, while the adjacent text says the pass merely becomes unable to match for `n > 1` via the literal `__IMPOSSIBLE_MATCH__`; the mutant can therefore be killed by forbidden early return rather than by the wrong entry condition, violating Mutation verification/Test discrimination. Mutate the matcher or otherwise skip only this pass while preserving fall-through, then verify the targeted AC fails for that mechanism.
- `tail-re-unanchored-agy` is pinned to a test whose prescribed mixed fixture exercises only Codex — AC-3.17 explicitly uses an OpenAI Codex banner and Codex prose and says grammar coverage lives in T2, yet the AGY-only mutant targets `test_tail_pass_prose_mentioning_agent_does_not_resolve`; that node has no AGY observation, so the mutant survives its designated test and `MUTATION: ALL_CAUGHT` is unreachable. Retarget both per-arm grammar mutants to `test_tail_matcher_corpus_decides_prose_vs_banner` as the AC-2.12 table already claims, or explicitly add an AGY subcase without changing the one-node RED accounting.
- Design v1.29 still omits `_agent_tail_re` from both its Implementation Order and API / Interface Changes — step 1 ships only `_orca_tail_sig`, step 2 consumes the omitted matcher, and the API section still says “One new private shell function” while listing only `_orca_tail_sig`; this contradicts its Components table, the implementation plan's T2 ownership, and the v1.29 history claim that all three surfaces were updated. An implementer following the authoritative design cannot execute the stated order without inventing when the matcher lands, so add `_agent_tail_re` and its direct corpus test to step 1 and list the second private helper in the API section.

## Should-fix
- The implementation-plan header cites design v1.28 and spec v1.13, while the current paired documents end at v1.29 and v1.14 — update both provenance labels so the plan names the revisions containing the v33 corrections.
- The design's bounded-grammar paragraph still contains two overlapping continuation lists and later calls the new independent helper “the anchor” / “the agent's existing signature,” while its pin citation still points only to AC-3.17 — collapse this to one exact `_agent_tail_re` rule and distinguish AC-2.12's direct helper corpus from AC-3.17's caller-connection fixture.

## Nit
- Fix the spec's “reading ... and resolves” grammar and remove the stray closing `**` after “both directions” in impl-plan AC-4.6.
