## Summary
The plan is highly specific and most task boundaries, fixtures, and wire tests are coherent, but it is not dispatch-ready. Two mutation anchors no longer match the prescribed helper, the RED-accounting prose contradicts its own 39-row table, and the new error-envelope safety rule was not propagated to the cited design.

## Must-fix
- `jq-r-not-jq-re` and `tail-empty-guard-dropped` still anchor on `| jq -re '(.result.terminal.tail? // empty)`, but T2's current code starts the filter with `| jq -re 'if (.ok? // false) ...`; both anchors therefore match zero times after implementation — AC-6.9/AC-6.10 cannot reach `ALL_CAUGHT`/`ANCHORS_OK`, and the two extraction guards have no verified permissive mutation, violating Mutation verification and Test discrimination. Re-anchor the first mutation on the current `jq -re 'if ...` prefix and the second on the inner `else (.result.terminal.tail? // empty) end`, changing only the intended control in each mutation.
- The authoritative node table and the plan's own row-anchored command produce **39 / 11 / 28**, but the live instructions still say the selector covers “all 38 nodes,” label the aggregate count as `38 total nodes`, say the per-task regex sees “all 38,” and later claim the commands were verified at `38 / 11 / 27` — these stale counts contradict the correct 39-node table and can feed or validate the wrong 5d expectations, breaching the Counts-a-dispatch-reports invariant. Sweep every non-history count surface to 39 / 11 / 28 while retaining the correct per-task split.
- T2 now rejects exit-0 `ok:false` envelopes before extracting tail evidence, with AC-2.9 and `envelope-ok-false-accepted`, but the cited design still prescribes the older `(.result.terminal.tail? // empty) | ...` extraction and never states the `.ok` rejection; the impl-plan header also cites design v1.14 although the paired design is v1.15 — an implementer following the declared source can omit the safety gate that the implementation plan calls load-bearing. Back-propagate the error-envelope rule, exact extraction form, and test expectation to the design (and make FR-4 explicit in the spec if its generic “errors” wording is not intended to include this case), then update the source-version citation.

## Should-fix
- The `timeout-override-ignored` mechanism still says AC-2.6 is discriminated by a `< 1.5 s` window, and `time-bound-removed` says the upper bound is the only witness, although v1.16 replaced override discrimination with an `_cmd_run` argv seam and retained only a loose `< 2.5 s` secondary witness — stale mutation rationale makes AC-6.9's mechanism audit describe a test that no longer exists; rewrite both `_mechanism` strings around the seam.
- The design and plan say the live tail shape is an array, yet the prescribed jq filter accepts every non-null scalar/object via `else tostring`, allowing malformed payloads containing a banner to become identity evidence — either justify and test the intended non-array compatibility case, or reject non-arrays and add a malformed-tail test/mutation.

## Nit
None
