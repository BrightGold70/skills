## Summary
GATING pass on `docs/01-plan/features/doc-block-exec.impl-plan.md` v1.51 at freeze `3f70eb3`, written by the `-b` re-dispatch leg. **Path note first, because it changes what the orchestrator should read:** my dispatch gave me `…impl-plan.audit.v46.teammate.md`, which a concurrent leg was already writing; my early-write skeleton overwrote that leg's first copy and it has since restored its own authoritative content there. I have therefore written to `…v46.teammate-b.md` and touched nothing at the colliding path. My one overlapping finding (the sweep sha stamp) was derived independently before I saw that report; **two of my three must-fixes do not appear in it at all**. One surface only — codex exhausted until 2026-09-07 11:28 — so no two-surface clean and no exit gate is claimed or supported. The inlined copy in `PROMPT` is a pre-repair v1.51 draft and differs from the freeze at the `_SCANNED` paragraph; every reading below is taken from the tree/blobs at `3f70eb3`, not from the prompt.
Evidence: 11 files opened, 48 greps/commands run.

## Must-fix
- The v1.51 Version History entry asserts a scope for two commits that is false, and it is the load-bearing justification for the freeze-sha field. `git show --stat dfae038` names **three** files: the handoff document, `docs/learnings.md` (5 insertions) and `docs/skill-candidates.md` (4 insertions). Only `df04e8e` is confined to `docs/handoffs/`. The conclusion the sentence draws is independently true and I verified it separately — `git diff --name-only 00b961f..dfae038` over the four feature documents is empty — so the repair is to publish the measurement that was actually run (the four-document diff) instead of a whole-commit scope claim that was never taken. An unmeasured scope claim standing in for a measured diff is the exact species this document's Conventions forbid.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `` `df04e8e` and `dfae038` touched only `docs/handoffs/` ``

- The per-needle sweep reading is stamped at a sha it was not taken at, and it contradicts the stamp series four lines below it inside the same paragraph. Re-run over the body (everything above `## Version History`) with the sweep's own five needles, one blob at a time: at `dfae038` the debt word matches **24** lines and the union **26**; at `3f70eb3` it is **23** and **25**. The published `23 / 1 / 1 / 0 = 25` is v1.51's own tree, not `dfae038`. The same paragraph then correctly publishes "**26** on the tree v1.50 ships" — and `dfae038` **is** the tree v1.50 ships (`git show dfae038:<this file> | grep -oE '^- v1\.[0-9]+' | tail -1` → `v1.50`; the blob is identical to `00b961f`'s, which also reads 26). So the document publishes 25 and 26 for one blob. The four-sha series itself is **correct** and I am not filing it: read against each revision's own needle set, `700c599` gives 23 on the four-needle form, `b3be433` 25 on the four-needle form, `00b961f`/`dfae038` 26 and `3f70eb3` 25 — so only the per-needle sentence's sha is wrong. Its composition bins also reproduce exactly at `3f70eb3` (18 substring / 5 standalone / 2 filename = 25), which is what makes the sha the only defect.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `At `dfae038`, over the body, by line: the debt word **23**, `spec\.md:` **1**, `design\.md:` **1**, `plan\.md:` **0** —`

- The Conventions residual that states the new `§`-reference rule routes the reader to the wrong task for the single member that rule was written from. Task 3 spans `:2059`–`:2252` and contains **no** delta block and **no** `§` character at all (`grep -n '§'` over that span returns nothing; the only delta blocks in the document open at `:1810`, `:1876` and `:2794`). The retired `§Scanning` site and its re-derivation are in **Task 1**, inside `# h-mad/tests/docsections.py  (delta)` — the block opens at `:1810` and the re-derivation runs `:1826`–`:1834`. A wrong internal locator inside the sentence that rules on locations is a live member of the very class the rule closes, and it is the same failure the residual's own text says the previous round committed. Prescription: name the block (`Task 1's `h-mad/tests/docsections.py` delta`) rather than the task ordinal, since the ordinal is itself a location that moves. `instance of: an internal cross-reference published as a task ordinal rather than as a needle` — the residual's rule should be extended to its own document, not only to siblings.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `(delta self-review r15; the site and its
  re-derivation are in Task 3's delta block)`

## Should-fix
- The v1.51 entry points at the v1.50 entry as sitting "below" it. The Version History ascends, so v1.50 is the line **above** — `:3391` against `:3392`. A direction word is a location and expires exactly as the section name one paragraph over does, which is the rule this same revision added.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `The v1.50 entry below carries the same wrong figure and takes a **bracketed appended correc`

- The body says the retired cardinal itself is inside the v1.50 entry's bracketed correction; it is not. Diffing that entry between `dfae038` and `3f70eb3` shows the bracket was appended at v1.51 and carries the *reading* — "returns **4**" plus the four glob forms — but never the retired value. The retired value sits in that entry's original prose ~400 characters earlier, outside the bracket. As written the sentence sends a reader to the bracket for a value that is not there. Prescription: "in the v1.50 entry, annotated by its bracketed correction", or move the value into the bracket.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `The wrong value and the reading that
  retired it are in the v1.50 Version History entry's bracketed correction`

## Nit
- The `§`-population claim is stamped at `dfae038` and the three siblings all moved after it (`59cc2ad`, `7b182b0`, `3f70eb3`). I re-derived nine of the roughly thirteen sibling `§`-references against the design and plan blobs **at `3f70eb3`** with fence-aware heading spans (the design's `## Test Strategy` runs `:2310`–`:2986` and `### Scanning (\`extract\`)` runs `:184`–`:1132` once `#`-comment lines inside fences are excluded — a naive heading grep truncates both) and all nine hold: `§Test Strategy` last paragraph is the transport split, `§Test Strategy` carries "seven module-level seams" at `:2436`/`:2440`, `§API` carries "all seven" at `:1716`, `§Implementation Order` carries `RunResult` at `:1433`/`:1454`/`:1455`, `§Test Plan` carries the eight-row wire table and the `mechanism column` locator at `:3107`, `§Scanning` carries `AmbiguousHeading(2)` at `:540`, `§Execution` carries `LaunchFailed`, and plan `§Measurements` carries both quoted sub-names at `:2143`/`:2650`. Nothing is wrong; the stamp is simply one revision-batch behind the freeze and re-stamping it would cost one line.

---

**Not checked, named so its absence does not read as clearance.** The repository test suite (this loop still never runs it). Four of the roughly thirteen sibling `§`-references (the two remaining `§Scanning` content claims at `:1209` and `:1370`, and the two `§Execution` provenance citations' amendment clauses) — I confirmed the sections carry the subject matter but did not re-derive their specific propositions. The Version History's stamp table and its per-sha reading census. The 81 / 8 / 8 mutation-row payloads beyond name-level counting: I counted the enumerated row names per task and got 29 / 5 / 24 / 28 against the published 25 / 5 / 24 / 27, but the surplus in Tasks 1 and 4 is accounted for by the four `docsections.json` rows and by `empty-key-accepted-by-api` being named as a *contrast* rather than as a Task 4 row, so my count neither confirms nor refutes the published split and I am **not** filing it — `unverified`, and it is the one figure here a 5d implementer should re-derive from the shipped JSON. All 42 `path:line` pins in the body were range-checked against the real files (0 missing, 0 out of range) and eleven of them opened and content-checked (`test_h_mad_portable_timeout.py:40/:151/:153/:158/:211`, `test_h_mad_collect_report_docs.py:22/:403/:412`, `docsections.py:31/:53`, `h_mad_mutation_harness.py:630-642`) — all eleven correct; the other 31 are range-checked only.

**Standing limits on this leg.** I have never been scored against a labelled corpus, and I share a model family with the authoring surface, so a real codex round is owed on the same tree before anything gated here is treated as settled.
