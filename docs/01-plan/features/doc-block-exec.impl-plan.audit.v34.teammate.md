## Summary
Re-checked cycle 33's four edits adversarially against the frozen tree at `d6aa0e6`: the Task 5 structural locator **resolves correctly** (heading at `h-mad/SKILL.md:1897`, the informational `grep -c` prints 4, the awk resolver prints exactly one line, `1938`, and `sed -n '1938p'` is a bare ` ```bash ` opener; the untagged `hmad-dispatch exec codex` fence is the second of four and the gating one is the last), both `docsections.json` re-spellings carry the right four-space indentation and the untouched row's `replace` binds nothing, the provenance header's design v1.92 / spec v1.53 / plan v1.84 are all current, both collect-only baselines reproduce exactly (2748 root / 2486 from `h-mad/`, delta 262), all four row counts and every derived count check out (25+5+24+27=81; 20 exception classes; `__all__` 25→29; 23 verdict heads; 15 head fields + 11 `DETAIL_KEYS` = 26 = 7 bare + 19 quoted; 8 injections; 12 injecting tests in Task 3; 5 `real_rmtree` tests; 7-node floor tuple), and every cited `path:line` in `test_h_mad_collect_report_docs.py`, `docsections.py`, `h_mad_mutation_harness.py`, `h_mad_assemble_tdd.py` and `h_mad_collect_report.py` is exactly what the document says it is. Nothing in RED step 0 makes a WIRE-PIN pass vacuously — step 0 makes no `dbe.extract`/`select`/`substitute`/`run_block` call, only three constructor references, so all four spy records are genuinely empty at RED step 1.
Three blocking defects remain, one of them fix-introduced by the v1.32 AC-6.1 edit and measured to make that AC unpassable at GREEN.
Evidence: 16 files opened, 36 greps run.

## Must-fix
- AC-6.1's transcribed census filter selects **zero** files, so `test_exactly_one_tagged_fence_in_the_tree` can never pass at GREEN — and it is not the plan's filter "verbatim". The document pairs an **absolute** `REPO_ROOT` (`REPO_ROOT = Path(__file__).resolve().parents[2]`, this document's own Conventions definition) with `p.parts[0] in ('h-mad', 'handoff')`. Measured on this tree just now: `Path('/Users/kimhawk/orca/skills').glob('*/**/*.md')` yields paths whose `parts[0]` is `'/'`, so the filter keeps **0** files, while the plan's actual sweep — `Path('.')`, run from the repository root — keeps **30**. The sweep would then count 0 tagged openers and the AC asserts exactly 1, so it fails at Task 5 GREEN for the same reason the pre-v1.32 unrestricted sweep failed, in the opposite direction; and it still fails on 0 at RED, so the RED prediction looks right and only GREEN breaks. `'archive' in p.parts` is wrong-scoped for the same reason (it would also match any `archive` component of the absolute prefix). Prescription: rebase both part tests on the relative path — `rel = p.relative_to(REPO_ROOT)`, then `rel.parts[0] in ('h-mad', 'handoff')` and `'archive' not in rel.parts` — and drop the word "verbatim", or spell the filter as the plan does. **The class** is a predicate copied out of a sibling document whose path base changes meaning in the copy; the rule over it is that any transcribed path predicate must be re-run against this tree in the base the *consuming* file actually uses before it is written down, with the observed keep-count recorded beside it. Residual after that rule: predicates transcribed with a base that is *correct* but a glob that drifts (e.g. `*/**/*.md` never matching a top-level `README.md`) are not caught by it and stay a reading matter.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `the plan's census filters verbatim (`Path(REPO_ROOT).glob('*/**/*.md')`, keeping `p.parts[0] in ('h-mad', 'handoff')` and dropping any path with `'archive'` in `p.parts``
  quote: docs/01-plan/features/doc-block-exec.plan.md › `for p in sorted(Path('.').glob('*/**/*.md')):`
- Three back-propagation flags against the paired design are **stale — the design already carries the fix at the version this document's own provenance header pins (v1.92)** — so each one buys a wasted design cycle and casts false doubt on a clean sibling. (i) AC-6.1 says the design carries the unrestricted wording; `docs/02-design/features/doc-block-exec.design.md:1135` already reads `exactly one tagged opener across the `*.md` files of `h-mad/` and `handoff/`, excluding `archive/``, and the design's own v1.92 entry at `:1446` records the restriction. (ii) The document says the design's verdict table "still shows `BAD_INFO key=<k>` bare"; `design.md:1005` reads `| `DOCBLOCK: BAD_INFO key="<k>"` | 0 | unrecognised info-string token |` — quoted. (iii) It says the table shows `overlap: "<a>" <b>` half-quoted; `design.md:1002` reads `overlap: "<a>" "<b>"` — both halves quoted. (ii) and (iii) were satisfied by **design v1.81** ("`key=` (BAD_INFO) and both halves of `overlap:` are quoted like every non-exempt field", `design.md:1435`), i.e. eleven design revisions ago, in direct response to impl-plan v1.24's own flag. **The class** is "a note in this document asserting what a sibling document currently says, or flagging it for back-propagation"; the rule is that every such note is re-derived against the sibling **at the version the provenance header pins** in the same pass that re-pins the header, and withdrawn the moment it is satisfied — the same treatment v1.34 just gave the header itself. Residual: notes that assert *agreement* rather than divergence are in the same class and are not caught by grepping for "flagged" (see the Should-fix below, which is the fourth member).
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `**The design carries the same unrestricted wording and needs the same back-propagation** (flagged, not edited here).`
  quote: docs/02-design/features/doc-block-exec.design.md › `| `DOCBLOCK: BAD_INFO key="<k>"` | 0 | unrecognised info-string token |`
- The Conventions bullet that fixes **when** each mutation row's payload is written states a partition it presents as exact, and two of `docsections.json`'s eight rows fall outside it with no rule covering them. The bullet says `docsections.json`'s rows carry their payloads here already "because their anchors sit in `h-mad/tests/docsections.py` and `h-mad/tests/test_h_mad_collect_report_docs.py`, files that exist at HEAD" — but Task 1 re-anchors `fence-tracking-removed` and `section-no-longer-owns-its-subsections` into `scripts/h_mad_doc_block_exec.py`, which does not exist until 5d. Verified: those two names occur exactly once in the whole document (impl-plan.md:238–239), carry no `find`/`replace`, and no sentence says when they get one; their current payloads in `h-mad/tests/mutation-specs/docsections.json` (`"find": "        if stripped.startswith(\"```\"):"` and `"find": "        elif not in_fence and re.match(rf\"^#{{1,{level}}} \", line):"`) anchor in source Task 1 deletes, so carrying them forward makes the anchor miss — scored a refusal, not a kill (`h_mad_mutation_harness.py:609–623`), against a spec AC-1's last bullet requires to report `ALL_CAUGHT` over eight rows. Prescription: one clause — those two rows follow the `doc_block_exec.json` rule (mechanism and `test` key fixed now; `file`, exact-once `find` and `replace` written at 5e from the landed helper source), and the bullet's "the other two specs" sentence is qualified to `docsections.json`'s **six** rows that still anchor in files existing at HEAD.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `**in this document already**, because their anchors sit in`

## Should-fix
- The fourth member of the stale-sibling-claim class, and it also carries the perishable line pin this document says it stopped writing. The 5f note asserts the spec agrees and "nothing is owed here", citing `spec :458`. Two things are wrong at this base: the root-pinned subshell is at **`spec.md:462`**, not `:458` (`:458` is the `full_collected >= 2748 + new_module + len(tuple)` assertion line — verified with `sed -n '458p'` and `grep -n 'git rev-parse --show-toplevel'`); and the subshell's own inline comment at `:462` still reads `from the REPOSITORY ROOT: the 2747 baseline is the root count; from h-mad/ the same command collects 2485`, i.e. the pre-v1.53 numbers, while `spec.md:451` and `:458` and this document all say 2748/2486. So something **is** owed upstream and the document says the opposite. Prescription: drop the `:458` pin (cite the spec's AC-6.4 gate command by name, as the SKILL.md locator rule already requires), and replace "nothing is owed here" with a flag on the stale 2747/2485 comment.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `so the three documents now agree and nothing is`
- RED step 0 is described as changing no behaviour, but the hoisted `_gate_block` drops today's two guards. Step 0 says it "resolves the block with today's `re.findall` over `_second_surface()` and today's `"h_mad_audit_gate.py" in b` filter" — today's `_gate_bash_block` also carries `h-mad/tests/test_h_mad_collect_report_docs.py:272` (`assert gating, "Second surface must contain a bash block that runs the gate"`) and `:273` (`assert len(gating) == 1, f"expected exactly one gating bash block, got {len(gating)}"`), and the `wire-revert-extract` body step 0 is defined as reproducing has neither. Between step 0 and Task 5 GREEN the consumer therefore raises `IndexError` where it used to name the fault, and the "exactly one gating fence" guard — the very invariant the Task 5 locator calls load-bearing — is absent from the executing path. At GREEN `dbe.select`'s `BlockNotFound`/`AmbiguousBlock` restore it, so this is a transient window, not a permanent loss; but "It changes no behaviour" is not true as written. Prescription: qualify the sentence to name the two dropped asserts and say the equivalent guards arrive with `select` at GREEN (keeping the asserts in step 0 instead would break the symmetry claim, since `wire-revert-extract` must remain literally step 0's body).
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `behaviour: the four AC-6.3 recipe behaviours pass across it, and the suite is green when it lands.`
- `unverified`: whether Task 4's new `h-mad/SKILL.md` Helper-scripts entry for `h_mad_doc_block_exec.py` can itself contain a column-0 ` ```bash hmad:exec ` opener. If it documents the tag by example rather than only in its verdict/detail table, AC-6.1's count becomes 2 at Task 5 GREEN and the AC fails. The document describes the entry as prose plus a one-row-per-emittable-line table, which would be safe, but it never says the entry must not carry a tagged fence, and I could not check text that does not exist yet. Cheap close: one sentence in Task 4 forbidding a tagged opener anywhere in the registry entry, tied to AC-6.1.

## Nit
- Step 0 never says the hoisted `_gate_bash_block` lands **without** today's docstring at `test_h_mad_collect_report_docs.py:268`, yet `hand-rolled-extraction-widened`'s exact-once two-line `find` (`def _gate_bash_block() -> str:` + `    return _gate_block().text`) depends on it, and the row's own note ("the landed function carries **no docstring**, unlike today's `:267` version") places that fact 300 lines away from the step that has to produce it. One clause in step 0 would make the dependency local.
- Task 1 says `docsections.json` gains a `target_command` and that all eight rows gain a `test` key, but never says whether the spec's existing whole-file `command` (`["python3.11", "-m", "pytest", "tests/test_docsections.py", "-q"]`, verified in the file today) widens. Three of the eight killers — `docsections-syspath-setup-removed`, `docsections-local-bounder-restored` and the `test_docsections_imports_from_an_unrelated_cwd` binding — live in `tests/test_h_mad_doc_block_exec.py`, which that `command` does not collect. Scoring is unaffected (the harness uses `target_command + [test]`, `h_mad_mutation_harness.py:606-607`), so this is only the survivor-branch diagnostic at `:679`; one clause saying `command` is left as-is, and why, would close the question.
