AUDIT-pin-agents-tail-banner-impl-plan-v31-BEGIN
## Summary
The plan remains blocked by an incomplete T2/T3 split, two mutation anchors that provably match nothing, and cross-document instructions that still prescribe the prose-unsafe matcher. A restore-verification command targets the wrong file, and count/proof accounting remains stale on the source-plan surface.

## Must-fix
- T2 says `_agent_tail_re` ships before T3, but T2's code structure contains only `_orca_tail_sig` while T3's code block still defines `_agent_tail_re` inside `_orca_find` — following the tasks literally either leaves AC-2.11 without its T2 production helper or adds the helper twice, and duplicate matcher arms would also make exact mutation anchors non-unique. Move the complete top-level matcher definition and its rationale into T2 and remove that definition from T3 so T3 contains only the two call sites; otherwise the declared caller-observable wiring RED split is not implementable as written.
- `tail-re-unanchored` and `tail-re-widened-to-launch-line` still use the pre-v30 escaping in their JSON `find` values — parsing the prescribed JSON yields `printf '%s\\\\n'` and doubled parenthesis escapes, while the corrected shell block contains `printf '%s\\n'` and single escapes; each decoded `find` occurs zero times in the prescribed source. Both mutants would therefore mutate nothing, violating Mutation verification and Test discrimination; re-anchor them to the corrected shell bytes and require exactly one match.
- The source plan and paired spec still contradict the impl-plan/design matcher contract — `pin-agents-tail-banner.plan.md` prescribes a line-start wrapper around unchanged `_agent_pv_re` and says rival rejection is reused from Pass 1, while the implementation requires the bounded line-complete `_agent_tail_re` for both wanted and rival checks; spec Measured basis 3 likewise says the pass reuses `_agent_pv_re` unchanged, and FR-1 says the new helper “wraps” it although the prescribed codex/agy arms are independent literals. Following those normative surfaces recreates the measured prose false-positive/false-negative path, so all current-body instructions must name the same concrete helper and grammar.
- AC-5.4 ends with `git diff --stat SKILL.md`, but verification is run from the repository root and the mutated file is `h-mad/SKILL.md` (there is no root `SKILL.md`) — the command can report an empty diff while a failed restore remains in the real manifest. Point the post-restore check at `h-mad/SKILL.md` and re-read that same file, as required by the Mutation-verification invariant.
- Reported proof counts remain inconsistent — the source plan states “31 of 44 nodes RED, 13 green” and immediately calls the green split “12 = 11 + 1”; the actual table is 13 green, comprising 12 mutation-backed nodes plus the AC-2.8 procedure. The live mutation mechanism for `tail-re-unanchored` also still says “all seven prose probes” although the authoritative corpus has 24; re-derive and synchronize these current-body counts under the Counts invariant.

## Should-fix
- `_isolated_env` scrubs the new timeout override but not the new test-only `HMAD_STUB_ORCA_READ_DIR` — an ambient export can silently opt every legacy `terminal read` test into the per-handle branch. Pop it before applying the explicit test `env`, so the branch remains genuinely opt-in and the existing-suite regression claim is environment-independent.
- T4's description contains the broken sentence “`$rival_re` in `_orca_find` and is reused unchanged” immediately after correctly saying it is not reused by this pass — clarify that `$rival_re` remains unchanged only for Passes 1–2, while T4 uses `rival_tail_re`.

## Nit
- The design's bounded-grammar paragraph repeats and splices its continuation list (“allowing just … cwd, — a dotted-numeric version …”), obscuring the exact accepted suffixes; reduce it to one list matching the regex.
AUDIT-pin-agents-tail-banner-impl-plan-v31-END
