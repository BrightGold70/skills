## Summary
The v28 rival-matcher and cross-document fixes landed, and the 43-node 31/12 split plus 32-mutation inventory re-derive correctly. However, the revised classifier still accepts prose-shaped lines, two new matcher connections evade the wiring workflow, and the advertised mutation selector and classifier evidence counts do not reconcile.

## Must-fix
- The “line-complete” `_agent_tail_re` still accepts ordinary prose/headings as identity evidence — the prescribed EREs matched `## OpenAI Codex v0.145`, `OpenAI Codex v0.145-release-notes`, `model: gpt-5-migration-notes`, `Antigravity CLI v1.2.3-release-notes`, and `Gemini 3.1 Pro (2026 release notes)`, while the stated real-banner controls also matched. The arbitrary punctuation prefix, unrestricted non-space version/model suffixes, and numeric-parenthetical arm therefore leave the wrong-pane path from spec AC-1.4 open; extend the negative corpus to these shapes, tighten the grammar against observed real banner forms, and replay the corrected boundary against the real artifacts before dispatch.
- The new `_agent_tail_re` connections are not fully declared or wire-pinned — T3 wires `_orca_find`’s wanted matcher through `tail_re="$(_agent_tail_re "$token")"` but declares only `_orca_find → _orca_tail_sig`, and its `tail-re-unanchored` mutation changes the callee rather than disconnecting that call with the callee intact; T4 then adds `_agent_tail_re "$rival"` while remaining `new-behaviour`. The live wire-pin gate consequently reports `tasks=6 wiring=1`, so both matcher connections evade the required caller-observable RED failure-mode and bidirectional wire-scoped checks; add the wanted-matcher wire/pin to T3 and classify/name T4’s rival-matcher wire and pin, with connection-only mutations in both directions.
- The mutation selector does not cover all 43 nodes as claimed — applying `test_tail_ or test_skill_md or test_os_evidence` to the authoritative test-name table misses exactly AC-3.18, `test_agent_pv_re_comment_matches_measurement`; the same `-k test_tail_` verification step misses it too. This leaves the mutation baseline and wrong-catcher diagnostic blind to a named feature test, contrary to the plan’s own stated role for that selector; rename the node or widen every scoped selector, then re-derive the selected node set.
- The classifier evidence count is not re-derived consistently — the T3 code comment, design body, and both version histories claim 12 positive banner/status controls, but AC-3.17 explicitly lists “11 in all”; the paired spec still cites the superseded 14-negative/11-positive corpus while the plan/design cite 19 negatives. This breaches the base count-reporting invariant and makes the claimed measurement unauditable; enumerate the actual corpus once, run it, and synchronize the resulting negative and positive counts across spec, design, implementation plan, and history.

## Should-fix
- Task 6 still opens with “Every guard this feature introduces gets a mutation,” and the design’s invariant-compliance section says “Every guard is mutation-tested,” even though the plan later correctly narrows the promise to enumerated guards and explicitly names unmutated controls such as the missing-file exit, `--limit 4000`, and `--json`. Align the executive summary, T6 description, and design compliance claim with the narrower inventory; the omitted controls remain discriminated by RED-failing tests, so this is a false coverage claim rather than a missing hard gate.
- The design’s later “Extraction” subsection still says the prescribed missing-key path emits literal `null` under `jq -r`, while its earlier corrected paragraph accurately says the actual `// empty` plus `else empty` filter emits zero bytes at rc 0 and that `-e` closes the return-code hole. Scope the old probe explicitly to the simple `.result.terminal.tail` filter or remove the stale causal conclusion so the declared source gives one explanation for the load-bearing `-e` guard.

## Nit
None
