## Summary

I re-derived every figure this revision publishes at its own scope and sha and found none arithmetically wrong, confirming my predecessor: the 9/32 screen readings, 26/11/30 on `74e126f`, 21 openers over 11 tokens, 49 AC anchors, 73/88, 30/35, 411, 2/6, 24/4/5, 37/37, 72 vs 81, per-branch 5/4/2 union 11, and stamp-driver 10/2/1→13 and 10/4/3→17 all reproduce exactly. All three of the revision's self-falsifications reproduce independently: fence-body de-indentation renders `'a\nb\nc\n  d\n'` on markdown-it-py **2.2.0** (interpreter-local) and **4.2.0** (`pip install --target` into a scratchpad outside the repo), so "left as is" was wrong; the untracked probe moved `grep -r` 6→7→6 while `git grep` held 6→6→6 and `git grep -n '```bash' 4e4a00c -- '*.py'` stayed 6, and the tree is clean of it (`git status --porcelain | grep '_q82'` → nothing; the only non-`.done` untracked path is the pre-existing `docs/02-design/features/doc-block-exec.design.audit.v91.p1.md`); screen one prints nothing on `MEASURED\nTODAY\nTHIS SESSION` while the mixed-case forms all print, absence is **0** at `4e4a00c` and the positive returns **1**. `4e4a00c..06ef40f` touches only `docs/` (`git diff --name-only 4e4a00c 06ef40f -- h-mad handoff` empty; the piped form prints `docs` alone), so every tree figure stamped `4e4a00c` stands at the freeze `06ef40f`. **DECISION Q result: of the 11 property claims this revision ships, 8 are executed and reproduce, and 3 are unexecuted** — the register's "the `awk` boundary probe was re-run here", the `audited`-word sweep's triage, and the register's "the four screen-two legs". All three are Must-fixes below, and all three are the same shape: a claim *about the document's own machinery* stated in the paragraph that decides what needs re-running.
Evidence: 6 documents opened at 5 shas (`06ef40f`, `4e4a00c`, `cf3a862`, `6f0ee85`, `8909ec4`), 128 greps/awk/git commands run.

## Must-fix
- **The inherited-unverified register contradicts itself on the scanner-grammar-corpus row: it lists that row's subject as unverified and then says that row is not in the register** — a reader cannot tell whether the markdown-it-py corpus was re-run at the freeze, which is the one question the register exists to answer, and the register's own closing rule ("Anything a later reader finds still outside the closure and outside the register below is a defect in the register") makes an ambiguous entry a defect rather than a gap. The carve-out table's row is titled `Scanner grammar corpus (§Measurements)` with argument `git ls-files markdown_it markdown-it-py` and stamp `markdown-it-py 2.2.0` and `4.2.0` — the same fourteen the register names. Prescription: drop the grammar corpus from the "remaining two" clause and say only what is true of it, that the row's fifteenth case was run and its fourteen were not, which the register already states three sentences earlier.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `the markdown-it-py **14-case** grammar corpus, which needs a throwaway venv —`
  quote: docs/01-plan/features/doc-block-exec.plan.md › `The table's remaining two exempt rows are **not** in this register: the `awk` boundary`
- **"the `awk` boundary probe was re-run here" is an unexecuted property claim, and it is the sentence that keeps that row out of the register** — under DECISION Q this is the exact target: the claim's stated evidence is `awk --version` plus "the three per-branch controls below", and 120 lines further down the same document says those controls are **not** that probe. The probe's own five legs (the `\b` form printing nothing, a bare `/today/` printing both lines, `printf 'a\bb\n' | awk '/\b/'` matching, the POSIX `today` branch printing the first line only, and the `Today it was measured` case line) carry no freeze re-run anywhere in the body — the only stamps on them are `74e126f`/`6f0ee85`-era. I re-ran all five at `06ef40f` on `awk version 20200816` and every one reproduces exactly, so the conclusion stands and only the "was re-run here" provenance is unsupported. Prescription: either paste the probe's five legs re-run at the freeze, or move the row into the register beside the five OS/runtime probes; do not let a re-confirmed *stamp* stand in for a re-run *probe*.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `probe was re-run here (`awk --version` → `awk version 20200816`, with the three per-branch controls`
  quote: docs/01-plan/features/doc-block-exec.plan.md › `such: the `\b`-versus-POSIX pair, named **the `awk` boundary probe** in the carve-out table below —`
- **The register's screen-two population is short by two, which is the population-short-by-N shape for the third consecutive round** — it names four legs as carried at `6f0ee85` and unverified at the freeze, but §Measurements publishes **six** legs executed at `6f0ee85`: the four named (the `335f535` positive, the true-negative pair as one leg, the blind form, the over-reach) **plus** the multi-word-gap leg ("three importing test files" declined as `74e126f` held the spec's block and returned as `6f0ee85` holds it) **plus** the cardinal-alternation leg (`printf 'zero files\n'` returns nothing while `printf 'one file\n'` matches, stated "At `6f0ee85`"). Neither of the last two is re-run at the freeze and neither is named, so by the register's own rule they are outside the closure and outside the register — a licensed gap the register says cannot exist. **Class, and this is the third member: every enumeration in this document that fixes a cardinal over one of its own surfaces — "the four screen-two legs", "Seven members at `cf3a862`", "the **five** OS- or runtime-determined probes", "admissible are …" — is a completeness measurement under DECISION G and must be derived by walking the surface it counts, not by recall.** The prior two members are v1.94's table (published as five, was seven) and v1.95's own register (said three OS probes, table listed five). **Residual, and it is why this is a rule and not a screen:** no shape filter can count a population whose members are prose paragraphs; the obligation is on the author, and the reader's check is to walk the section and count the stamped legs against the cardinal.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `screen-two legs below — the `335f535` positive, the true-negative leg (two declined sentences, one`
  quote: docs/01-plan/features/doc-block-exec.plan.md › `- **The cardinal alternation still declines `zero`, and this half of the v1.91 residual stands.**`

## Should-fix
- **The `audited`-word sweep's admissibility list is short by one, and the sweep itself is never executed** — I ran the document's own sweep over the `06ef40f` body and it returns **8** hits, at body lines 690, 695, 697, 702, 706, 707, 1056 and **2159**. The paragraph admits exactly two categories: its own quotations of the wrong label (690–707) and the slot census's "the commit v1.93 was audited at" (1056). Line 2159 — `This plan and the paired design are audited together, each cycle on **two different surfaces**,` — is in neither, and it is also not "the label *the audited commit* standing beside a sha", so a reader running the sweep gets a hit the triage cannot classify in either direction. The sweep's stated reason for keying on the word rather than the phrase does hold when executed (word **8** versus `grep -c 'the audited commit'` **1** over the same body), but the document executes neither number and publishes neither; the stated reason for publishing nothing — that the body exists at no commit until the revision lands — argues for stamping it at the *next* freeze, not for leaving the triage unexecuted. Prescription: add a third admissible category for `audited` used as an ordinary verb about the process, and re-run the sweep at the next freeze with its hit list read.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `and the slot census's "the commit v1.93 was audited at", which is correct usage; the label *the`
- **The ALL-CAPS control is introduced as a run of "the screen" but is a run of the alternation alone** — the published fixture pipes through the bare three-branch regex, without the `/^## Version History/{exit}` stage or the `!/[0-9a-f]{7}/` stage the shipped screen carries. Running one branch-set alone is exactly right under the per-branch rule, but naming it "the screen" is the same conflation this revision spent two paragraphs separating for the `\b` probe and the six-line widening fixture. Narrowing "the screen declines all three" to "the alternation declines all three" costs one word.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `Nothing printed — the screen declines all three. That is a **stated residual, not a defect being`

## Nit
- The register's clause "The table's remaining two exempt rows are **not** in this register" is immediately qualified for one of the two by "was re-run only for its one new case", which is a partial re-run; even after the Must-fix above, a flat "not in this register" reads as a stronger claim than the clause that follows it supports. Stating each of the two rows on its own line would make both readable without parsing one sentence twice — the same reason the per-branch control block was rewritten as one fenced command per branch in this revision.
