## Summary
Impl-plan v1.47 at `700c599` re-runs cleanly on almost everything it publishes. I executed the marker screen (5 / 5 / five lines each count 1), the restated-cardinal screen (body 0/0, file 2 lines / 3 occurrences), the four `.py:` screens (49 / 49 / 0 / 0 at 20 folded paragraphs, base `1cbddb7` and shipped tree alike, populations byte-identical), both `SKILL` screens (0 / 0), the `h_mad_precheck_doc.py` wrapped/flat probe (flat 1, wrapped 0, unedited 0) with its `_CODE` pattern read out of the shipped source, the three fence/slicer sweeps (22 / 7 / 2, member-for-member), AC-6.1's four commands (30 / 30 / 35 / 0, the five `.pytest_cache` paths exactly as named, 27 + 3 = 30), AC-6.4's eight readings (all eight reproduce, including 1474 and the 2697 `sort -u` collision), the sha-stamp table (15 shas, 109 readings, identical to the published table), all 13 sibling locators at both `1cbddb7` and HEAD (9 design / 2 plan / 2 spec, with the `git rev-parse --show-toplevel` plan caveat exactly as stated), the 81-row derivation over the design matrix (81 rows, 81 distinct names, 1 `SKILL.md` mechanism, 0 missing, design moved 136/33), the 25 + 5 + 24 + 27 split (union 81, no design row unplaced), the Task 5 SKILL.md resolver (4 openers, one line printed, gating fence 4th of 4, dispatch fence 2nd of 4), and roughly twenty-five `path:line` pins into `h-mad/` and `handoff/` — every one of which I opened, and every one of which matched. The second half of the register rule was checked too and holds: no body line describes `2748`, `2486`, `2675`, `263` or `268` as `verified` in the present tense. What did not survive is a single self-referential class, swept across the whole body: eight sentences say "this revision" or "the freeze sha this revision is authored against" while naming a sha or a state belonging to v1.41, v1.44 or v1.45, and one of them is the register's own PARTLY-DISCHARGED claim. Separately, the enumeration that closes the needle-inside-scope exposure names six body-scoped instruments when there are at least seven.
Evidence: 24 files opened, 128 greps run.
Read coverage, stated rather than implied: Conventions `21`–`897` in full; Task 1 `1088`–`1130` and `1349`–`1478`; Task 4's mutation-row block `2199`–`2325`; Task 5 `2326`–`2650`; AC-6.1 and AC-6.4 in full; Verification (5f) `2865`–`2968`; the v1.47 Version History entry in full. **Not read in full**: Tasks 2 and 3, Task 4's prose outside the row block, Task 5 `2650`–`2865`, and the AC checklist outside 6.1/6.4 — tree pins from those spans were verified by batch `sed`, and the self-reference sweep below covers them, but their prose was not swept for consistency. Roughly 55–60% of the body read line by line.
This pass is GATING and codex is unavailable, so nothing downstream re-checks me. I have never been scored against a labelled corpus and I share a model family with the author, so a real codex round on this same tree is still owed before anything here is treated as settled.

## Must-fix
- Eight sentences in the body attribute an earlier revision's sha or state to "this revision", and the class reaches the register's own discharge claim. The sweep is `awk '/^## Version History/{exit}{print NR"\t"$0}' <this file> | grep -E 'this revision|freeze sha'`, run at `700c599`; each hit read, each stale one confirmed carried by `grep -cF` against `git show 1cbddb7:<this file>` and `git show 68a70d6:<this file>`. The members: **(a)** `:466`, `:773` and `:784` each write the span quoted below, calling `4e4a00c` this revision's freeze sha, while `:557`, `:644` and `:839` correctly name `1cbddb7`; **(b)** `:2911` writes "Executed at `68a70d6` in this revision" — `68a70d6` is v1.45's base, the span is absent at `68a70d6` and present at `1cbddb7`, so it is v1.45's execution presented as v1.47's, and it is the sentence that discharges half of the INHERITED-UNVERIFIED register; **(c)** `:1602` writes "Class, re-swept at `35698f9` on this revision" and "Task 1's fence-opener residual, added in this revision" — `35698f9` is v1.41's freeze and both spans are already present at `68a70d6`; **(d)** `:1158` writes "re-read in this revision at `4e4a00c`", present at `68a70d6`; **(e)** `:992` writes "no AC in this document prescribed a tab-indented fence opener before this revision", also present at `68a70d6`. The consequence at `:466` is measurable, not stylistic: it publishes `4e4a00c`'s reading as this revision's "before" and the shipped tree's as its "after", inside the sentence that invokes decision K to assert the reading was taken after this revision's edits — but at `1cbddb7`, v1.47's actual base, that screen already reads body 0/0, whole file 2 lines / 3 occurrences, identical to the "after". So the published contrast measures nothing v1.47 did. Commands: `for sha in 4e4a00c 68a70d6 7d8e797 1cbddb7; do git show $sha:<this file> > /tmp/x.md; awk '/^## Version History/{exit}{print}' /tmp/x.md | grep -oiE "list (had |has |now )?(reached|holds|numbers|contains|stands at) (one|two|three|four|five|six|seven|eight|nine|ten|[0-9]+)" | wc -l; done` → `1 / 0 / 0 / 0`; and `git show 1cbddb7:<this file> | grep -n 'freeze sha this revision is authored against'` → `426`, `719`, `730`, all three carried unchanged from v1.46. **The class**: a relative self-reference — "this revision", "the freeze sha this revision is authored against", "before this revision" — carried across a revision boundary without re-labelling. **The rule**: every such phrase is either re-derived in the revision that ships it, or rewritten to name the revision it belongs to (`v1.44's base`, `re-swept at v1.41`, `executed at v1.45`). **Residual**: the phrase has no detector that separates members from non-members; the sweep above returns both — `:387`, `:468`, `:751`, `:839`, `:2275`, `:2459`'s "Re-derived at `1cbddb7` in this revision" and `:2462` are all correct at v1.47 — so it is a triage grep a reviser reads, not a gate, and it has to be re-run and re-read every revision.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `` `4e4a00c`, the freeze sha this revision is authored against ``
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `` `1cbddb7`, the sha v1.47 is authored against ``
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `body **1** line / **1** occurrence, whole file **3** lines / **4** occurrences`
- The enumeration closing the needle-inside-scope exposure names six body-scoped screens and asserts a universal about all the others that is false: a seventh instrument at `:892` is scoped to this document and its needles are literal strings that occur in the scope it counts. The pre-dispatch sweep at `:892` counts this document, not `h-mad/`, `handoff/`, a sibling under `docs/` or a `.py` source file. Run against the body it returns **23** hits, and three of them are the exposure itself rather than members: `:892` (the sweep's own publication, which writes all four needles literally), `:2083` (a literal `design.md:` inside prose recording that the flags were withdrawn) and `:2940` (a literal `spec.md:` inside prose recording that three such pins went with them). Commands: `awk '/^## Version History/{exit}{print NR": "$0}' <this file> | grep -cE 'owed|spec\.md:|design\.md:|plan\.md:'` → `23`; `… | grep -oE '(spec|design|plan)\.md:' | sort | uniq -c` → `1 design.md:`, `1 spec.md:`. The bullet already names one confound — `owed` also matches `followed` — and does not name this one. **The class**: body-scoped instruments omitted from the exposure enumeration. **The fix**: either add `**(vii)**` with its disposition — needles are literal and three occur in the counted scope, but the sweep publishes no reading, so a corrupted count is not its failure mode; what it costs is triage a reviser is not warned about — or narrow the universal to "every other screen *that publishes a reading*". **Residual, and it is why this is filed as a class rather than as one added member**: there is no clean enumerator. `awk '/^## Version History/{exit}{print NR": "$0}' <this file> | grep -E 'impl-plan\.md|<this file>'` returns **14** body lines at `700c599` — `:380`, `:459`, `:496`, `:545`, `:552`, `:566`, `:573`, `:580`, `:589`, `:626`, `:731`, `:892`, `:1602`, `:2283` — of which `:566`, `:580` and `:626` are `git diff --name-only` provenance rather than screens, `:1602` is an AC, and `:573`/`:2283` are the one stamp screen written twice; and it **misses** the two `SKILL` screens at `:755` and `:757`, which reach this file through the `"$D"` variable set at `:731`. So the enumeration stays a hand-count and its completeness is re-established by reading, not by a grep.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › ``counts `h-mad/`, `handoff/`, a sibling under `docs/` or a `.py` source file, where this``
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › ``grep -n 'owed\|spec\.md:\|design\.md:\|plan\.md:' docs/01-plan/features/doc-block-exec.impl-plan.md``

## Should-fix
- The inherited-unverified label does not reach the site where `263/76/0` and `268/76/0` are actually used, which is the widening AC-6.4 states as a rule over the class. AC-6.4 (`:2462`) writes the rule in bold; the 5f paragraph (`:2920`) puts those four integers in the register. But the site that uses them is Task 1's wire description at `:1102`–`:1105` — "the old regex and `find_heading` agree on **263** headings, `new_only=0` … and `old_only=76`" / "agreement is **268** with the same `old_only=76`" — and that site carries no register label. It carries an equivalent narrative at `:1108`–`:1110` ("this document transcribes it and did not re-run it, so it is not re-derivable here and is left stamped at the commit it was measured on"), which is why this is a Should and not a Must: the reader is not misled, but the rule as written says the *label*, and this is the site a 5d implementer reads the figures from. `instance of:` the class "register members used outside AC-6.4 and the 5f paragraph". Sweep run over the body only, with the `awk` NR prefix kept out of the match so line numbers cannot match themselves: `2748|2486|2675` hits `:2462`, `:2472`, `:2883`, `:2885`, `:2891`, `:2892`, `:2895`, `:2903`, `:2904`, `:2905`, `:2916`, `:2917`, `:2918`, `:2920`, `:2924`, `:2933`, `:2939`, `:2956` — all inside AC-6.4 or the 5f paragraph, all covered; `\b263\b|\b268\b` hits `:1102`, `:1105`, `:2796` and `:2920`, of which `:2796`'s `268` is the line pin `h-mad/tests/test_h_mad_collect_report_docs.py:268` and not the differential. So the unlabelled sites are exactly `:1102` and `:1105`, and the class has two members, not more.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `a figure in that register carries the label at every SITE that uses it — an AC, a gate re`
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `the plan's 263/76/0 and 268/76/0 heading`
- AC-6.1's last dated control reading is attributed to this revision and belongs to v1.46; v1.47's own Version History publishes a different number for its own working tree. `:2459` writes the without-root-clause reading as "**2088** on the working tree while this revision was being written, one round later", while the v1.47 entry says "Re-run on this revision's working tree it reads **2091** … it is deliberately not added to the illustration list". Declining to extend the list is a legitimate call — the figure is declared unbounded and I measured **2092** today with `python3 -c "from pathlib import Path; R=Path('.').resolve(); print(len([p for p in R.glob('*/**/*.md') if 'archive' not in (r:=p.relative_to(R)).parts and not any(x.startswith('.') for x in r.parts)]))"`, which is the stated behaviour and not a defect — but leaving 2088 labelled "while **this** revision was being written" makes the last member of the datings list say v1.47 when it means v1.46. `instance of:` the Must-fix-1 class; the fix is to name the revision, not to add a number. The 30-side of that control is exact and re-ran clean for me at 27 `h-mad` + 3 `handoff` = 30.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `**2088** on the working tree while this revision was being written, one round later`
- Task 5 attributes a post-migration variable name to the pre-migration source and pins it one line short. `:2350` says the consumer's pre-migration `:270` extraction uses `[b for b in _bodies if "h_mad_audit_gate.py" in b]`. Opened: `sed -n '270p;271p' h-mad/tests/test_h_mad_collect_report_docs.py` gives `    blocks = re.findall(r"```bash\n(.*?)```", section, re.S)` at `:270` and `    gating = [b for b in blocks if "h_mad_audit_gate.py" in b]` at `:271` — the comprehension is at `:271` and binds `blocks`, not `_bodies`. `grep -rn '_bodies' h-mad handoff` returns **nothing**; `_bodies` is the name this document's own `wire-revert-extract`, `wire-unconditional` and `hand-rolled-extraction-widened` `replace` payloads introduce at `:2527`, `:2613` and `:2632`. The substance survives — the predicate `"h_mad_audit_gate.py" in b` really is shared by the locator and the mutants — but the backticked span is presented as the consumer's current source and is not, in a document whose anchors are load-bearing. Fix: write `[b for b in blocks if "h_mad_audit_gate.py" in b]` and cite `:271`, or drop the variable and cite the predicate alone.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › ``uses (`[b for b in _bodies if "h_mad_audit_gate.py" in b]`) and the one every `wire-revert-*` ``

## Nit
- `:2920`'s "are **only cited here**" reads two ways — "merely cited rather than derived" (the intended sense) and "cited at this location only" (false: they are used at `:1102`–`:1105`). One word — "only *cited*, never re-derived" — removes the second reading, and the ambiguity matters because this document treats absence claims as measurements.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `and remain inherited-unverified with no round having re-run them`
- `:622`–`:643` is a v1.44 measurement written in the first person ("this revision's input", "this revision ships") and disambiguated only at its end, at `:635` ("no v1.44 edit adds or removes a `.py:` pin") and `:643` ("stays a v1.44 fact"). A reader who stops before the disambiguation reads it as v1.47's. Naming v1.44 at the head of the paragraph costs one clause and removes the same hazard the Must-fix above is about.
