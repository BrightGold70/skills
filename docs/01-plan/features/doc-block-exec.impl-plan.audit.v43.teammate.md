## Summary
v1.45's five advertised repairs all reproduce on the tree at `6dcb70f`: the marker screen reads body 5 / file 5 / five markers at count 1, both mutation arms fire exactly as published (arm (a) 6/6 with count 2 on the fourth marker; arm (b) 5/5 all counts 1), the string-replacement mechanism is named at the site, the four `.py:` screens read 49/49/0/0 at 20 folded paragraphs on base `68a70d6` and on the shipped tree with a byte-identical pin population, and the AC-6.4 262 divergence executes live at 2809 root / 2547 `h-mad/` with staleness +61 on both. I re-derived every source line pin in the document (23 of 23 exact), all three residual sweeps (22 / 7 / 2, same members), the 81-row derivation split 25+5+24+27, the AC-6.1 corpus figures (30 / 30 / 35 / 0, 27+3, five named extras, symmetric difference empty), the twelve sibling locators (all still one hit at `68a70d6` and at HEAD), and the shipped `docsections.json` (4 rows, all `"file": "tests/docsections.py"`) — nothing there is wrong. One unexecuted property claim remains, and it is exactly the decision-Q shape: AC-6.4 publishes a predicate for `2675` and calls it verified, and that predicate returns 1450 at all three commits it names.
Evidence: 26 files opened, ~130 greps and commands run.

## Must-fix
- AC-6.4 publishes a predicate for its `2675` census, calls it **verified**, and the predicate returns **1450** — so a figure this revision's own Version History files under inherited-unverified is asserted as measured at the one site a 5c/5d implementer transcribes it from. I ran the published predicate against all three commits it names: `git grep -hE '^\s*def test_' <sha> -- '*test_*.py' | wc -l` gives **1450 / 1450 / 1450** at `e8eaf6f` / `b7d0d77` / `1861157`. I then tried ten readings of "counting `def test_` across every `test_*.py` in the tree" at `e8eaf6f` — anchored in `test_*.py` 1450, unanchored in `test_*.py` 2721, anchored over all `*.py` 1455, unanchored over all `*.py` 2748, `*test*.py` unanchored 2731, `h-mad`+`handoff` only 1447/2289, unique test names 2691, `async`-inclusive 1474, every tracked file 2808 — **none is 2675**, and `2675` occurs in no sibling document (`grep -c` over design/plan/spec is 0/0/0 at HEAD), so nothing external corroborates it either. **This is one member of a class and the class is the finding**: the inherited-unverified register lives only in the 5f note — `grep -oic 'inherited-unverified'` over the body is **2**, both at `:2825` and `:2836` — while AC-6.4 at `:2384`, which carries `2748`, `2486` and `2675` as the constants 5c re-measures from, carries no such label at all. **The rule over the class**: every figure named in the 5f note's inherited-unverified register must carry that label at the AC site that uses it, not only in the register; and no figure in that register may be described as `verified` anywhere. **Residual**: I could not construct the predicate that does produce 2675, so I cannot say whether the number is right — only that the predicate the document publishes for it is falsified and the word `verified` is not earned. The load-bearing half survives independently and should be kept: the **invariance** across `e8eaf6f..1861157` is real, measured flat at 1450/1450/1450 by me this pass.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `by counting `def test_` across every `test_*.py` in the tree at each commit`
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `verified at `1861157``
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `the predicate behind 2675 is not published here`

## Should-fix
- The v1.45 Version History's closure that the `docs/`-facing figures were re-run is false, and the 81-row derivation is the concrete instance. `git diff --stat 4e4a00c 68a70d6 -- docs/02-design/features/doc-block-exec.design.md` is **161 insertions / 37 deletions**, so the design's helper matrix moved across exactly the span the closure covers — yet the derivation over it still reads "re-run at `4e4a00c` in this revision rather than inherited", by the document's own rule that "a count over a moved table is not a carried count". Thirteen body sites still read `verified at `4e4a00c`` (`awk '/^## Version History/{exit}{print}' | grep -oE 'verified at .4e4a00c.' | wc -l` = 13). Nothing is factually wrong — I re-ran all twelve named sibling locators at `68a70d6` and at HEAD and every one returns exactly one hit, and I re-derived the 81 data rows / 81 distinct names / exactly one `SKILL.md`-mechanism row (`registry-row-removed`) / 0 missing from this document at `4e4a00c`, `68a70d6` and HEAD alike — so this is a stamp-and-closure defect, not a figure defect. **The rule**: a closure sentence claiming a set was re-run is itself a property claim and must be screened, not asserted — `grep -oE 'at .<old-sha>.'` over the body is the screen, and it reads 28 for `4e4a00c` against 7 for `68a70d6` on the shipped tree.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `so the `docs/`-facing figures were re-run rather than carried`
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `re-run at `4e4a00c` in this revision rather than inherited`
- AC-6.1's without-root-clause integer has now drifted a third time and the AC does not say the reading is unbounded. The AC dates it 2075 / 2076 / 2079 and explains honestly that the glob walks untracked files; run today it is **2082**. The honesty is right and I am not asking for the number to be dropped — but the sentence dates three readings without stating that the sequence is monotone in untracked `docs/` writes by concurrent agents, so a later reader gets a fourth number and no rule telling them it is expected. Prescription: state it as "a reading that rises with untracked `docs/` files and is not reproducible at any sha", with the three datings kept as illustration.

## Nit
- The `sed` mechanism sentence compresses two different failures into one causal chain. I ran it: `sed 's/**(4)** the 5f/…/'` exits **1** with `sed: 1: "s/**(4)** the 5f/(4) th ...": RE error: repetition-operator operand invalid` on stderr — it is loud, not silent — and the empty file comes from the shell truncating the `>` redirect target before `sed` ever runs. Naming the redirect as the truncating step would make the mechanism reproducible from the sentence alone.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `silently produced an empty file`
- The exception-hierarchy header writes four addends against five headed groups. The block below is headed `— 6`, `— 3`, `— 4`, `— 5`, `— 1` (I counted 19 `class …(DocBlockError)` definitions plus `DocBlockError` itself, so the arithmetic is right either way), but a reader checking the parenthetical against the enumeration finds four numbers and five lists. By the document's own decision H — a count in a tuple carries its identity — this should read `(6 + 3 + 4 + 5 + 1)`.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `DocBlockError + 19 subclasses (6 + 3 + 4 + 6)`
