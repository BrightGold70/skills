## Summary

GATING pass over impl-plan v1.49 at `b3be433`. I first proved the inlined copy is byte-identical to the working tree (`diff` of prompt lines 87–3306 against doc lines 2–3220 differs only by one trailing blank line), so every citation below is a **tree line number in `docs/01-plan/features/doc-block-exec.impl-plan.md`**, not a prompt line. I read the body line by line from `:1` to `:3169` — including every range the previous gating auditor declared unswept (Tasks 2–3 `:1869`–`:2107`, Task 4's prose outside the mutation-row block `:2110`–`:2330`, Task 5 `:2520`–`:2660`, the mutation-row block `:2659`–`:3060`, and the whole AC checklist) — plus the v1.49 Version History entry `:3220` in full; entries v1.1–v1.48 (`:3171`–`:3219`) I read only by their opening ~200 characters plus targeted greps, and absence of findings there is unmeasured. Under DECISION Q I executed every screen, control, sweep, probe and derivation the document publishes rather than reading them: the marker screen, the restated-cardinal screen, all four `.py:` screens, both `SKILL` screens, the self-reference screen, the six-token divergence sweep, the pre-dispatch sibling sweep and its 25-hit classification, the sha-stamp table (116 readings over 16 shas), the three AST sweeps (22 / 2 / 7), the fence-opener corpus scan, the wrapped/flat precheck probe, the shared precheck on both this body and `8c6539a`'s, the standing `SKILL`-pin control, the 13 sibling locators at `700c599`, the 81-row design-matrix derivation, the module-level glob census, and both `h-mad/` path-pin batches. **Every one reproduced its published reading exactly.** The three musts below are not figure errors — they are false statements about where a member sits, where a label stands, and where a sibling line lives.

Evidence: 26 files opened, 180 greps run.

## Must-fix

- The exposure enumeration's cardinal and its partition contradict each other: `:446` attributes **nine** instruments to the walk, while `:498` assigns member (vii) to a bin defined as *not* found by the walk — so the walk returned eight by the partition and nine by the sentence, and the ten cannot both be partitioned and counted as written. Measured rather than argued: the walk's own first signature is "a body line naming this file as a scope (its path written out …)" (`:441`–`:442`), and member (vii)'s sweep line writes this document's path out — `awk '/^## Version History/{exit}{print NR": "$0}' docs/01-plan/features/doc-block-exec.impl-plan.md | grep -n 'doc-block-exec\.impl-plan\.md'` returns `:1004` on the shipped tree, and the same command over `git show 700c599:<this file>` — the base the walk was run against — returns line **892**, the identical sweep line. The walk therefore *did* reach (vii) at the time it ran, so **`nine` is the correct half and the bin label is the false one**. This is the exact class the bullet it sits in is about (a partition carried forward rather than derived), it is fix-introduced by v1.49 (`git diff 8c6539a b3be433` shows the whole partition paragraph is new), and the same wording is repeated in v1.49's Version History entry at `:3220`. Prescription: relabel the fourth bin to what is true of (vii) — found by the walk *and* independently named by impl-plan audit v44 — or, if the intent is that the walk's hit list genuinely excluded it, restate `:446` as eight-plus-one-plus-one and say which signature missed it. **Close the class**: the rule is that a bin's defining predicate must be re-derived against the enumerated members with the walk's own signatures re-run, never against the previous revision's sentence; the residual is that nothing mechanical parses prose bins, which the bullet already states.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `**nine** instruments scoped to *this document* and therefore exposed at all — **ten** with the`
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `**one** was named by impl-plan audit v44 rather than found by the walk — member (vii);`

- The INHERITED-UNVERIFIED register's own placement claim is false: `:1284`–`:1285` says the four heading-differential integers (`263/76/0`, `268/76/0`) "carry the INHERITED-UNVERIFIED label here as well as at **AC-6.4** and in the §Verification (Phase 5f) paragraph", and AC-6.4 does not contain those figures at all. Measured: AC-6.4 is confined to the single line `:2658` (`:2659` starts the mutation-row node-ID list), and `sed -n '2658p' <this file> | grep -c '263'` is **0**, as is `grep -cE 'old_only|new_only|differential'` on the same line; AC-6.4's own label sentence names only `2748`, `2486` and `2675`. The 5f note contradicts `:1285` directly at `:3122`, saying those figures are "used in Task 1's wire description as well as here" — two sites, and AC-6.4 is not one of them. This is fix-introduced at v1.48 answering impl-plan audit v44's register finding (`git show 700c599:<this file> | grep -c 'INHERITED-UNVERIFIED label here as well as at AC-6.4'` is **0**; the same grep is **1** at `8c6539a` and on the shipped tree), and v1.49's delta pass did not touch it. It matters because the register rule is precisely "the label stands at every SITE that uses a member": a reader auditing that rule goes to AC-6.4, finds the figures absent, and cannot tell whether the label was dropped or the site never used them. Prescription: name the two sites the 5f note names and drop "at AC-6.4" from `:1285`. **Close the class**: every "carries the label at X" sentence in the register is a placement measurement under decision G and must be derived by grepping X for the figure, not by recalling where the label was added.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `INHERITED-UNVERIFIED label here as well as at AC-6.4 and in the §Verification (Phase 5f)`

- A **live sibling line pin survives at `:1691`**, in a spelling no screen this document ships can see, and it is stale on the shipping tree. The pin is `(design v1.79 §Scanning, line 43)` attached to the present-tense claim "the design's spelling verbatim". Measured: `git show 700c599:docs/02-design/features/doc-block-exec.design.md | sed -n '43p'` is the `sys.path.insert(0, str(Path(__file__).resolve().parents[1] / "scripts"))` line, but on the shipped tree `sed -n '43p' docs/02-design/features/doc-block-exec.design.md` prints a bare fence marker and `grep -n 'sys.path.insert(0, str(Path(__file__).resolve().parents\[1\]' docs/02-design/features/doc-block-exec.design.md` puts that content at **:64**. The content claim still holds; the *pin* does not. This document dropped three `design.md:` pins for exactly this reason and declares the class closed, and it is invisible to every instrument here: the pre-dispatch sibling sweep at `:1004` keys on `spec\.md:|design\.md:|plan\.md:`, the `.py:` shape greps key on `\.py:[0-9]+`, and the `SKILL` screens key on `SKILL\.md:[0-9]+` — none matches the `<sibling> … line N` spelling. **Close the class**: the class is *a sibling location written as prose (`line N`, `§X line N`) rather than as `<file>.md:N`*, and the rule is that a sibling location is admissible only as a locator returning exactly one hit at the shipping commit — here `` grep -n 'sys.path.insert(0, str(Path(__file__).resolve().parents[1]' docs/02-design/features/doc-block-exec.design.md `` (one hit, re-run on the shipped tree). Sweeping the body with `grep -onE 'lines? [0-9]+'` outside the Version History returns exactly this one live member, so the class is one member wide today; the residual after the fix is that the same spelling in a further synonym ("at line 43 of", "the 43rd line") stays undetected, and the sibling sweep's needle list should gain the `line [0-9]` form so the next one surfaces in triage.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `# ^ the design's spelling verbatim (design v1.79 §Scanning, line 43) and the same idiom every`

## Should-fix

- `:517`–`:519` names the wrong signature: it says the bound variable "**is** the walk's **second** signature", but `:441`–`:444` define signature one as "a body line naming this file as a scope (its path written out, the shell variable the fenced screens bind it to, or the angle-bracketed stand-in …)" and signature two as the `awk` idiom — the bound variable is signature *one*'s third form. The conclusion survives (I checked: both `SKILL` screen lines, `:858` and `:859`, write the bound variable **and** the `awk` idiom, so they are hit by both signatures), and the fence claim v1.49 corrects is right — the variable-binding fence at `:833`–`:848` opens and closes entirely above the `SKILL`-screen fence at `:857`–`:861`. Only the signature label is wrong, and it is repeated verbatim in the v1.49 Version History entry at `:3220` ("both screen lines write the bound variable, which **is** the walk's second signature"). Fix both occurrences together, or the next revision inherits the mislabel from the entry.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `**second** signature is the bound variable itself, which both screen lines write — so they are`

- Three screens publish their readings stamped to "the tree **v1.48** ships" at the site a 5d reader reads, while v1.49 shipped further body edits: the marker screen (`:394`–`:396`), the restated-cardinal screen (`:559`–`:562`) and both `SKILL` screens (`:853`–`:855`). The document's own standing requirement at `:438`–`:439` is that every screen be re-run after the **last** edit lands, and v1.49's Version History entry does publish them re-run on the shipped body — so the site and the entry disagree about which tree the published integers belong to. This is a stamping gap and not a figure defect: I ran all of them verbatim on `b3be433` and every reading holds (marker body **5** / file **5** / five per-marker lines each **1**; restated-cardinal body **0** lines / **0** occurrences, whole file **2** lines / **3** occurrences; both `SKILL` screens **0**). Re-stamp the three sites to the shipping revision, or state at each that the reading is unmoved by this revision's edits and was re-run to establish that.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `On the tree v1.48 ships, re-run **after** v1.48's own edits landed rather than`
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `this document's body, both returning **0** on the tree v1.48 ships and both re-run after`

- The Task 4 residual at `:2311`–`:2317` derives `_SCANNED`'s membership from "eight sources" and asserts `tests/` is in none of them. I verified the eight sources at `h-mad/tests/test_h_mad_portable_timeout.py:154`–`:161` and the conclusion holds — but the derivation is stamped `335f535` and `_SCANNED` is a **glob-fed** list, so a future `references/*.md` or `scripts/*.py` addition changes it without any screen here noticing, exactly as the AC-6.4 floor does. The bullet already names the class for parametrised collection counts (`:2671`); it should say the same thing about its own membership claim — that `_SCANNED` membership is re-derived at 5d rather than read from here.

- `:1004`'s pre-dispatch sweep is described as "the one mechanical aid" for the sibling-prose class, and its needle set (`owed`, `spec\.md:`, `design\.md:`, `plan\.md:`) provably misses the live member this report's third must-fix names. Widen the needle set in the same edit that repairs `:1691`, otherwise the reviser who runs the sweep next round reads a clean triage list over a document that still carries a stale sibling pin.

## Nit

- `:7` states "**This header is the one place a sibling version number may appear at all**", which reads as contradicting `:982`–`:985`, where the Conventions bullet explicitly blesses bare provenance citations of the form "(design v1.85)" and says the document carries ~40 of them deliberately. Narrow the header sentence to a sibling's *current* version number and the tension disappears.

- `:2616` says "every later `:412` in this document is that same line in that same file" — true (I checked: `re.findall` occurs at `:270` and `:412` only in `test_h_mad_collect_report_docs.py`), but the sentence reads as a claim about the impl-plan's own line 412, which is inside Task 1's AC checklist. Spelling it "every later `test_h_mad_collect_report_docs.py:412`" removes the ambiguity.

- `:1215`'s true-negative sentence packs the positive control, the decline count and the file count into one clause ("prints the 29 openers with their files and lines; *true negative* — it **declines 2**"); the split published two lines later (`:1224`, space-disjunct 2 / tab-disjunct 0) is the reading that matters and reads as a correction rather than as the control's own result.
