## Summary
Gating pass over `docs/01-plan/features/doc-block-exec.impl-plan.md` v1.52 at HEAD `09e9307`. Every tree pin I opened resolved exactly (22 residual-(a)/(b) symbol pins, the four `h_mad_mutation_harness.py` regions, `:403`/`:412`, the eight `_second_surface()` call sites, both `docsections.py` docstring spans), the 81/8/8 mutation-row counts and the 6+3+4+5+1 exception split re-derive, and the marker / restated-cardinal / `.py:` / `SKILL` screens all reproduce their published integers on the shipping tree. The failures are concentrated in exactly the class this round was about: v1.52's own repairs drove a self-screening gate from 0 to 4, drove a locator from 1 hit to 2 in the sentence claiming one, and the entry's blanket clearance of the AST censuses across the freeze is false.
Evidence: 24 files opened, 95 greps run.

## Must-fix
- The self-reference screen — member (x) of the needle-inside-scope enumeration, the one whose target is a hard `0` — returns **4** on the body v1.52 ships, and all four hits are text v1.52 itself wrote. Run verbatim at `09e9307`: `awk '/^## Version History/{exit}{print NR": "$0}' "$D" | grep -cE 'thi[s] revision|freez[e] sha'` → `4`, at lines `1159`, `1173`, `1216` and `1221`, every one inside the per-needle sweep bullet and the stamping-rule paragraph this revision rewrote. The same command reads `0` at `b3be433`, `00b961f`, `dfae038`, `3f70eb3` and `af19d53` and `4` only at `09e9307`. This is a published-green gate that is red on the shipped artifact, and the site simultaneously asserts it is "a gate rather than a triage grep, because after the relabel there are no legitimate body members left". Prescription: either relabel the four sentences the way every other self-reference in this body is relabelled (`v1.52's edits`, `the tree v1.52 ships`), or re-scope the screen and say in the same edit which construction now makes a body member legitimate — but the site cannot keep publishing `0`.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `It reads **0** on the body v1.50 ships, re-run after v1.50's last edit landed — the stamp is the`
- The enumeration of the stamp-carry class is incomplete, and the enforcement condition v1.52 invented was not applied over the class it was declared to bind. The marker screen states the four members and says the condition "binds every screen, control and sweep in this document"; the self-reference screen above is a **fifth** member, and `git show <sha>:<doc> | grep -n 'body v1\.[0-9]* ships'` returns the identical line `It reads **0** on the body v1.50 ships` at `00b961f`, `dfae038`, `3f70eb3`, `af19d53` and `09e9307` — carried through v1.51 *and* v1.52 while both shipped body edits. `instance of:` the class "a screen site whose stamp is not rewritten in the edit that re-runs it". The rule over the set, since v1.52 has now missed it twice by enumerating members rather than deriving them: the members are every site in the body that publishes a reading of this document, found by sweeping the body for a `the tree/body vN ships` or `re-run after vN` phrase and requiring N to equal the shipping revision; publish that sweep's residual (a site that stamps by sha rather than by revision word is invisible to it) instead of a hand-written list of four.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `The four sites that carried the v1.50 stamp into v1.51 are this one, the`
- v1.52's repair of the `Task 3's delta block` ordinal introduced a needle-inside-scope defect in the sentence that states the needle's own one-hit property. `grep -Fc '# h-mad/tests/docsections.py  (delta)' docs/01-plan/features/doc-block-exec.impl-plan.md` returns **2** at `09e9307` — line `1028`, which spells the needle out, and line `1901`, the block header — against **1** at `b3be433`, `00b961f`, `dfae038`, `3f70eb3` and `af19d53`. The prior wording carried no literal needle, so the count moved 1 → 2 with the fix. This is the same device the document already prescribes one level down (member (x) writes its needles as regex character classes precisely so the published screen cannot match itself) applied nowhere here. `instance of:` the class "a locator published in the body it is scoped to". Prescription: write the header needle with a character-class stand-in at line `1028` (e.g. `# h-mad/tests/docsection[s].py  (delta)`) or state the one-hit property without spelling the needle, as the sibling site at line `1119` already does — and state the residual, that every other literal internal needle in this body is exposed the same way.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `own header comment, which `grep -Fn '# h-mad/tests/docsections.py  (delta)'` reaches at exactly`
- The v1.52 entry's freeze-scope check clears the AST censuses wholesale and that clearance is false: residual (a)'s over-count sweep moved **22 → 23** across the freeze. Re-running the sweep exactly as published, against blobs read with `git show` at each sha: `335f535` **22**, `74e126f` **22**, `af19d53` **23**, `09e9307` **23**. The new member is `h-mad/scripts/h_mad_assemble_audit.py:247 _trim_version_history` — the function the freeze commit itself added (`git diff --name-only 3f70eb3 af19d53` names `h-mad/scripts/h_mad_assemble_audit.py`), verified by `sed -n '247p'` reading `def _trim_version_history(text: str, keep: int | None, *, ref: str) -> str:`. The entry checked the freeze against this document's `path:line` pins and against the two tracked corpora (both of which I reproduce: **30** `.md`, **127** `.py`) and then excused the censuses by assertion. That is the same species the entry's own item (3) files against itself. Prescription: re-run residual (a)'s sweep at the shipping tree, publish it as 23 with `_trim_version_history` named alongside `traced_bindir`/`run_with_bindir` as a non-slicer the over-count admits, and replace "do not move with the freeze in any case" with the measured statement — the fence-state sweep (7) and the source-guard screen (2) are unmoved, the `## `-slicer sweep is not.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `The AST censuses and the corpus figures are stamped at `335f535`, `74e126f` and `1861157` and are blob readings that do not move with the freeze in any case.`
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `below prints **22** named helpers under `h-mad/tests`, `h-mad/scripts` and `handoff` holding the`

## Should-fix
- The six-token GNU-divergence sweep is a second member of the not-re-stamped class: the v1.52 entry republishes its reading ("**3** lines, all three prose about the class, unmoved") while the site still stamps it at v1.41 and v1.48 only. I reproduce **3** at `09e9307` (lines `1415`, `2023`, `2271`), so no integer is wrong — but under the enforcement condition this revision introduced, re-running a screen for the Version History and leaving the site's stamp behind is the defect, not the avoidance of it.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `**Class, first swept at `35698f9` for v1.41 and re-swept at `700c599` for v1.48**`
- The freeze-scope sentence's category label contradicts its own enumeration. `git diff --name-only 3f70eb3 af19d53` names **8** files; four are `doc-block-exec.impl-plan.audit.v46.*` and `doc-block-exec.plan.audit.v86.*`, and the four the sentence keeps are two `docs/02-design/features/doc-block-exec.design.audit.v95.*` reports plus the assembler and its test. The two design audit reports are this feature's audit reports, so "outside this feature's audit reports" is false of half the four it names. The conclusion (byte-identity of the four documents) is independently true and I reproduce it; only the label is wrong.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `names four files outside this feature's audit reports: two design audit reports, `h-mad/scripts/h_mad_assemble_audit.`
- Residual (a)'s 22 is written in the present tense ("the sweep below prints **22**") with its stamp two sentences back and three shas old, which is why the drift in the must-fix above was invisible to a reader of the paragraph. Every other screen in this document now carries `on the tree vN ships` inline. Give this one the same form so the stamp travels with the integer rather than with the paragraph.
- The live suite-collection readings **2809** / **2547** are stamped at `700c599`, and the freeze added tests under `h-mad/tests/test_h_mad_assemble_audit.py`, so both absolutes are stale on the shipping tree even though the **262** divergence the paragraph actually rests on is invariant under an addition. `unverified` — I did not run `pytest --collect-only` at `09e9307`, so I do not know the current pair; the point is that the freeze-scope check did not consider this pair at all, and the entry lists the floors as inherited-unverified without noting that its own freeze moved the live comparison beside them.

## Nit
- The marker screen's "four sites" reads as four when the clause enumerates five noun phrases ("this one, the restated-cardinal screen, both `SKILL` screens and the `.py:` series"); "both `SKILL` screens" is one *site* carrying two screens, which is correct but takes a second read. Naming the sites by their headings rather than by their screens would remove the ambiguity — and would also make the missing fifth member visible.
- `test_h_mad_collect_report_docs.py`'s `_second_surface()` is defined at `:49` and the document pins its eight call sites but never the definition; a reader tracing the Task 5 migration reaches the call list with no anchor for the callee. One `path:symbol` pin would close it.
