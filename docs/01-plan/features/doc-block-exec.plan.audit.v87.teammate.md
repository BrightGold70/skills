## Summary

GATING pass over `docs/01-plan/features/doc-block-exec.plan.md` at v1.103, freeze `09e9307` (HEAD), with the r16 measurement commit `af19d53` as the subject of every stamped reading. The ledger series, the register interval arithmetic, the `af19d53` closure break, the corpus-30 reading, the suite-floor `7`→`12` move, the `df04e8e`/`dfae038` path lists, the spec-discharge pair and every sibling-derived `docs/` figure were re-derived and all reproduce exactly; four claims do not. Three of the four sit in text v1.103 itself wrote, two of them in the two changes no leg requested — which is the fifth consecutive round of that pattern.

Evidence: 6 files opened, 150+ greps run.

## Must-fix

- The rewritten standing-debt sentence is off by one at its lower bound: cycle 72 **did** carry a codex leg, so "from cycle 72 … ran on the substitute leg alone" is false of the first cycle it names. `git ls-tree -r --name-only af19d53 -- docs/01-plan/features/` lists `doc-block-exec.plan.audit.v72.codex.md` (and `…v72.p1.md`); the codex half of the ledger printed four lines below reads `72` precisely because that file exists, and the v1.103 Version History entry calls the same gap "the thirteen-cycle gap", which is 73–85, not 72–85. The paragraph therefore contradicts both the series it introduces and its own entry. Prescription: "From cycle **73** through cycle **85**", or restore the original predicate form ("the last audit carrying a codex leg is cycle 72"). instance of: the class is *a span rewritten from a boundary predicate* — converting "the last X is N" into "from N through M" moves the endpoint by one every time; the rule over that set is that a span derived from a boundary predicate is re-derived from the underlying list, and here the list is the `git ls-tree` output the same paragraph already runs.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `rule.** From cycle **72** through cycle **85** every audit of this document ran on the substitute`

- The register's stamp move `dfae038`→`af19d53` rests on a uniform property asserted over four Version History entries, and two of the four falsify it. Run at `59cc2ad` (the commit that lands all four revisions): `git show 59cc2ad:docs/01-plan/features/doc-block-exec.spec.md | awk '/^- v1\.62:/{print}' | grep -ci 'NOT RE-RUN'` → **0**, and the same over `docs/02-design/features/doc-block-exec.design.md` with `/^- v1\.107:/` → **0**. Neither entry carries a `NOT RE-RUN` enumeration at all, and neither names any member of *this* register — `grep -oiE '(markdown-it-py|screen-two|Setext|doc-auditor\.md|74e126f self-count)'` over each returns nothing. Only this document's own v1.102 entry does what the sentence claims all four do (its "Also not re-run: the markdown-it-py 14-case corpus, the five cf3a862 OS/runtime probes, …" clause). This breaks the document's own rule at the register lead — "a lead sentence asserting a uniform property of a list is checked against every member of that list before it is written" — inside the paragraph that states it, and it is the *sole* stated justification for moving the stamp, which is the one thing the register exists to refuse taking on trust. The conclusion is probably salvageable (only a revision of *this* document can re-run *this* document's figures, and v1.102's entry does the enumeration); the argument as written is not. Prescription: narrow the claim to v1.102's entry and state separately why the three sibling revisions cannot have re-run a plan-scoped figure.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `impl-plan v1.51 and design v1.107), and **each of those revisions' Version History entries`

- The closure-retirement paragraph names the wrong section for the one site it says reads the changed file. The suite-floor bullet — the site that reads `h-mad/tests/test_h_mad_assemble_audit.py` — is at plan line 3296, under `## Success Criteria` (heading at line 3255). `## Convention Prerequisites` spans lines 3231–3253 and contains no reading of either file `af19d53` touched; `grep -n 'test_h_mad_assemble_audit'` over the body returns only lines 1013, 1022 (this paragraph's own command) and 3296. This is the document's own placement rule broken in the same revision that restates it — "a placement claim is a claim about a heading, and this document locates by heading and never by proximity … Residual … nothing detects a *wrong* section name, only a missing one" — with the residual realised rather than hypothetical. The `7`/`7`/`7`/`12` reading itself is correct and reproduces at `74e126f`, `4e4a00c`, `3f70eb3` and `af19d53`. Prescription: `§Success Criteria`.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `either changed file is the suite floor in §Convention Prerequisites, and `af19d53` moved it again`

- The `awk`-prefix paragraph publishes a property of the bare `sed` form that does not reproduce at any commit in this feature's history, and the mechanism it states is refuted by the anchor's own count. Run with `/usr/bin/sed`, `/usr/bin/awk` and `/usr/bin/grep` (the interactive `grep` here is `ugrep 7.8.4`, so the system binaries were used): `git show 00b961f:<spec> | sed -n '/^  - AC-4.2:/,/^  - AC-4.3:/p' | grep -c 'BAD_ARGS'` → **0**, identical to the `awk`-scoped form, and `grep -c '^  - AC-4.2:'` **whole-file** at that sha → **1**, as does `^  - AC-4.3:` → **1**. So `sed` never opens a second range and never runs to EOF. Checked at ten shas — `b3be433`, `00b961f`, `df04e8e`, `dfae038`, `59cc2ad`, `7b182b0`, `3f70eb3`, `4c1c3a5`, `af19d53`, `09e9307` — the whole-file and body-scoped readings are equal at every one (`0/0` through `dfae038`, `2/2` from `59cc2ad`), and the whole-file `^  - AC-4.2:` anchor is `1` at all ten; the working tree agrees. The prose confuses the bare token `AC-4.2` (7 whole-file / 3 body at `af19d53`, so it *does* recur in §Version History) with the anchored address the published command actually uses, which does not. The general property of `sed` ranges the paragraph generalises to is true; the executed instance offered as its evidence is not, which is the document's own "every *property* a screen claims is a claim about code and is executed, never reasoned" applied against it. This clause is carried from before v1.103, not fix-introduced. Prescription: state the whole-file reading as **0** and the anchor as unique, and either find a real instance of the leak or mark the mechanism `unexecuted`.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `decorative** — this was written first as a bare `sed` range over the whole file, and it returned`

## Should-fix

- The v1.103 list of new readings closes with a completeness claim that is false for one of its three members. The `df04e8e`/`dfae038` per-commit path lists are published in the body at lines 1123–1126 and 1392–1395 with their command, and the `af19d53` status of the two spec commands at lines 190–196 with both commands — but the `ten of the eleven` sweep's readings (3 whole-file, 0 body-scoped) appear **only** in the §Version History entry. Body-scoped, `ten of the eleven` occurs exactly once in the whole body, at line 1147 — this list entry itself — so a reader sent to "its own site" arrives at the pointer and nothing else. This is the same shape as the `"admissible are …"` member the document repaired one revision earlier by requiring `≥ 2` occurrences, recurring on a needle the same revision introduced.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `owed. Each is published at its own site with its own command. *The freeze* keeps its meaning —`

- The `handoffs/`-alone value sweep the v1.103 entry publishes as exhaustive ("All four are corrected or bracketed") uses a needle blind to two of the three shapes present in the body it swept. Over the current body, newline-collapsed, `grep -oE 'handoffs/.{0,4}alone'` returns three distinct forms — `` handoffs/` *alone `` (line 1122, emphasised), `` handoffs/`-alone `` (line 1146, hyphenated compound) and `` handoffs/` alone `` (line 1396) — while the published needle `handoffs/.{0,2} alone` sees only the third: fed lines 1121–1123 collapsed it returns **0**. The entry already names its own residual for this ("the `.{0,2}` form admits a delimiter and cannot admit an arbitrary one"), so the defect is that the completeness verdict was stated anyway. All three surviving sites are correct quotations of the retired claim, so nothing is factually wrong here — the sweep's *coverage* claim is what fails.

- The `docs/`-scoped sibling figures are neither re-run at `af19d53` nor entered in the register, which the document itself defines as a defect rather than a licensed gap. Line 1138 states "v1.103 re-derives no `docs/`-scoped figure that v1.101 stamped at `b3be433`"; line 1205 states the same closure "drifts by construction and must be re-run every round … it is re-taken there, not extended by assumption"; line 1218 states "Anything a later reader finds still outside the closure and outside the register below is a defect in the register, not a licensed gap." The nine members of that walk are in neither the `h-mad`/`handoff` closure (retired) nor the seven register bullets. I re-ran all of them at `af19d53` and every value holds — design `seven-plus-two-plus` **1**, design mutation-target derivation **1**, spec AC anchors **49** with the `uniq -c | awk '$1>1'` duplicate check silent, AC-6.1's two spec greps **1**/**0**, spec `len(tuple)` **2**, spec `^  $ awk ` **1** with design and impl-plan **0** each, "Residual on the enumeration itself" **1**, the spec opener census **21** openers over **11** distinct tokens with the distribution unchanged, and the impl-plan's `found = _dbe.find_heading(text, heading)` **4** — so this is a rule contradiction, not a stale figure. Resolve it by either re-stamping the walk at `af19d53` or adding it as an eighth register bullet; the third option the text takes now is the one line 1218 forbids.

- The register's own body self-count moved at the landing commit in a way the entry's self-match note does not cover. The v1.103 entry publishes "body-scoped … returns 0" for `ten of the eleven` at `af19d53` and accounts for the whole-file self-match by noting the needle "appears literally inside this entry too" — but the *body* reading also moved, from **0** at `af19d53` to **1** at `09e9307`, because line 1147 is body text and not a Version History line. Both readings are correctly stamped at a predating commit, so neither is wrong; the explanation covers only one of the two grammars it publishes.

## Nit

- Line 3473's `From cycle **72**` and line 3538's `(1) **The unit is the highest cycle number, not a file count**` are four lines apart and the second is what makes the first checkable; a reader who takes the `72` as a boundary rather than as a series row has no local cue. Consider putting the boundary sentence *after* the series fence rather than before it.
- `## Convention Prerequisites` and `## Success Criteria` are adjacent and both carry suite-related bullets ("Full suite run alone before the Phase 5f gate" vs the `2748` floor); the Must-fix above is the second time a cross-reference has landed on the wrong one of the pair. A one-clause disambiguator at each ("the floor is in §Success Criteria; the run-alone rule is in §Convention Prerequisites") would cost less than a third repair.
