## Summary
The prescribed matcher executes correctly on the stated 24-negative/12-positive corpus, the mutation JSON parses with 37 entries, and the current test module still collects 290 nodes. The plan is nevertheless blocked by an impossible T3 RED classification/wire pin and by unresolved matcher-contract and ownership drift across the authoritative documents.

## Must-fix
- AC-3.17 cannot be `RED: FAIL` at Task 3 RED, and therefore cannot be the claimed caller-observable WIRE-PIN in its current form — Task 2 has already installed `_agent_tail_re`, but Task 3 has not installed the tail pass, so a prose-only tail does not resolve and `test_tail_pass_prose_mentioning_agent_does_not_resolve` passes; helper-level positive/negative matcher controls also pass because the helper already exists. The actual split is therefore T3 11/7 and aggregate 30/14, not 12/6 and 31/13, so the prescribed 5d counts halt a correct RED. Use a caller-observable mixed fixture (for example, one real-banner candidate plus one prose decoy, expecting the real handle) that fails both before the pass exists and when only the matcher connection is removed, or reclassify the negative node and register a different wire pin.
- The paired spec still defines AC-1.1 in terms of an agent's `_agent_pv_re` signature while AC-1.4 says `_agent_pv_re` matches ordinary prose 24/24 and requires the independent `_agent_tail_re` grammar — those two ACs admit different candidate sets, including the wrong-pane class AC-1.4 forbids. AC-1.1 must name `_agent_tail_re` (and its vendor/model-banner subset) so the authoritative acceptance contract agrees with the design and implementation plan.
- Design v1.28 requires `_agent_tail_re` but never adds that new function to Components Changed, API / Interface Changes, or Implementation Order; step 1 still ships only `_orca_tail_sig`, while step 2 immediately consumes the omitted matcher. The implementation plan consequently does more than its claim to “carry all seven” design steps plus only the T1 prerequisite; add matcher creation and its direct tests to the design's component/order/API surfaces and map them explicitly to T2.
- The source plan's current Success Criteria says 13 green nodes split 12+1, then assigns the timeout procedure to “the twelfth” after already saying twelve nodes are mutation-backed, leaving the thirteenth unaccounted and contradicting the implementation plan's 12-mutation-plus-1-procedure proof map. This is the current normative body, not history-only prose, and must be corrected to “the thirteenth” so the base Test-discrimination accounting is unambiguous.

## Should-fix
- Task 2 says `_agent_tail_re` is “proven before anything consumes it,” but AC-2.11 checks only that its output is syntactically accepted by `grep -E`; an always-matching valid ERE passes T2, while the 24/12 semantic corpus and all matcher-widening mutations are deferred to T3/T6. Put the direct 24-negative/12-positive matcher test in T2, leaving T3 to test the caller connection and pass-level selection behavior.
- The design's bounded-grammar paragraph still contains two spliced continuation lists and later says “the anchor is applied” and “match the agent's existing signature,” despite the selected implementation being a new bounded, line-complete helper. Collapse this to one exact `_agent_tail_re` rule so the prose matches the implementation block.

## Nit
- Fix the spec's “reading ... and resolves” grammar and the stray closing `**` after “both directions” in AC-4.6.
