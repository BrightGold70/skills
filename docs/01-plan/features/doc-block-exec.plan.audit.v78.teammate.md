AUDIT-doc-block-exec-plan-v78-BEGIN

## Summary
Gating pass at freeze sha `35698f9`. Every tree-derived figure this plan stamps at `74e126f` I re-derived at the freeze sha and **all of them reproduce** — 3 importers, `def` plus 3 `_gate_bash_block()` call sites, 0 `returncode`, `^def test_` 6 / `titled_section(|section_from(` 6 / loose 8, `P<marks>` 1, 5 `.pytest_cache/README.md` at 0 fences each, `ls`/`git ls-files` 37/37, `parametrize` 2, `docsections.json`'s four `file` keys and its key sets, the `_second_surface()` census (7 blocks, gate at 4, `exec codex` at 2, each unique under its own filter), and screen one's triple 21/18/9 with the 9-line triage exact by category. The boundary probe reproduces on `awk version 20200816`: the `\b` form prints nothing, bare `/today/` prints both, `printf 'a\bb\n' | awk '/\b/'` matches, the POSIX form prints the first line only. Every finding below is therefore a **provenance or citation defect on a claim that is factually true**, plus one cross-document contradiction the freeze commit itself created in a sibling.
Evidence: 21 files opened, 61 greps run.

## Must-fix
- The Risks row's provenance points at a section that does not exist and at a sha no recorded run of that probe carries — this is the one member the widened screen surfaced in v1.91, and its fix is the new defect. This plan has no `§Second surface` section (`grep -n '^#\{1,4\} '` at `35698f9` returns Executive Summary, Overview, Scope, Goals, Requirements, Implementation Strategy, Architecture Considerations, Deliverables, Risks and Mitigation, Measurements, Convention Prerequisites, Success Criteria, Out-of-Scope, Next Steps, Version History — no Second surface); the only `## Second surface — the codex leg` heading is in `h-mad/SKILL.md:2179`, which is the probe's *subject*, not where it is recorded. The two surfaces that do record the probe are plan:488 and plan:507 and **both stamp `335f535`**, so the Risks row's `74e126f` contradicts them and the row carries no command at all — the document's own binding rule says "a sha with no command is uncheckable". `instance of:` the v1.91 subject-grep rule applied to its own fix — `grep -n '_second_surface' docs/01-plan/features/doc-block-exec.plan.md` → 5 surfaces at `35698f9` (429, 464, 488, 507, 565), two of which now disagree on the sha. The underlying claim is TRUE: I re-ran the probe at `35698f9` (importing the consumer's `_second_surface()` and running `re.findall(r"```bash\n(.*?)```", section, re.S)`) → 7 blocks, `exec codex` in block 2 only, `h_mad_audit_gate.py` in block 4 only. Prescription: stamp the row `335f535` to match §Implementation Strategy and cite the probe command inline, or record a real re-run at the audited sha in §Measurements and point at that section by its actual name.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `re-derived at `74e126f` with the `_second_surface()` probe recorded under §Second surface`
- The plan's stated residual on screen two is FALSE against the spec at the audited commit, because the spec was widened in the **same** commit — the exact commit-scoped sibling-edit hazard the plan warns about one paragraph earlier. At `35698f9` the spec's gap between cardinal and noun is `([^ ]+ ){0,3}` — zero to three arbitrary tokens (`grep -n '{0,[0-9]}' docs/01-plan/features/doc-block-exec.spec.md` → line 703 and line 746 at `35698f9`); `git show 74e126f:docs/01-plan/features/doc-block-exec.spec.md | grep -n '{0,[0-9]}'` returns **nothing**, and the same line at `74e126f` reads `([a-z]+ )?`. So the plan's residual was true when written and is false at the freeze sha. **Half of it still stands**: the cardinal alternation still has no `zero` at `35698f9`. Two consequences the same sentence carries: the plan says the item was "reported to the spec author rather than patched here" when the spec author patched it in this commit; and the plan restates the spec's residuals as **two** when the spec now lists **three**, the missing one being the very gap the plan mis-describes. Prescription: re-read the spec's enumeration at the audited sha before restating it, drop the word-gap half of the residual, keep the `zero` half, and count the spec's residual categories rather than carrying two.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `the enumeration allows exactly one word`
  quote: docs/01-plan/features/doc-block-exec.spec.md › `` `([^ ]+ ){0,3}` and was `([a-z]+ )?` through v1.57: one optional *lowercase* word cannot span ``
- The published negative control for screen two attributes the filtering to a stage the checker does not have, so the conclusion drawn from the control pair is not supported. The plan says it runs the spec's enumeration **verbatim**, substituting only the path. Run verbatim at `35698f9`, that enumeration is `awk '/^## Version History/{exit} {print FILENAME":"NR": "$0}' <path> | grep -Ei '<markers|cardinal…noun>'` — there is **no sha stage** anywhere in it. I ran both legs at the freeze sha: the positive returns `335f535:1003:   `h-mad/scripts/*.py` is 37 files today;` and the negative line is indeed absent — but the reason is that the reworded claim reads `**37** at`, and `at` is not in the closing noun alternation within three tokens, so the pattern never matches. Provenance plays no part. As written the paragraph tells the next author that screen two discriminates on provenance; it is provenance-blind, which matters because this plan imports it as its class closure for tree-derived counts. Prescription: either state that the plan appends a `!/[0-9a-f]{7}/` reading step (and stop saying verbatim), or rewrite the conclusion as "the enumeration is provenance-blind; the sha reading is a manual step over its hits", and give the actual reason the negative is filtered.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `test filters it out. A screen that returns the first and not the second is discriminating on`
- A bare `path:line` pin into `h-mad/tests/test_h_mad_collect_report_docs.py` survives in the body at `35698f9`, in a document that asserts twice that it stopped writing them — the value sweep the Version History records (`:270`/`:309`/`:412`) could not see it because it enumerated values, not shape. Shape grep at the freeze sha: `awk '/^## Version History/{exit}{print NR": "$0}' docs/01-plan/features/doc-block-exec.plan.md | grep -E '\.py:[0-9]+'` → **3** hits — 798 and 799 are recorded grep output and legitimately exempt, but **254** is prose. It resolves at `35698f9` (`sed -n '22p' h-mad/tests/test_h_mad_collect_report_docs.py` → `sys.path.insert(0, str(SCRIPT_DIR))`), so it is not wrong today; it is the class the document declares closed, unprovenanced (no sha, no command), pinning by line into the one file the same paragraph family says has gone stale once already. `instance of:` prose `path:line` pins into consumer files. The rule over the class is the shape grep above, not the three-token value grep; residual on that rule: a pin written without the `.py` suffix, or into a non-`.py` file, is invisible to it and needs the structural-nickname policy stated as a rule rather than applied per-instance. Prescription: replace with the structural form the document already uses — "the `sys.path.insert(0, str(SCRIPT_DIR))` at the head of `test_h_mad_collect_report_docs.py`" — and re-run the shape grep, not the value grep, when declaring the class closed.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `(`test_h_mad_collect_report_docs.py:22`): `docsections.py` itself does`
  quote: docs/01-plan/features/doc-block-exec.plan.md › `a bare line pin for it anywhere in this document`
- A tree-derived count carries a *description* of its command rather than a runnable command, which the base invariants classify as a Must and forbid downgrading. §"Behavioural premises carry their command" requires "the exact command that produced it, inline and runnable"; "a fence-toggling one-liner" is neither. The claim itself is TRUE — I re-derived it at `35698f9` with a real fence toggler over `h-mad/agents/*.md`: `doc-auditor.md` carries exactly 4 in-fence `#` lines (98 `## Summary`, 101 `## Must-fix`, 105 `## Should-fix`, 108 `## Nit`) and `design-author.md`, `implplan-author.md`, `plan-author.md`, `spec-author.md` carry 0 each. Prescription: paste the actual one-liner beside the number, since the claim it supports (`old_only` above 76 at 5c) is load-bearing for the Guard-narrowing accounting.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `(re-derived at `335f535` with a fence-toggling`

## Should-fix
- This plan is the only document that attributes to the spec's enumeration, and it uses the needle form the spec deprecates by name. The spec designates a line-anchored address; the plan uses a prose phrase. Both resolve to 1 at `35698f9` (`grep -c 'How the members are found' docs/01-plan/features/doc-block-exec.spec.md` → 1; `grep -cE '^  \$ awk ' docs/01-plan/features/doc-block-exec.spec.md` → 1, and `grep -oE '^  \$ [a-zA-Z0-9._-]+' | sort | uniq -c` → `awk` 1, `curl` 1, `git` 5, `printf` 1, `python3.11` 1), so nothing is broken today. The fragility is concrete and is the one the spec states: a spec §Version History entry quoting the phrase takes the plan's count to 2, whereas prose references cannot inflate the anchored needle because they sit mid-line. Neither sibling uses either needle — `grep -n 'How the members are found'` over `docs/02-design/features/doc-block-exec.design.md` and `docs/01-plan/features/doc-block-exec.impl-plan.md` returns 0 at `35698f9` — so the plan is the sole attributing surface and the sole place the designated address can be honoured.
  quote: docs/01-plan/features/doc-block-exec.spec.md › `the fenced block immediately above, and the needle is **line-anchored**, not a prose phrase:`
- The `_second_surface()` census is recorded twice in §Implementation Strategy at plan:486–489 and plan:505–512, at the same sha, with near-identical wording ("**7 blocks, 1 of them gating**" and "the section holds **7** bash blocks there, 4 at `e8eaf6f`"). Both are correct at `35698f9`, but a claim stated on two surfaces is exactly what the v1.91 subject-grep rule exists to catch, and the Risks-row defect above is what happens when the two drift. One authoritative record plus a pointer would remove the maintenance surface rather than adding a rule to police it.
- Screen one's third leg is stamped to a tree state that no longer exists. It reads "over the v1.91 body in the working tree at `74e126f`" — the v1.91 body is committed at `35698f9`. I re-ran all three legs at the freeze sha and every number reproduces (21 / 18 / 9), so this is a stamp repair and not a number repair; per the plan's own rule a reading of the *current* body should name the commit that holds it.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `v1.90 body as `74e126f` shipped it, **18**; over the v1.91 body in the working tree at `74e126f`,`

## Nit
- plan:565's Risks row opens its bolded clause with a lowercase "the": `**the exec-codex scan is NOT affected**`. The v1.90 form began `**`:412` is NOT affected**`, and the rename dropped the capital mid-substitution.
- The six-hits sentence splits its number from its sha across the line break — plan:802 ends "returns **six hits" and plan:803 opens "at `a8e0372`**". The document names this as screen one's known residual, and it is cheaply re-derivable now: `grep -rn '```bash' --include='*.py' .` → **6** at `35698f9`, split 1 `h_mad_precheck_doc.py`, 1 `test_docsections.py`, 2 `test_h_mad_assemble_tdd.py`, 2 `test_h_mad_collect_report_docs.py`, which matches the prose exactly.
- The corpus-invariance parenthetical stamps its `find` at `74e126f`; re-run at `35698f9` it is unchanged — 5 files, `grep -c '^```'` → 0 on each of `h-mad/.pytest_cache/README.md`, `h-mad/scripts/.pytest_cache/README.md`, `h-mad/tests/.pytest_cache/README.md`, `handoff/.pytest_cache/README.md`, `handoff/tests/.pytest_cache/README.md`. Worth re-stamping in the same sweep as the screen-one triple rather than as a separate edit.

AUDIT-doc-block-exec-plan-v78-END
