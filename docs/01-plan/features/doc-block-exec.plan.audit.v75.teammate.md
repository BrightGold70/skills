## Summary

Gating pass over `docs/01-plan/features/doc-block-exec.plan.md` at v1.87 / commit `a8e0372`. The v1.87 change itself verifies: the structural locator resolves (design `## API / Interface Changes` at :658 → the `__all__` paragraph at :723, one grep hit), and its stated rationale is confirmed in the tool — `h_mad_precheck_doc.py:180-183` runs `git diff --name-only <prov>..HEAD -- <rel>`, so PINDRIFT is file-level and a re-pin could not have cleared it. Two Must-fixes elsewhere: a premise about the spec that the spec deleted in the very commit that wrote the premise, and a class of tree-derived counts restated without the sha they were measured at, one member of which has drifted into a number collision that inverts what the paragraph is trying to say. Every other premise I probed held — the Second-surface block census (4 blocks, gate at 4, `exec codex` at 2) still measures identically at HEAD, the `.py` line pins all resolve, the 29-name `__all__` arithmetic is right, and the design's mutation matrix really is 81 rows with exactly 1 naming `SKILL.md`.

Evidence: 13 files opened, 38 greps and commands run.

## Must-fix

- **The paragraph justifying "AC-6.1's tree sweep is deliberately NOT this filter" rests on a premise the spec deleted, and quotes a phrase the spec does not contain.** The plan asserts, present tense, that the spec reaches AC-6.1's scope *by reference to this census*, and puts `"the same sweep as the plan's fence census"` in quotation marks as the spec's wording. At HEAD the spec states the opposite in bold, and `grep -n "same sweep as the plan" docs/01-plan/features/doc-block-exec.spec.md` returns **no hit**; `git log -S "same sweep as the plan" -- docs/01-plan/features/doc-block-exec.spec.md` shows the phrase left the spec at `b68ef48` — the same commit that produced plan v1.86, which is the revision that wrote this sentence. So the sentence was born stale, and the plan's own v1.86 version-history entry repeats the dead premise ("the spec reaches its scope by reference to this census"). This breaks the cross-document consistency the Next Steps section makes the gate condition, and it misdirects a 5c reader: it tells them AC-6.1 inherits its scope from a census it no longer touches, so a well-meaning "harmonisation" would be applied to a reference that does not exist. The paragraph's *conclusion* survives untouched — the two realisations do differ on purpose, and the spec now argues that independently and better (`a reference inherits whatever the referent becomes`). Prescription: delete the premise clause and the phantom quotation, and restate as — the spec spells AC-6.1's sweep out in full (spec v1.55, AC-6.1: `*.md` under the two roots, `archive/` and any dot-directory excluded); this census remains a separate one-off human measurement and is deliberately not the same filter.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `spec reaches AC-6.1's scope by reference to this census ("the same sweep as the plan's fence`
  quote: docs/01-plan/features/doc-block-exec.spec.md › `stated here rather than by reference**: `*.md` files under `h-mad/` and `handoff/`, excluding`

- **CLASS — a tree-derived count restated without the sha it was measured at. Five instances, all correct at their cited shas, all false at HEAD; the load-bearing member has drifted into a collision that inverts the paragraph.** The anchor instance is the corpus-definition bullet, which is *definitional* ("the corpus for every `*.md`-scoped measurement below"), carries a command block with **no sha at all** — the only such block in §Measurements — and asserts tracked `25` versus glob `30`. Re-run at HEAD from the repository root: the cited command returns **30**, and the glob returns **35**. The five `h-mad/agents/*.md` files landed at `6db8e50` moved the tracked count; the structural claim survives (the glob is still tracked + exactly five `.pytest_cache/README.md` artifacts, now five of them at `h-mad/`, `h-mad/scripts/`, `h-mad/tests/`, `handoff/`, `handoff/tests/`). But a reviewer who re-runs the cited command reads `30`, matches it against the plan's stated **glob** figure, and concludes the plan defines its corpus as the contaminated glob — which is precisely the contradiction design v1.93 / plan v1.86 were written to remove, now re-created by arithmetic rather than by wording. That collision is why this is a Must and not an arithmetic nit. Everything was right when written: I re-derived the fence census at both cited shas (`68 across 10 files`, control `83`, at `1861157` **and** `a469493`) and the extractor-census control at its cited sha (`git grep -l '```' -- '*.py' | wc -l` → `23`, `'```bash'` → `3`, at `1861157`). At HEAD they are `73`/`88`, `24` and `4`; the new `.py` hit is a comment at `h-mad/scripts/h_mad_precheck_doc.py:100`, not an extractor, so the census's *conclusion* (exactly two extractors, at `:270` and `:412` — re-verified) is unaffected. The rule over the class, which the plan already applies to the suite floor and to the heading/Setext differentials and is its own rule 7: every count derived from a tree scan carries the sha it was measured at *at every surface that states it*, not only in the §Measurements block it came from. Residual after that fix: the AC count (`49`, re-derived and still `49` at spec v1.55) is exempt by its own stated rule because it is anchored to a spec version and to a grep, not to a commit; the four measurement blocks that already name a sha need only the sha repeated downstream. Corroboration that the drift is live rather than theoretical: `python3.11 -m pytest --collect-only -q | tail -1` returns **2808** at HEAD against the plan's `2748` at `e8eaf6f` — the plan predicts that one and assigns it to 5c, so it is not a separate finding. Cross-document (rule 7, routed not filed): the design carries the same stale pair at `:177-179` and inside the AC-6.1–6.6 row at `:1169`.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `excluded, **25 files** — and not a filesystem glob. The glob returns **30**: the five extra are`
  quote: docs/02-design/features/doc-block-exec.design.md › `glob, which returns 30 on any tree where pytest has run.`

- **instance of: the class above — §Scope's "67 bash fences".** `72` at HEAD (73 total, minus the one to be tagged). No sha at the point of use.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `Deliberately untouched: the 67 bash fences that will not carry the tag, and the installed copy`

- **instance of: the class above — §Out-of-Scope's "68 bash fences", the surface a 5c task split reads first.** `73` at HEAD. No sha at the point of use. Decision-neutral on its own — no scope call changes at 73 — which is exactly why it survives unswept.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `- Any blanket or directory-wide sweep of the 68 bash fences under `h-mad/` and `handoff/`.`

- **instance of: the class above — the extractor census's "five hits", and the risk row whose mitigation is "this session" rather than a commit.** `grep -rn '```bash' --include='*.py' .` returns **six** at HEAD; the three named fixture sites are all still correct (`test_docsections.py:27`, `test_h_mad_assemble_tdd.py:489`, `:551` — each opened and confirmed). The risk row that exists *to* record staleness dates its own mitigation to a session rather than to a sha, which is the only mitigation cell in the table a reviewer cannot check.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `A broader grep for the bare literal returns five hits; the other three are inline fixture strings`
  quote: docs/01-plan/features/doc-block-exec.plan.md › `| The carried "68 fences" figure is stale | Low | Re-measured this session; command and output cited below under Measurements |`

## Should-fix

- **"all five `titled_section`/`section_from` assertions" — there are six, under every reading, and the count came in with the previous cycle's own fix.** I opened `h-mad/tests/test_docsections.py` in full. Six test functions call `titled_section`/`section_from` (`:44`, `:52`, `:57`, `:62`, `:68`, `:77`), there are six such call sites (`:47`, `:53`, `:58`, `:65`, `:69`, `:87`), and there are ten assert/raises statements. No reading yields five once `raises` is included, and the sentence explicitly includes it. The **conclusion is verified true** — every one of those assertions is `in`, `not in`, or `pytest.raises`, none compares exact bytes — so "no existing test pins the old behaviour" holds and the intentional loss of one leading `\n` is safe. This is a count error, not a broken premise. Worth flagging how it arrived: the v74 teammate report's own nit says "all five", and v1.85 copied the count along with the wording, which is the previous-cycle's-fix-introduces-the-next-finding pattern this feature keeps producing. Prescription: `all six` (or drop the number and say "every `titled_section`/`section_from` assertion").
  quote: docs/01-plan/features/doc-block-exec.plan.md › `loses one leading `\n` in that case. The leading newline is intentionally dropped: all five`

- **"Two tests pin it" has no recoverable antecedent, and the nearest reading contradicts the clause immediately before it.** The preceding sentence establishes that *nothing* pins the old byte-level behaviour; the next sentence then says two tests pin "it". The two things named — a collected-alone `pytest` subprocess from the repo root and an isolated `python3 -c "import docsections"` with an unrelated cwd — pin the **cross-directory import** (the AC-1.8 collect-alone pins referenced in Success Criteria), not the newline and not the assertion set. A 5c implementer reading top-to-bottom lands on the wrong referent. Prescription: name it — "Two tests pin the cross-directory import: …".
  quote: docs/01-plan/features/doc-block-exec.plan.md › `behaviour" above rests on. Two tests pin it: `pytest h-mad/tests/test_docsections.py -q` run as a subprocess from the`

- **Already-owed, NOT re-filed as new — cycle 73's "two leave / two stay" should-fix on `docsections.json` is still open at HEAD.** I confirm it still holds and I am recording it only so the orchestrator does not read its absence as closure: the plan still says two anchors "remain there" / "stay where they are", while its own two sentences earlier specify that `section_from`'s line becomes `text[offset:_dbe.fence_aware_end(text, offset, level)]` and `titled_section`'s `assert match, …` loses the `match` binding — so none of the four `find` anchors survives verbatim. I read the live spec: `h-mad/tests/mutation-specs/docsections.json` has exactly four rows with those exact names, `root` `../..`, and full-node-ID `_killed_by` values, all as the plan describes. The work is covered by the "re-reads the landed lines to set each `find`" sentence; the characterisation is what an implementer would use to decide which rows to leave alone.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `the other two stay where they are, and the harness's exact-once`

## Nit

- `exactly one hit at HEAD 048ef1f` — `048ef1f` is `HEAD~1`; HEAD is `a8e0372`, the commit that wrote this sentence. The grep does still return exactly one hit at HEAD, so only the label is wrong. It is also the one sha in the document written without backticks, unlike every other sha here.
- The docsections mutation rows are introduced out of order: the paragraph presents "A seventh, `docsections-heading-lookup-reverted`" before "A sixth pins the import that carries the wire". The eight-row total is correct and every row is bound; only the reading order is odd.
- The 5f bound's derivation no longer produces its own number: `1200 s` is offered as "three times the **383 s** baseline", and three times 383 is 1149. The bound is a generous ceiling and the same sentence already defers the re-derivation to 5c, so nothing is wrong operationally — but it is the same shape as the `397 s` drift v1.85 fixed here.
