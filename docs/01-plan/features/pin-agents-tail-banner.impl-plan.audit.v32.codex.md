## Summary
The mutation JSON parses and the authoritative table re-derives to 44 nodes (31 RED-fail, 13 RED-pass), but the plan is still blocked by a task-boundary error and guard-discrimination gaps. Matcher instructions and reported counts also remain contradictory across current normative bodies despite version-history entries claiming those fixes landed.

## Must-fix
- `_agent_tail_re` is still physically inside Task 1 (between Task 1's helper prose and the `## Task 2` heading), although Task 1 declares only the stub as its production file and says it has no production behaviour — a Task 1 dispatch therefore receives and may implement the matcher, making Task 2's AC-2.11 green before Task 2 RED and invalidating the stated T2 9/1 split; deferring it instead leaves the owning task without its required code block. Move the complete matcher block below the Task 2 heading and reconcile Task 2's contradictory claim that `_orca_tail_sig` “is added alone.”
- The matcher contract remains internally and cross-document inconsistent — T4 still ends its corrected paragraph by saying `$rival_re` “is reused unchanged”; spec FR-1 says `_agent_tail_re` wraps `_agent_pv_re` while AC-1.4 says its arms are independent; the source plan says the work is running the existing helper against `.tail`; and the design still follows the bounded 24/12 rule with the superseded “anchor applied” 7/7 account. These instructions select different implementations, including the measured prose-unsafe path, so the current bodies—not only their histories—must consistently prescribe the independent bounded `_agent_tail_re` grammar for both wanted and rival matching.
- The green-at-RED AC-3.2 asserts both Codex and agy launch-command rejection, but `tail-re-widened-to-launch-line` mutates only the Codex arm; likewise `tail-re-unanchored` permissively stubs only Codex's prose guard — the independent agy arm has never been observed failing when widened, violating Test discrimination. Add agy-arm mutations (one guard per mutation) and verify each exact anchor lands and is killed by the intended corpus node.
- `stub-read-dir-writes-one-file` changes iteration to `list(envelopes.items())[:1]`, but AC-1.5 never requires `test_tail_stub_read_helpers_shape` to pass at least two mapping entries and assert both files — with the natural one-entry fixture the mutant is equivalent and its advertised green-at-RED proof survives. Specify the test body or, at minimum, a two-handle fixture with exact existence/content assertions so the mutation's reject direction is guaranteed.
- Current-body counts still contradict their own enumerations — impl-plan AC-3.17 says “all seven” immediately before a 24-row negative corpus, and the source plan reports 13 green nodes but calls their proof split “12 = 11 + 1”; the actual split is 12 mutation-backed nodes plus one procedure. These are re-derivable reported counts, so leaving them stale breaches the Counts invariant.

## Should-fix
- The implementation-plan header cites design v1.26 and spec v1.11 although the paired files' latest histories are v1.27 and v1.12 — update the provenance after fixing those current bodies so implementers can tell which audited contract is authoritative.
- Task 6 still opens with the absolute claim that “Every guard this feature introduces” is mutated, then later narrows the claim to enumerated guards and explicitly names unmutated controls — use the narrow claim at the task definition rather than contradicting it hundreds of lines later.
- AC-4.6 says the tail pass does not exist at Task 4 RED, but Task 4 depends on completed Task 3; the node is green because rival rejection does not exist yet, not because the pass is absent.

## Nit
None
