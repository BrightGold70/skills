## Summary

Audited plan v1.92 at freeze sha `6f0ee85` (HEAD, working tree clean for this file), re-deriving every figure rather than carrying it: all three legs of screen one's triple (21 / 18 / 9) reproduce exactly, the 5/2/2 triage of the nine is exact by category, the block census prints `blocks 7 | gate [4] | exec codex [2]`, the shape grep returns 3 on the v1.91 body and 2 on this one, both screens' published controls reproduce leg for leg, and every re-stamped figure (13/88/48, 1, 2, 6, 4, 24, 73/88, 30/35, 49, 1/0, markers 8, 72, 37/37, 2, 3, def+3, 0, 6/6/1) reproduces at `6f0ee85` — so the v1.92 fixes landed and **no correct number was disturbed**; I also confirmed the "6 blocks, 0 gating" arithmetic empirically by simulating the tag (7 → 6, gate list empties). The two blocking findings are both new members of the provenance class this document declares closed, found where neither published screen looks: a behavioural probe of a **tracked repository script** stamped with a calendar date the subject outran, and screen one's own `measured` alternative still anchored on punctuation, which is why it cannot see it. Not re-run and therefore not challenged: the `2748` / `383.05s` baseline at `e8eaf6f`, the heading/Setext differential at `1861157`, the markdown-it grammar corpus, the `+2/+0/+0` collect probe at `a8e0372`, and the per-file `grep -c '^```'` on the five `.pytest_cache/README.md` files (I confirmed the count 5, not the zero fences).

Evidence: 26 files opened, 110 greps and probe commands run.

## Must-fix

- The 5f wrapper probe is stamped with a calendar date, and the carve-out that licenses the date rests on a premise the tree refutes — `hmad-dispatch` is not OS behaviour, it is tracked repository code (`h-mad/bin/hmad-dispatch`, `h-mad/scripts/hmad-dispatch.sh` both in `git ls-files`), and the last commit touching it is `3f50b95`, dated 2026-09-04 and titled "make rc=124 legible", i.e. it landed on the exact behaviour the probe measures **one day after** the stamp `2026-09-03` (`bea1b60`, "the wrapper tore its own read", is a second). Both asserted behaviours re-verified live by me at `6f0ee85` — `h-mad/bin/hmad-dispatch run --timeout 5 -- sh -c 'exit 3'` → `rc=3`, `run --timeout 1 -- sleep 3` → `hmad-dispatch: run_timeout after 1s — sleep 3`, `rc=124` — so the conclusion is true and only the provenance is defective, this round's pattern exactly. **Closing the class rather than the instance**: the rule is *a probe carries a sha whenever its subject is a tracked repository artifact; the carve-out is only for behaviour the OS or interpreter alone determines*, and its residual is that no shape grep can apply it — the test is `git ls-files <the subject the probe names>`, which a human must run. I ran that test over all five carve-out members in the body: the AC-3.14 probe (fence at plan:1027, output `python: 3.11.8 | darwin`), the AC-3.10 probe (plan:1057, `python 3.11.8 darwin`) and the AC-5.5 probe (plan:1085, `python 3.11.8 darwin`) are correctly OS/interpreter-scoped and correctly stamped; the AC-5.2 probe (plan:985) is correctly OS-scoped but **carries no interpreter or platform line in its recorded output at all** — its last printed line is `killpg on an already-reaped group: ProcessLookupError` — and is the only probe run under `python3 -u` rather than the supported `python3.11 -u`, so it fails the carve-out's own stamping rule; and plan:1359 is the one member whose premise is false. Prescription: stamp plan:1359 with `6f0ee85` (or the sha of the last `hmad-dispatch` commit) and add the platform line to the AC-5.2 output. Checked and not a re-litigation: no `doc-block-exec.plan.rejections.md` exists, and the only ledger in the feature (`docs/01-plan/features/doc-block-exec.impl-plan.rejections.md`) contains no match for `hmad-dispatch`, `2026-09-03`, `timeout wrapper` or `124`.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `(\`killpg\` on an emptied group, the \`timeout\` wrapper's \`124\`), which no repository sha determines`
  quote: docs/01-plan/features/doc-block-exec.plan.md › `the wrapped command's exit status and reports 124 on expiry — measured 2026-09-03,`

- Screen one's `measured` alternative is **still anchored on neighbouring punctuation** — the v1.91 repair removed only the *leading* comma, while `measured[,)]` still requires a `,` or `)` immediately *after* the word — so the axis the document states as closed is not the axis the awk program implements, and the residual is a live member: the finding above. Run by me at `6f0ee85`, the screen as published returns **9** lines over this body (208, 613, 630, 631, 677, 689, 699, 977, 1335) and **1359 is not among them**, because `measured 2026-09-03,` puts the comma after the date; the line carries no 7-hex run, so the `!/[0-9a-f]{7}/` stage is not what suppresses it. Replacing that one alternative with the POSIX word-boundary form the `today` alternative already uses — `(^|[^[:alnum:]_])[Mm]easured([^[:alnum:]_]|$)` — returns **31** lines on the same body and **does** include 1359. That is the costed prescription: the reading burden goes 9 → 31 on this body, which is what "its hits are **read**, never counted" already commits the document to; the class is *punctuation adjacency in either direction*, and its residual after the widening is that the bare word self-matches roughly twenty times wherever the rule is stated, which is why the hits are triaged and not counted. I separately verified that the published triage of the nine at `35698f9` is exact: the 5 self-matches are 587/588 (the rule sentence), 616 (the screen's own command), 628 (the boundary-probe fixture) and 638 (the old-and-new marker forms); the 2 probe references are 570 and 834; the 2 marker-word-no-count sentences are 208 and 1161 — 5 + 2 + 2 = 9, zero members.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `a marker alternative must not be anchored on neighbouring punctuation at all, because`
  quote: docs/01-plan/features/doc-block-exec.plan.md › `now \`measured[,)]\` with no leading punctuation required.`

## Should-fix

- The closure paragraph offers a command as the derivation of its own scope, and that command counts the very lines the same paragraph excludes two sentences later. Run by me at `6f0ee85`, `awk '/^## Version History/{exit}{print}' docs/01-plan/features/doc-block-exec.plan.md | grep -c '74e126f'` returns **27**. Reading the 27 rather than counting them: **15** are genuine `h-mad/`- or `handoff/`-scoped figures stamped `74e126f` (lines 28, 242, 281, 282, 290, 306, 321, 420, 423, 476, 659, 662, 663, 888, 889) — the closure's actual subject; **6** are the closure paragraph's own prose (634–637, 641, 648), which the paragraph does acknowledge as self-inclusive; and **6** are lines the paragraph explicitly puts outside the closure — 792/793 derive from the **spec** at `74e126f`, 815/816 derive from **this document** at `74e126f`, 878 is a sentence about figures stamped *older* than `74e126f`, and 813 quotes a stamp this revision withdrew. So the published derivation over-counts its stated subject by 12 of 27, and 6 of those 12 are the exact cases the next sentence says the closure "does not reach". The fix is to scope the command (e.g. restrict it to lines that also name `h-mad` or `handoff`) or to drop the offer of a derivation, since the paragraph's argument does not need the number. Both legs of the closure itself reproduce: `git diff --name-only 74e126f 35698f9 -- h-mad handoff` prints nothing, and the piped form prints `docs` alone.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `closure is checkable in two commands where that many edits are not. The closure does **not** reach figures derived`

- The companion sweep is published as a bare `0`, in the same revision that established a bare zero is not a control. The shape grep's residual (1) cites `grep -nE '\.(md|json|sh|toml):[0-9]+'` "over the same body, which returned **0** at `35698f9`" — I reproduce 0 at `35698f9` and 0 at `6f0ee85`, and 0 on all three siblings, so the screen has **never been shown to fire on anything**. It can: `printf 'see \`docs/02-design/features/doc-block-exec.design.md:412\` for the rule\n' | grep -nE '\.(md|json|sh|toml):[0-9]+'` matches. This is the same defect the same commit fixed for the agents `awk` screen under DECISION A ("a **true negative**, not a bare zero"), applied to one new screen and not to the other new screen in the same revision — and it is the standing hazard in its mirror form: not a negative half never run, but a positive half never run. Prescription: publish a one-line constructed positive beside the 0, as the agents screen now does.

- The spec-opener distribution is correct at its stamp and stale at the commit this document lands at, and the plan and the spec now publish two different outputs for the same command at the same stated sha. The plan states the distribution "at `35698f9`" as `awk` ×1, `curl` ×1, `git` ×5, `printf` ×1, `python3.11` ×1 (9 openers); I ran `grep -oE '^  \$ [a-zA-Z0-9._-]+' docs/01-plan/features/doc-block-exec.spec.md | sort | uniq -c` against `git show 35698f9:` and got exactly that, so the plan is right about `35698f9` — but at `6f0ee85` the same command returns 20 openers across 11 tokens (`awk` 1, `curl` 1, `git` 7, `pairs` 1, `printf` 2, `python3.11` 1, `RULE` 1, `S` 1, `sed` 1, `split_only` 3, `while` 1). This is the hazard the same sentence names two lines earlier — "a locator that was unique when written can be broken by a concurrent sibling edit landing in the **same** commit" — realised, since spec v1.59 landed in `6f0ee85` alongside this plan and the document's own rule requires sibling-derived figures to be re-derived at the audited commit. **The load-bearing conclusion survives and I verified it**: `grep -cE '^  \$ awk '` on the spec → `1` at `6f0ee85`, and → `0` on the design and `0` on the impl-plan, so `awk` still holds its slot alone and the plan is still the sole attributing document. Cross-document half, owed to the spec and not fixable here: spec v1.59 publishes the `6f0ee85` distribution (20 openers) under the stamp `35698f9`, where the true value is 9 — so of the two documents, **the plan's figure is the correct one and the spec's is mis-stamped**.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `line with two spaces and \`$ awk \` makes it 2 — at \`35698f9\` the \`^  $ \` command openers there are`
  quote: docs/01-plan/features/doc-block-exec.spec.md › `The slot census, re-derived in this revision at \`35698f9\`, is 20 \`^  $ \``

- **[CROSS-DOCUMENT RULING — spec v1.59; routed to the spec author, not a plan defect]** The orchestrator asked whether a behavioural predicate delivers the self-repair property an enclosing symbol name gives. **Ruling: only when the predicate is itself greppable, or sits in the same clause as a runnable locator that reproduces the candidate set — a predicate alone does not, because it is not a needle.** Decided on evidence, per pin, at `6f0ee85`. **spec:553 passes** — `test_h_mad_collect_report.py:287` "loops but filters to two named writer modules" is greppable as written (`git grep -n 'writer_modules' -- 'h-mad/tests/*.py'` → 2 hits, both in that file, `:285` the tuple `("h_mad_audit_cycle.py", "h_mad_collect_report.py")` and `:288` the `continue`), the predicate is factually exact (`:287` is the glob loop), and the same sentence publishes `git grep -n 'glob("\*\.py")' -- 'h-mad/tests/*.py'` → 3 hits, which I ran and which the three predicates partition 1:1. **spec:554 passes** — `:250` is `for script in script_dir.glob("*.py"):` inside `install_audit_cycle_stubs(tmp_path)` (`def` at `:142`) with `script_dir = tmp_path / "script-stubs"` at `:143`, so "globs a `tmp_path` fixture directory" is exact and greppable. **spec:675 passes and is the strongest form** — it carries the literal code token, `git grep -n 'stripped.startswith' -- 'h-mad/tests/docsections.py'` → exactly 1 hit, and line 37 **is** that line. **spec:664 passes** by file uniqueness — `h_mad_precheck_doc.py` holds exactly 1 ` ```bash ` and `:100` is the comment. **spec:663 fails** — "are fixture strings" is not greppable (`git grep -cn 'fixture string' -- 'h-mad/tests/*.py'` → nothing), and the census command immediately above it is written `git grep -n '```bash' -- '*.py' | wc -l`, so a reader re-running it as printed gets `6` and no locations; recovering the candidate set requires editing the published command. Two facts the brief has wrong and the spec author needs: the spec body carries **7 bare pins across 5 prose lines** (553, 554, 663×3, 664, 675), not three — 651/652 are recorded output and exempt; and the impl-plan's "0" is 0 *bare* pins, not 0 pins (it carries 44 `.py:N` locators, each with its enclosing symbol). Prescription for 663 only: name the enclosing test function for `test_docsections.py:27`, or print the census with `-n` and without `| wc -l` so the pins become reproduced output.
  quote: docs/01-plan/features/doc-block-exec.spec.md › `— \`test_docsections.py:27\`, \`test_h_mad_assemble_tdd.py:489\` and \`:551\` are fixture strings, and`
  quote: docs/01-plan/features/doc-block-exec.spec.md › `plus \`test_h_mad_collect_report.py:287\`, which loops but filters to two named writer modules`

## Nit

- The plan says "Two residuals on the needle" where the spec says "Three residuals, all concrete". The plan is content-complete — its (1) merges the spec's (1) file-scoped and (3) pins-the-block-not-the-clause — but a reader comparing the two counts reads a contradiction that is not there. State it as "the spec's three, merged into two here".

- Three different spec-version anchors sit in one body — `spec v1.55` (AC-6.1), `spec v1.56` ×2 (AC-6.4) and `spec v1.58` (the AC count) — while the spec ships v1.59 in the same commit as this document. Every underlying premise still holds at `6f0ee85` (49 ACs; `stated here rather than by reference` → 1; `same sweep as the plan` → 0; the AC-6.4 source (1)/(2) rule is intact), so nothing is wrong, but the labels record three different eras with no sentence saying which, if any, is current.

- The triage at plan:827–828 describes one of the two marker-word-no-count lines as "prescribing how a *report* must read". Line 208 prescribes what a **cannot-judge verdict line** may carry, not a report; the category is right and only the gloss is off.
