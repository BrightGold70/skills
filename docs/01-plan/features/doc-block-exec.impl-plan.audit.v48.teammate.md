## Summary

Impl-plan v1.53 at freeze `cb4fe99` is the most heavily self-verified revision of this document I have audited: every one of the ~45 tree claims I re-derived holds exactly, including all four `.py:` screens (55/55/0/0 at 23 folded paragraphs), the marker screen (5/5, five markers at count 1), the v1.53 stamp sweep (15 occurrences across 12 lines), the `## `-slicer sweep (23), the published fence-state sweep (7, same six named bodies and line numbers), the source-guard screen (2), the fence-opener corpus scan (`files 30 space-arm 29 in 4 files tab-arm 0 declined-inside-fence 2`), the committed 09-04 heading differential (`TRACKED files=30 both=292 old_only=82 new_only=1`, `GLOB files=35 both=297`), the collection pair (2814 root / 2552 `h-mad/tests`, divergence 262), the 1450 function census at all three shas, and all 44 distinct path-qualified `.py:`*N* pins. The AC identifier sets in this document and the spec are identical (49 each) and every AC appears in exactly one task checklist. Two internal contradictions survive: a stale cardinal inside Task 4's code structure, and an enumeration in Conventions that is short by one wire row.

Evidence: 19 files opened, 150 greps run.

## Must-fix

- `_field`'s docstring in Task 4's code structure states **19** dynamic values while the same task's prose derives **20** and names the `19 → 20` move as round seventeen's decision 3a — 19 plus the 7 bare fields is 26, against this document's own 27 rendering slots, so the two sites cannot both be true. This is not prose: the code-structure block is what a 5d implementer writes verbatim into `h-mad/scripts/h_mad_doc_block_exec.py` and what 5e quotes as exact-once mutation `find` anchors, so the wrong cardinal lands in shipped source and in the anchors of `field-escape-removed`, `c1-escape-removed` and `field-quoting-removed`. Prescription: change the docstring's `19` to `20`; the 12-member `DETAIL_KEYS` tuple three lines above it and the `8 quoted head fields + 12 quoted detail values = 20` derivation are both correct and need no edit.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `"""The ONE renderer the 19 dynamic values pass through (the 7 bare fields never reach it):`
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `exactly 20 dynamic values are rendered through one module-level renderer,`

- The Conventions bullet's enumeration of wire rows whose mutant reds a second named test names only `wire-revert-extract` and `consumer-from-import`, but Task 5's `wire-unconditional` row documents its own collateral red on `test_only_the_exec_scan_hand_rolls_extraction` and is not named there. That sentence is the carve-out that lets the preceding "Those five are the only rows in this document whose mutant reds a second named test" stand, so an omitted member falsifies the completeness claim it exists to protect — the same defect the same bullet repaired on the `doc_block_exec.json` side when it took that population "from two to FIVE". `instance of: the class is "every mutation row whose mutant reds a named test other than its own key". The rule over it: the enumeration is DERIVED by reading each of the 8 `doc_block_exec_wire.json` rows, the 8 `docsections.json` rows and the 85 `doc_block_exec.json` rows for a per-row clause of the form "goes red with it / reds too / stays a regression test, not this row's key", never recalled from the previous revision's sentence. Residual: a row whose collateral is stated in a third wording is invisible to that reading and is closed only by a reviser reading each row, exactly as the sibling-prose class beside it is.` Prescription: add `wire-unconditional`'s to the list, and state the derivation rule at that site.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `wire rows to one pin each, with `wire-revert-extract`'s and `consumer-from-import`'s collateral`
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `is a second `re.findall(r"```bash` — and stays a regression test, not this row's `test` key.`

## Should-fix

- `spawn-valueerror-unmapped` is called "the third row in this document" whose mutant reds a second named test, with no base given, and the two available bases disagree: in **document order** it is the second, because `duplicate-heading-takes-first` (Task 1) precedes it and `final-write-close-not-in-finally`, `field-quoting-removed` and `field-escape-removed` are all Task 4 rows; it is third only in the **Conventions bullet's enumeration order**, which lists `final-write-close-not-in-finally` first. This document's own standard elsewhere is that an ordinal carries its base — "the base is written down because the same census read 0-based names a different block, and a bare 'index N' is off by one depending on the reader" — and this ordinal does not. Prescription: write "the third member of the Conventions enumeration", or drop the ordinal and name the population.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `other composition path — the third row in this document whose mutant reds a second named test, and`

- **unverified**: the v1.53 stamp sweep's mechanical halves reproduce exactly (15 occurrences across 12 lines, at body lines 209, 216, 440, 672, 883, 1000, 1107, 1265, 1305, 1311, 1370 and 2184), but I could not confirm the "**9** sites, and the members are **eight** of those nine" partition, because deciding membership needs a human read of each hit against "the reading it stamps was taken over the shipping revision's own body" and I did not read all nine. I am not claiming it is wrong; I am recording that this leg of the sweep is unchecked by me. The four non-v1.53 stamps I did read — the two `the body v1.50 ships` occurrences and the `the tree v1.52 ships` / `the tree v1.49–v1.51 ships` series — are all narration or dated chain entries, not live stamps, so nothing on that side contradicts the published reading.

## Nit

- Task 3's `wait-unbounded` / `wait-expiry-unmapped` pair and Task 4's three `_field` rows are both cases where two or more rows share one killer and the discrimination matrix is what carries the evidence; Task 4 publishes its matrix as a table and Task 3 states its pair in prose. The prose form is correct but is the one place in the document where a reader has to reconstruct a two-by-two by hand. A three-line table beside the Task 3 pair would match the form Task 4 already sets.
