# Learnings — Durable Cross-Session Knowledge

Project-local kernel of gotchas, solutions, and patterns worth surviving
across sessions. Newest entries at the top. One line per entry.

Format: `- ISO-date · category · [confidence] · ` `` `tags` `` ` — pattern text`

Confidence: 0.3=tentative  0.5=moderate  0.7=strong  0.9=near-certain

Search via `grep <term> docs/learnings.md` or
`python3 ~/.claude/skills/handoff/scripts/learn.py search <term>`.
- 2026-09-02 · gotcha · [0.7] · `handoff:2026-09-02-audit-report-docs-copy-phase7,h-mad,6a-prime,mutation` — A mutant's discriminating input is not the obvious one: a project-root that is a FILE cannot discriminate (mkdir -p raises anyway); one that does not EXIST can — the mutant mkdir -p's a docs tree under the typo.
- 2026-09-02 · gotcha · [0.9] · `handoff:2026-09-02-audit-report-docs-copy-phase7,h-mad,6a-prime,review` — Verify a review's PRESCRIPTION separately from its facts: 'revert to the design's literal text' would reintroduce 2 measured defects; a boundary-slicing fallback collected agent narration as COLLECT: OK. Facts right, remedy wrong.
- 2026-09-02 · gotcha · [0.7] · `handoff:2026-09-02-audit-report-docs-copy-phase7,h-mad,6a-prime,flaky,hmad-dispatch` — Bash SECONDS-based deadlines are load-sensitive: deadline=SECONDS+t then remaining=deadline-SECONDS straddles a tick under a full suite (599000 vs 600000 ms). Assert a range or capture start once; passes alone, fails under load.
- 2026-09-02 · gotcha · [0.7] · `handoff:2026-09-02-audit-report-docs-copy-phase7,h-mad,6a-prime,fixtures` — Build test fixtures the way production produces them: an --out capture without the dispatch boundary, an unquoted path preamble, and an unframed report body each read as a code defect when only the probe was wrong (3 times in one session).
- 2026-09-02 · pattern · [0.9] · `handoff:2026-09-02-audit-report-docs-copy-phase7,h-mad,6a-prime` — 6a-prime sees design-vs-implementation drift that no code-level gate can: 6 real deviations survived 4 5e reviews, an anti-gaming verifier, 23 ALL_CAUGHT mutations and a 2424-test suite. Never skip it on a green run.
- 2026-09-02 · gotcha · [0.7] · `handoff:2026-09-02-audit-report-docs-copy-phase7,h-mad,6a-prime` — A fix on one rung of a fall-through ladder must be probed at the rungs BELOW it: a grace<=0 early return passed its own case and silently disabled the --out rung downstream; the next 6a-prime cycle caught it.
- 2026-09-01 · pattern · [0.7] · `h-mad,audit,evidence,handoff:2026-09-01-backlog-drain-and-tail-banner-design` — H-MAD audit passes at tools<=2 (low-evidence) find only doc-internal defects: contradictions, stale counts, invariant misses. Any claim about what CODE does needs a targeted dispatch gated on h_mad_review_evidence.py.
- 2026-09-01 · gotcha · [0.9] · `bash,shell,guards,handoff:2026-09-01-backlog-drain-and-tail-banner-design` — bash: 'if local out=$(cmd); then' returns local's own status (always 0) and discards cmd's rc. Declare 'local out' on its own line first. The broken form is what clean-scoping instinct writes.
- 2026-09-01 · gotcha · [0.9] · `jq,shell,guards,handoff:2026-09-01-backlog-drain-and-tail-banner-design` — jq -r on a missing key prints the literal null and exits 0, so an absent field scores as a value rather than an error; use jq -re to make a missing key non-zero.
- 2026-09-01 · gotcha · [0.9] · `orca,terminal,retention,handoff:2026-09-01-backlog-drain-and-tail-banner-design` — Orca terminal tail retention is hard-capped at 2000 lines regardless of --limit; agent panes survive it only because TUIs run on the alternate screen, so their output never enters normal-buffer scrollback.
- 2026-09-01 · gotcha · [0.7] · `shell,zsh,git,handoff:2026-09-01-wire-pin-gate-and-skill-upgrades` — zsh applies `:h` in `$VAR:h-mad/x` as a HISTORY MODIFIER (dirname), silently mangling a git ref into `origin/BrightGold70-mad/x`. Quote the whole string. Third distinct zsh fail-open this session.
- 2026-09-01 · solution · [0.7] · `mutation-testing,h-mad,handoff:2026-09-01-wire-pin-gate-and-skill-upgrades` — A mutation naming a `test` needs the spec to carry `target_command`, else the harness answers UNREADABLE. Adding one runs the whole suite PER MUTATION — for doc-rule mutants keep intent in `_killed_by` instead.
- 2026-09-01 · gotcha · [0.7] · `testing,doc-rules,h-mad,handoff:2026-09-01-wire-pin-gate-and-skill-upgrades` — A doc test slicing a FIXED character window from a heading goes vacuous as the section grows — it silently stops covering its own assertions. Bound a section on the next heading, never on a byte count.
- 2026-09-01 · gotcha · [0.7] · `h-mad,registry,wire-pin,handoff:2026-09-01-wire-pin-gate-and-skill-upgrades` — When a parser gains multi-value support, check the CONSUMER's identity key too: wire-pin's registry keys on (feature, task-id), so two wires per task upsert each other. Regex-only fix turns a loud blocker into silent under-registration.
- 2026-09-01 · pattern · [0.7] · `gates,h-mad,review,handoff:2026-09-01-wire-pin-gate-and-skill-upgrades` — A gate that fails CLOSED can hide for weeks: a false PASS gets hunted, but a gate blocking correct work gets worked around by hand and the workaround leaves no trace pointing at the gate. Ask if it can fail what it should pass.
- 2026-08-31 · solution · [0.7] · `orca,pane-identity,h-mad,handoff:2026-08-31-j1-pane-pin-takeover-and-handover` — orca terminal read .result.terminal.tail identifies panes when worktree-ps agents[] and previews are both empty. The field is `tail`: .content/.output/.preview are absent and return nothing in a way that looks exactly like an empty pane.
- 2026-08-31 · gotcha · [0.7] · `orca,handoff,worktree,handoff:2026-08-31-j1-pane-pin-takeover-and-handover` — `orca worktree create` snapshots the branch at that instant: commits pushed afterward never reach the lane's checkout. A late brief correction reaches the receiver only via the canonical main-worktree handoff store. Create the lane last,…
- 2026-08-31 · pattern · [0.7] · `h-mad,orca,guard-design,handoff:2026-08-31-j1-pane-pin-takeover-and-handover` — Same response field, opposite correct handling, decided by the PRODUCT: a durable pin fails loud on a bad value (it poisons the session); an already-running dispatch falls back (refusing strands live work). Test both directions.
- 2026-08-31 · gotcha · [0.7] · `mutation-testing,h-mad,measurement,handoff:2026-08-31-j1-pane-pin-takeover-and-handover` — A mutant that lands on the SAME behaviour survives while proving nothing: `; true` inside $() emptied the variable, which is exactly the fallback the test expects. Mutate toward a DIFFERENT observable — prefer deleting the guard's return.
- 2026-08-31 · gotcha · [0.9] · `shell,zsh,mutation-anchors,measurement,handoff:2026-08-31-j1-pane-pin-takeover-and-handover` — zsh does not word-split unquoted expansions: `cmd $LIST` passes every path as ONE argument, so an h-mad anchor sweep reports ANCHORS_NOTHING_SWEPT — a shell artifact, not repo state. Run multi-path calls under bash -c with mapfile.
- 2026-08-31 · gotcha · [0.7] · `codex,h-mad,dispatch,orca,config,handoff:2026-08-31-codex-agy-model-inheritance` — $CODEX_HOME is NOT ~/.codex under Orca — it is ~/Library/.../orca/codex-accounts/<id>/home. The stale ~/.codex/config.toml made a model pin look needed while the LIVE config already had the wanted model.
- 2026-08-31 · gotcha · [0.5] · `codex,h-mad,dispatch,handoff:2026-08-31-codex-agy-model-inheritance,rtk,shell` — ls -t <dir>/*.log is dead under this shell's rtk rewrite ('invalid value <path> for --time <FIELD>'), so newest-file discovery silently yields nothing. Use python sorted(glob, key=lambda p: p.stat().st_mtime).
- 2026-08-31 · gotcha · [0.7] · `codex,h-mad,dispatch,handoff:2026-08-31-codex-agy-model-inheritance,probe` — Score a tool-execution probe on a side effect (marker file), never on the STATUS line: gpt-5.6-luna writes fluent prose while every tool call dies, so it returns a well-formed STATUS: BLOCKED indistinguishable from a real verdict.
- 2026-08-31 · pattern · [0.7] · `codex,h-mad,dispatch,handoff:2026-08-31-codex-agy-model-inheritance,agy` — Do not pin a model in a dispatch wrapper: a pin silently outranks the CLI's own config, so the model the TUI shows and the model that runs drift apart with no error. Inherit by default; keep --model/--effort as per-dispatch overrides.
- 2026-08-31 · solution · [0.7] · `codex,h-mad,dispatch,handoff:2026-08-31-codex-agy-model-inheritance,agy` — codex has no --effort flag: reasoning effort is -c model_reasoning_effort=<e> (bare 'high' is fine, -c falls back to raw string). hmad-dispatch exec codex maps --effort to it; agy takes --effort natively.
- 2026-08-29 · gotcha · [0.7] · `h-mad,mutation-testing,pytest,handoff:2026-08-29-hmad-tooling-defects-closed` — Never run h_mad_mutation_harness.py and pytest against one tree concurrently: the harness edits source in place and reverts per mutation, so the suite reads half-applied mutants and reports phantom regressions. Serialise them.
- 2026-08-29 · gotcha · [0.5] · `pytest,monkeypatch,testing,handoff:2026-08-29-hmad-tooling-defects-closed` — Adding a precondition to a function whose result is passed to a monkeypatched stub breaks those tests: the arg is computed eagerly even though the stub ignores it. Let it fail at the layer that actually consults the resource.
- 2026-08-29 · pattern · [0.7] · `guards,validation,api-design,handoff:2026-08-29-hmad-tooling-defects-closed` — A guard's escape hatch must never be the practice the guard prevents: a writer validating the whole merged record left a hand-edited key repairable only by hand-edit. Ship a sanctioned repair verb, not a wider schema.
- 2026-08-29 · gotcha · [0.9] · `git,paths,worktree,handoff:2026-08-29-hmad-tooling-defects-closed` — git show <sha>:<path> resolves path from the git work-tree ROOT, not the cwd it runs in — unlike pathspec args (check-ignore, ls-files) which are cwd-relative. Mixing both in one module reads identically and compares different files.
- 2026-08-28 · gotcha · [0.5] · `testing,fixtures,handoff:2026-08-28-silent-pass-defects` — A fixture can make the bug it tests unreachable: install-check passed 6 unit tests yet still reported PASS on the real stale copy, because the fixture flattened a two-level layout. Verify a new check against the live system.
- 2026-08-28 · gotcha · [0.5] · `claude-code,skills,install,handoff:2026-08-28-silent-pass-defects` — Never leave a backup directory inside ~/.claude/skills/ — it registers immediately as a duplicate skill. Move install backups outside the skills tree.
- 2026-08-28 · gotcha · [0.7] · `bash,hmad-dispatch,sourcing,handoff:2026-08-28-silent-pass-defects` — hmad-dispatch.sh cannot be sourced whole: unguarded main "$@" plus set -euo pipefail dispatches the empty verb and exits the SOURCING shell before any helper is reachable; || true does not save it. Strip main "$@" first.
- 2026-08-28 · gotcha · [0.7] · `mutation-testing,h-mad,handoff:2026-08-28-silent-pass-defects` — A red test can disable a whole mutation spec, not just an assertion: if the spec's baseline command includes that test file, the harness returns BASELINE_NOT_GREEN and every mutation goes unrun. Check specs before dismissing a failure.
- 2026-08-28 · gotcha · [0.7] · `verdict-tokens,cross-repo,hooks,handoff:2026-08-28-silent-pass-defects` — Adding a NEW verdict word is silently non-blocking when the consumer scores verdicts with an ordered substring case whose default arm allows. Fold into an existing blocking verdict, or ship both sides in lockstep.
- 2026-08-28 · solution · [0.7] · `claude-code,todo-tools,handoff:2026-08-28-silent-pass-defects` — CLAUDE_CODE_ENABLE_TODO_TOOLS applies MID-SESSION, not at launch: writing it to ~/.claude/settings.json surfaced TaskCreate/TaskList as deferred tools in the same conversation. Re-probe immediately, do not defer to a restart.
- 2026-08-28 · gotcha · [0.7] · `install,skills,symlink,handoff:2026-08-28-silent-pass-defects` — A correct symlink for one installed skill vouches for nothing about its siblings: h-mad was linked while handoff was a 68-day-old copy, so a committed fix was invisible at runtime and install-check still said PASS.
- 2026-08-28 · gotcha · [0.7] · `testing,fixtures,mutation-testing,handoff:2026-08-28-monitoring-backlog-drained` — A tidy fixture can make a mutation survive: a prompt-echo line starting with 'emit ASSESSMENT:' never matched the line-anchored regex, giving one match where first-vs-last needed two. Hostile fixtures, or the test proves nothing.
- 2026-08-28 · gotcha · [0.7] · `shell,race,tempfile,handoff:2026-08-28-monitoring-backlog-drained` — A temp path keyed on $$ collides when one process writes the same output twice (audit-cycle writes --out per pass): two writes shared a temp and one report vanished. Use mktemp. Only the FULL suite caught it.
- 2026-08-28 · pattern · [0.7] · `atomicity,mutation-testing,testing,handoff:2026-08-28-monitoring-backlog-drained` — Atomicity has no externally observable trace that cp+rm does not also produce - content lands, no temp remains, either way. A mutation swapping mv for cp;rm left every behavioural assertion green. Assert the mechanism structurally.
- 2026-08-28 · gotcha · [0.7] · `h-mad,verdict-tokens,guards,handoff:2026-08-28-monitoring-backlog-drained` — Adding a verdict word to a token contract silently un-guards its consumers: a new ANCHORS_UNREADABLE made a pre-push hook matching only *ANCHORS_DRIFTED* fall to its catch-all and ALLOW the push. Grep every consumer before adding a token.
- 2026-08-27 · gotcha · [0.7] · `zsh,shell,handoff:2026-08-27-anchor-precheck-shipped` — zsh does NOT word-split unquoted parameter expansions (only command substitution). SPECS=$(find ...); cmd $SPECS passes ONE argument, so a 17-spec sweep silently reports specs=1.
- 2026-08-27 · gotcha · [0.5] · `codex,dispatch,counts,handoff:2026-08-27-anchor-precheck-shipped` — A dispatch report can be right about its actions and wrong about its numbers: one stated mutations=238 where the tree held 244, every spec byte-identical, with all its other figures correct. Re-derive counts.
- 2026-08-27 · gotcha · [0.7] · `verification,premise-check,handoff:2026-08-27-anchor-precheck-shipped` — Proving a null needs a positive control. A grep returning nothing looks identical to a grep against the wrong file - run a query you KNOW hits before reporting absence as evidence.
- 2026-08-27 · pattern · [0.9] · `h-mad,audit,6a-prime,handoff:2026-08-27-anchor-precheck-shipped` — One clean audit cycle is not a stopping signal - measured: 6a-prime cycle 3 returned READY_TO_MERGE and cycle 4 then found a Critical vacuous pass. Sequence was 3,2,CLEAN,3,2,2,CLEAN findings.
- 2026-08-27 · gotcha · [0.7] · `h-mad,mutation,wire-pin,handoff:2026-08-27-anchor-precheck-shipped` — A fix can silently hollow an assertion written cycles earlier: changing a returned dict's SHAPE made a WIRE-PIN's 'count keys absent' true by construction. Re-verify pins after any change to what they assert on.
- 2026-08-26 · gotcha · [0.7] · `h-mad,tdd,test-discrimination,handoff:2026-08-26-phase5d-task1-red` — RED tests can match the stated failing/passing counts and still assert the wrong thing: source.count(sym)==N passes if one caller invokes twice and the other never does. Assert the call form per entry point, not an occurrence count.
- 2026-08-26 · gotcha · [0.7] · `h-mad,version-history,tooling,handoff:2026-08-26-phase5d-task1-red` — Never bump a version-history in the same command as the edit it describes: a drifted .replace() anchor wrote nothing and raised, while h_mad_version_history.py ran anyway and logged a fix the doc did not contain.
- 2026-08-26 · gotcha · [0.7] · `h-mad,audit,effort,dogfood,handoff:2026-08-26-phase5d-task1-red` — h-mad audit Effort block: low-evidence keys on TOOL COUNT, which discriminates nothing when the prompt inlines everything (1-2 calls is the floor). Weight THINKING instead: <6k found 0 defects, 8.8k-14.7k found all of them.
- 2026-08-26 · gotcha · [0.7] · `cli,reporting,grep,handoff:2026-08-26-loop-drain-five-tools` — An excerpt truncated from the START of a line may not contain the thing being searched for: 160 chars of a 900-char JSON log line omitted the identifier entirely. Centre the excerpt on the match.
- 2026-08-26 · pattern · [0.7] · `backlog,measurement,handoff:2026-08-26-loop-drain-five-tools` — Re-measure a backlog row's premise before building it. In one session that changed the work three ways: build (19/20 plans affected), do-not-build (below the file's own promotion bar), and no-new-vocabulary (20/23 rows already resolved it).
- 2026-08-26 · gotcha · [0.7] · `testing,fixtures,doc-lint,handoff:2026-08-26-loop-drain-five-tools` — Pin a doc-parsing guard against the REAL file, not a fixture: a TRIAGE regex built from the tight form matched 2 of 22 rows and called the other 20 unqualified. A fixture written from that same tight form is green on the bug.
- 2026-08-25 · gotcha · [0.7] · `parsers,census,guards,handoff:2026-08-25-candidate-backlog-drain` — A coverage guard that hardcodes the shape it knows filters the rows it missed out of its own denominator: it reports clean while dropping them. Count with a LOOSER pattern than the reader uses.
- 2026-08-25 · pattern · [0.7] · `destructive-tools,orca,panes,handoff:2026-08-25-candidate-backlog-drain` — Identifying targets by subtraction (live minus baseline minus self) cannot tell your process's artifacts from the operator's. Require POSITIVE identification; report the rest as unidentified rather than acting on them.
- 2026-08-25 · gotcha · [0.9] · `mutation-testing,pytest,handoff:2026-08-25-candidate-backlog-drain` — MUTATION ALL_CAUGHT is satisfied by mutants that never ran: a collection error exits 2 before the assertion, and pytest.mark.skip exits 0 so a pre-check calls a DISABLED test green. Score on the summary, not the exit code.
- 2026-08-25 · gotcha · [0.9] · `mutation-testing,pytest,bytecode,handoff:2026-08-25-candidate-backlog-drain` — A byte-size-identical mutation applied inside the same filesystem-mtime second reuses the stale .pyc and never executes, while the file on disk IS mutated so did-it-land passes. Reports as survived. 4/6 trials.
- 2026-08-25 · gotcha · [0.9] · `agy,dispatch,orca,handoff:2026-08-25-candidate-backlog-drain` — hmad-dispatch exec agy … & inside an already-backgrounded shell kills the dispatch: parent exits, agy dies after init, exit 0. Reads as 'agy is down'. Background the BLOCKING form; never add & inside it.
- 2026-08-25 · gotcha · [0.9] · `h-mad,hooks,dispatch,handoff:2026-08-25-timeout-premise-and-audit-cycle-dispatch` — A Claude Code PreToolUse hook cannot see dispatched agents. hmad-dispatch exec launches agy/codex as direct child processes and neither reads ~/.claude/settings.json, so hooks only ever observe the orchestrator's own tool calls.
- 2026-08-25 · solution · [0.7] · `h-mad,mocked-seams,tests,handoff:2026-08-25-timeout-premise-and-audit-cycle-dispatch` — Fixing a mocked SEAM can beat writing a new test: once both audit-cycle stub assemblers matched production's report-path cardinality, reverting the guard failed 13 EXISTING tests. The coverage existed and was aimed at the wrong shape.
- 2026-08-25 · gotcha · [0.9] · `h-mad,verification,observability,handoff:2026-08-25-portable-timeout-run-verb` — Exit code cannot distinguish a compliant time bound from an improvised one: 'hmad-dispatch run --timeout 5' and 'timeout 5' BOTH returned 124 on a coreutils box. Only the command form in the log discriminates.
- 2026-08-25 · solution · [0.7] · `h-mad,dispatch,rules,handoff:2026-08-25-portable-timeout-run-verb` — A/B on a dispatched agy agent: with h-mad's time-bound rule it ran 'hmad-dispatch run --timeout 5'; without it, it probed 'which timeout' then improvised 'timeout 5'. The rule is causally effective.
- 2026-08-25 · gotcha · [0.7] · `h-mad,rules,prompting,handoff:2026-08-25-portable-timeout-run-verb` — Agents reason from a local probe to an exemption: an unruled agy agent ran 'which timeout' FIRST, saw /opt/homebrew/bin/timeout, and used it. Rules about environment must preempt the probe explicitly, not just state the prohibition.
- 2026-08-25 · solution · [0.7] · `h-mad,mutation,handoff:2026-08-25-portable-timeout-run-verb` — A mutation anchor reaching into trailing context breaks when an unrelated edit lands between. Keep anchors self-contained and assert uniqueness — h-mad's verb-helper-force anchor swallowed the next loop header and stopped matching.
- 2026-08-25 · gotcha · [0.7] · `h-mad,tests,mocked-seams,handoff:2026-08-25-portable-timeout-run-verb` — Grep for DUPLICATE fakes before trusting a stub fix. h-mad's audit-cycle tests carry two copies of the fake assembler; the _cmd_exec trace harness uses the second, so aligning only the first changed nothing and looked like failure.
- 2026-08-25 · gotcha · [0.9] · `h-mad,mocked-seams,handoff:2026-08-25-portable-timeout-run-verb` — Calibrate guards on production output, not a stub. audit-cycle hardcoded 'diff == 2 lines' because the fake assembler wrote the report path once; the real one writes it twice, so the verb never dispatched at its default --passes 2.
- 2026-08-25 · gotcha · [0.9] · `h-mad,guards,mutation,handoff:2026-08-25-portable-timeout-run-verb` — Mutation coverage proves only the REJECT direction. audit-cycle's divergence guard was fully mutation-covered and still rejected every real prompt — a guard that rejects everything kills every mutant. Test that guards ACCEPT valid input.
- 2026-08-25 · solution · [0.7] · `h-mad,hooks,verification,handoff:2026-08-25-portable-timeout-run-verb` — To prove a hook FIRED rather than that its text surfaced, look for a side effect it only performs when emitting — h-mad-advisor-warn writes $TMPDIR/h-mad-advisor-warn.<session_id>.stamp. Note TMPDIR is /var/folders/... on macOS, not /tmp.
- 2026-08-25 · gotcha · [0.7] · `h-mad,hooks,headless,measurement,handoff:2026-08-25-portable-timeout-run-verb` — A PostToolUse hook's additionalContext never appears in claude -p --output-format stream-json stdout; it is injected into the model's context. Grep the SESSION TRANSCRIPT (~/.claude/projects/<slug>/<session_id>.jsonl), not stdout.
- 2026-08-25 · gotcha · [0.7] · `h-mad,rules,portability,handoff:2026-08-25-portable-timeout-run-verb` — Never justify a prohibition by what a box lacks. 'macOS ships neither timeout nor gtimeout' went false the day coreutils was installed — a reason refutable in one command is a rule agents discount. Ground it on portability.
- 2026-08-25 · gotcha · [0.5] · `h-mad,agy,dispatch,handoff:2026-08-25-portable-timeout-run-verb` — agy audit passes WRITE into the repo: three test_*.py scratch files appeared in h-mad/ after four 'read-only' review dispatches. Check git status --short before staging; a stray test_*.py in a package root also perturbs pytest collection.
- 2026-08-25 · solution · [0.7] · `h-mad,guards,markdown,handoff:2026-08-25-portable-timeout-run-verb` — Scan doc-lints on whitespace-collapsed, markdown-stripped text. A line-scoped guard false-fired on 'stock \n macOS ships neither' and '**stock** macOS' — letting paragraph reflow and emphasis decide whether a guard fires.
- 2026-08-25 · solution · [0.7] · `h-mad,guards,regex,handoff:2026-08-25-portable-timeout-run-verb` — In a doc-lint, a hedge must ADJOIN its claim, not sit nearby. A 90-char proximity window accepted 'macOS ships neither' because an unrelated 'system component' sat 40 chars upstream: it passed the very text it existed to reject.
- 2026-08-25 · gotcha · [0.7] · `h-mad,guards,observability,handoff:2026-08-25-portable-timeout-run-verb` — Installing a missing tool can DELETE a forcing function. Improvised 'timeout 30 cmd' used to die at 127 loudly; with coreutils it silently works and fails only elsewhere. The error WAS the detector — removing it is a regression.
- 2026-08-25 · gotcha · [0.5] · `shell,macos,eza,handoff:2026-08-25-portable-timeout-run-verb` — ls is shimmed to eza on this machine: -t takes a --time FIELD value, so 'ls -t <dir>' errors with 'invalid value ... for --time <FIELD>' instead of sorting by mtime. Use find or handoff_paths.py latest.
- 2026-08-25 · pattern · [0.7] · `h-mad,docs,guards,handoff:2026-08-25-portable-timeout-run-verb` — A prohibition with no replacement reproduces the failure it forbids. Every site banning a command must name the exact substitute, and the test should assert the SUBSTITUTE is present, not only that the banned form is absent.
- 2026-08-25 · pattern · [0.7] · `h-mad,api-design,dispatch,handoff:2026-08-25-portable-timeout-run-verb` — A private helper is not a surface: h-mad's _exec_run watchdog was correct for months but had no verb, so agents improvised timeout instead. Capability with no callable name is capability nobody has.
- 2026-08-25 · gotcha · [0.7] · `h-mad,macos,timeout,dispatch,handoff:2026-08-25-portable-timeout-run-verb` — macOS has no timeout/gtimeout. The 127 is not the hazard — the measured reflex is to re-run the command UNBOUNDED, turning a deadline into a hang, and a hang is byte-identical to slow work in a log. Use hmad-dispatch run --timeout.
- 2026-08-24 · pattern · [0.7] · `handoff,triage,handover,handoff:2026-08-24-registry-zero-j44-j49` — When a parked item is small and checkable, CHECKING it is the routing decision. Falsifying one unverified finding cost 3 greps; handing it over would have cost a brief, claim check, worktree stamp and delivery — to transfer something false.
- 2026-08-24 · pattern · [0.7] · `h-mad,audit,j49,handoff:2026-08-24-registry-zero-j44-j49` — Derive a 'did it do real work' marker from the delivery CONTRACT, not from tool names: the report-file path costs 2 successful calls, so ok<=2 means nothing was read. Naming tools re-creates the false zero when the agent switches tool.
- 2026-08-24 · pattern · [0.7] · `mutation-testing,h-mad,handoff:2026-08-24-registry-zero-j44-j49` — Drop mutants proven EQUIVALENT rather than keeping them: a spec that retains them reports coverage it does not have. `set -euo` was the central defect in a blocking gate and inert in the advisory that replaced it.
- 2026-08-24 · solution · [0.9] · `hooks,claude-code,advisor,j44,handoff:2026-08-24-registry-zero-j44-j49` — Claude Code's `advisor` is a server_tool_use run server-side, so it never reaches local tool dispatch and NO tool-scoped hook event can attach. No matcher string fixes it; put advisory logic on PostToolUse.
- 2026-08-24 · pattern · [0.7] · `hooks,claude-code,debugging,transcript,handoff:2026-08-24-registry-zero-j44-j49` — To test whether the harness treats a tool as ordinary, count block TYPES in the session JSONL: hooked tools appear as `tool_use`, server-side ones as `server_tool_use`. Cheap, in-situ, and beats grepping the binary.
- 2026-08-24 · gotcha · [0.7] · `hooks,claude-code,settings,handoff:2026-08-24-registry-zero-j44-j49` — Hooks are NOT snapshotted at session start on Claude Code 2.1.241: a PostToolUse hook added to settings.json mid-session fired ~13 min later in the SAME session. Verify a hook fires; never assume a relaunch is or isn't needed.
- 2026-08-24 · pattern · [0.7] · `testing,assertions,handoff:2026-08-24-j30-closed-advisor-gate-never-fires` — all()/not any() over a collection the code under test PRODUCES is vacuous when empty, so 'produced nothing' scores as 'produced nothing bad'. Pin its size first. No mutant finds this; reading the assertion does.
- 2026-08-24 · solution · [0.7] · `hooks,claude-code,advisor,j44,handoff:2026-08-24-j30-closed-advisor-gate-never-fires` — A PreToolUse hook that allows at the default threshold is indistinguishable from one that never runs. Prove routing by instrumenting the hook to write a marker on entry (line 1, above every early-return) and self-test the detector first.
- 2026-08-24 · gotcha · [0.7] · `testing,mutation,h-mad,handoff:2026-08-24-j30-closed-advisor-gate-never-fires` — Mutation harness ALL_CAUGHT is not evidence: apply each mutant by hand and read WHICH assertion died. 2 of 3 'caught' mutants died on an unbound variable and tripped assert returncode==0, never reaching the property under test.
- 2026-08-24 · gotcha · [0.7] · `hooks,claude-code,h-mad,testing,handoff:2026-08-24-j30-closed-advisor-gate-never-fires` — A hook's REGISTRATION and launch env are snapshotted at session start, but the hook FILE is read at every invocation — so it can be instrumented mid-session. Three handoffs deferred a routing test for a relaunch that was never required.
- 2026-08-22 · solution · [0.7] · `h-mad,agy,exec,report-file,recovery,j30,handoff:2026-08-22-j30-offcontract-locator` — agy can write a real audit report off-contract: a workspace dotfile (.design.audit.v14.md) or ~/.gemini/antigravity-cli/scratch/. Search both, dotfiles included, no audit.vN stem, BEFORE re-dispatching — h_mad_offcontract_scan.py.
- 2026-08-22 · gotcha · [0.7] · `h-mad,dispatch,exec,prompt-size,agy,j30,handoff:2026-08-22-j30-offcontract-locator` — exec agy honours --report-file+sentinel 8/8 at 266,342B (agy 1.1.18, stream-json); J30's 5/5 drop at that size did NOT reproduce. A size-gated repro can dissolve when the transport changes — re-probe before building a ceiling for it.
- 2026-08-22 · pattern · [0.7] · `h-mad,phase7,handoff:2026-08-22-audit-cycle-verb-shipped-j-sweep` — Phase 7 is a detector, not a formality: it runs the reporter against real filenames and MOVES files. Two defects this session lived outside any diff, so the diff-scoped architectural review was structurally blind to both.
- 2026-08-22 · gotcha · [0.9] · `zsh,shell,handoff:2026-08-22-audit-cycle-verb-shipped-j-sweep` — zsh does not word-split unquoted expansions. 'set -- $spec' in a loop and $PATHS as a git pathspec both silently pass one string, so a flag gets nothing and a diff reads as empty. Quote-and-split explicitly or use arrays.
- 2026-08-22 · gotcha · [0.9] · `agy,review,evidence,handoff:2026-08-22-audit-cycle-verb-shipped-j-sweep` — extract_verdict closes SILENCE, not FLUENCY. A headless review whose only tool call errored returned ASSESSMENT: READY_TO_MERGE in 1510 confident bytes. Gate on >=1 successful tool call; count any tool, never a hardcoded name list.
- 2026-08-22 · gotcha · [0.9] · `testing,fixtures,false-coverage,handoff:2026-08-22-audit-cycle-verb-shipped-j-sweep` — A test fixture's default can masquerade as the program's. dispatch_args(passes='2') emitted the flag into all 44 call sites, so 1560 tests could not see the verb had no default and spec AC-3.1 was unimplemented.
- 2026-08-22 · gotcha · [0.9] · `pytest,coverage,archive,handoff:2026-08-22-audit-cycle-verb-shipped-j-sweep` — An empty pytest parametrize SKIPS, it does not fail. A corpus globbing only live dirs is guaranteed to empty at archive time, retiring the test as one 's' among 1580 dots. Assert the corpus is non-empty.
- 2026-08-22 · gotcha · [0.9] · `telemetry,silent-failure,handoff:2026-08-22-audit-cycle-verb-shipped-j-sweep` — A silent zero is a claim about the WORK; an error is a claim about the TOOL. A counter reporting audit_cycles=0 for a 48-cycle feature survived because nothing looked broken — it read as 'no audits were run'.
- 2026-08-21 · pattern · [0.7] · `handoff,ux,handoff:2026-08-21-phase5-complete-todo-tool-optin` — Durability and visibility are different properties of a todo sink. .omc/notepad.md survives /clear and is invisible, so 'Todos restored: 6' is truthful and still reads as 'my todos disappeared'.
- 2026-08-21 · gotcha · [0.7] · `git,gitignore,handoff:2026-08-21-phase5-complete-todo-tool-optin` — gitignore: a negation cannot re-include a file whose PARENT DIRECTORY is excluded. .h-mad/ must become .h-mad/* before !.h-mad/wires.jsonl does anything; the one-line form silently no-ops.
- 2026-08-21 · gotcha · [0.7] · `git,gitignore,handoff:2026-08-21-phase5-complete-todo-tool-optin` — git check-ignore -v exits 0 when a file matches ANY rule INCLUDING a negation, so it reads as 'ignored' when the file is not. git status showing ?? is the authoritative signal.
- 2026-08-21 · solution · [0.9] · `claude-code,todo-tools,handoff:2026-08-21-phase5-complete-todo-tool-optin` — Claude Code 2.1.236 made the todo tools OPT-IN, not removed. Set env CLAUDE_CODE_ENABLE_TODO_TOOLS=1 in settings.json. todoFeatureEnabled is a DIFFERENT switch (TUI panel) and enabling only it leaves tools absent.
- 2026-08-21 · pattern · [0.9] · `testing,seams,handoff:2026-08-21-phase5-complete-todo-tool-optin` — Two unit tests disagreeing about a TYPE at a seam, with no test composing them, means the seam is where the bug lives. collect() asserted node ids, partition() was fed bare names; 60 green tests encoded the defect.
- 2026-08-21 · gotcha · [0.9] · `mutation-testing,h-mad,handoff:2026-08-21-phase5-complete-todo-tool-optin` — A mutation can fail the RIGHT test for the WRONG reason. MUTATION: ALL_CAUGHT refused=0 and per-row isolation are both blind to it — 4 of 12 were strawmen. Ask per row WHY the test failed, not whether it did.
- 2026-08-21 · gotcha · [0.7] · `handoff,docs,handoff:2026-08-20-feature-audit-cycle-verb-phase5-task1` — A handoff written before its own push states the push as PENDING and nobody corrects it after — the doc's last claim is false the moment the finale runs. Re-read the doc's git claims AFTER pushing, not before.
- 2026-08-21 · gotcha · [0.7] · `h-mad,dispatch,git,handoff:2026-08-20-feature-audit-cycle-verb-phase5-task1` — Never run a workspace-write codex dispatch concurrently with a job writing UNTRACKED artifacts into the same tree. Verify gating evidence is on disk before committing: clean git status + absent artifacts looks like nothing-to-commit.
- 2026-08-21 · gotcha · [0.9] · `verification,logs,handoff:2026-08-20-feature-audit-cycle-verb-phase5-task1` — A filename in a log is not evidence anything touched it: pytest parametrization IDs embed real paths, and grep -c counts your own prompt text. Refute a destructive-cause hypothesis empirically before acting on it.
- 2026-08-21 · gotcha · [0.9] · `h-mad,guards,handoff:2026-08-20-feature-audit-cycle-verb-phase5-task1` — A write-then-verify guard is blind if the destination is not cleared: a stale file makes exists()/st_size>0 True on the OLD file, so a silently-failed write scores the stale artifact. unlink(missing_ok=True) first.
- 2026-08-21 · gotcha · [0.9] · `mutation-testing,tdd,handoff:2026-08-20-feature-audit-cycle-verb-phase5-task1` — A guard's negative test must fail for the GUARD's reason, not merely fail. A read-only dir tests a write that raises BEFORE the guard, so deleting the guard still crashes and the mutation survives while looking caught.
- 2026-08-21 · gotcha · [0.9] · `h-mad,audit,design,handoff:2026-08-20-feature-audit-cycle-verb-phase5-task1` — An unaudited edit to a gated doc is an ungated doc. A re-audit of 'errata only' changes found 9 issues in 7 cycles — FOUR introduced by the edits fixing the previous cycle. Re-gate after any edit, including a one-word nit fix.
- 2026-08-20 · gotcha · [0.7] · `h-mad,agy,verdict,handoff:2026-08-20-feature-audit-cycle-verb-phase5-task1` — Demand a review agent's verdict token unbolded on its own line: h_mad_extract_verdict.py rejects **VERDICT: COMPLIANT** (key must start the line), demoting the extractor to something a human overrides by eye.
- 2026-08-20 · gotcha · [0.9] · `tdd,revert-test,git,h-mad,handoff:2026-08-20-feature-audit-cycle-verb-phase5-task1` — git add -N + git stash push reverts NOTHING on an untracked file: exit 0, file present, suite still green — reads as a PASSING revert test over un-reverted code. Use physical mv; assert absence.
- 2026-08-20 · pattern · [0.7] · `testing,design,h-mad,handoff:2026-08-20-feature-audit-cycle-verb-phase5-task1` — Tests written after an implementation inherit its assumptions and can only confirm it. Several tests asserted the verdict line — they asserted what the code emitted, not the design's documented shape, so four format defects survived.
- 2026-08-20 · gotcha · [0.9] · `mutation-testing,h-mad,handoff:2026-08-20-feature-audit-cycle-verb-phase5-task1` — An unlanded mutation plus a green suite reads as 'connection enforced'. Assert the anchor matches EXACTLY ONCE and refuse otherwise; a non-matching regex prints an empty failing set, which looks like a wire that is not enforced.
- 2026-08-20 · gotcha · [0.7] · `shell,grep,pytest,verification,handoff:2026-08-20-feature-audit-cycle-verb-phase5-task1` — grep -c exits 1 on zero matches, so a guard whose success is 'no matches' reports the command as failed. And grep -qx never matches a pytest FAILED line — pytest appends ' - AssertionError...'; that printed a false 'wire NOT enforced'.
- 2026-08-20 · gotcha · [0.9] · `testing,stubs,h-mad,handoff:2026-08-20-feature-audit-cycle-verb-phase5-task1` — A stubbed sibling binary hides type mismatches: --grace float vs the real --timeout int crashed EVERY production wait while 10 stubbed tests stayed green. Any test whose subject is the real binary's behaviour must unset the stub dir.
- 2026-08-20 · gotcha · [0.9] · `testing,review,h-mad,handoff:2026-08-20-feature-audit-cycle-verb-phase5-task1` — A constant standing in for a real input is invisible to a green suite, because no test supplies that input. Seen twice in one feature: combine([]) and cycle=1. Ask a reviewer to hunt that CLASS by name, not for 'any issues'.
- 2026-08-20 · gotcha · [0.9] · `codex,h-mad,dispatch,handoff:2026-08-20-feature-audit-cycle-verb-phase5-task1` — codex config default gpt-5.6-luna cannot execute ANY tool: every shell/patch call dies on 'timed out negotiating with the code-mode host', even true. It still writes prose, so it returns a well-formed STATUS: BLOCKED. Pass --model gpt-5.5.
- 2026-08-20 · gotcha · [0.7] · `h-mad,exec,dispatch,j29,handoff:2026-08-20-audit-cycle-verb-phases-3-4` — hmad-dispatch exec on a shared --out is FIRST-writer-wins with an explicit refusal, not last-writer-wins: the second dispatch's --out holds the FIRST one's report. Plausible and wrong beats empty. Template paths per dispatch.
- 2026-08-20 · gotcha · [0.9] · `h-mad,audit-gate,union,handoff:2026-08-20-audit-cycle-verb-phases-3-4` — Concatenating two audit reports under-counts: measured must=1 against a true 2. The gate's prose fall-back fires only when a section has NO bullets, so a prose finding beside a bulleted one vanishes. Gate each separately.
- 2026-08-20 · gotcha · [0.9] · `h-mad,audit-gate,cannot-judge,handoff:2026-08-20-audit-cycle-verb-phases-3-4` — h_mad_audit_gate.py GATE: INVALID prints must=0 should=0 it never measured, at exit 2. Key on the verdict word, never the counts, or a header-less report scores as a clean pass. Either missing section triggers it.
- 2026-08-20 · gotcha · [0.7] · `testing,mocking,python,subprocess,handoff:2026-08-20-audit-cycle-verb-phases-3-4` — Stubbing a subprocess via PATH is a silent no-op when the caller resolves it __file__-relative: the absolute path bypasses PATH, so every mocked test runs the real script behind a green suite. Use a dir-override env var.
- 2026-08-20 · gotcha · [0.7] · `h-mad,audit,review,handoff:2026-08-20-audit-cycle-verb-phases-3-4` — Audit findings arrive independently but their FIXES interact: 3x here, two individually-correct prescriptions composed into a defect neither described. Reconcile a cycle's findings against each other before applying any.
- 2026-08-20 · pattern · [0.9] · `h-mad,audit,agy,two-pass,handoff:2026-08-20-audit-cycle-verb-phases-3-4` — Two independent agy audit passes agreed on 4 of 79 findings over 27 cycles, and from cycle 8 on every cycle had one pass gate clean while the other found real defects, alternating sides. Single-pass gating ships defects.
- 2026-08-20 · gotcha · [0.7] · `docs,editing,handoff:2026-08-20-advisor-context-budget-and-hook-wiring` — Replacing a doc slice between two anchors silently deletes any subsection that sat between them; the edit reports success. Only a docs test that asserts the inner section's content catches it. Anchor on the section you mean.
- 2026-08-20 · pattern · [0.7] · `claude-code,settings,hooks,handoff:2026-08-20-advisor-context-budget-and-hook-wiring` — Claude Code settings resolution: project .claude/settings*.json is found by walking UP from cwd, and CLAUDE_CONFIG_DIR relocates the user scope off ~/.claude. Checking only cwd+~/.claude reports a wired machine as unwired.
- 2026-08-20 · solution · [0.7] · `claude-code,transcript,handoff:2026-08-20-advisor-context-budget-and-hook-wiring` — Claude Code transcript lookup: the ~/.claude/projects slug is the SESSION's project root, not the process cwd, and newest-mtime picks another open session. Resolve by CLAUDE_CODE_SESSION_ID; skip isSidechain lines.
- 2026-08-20 · gotcha · [0.7] · `claude-code,hooks,fail-open,handoff:2026-08-20-advisor-context-budget-and-hook-wiring` — PreToolUse hook exit 2 means BLOCK. A hook with 'set -e' that shells out to a checker exiting 2 on cannot-judge turns 'could not measure' into 'denied'. Use 'set -uo pipefail' and branch on the verdict token, never the rc.
- 2026-08-20 · gotcha · [0.9] · `advisor,context-budget,handoff:2026-08-20-advisor-context-budget-and-hook-wiring` — advisor() takes NO parameters — the payload IS the whole transcript, billed into the same turn (~2.0x current context). Only levers: shrink the transcript or don't call. 'Send it less' is impossible.
- 2026-08-19 · pattern · [0.7] · `testing,stubs,mutation-testing,handoff:2026-08-19-headless-dispatch-visibility` — A test stub with a STATIC world-state cannot express 'alive when the wait began, dead before it ended' — the only path a give-up/timeout guard covers. Let the stub read state from a file the test rewrites mid-run.
- 2026-08-19 · pattern · [0.9] · `editing,tooling,self-defect,handoff:2026-08-19-headless-dispatch-visibility` — A multi-edit script that writes the file once at the END loses its already-SUCCESSFUL edits when a later one raises. The traceback names the only edit you don't need told about. One edit, one assert, one write.
- 2026-08-19 · gotcha · [0.7] · `orca,zsh,osc-title,handoff:2026-08-19-headless-dispatch-visibility` — A tab title cannot mark terminal state: zsh emits an OSC title sequence at every prompt, so a pane renamed via 'orca terminal rename' reverts to the cwd the moment it reaches a prompt. Put pane state in files.
- 2026-08-19 · gotcha · [0.9] · `agy,streaming,observability,h-mad,handoff:2026-08-19-headless-dispatch-visibility` — agy --print (text) emits NOTHING until the turn completes, so a --log fed from it is 0 bytes for the whole run. --output-format stream-json is the only live channel; --log-file is language-server noise with no step info.
- 2026-08-19 · gotcha · [0.9] · `orca,terminal,exit-code,h-mad,handoff:2026-08-19-headless-dispatch-visibility` — orca terminal wait --for exit does NOT carry the command's exit code: a pane running 'sleep 2; exit 9' reports exitCode 0. Anything gating on it reads every failure as success. Capture rc from the command itself.
- 2026-08-19 · gotcha · [0.9] · `shell,skill-authoring,claude-code,handoff` — Shell vars do not survive between Bash tool calls, so a doc's later code block sees them EMPTY. Worst case is not an error: empty ROOT failed an equality test and routed to a branch that skips committing on purpose.
- 2026-08-19 · gotcha · [0.9] · `bash,zsh,glob,handoff,silent-failure` — bash ships globstar OFF, so <dir>/**/x matches ONE level and a real depth-2 file is NOT found — a locator then reports 'nothing here' with no error. zsh matches but errors past 2>/dev/null on no-match. Use find.
- 2026-08-19 · gotcha · [0.9] · `bkit,pdca,h-mad,doc-templates` — bkit isPlanPlus reclassifies a plan on a case-SENSITIVE substring of its CONTENT (plan-plus, Plan-Plus, Plan Plus, Brainstorming-Enhanced, Intent Discovery), demanding 13 sections h-mad lacks. A compliant template is not a compliant doc.
- 2026-08-19 · gotcha · [0.9] · `pytest,testing,self-inflicted,h-mad` — A module-level def appended after a test class silently ends that class: methods below it become nested and stop being collected. Suite still reports green — I lost 10 tests this way. Assert the collected COUNT.
- 2026-08-09 · gotcha · [0.7] · `zsh,shell,globbing,verification,handoff:2026-08-09-main__j29-out-clobber-guard` — zsh nomatch aborts the WHOLE command when ANY glob misses: 'ls -d .venv venv */.venv */venv' listed nothing and read as 'no venv exists'. Empty output means one pattern missed. Use bash -c.
- 2026-08-09 · gotcha · [0.5] · `h-mad,hmad-dispatch,monitoring-log,j29,session:2026-08-09` — J29 own remedy was wrong: refuse an --out that CHANGED since dispatch start, not one merely non-empty — the documented no_verdict recovery re-dispatches to the SAME templated path.
- 2026-08-09 · pattern · [0.7] · `h-mad,invariants,prompt-size,handoff:2026-08-09-main__guard-patches-and-h-mad-install-gate` — Adding a rule to a file inlined verbatim into every prompt has a global byte cost. Recalibrate the size fixture rather than widen the band - the band is the assertion
- 2026-08-09 · gotcha · [0.7] · `git,stale-clone,commit-hygiene,handoff:2026-08-09-main__guard-patches-and-h-mad-install-gate` — Verify commit-message claims against the TARGET commit, not the base your clone has. A 1519-commit-stale clone made an importer list in a commit message silently wrong
- 2026-08-09 · pattern · [0.7] · `review,testing,mutation,handoff:2026-08-09-main__guard-patches-and-h-mad-install-gate` — Mutation-test a review finding that claims a test is non-discriminating - it decides which claims are real. Of 3 alleged word-matching doc-tests, 2 bit and only 1 did not
- 2026-08-09 · solution · [0.9] · `plugins,patching,durability,handoff:2026-08-09-main__h-mad-symlink-install-repair` — Patching a plugin in ~/.claude/plugins/cache is volatile: keep the diff plus a red-green tested verify script in the repo, and confirm patch -p1 recovery reproduces a byte-identical file
- 2026-08-09 · pattern · [0.9] · `hooks,security,scanners,handoff:2026-08-09-main__h-mad-symlink-install-repair` — Both plugin guards I hit were substring matchers on raw text with no syntax awareness. When a scanner blocks rather than warns, a substring false positive stops real work - prefer anchored regexes
- 2026-08-09 · gotcha · [0.7] · `hooks,claude-code,false-positive,handoff:2026-08-09-main__h-mad-symlink-install-repair` — security-guidance's security_reminder_hook.py blocks on bare substring 'exec(' so regex .exec() trips it, but only ONCE per file+rule per session - an immediate retry succeeds
- 2026-08-09 · pattern · [0.9] · `bash,security,parsing,handoff:2026-08-09-main__h-mad-symlink-install-repair` — A quoted heredoc tag (<<'TAG') disables ALL expansion in the body - a bash guarantee, not a heuristic. That makes it a safe basis for narrowing a scanner, unlike parsing quotes
- 2026-08-09 · solution · [0.9] · `bkit,hooks,patch,enh310,handoff:2026-08-09-main__h-mad-symlink-install-repair` — bkit ENH-310 false positive FIXED locally (bkit 2.1.19): quoted-tag heredoc bodies now excised for the sub vector. Re-run docs/patches/bkit-enh310-quoted-heredoc-body/verify.js after any bkit update
- 2026-08-09 · pattern · [0.7] · `hooks,claude-code,blast-radius,handoff:2026-08-09-main__h-mad-symlink-install-repair` — settings.json PreToolUse hooks fire in EVERY project, so a repo-scoped find under-measures blast radius when a hook's state-resolution changes - sweep all project roots
- 2026-08-09 · pattern · [0.7] · `verification,hooks,claude-code,handoff:2026-08-09-main__h-mad-symlink-install-repair` — A green test suite can't confirm a hook-registration change if the tests invoke the hook directly and never read settings.json - verify the arming surface with a real tool call
- 2026-08-09 · gotcha · [0.5] · `handoff-skill,learnings,handoff:2026-08-09-main__h-mad-symlink-install-repair` — learn.py rejects patterns >200 chars and exits non-zero, but entries earlier in the same batch already wrote - a tripped batch lands partially; re-run only the rejected entry
- 2026-08-09 · gotcha · [0.7] · `bkit,hooks,bash,handoff:2026-08-09-main__h-mad-symlink-install-repair` — bkit ENH-310 heredoc guard is a TEXT match on the command string: even a quoted example of $(cmd <<TAG) inside an unrelated heredoc is denied. Write a script file and run that instead
- 2026-08-09 · solution · [0.7] · `h-mad,install,symlink,handoff:2026-08-09-main__h-mad-symlink-install-repair` — h-mad's canonical install is TWO symlinks into the checkout: ~/.claude/skills/h-mad and ~/.claude/hooks/h-mad-tdd-gate.sh (tests hardcode the latter). A copy install silently stops tracking the repo
- 2026-08-09 · gotcha · [0.7] · `skills,claude-code,backup,handoff:2026-08-09-main__h-mad-symlink-install-repair` — A backup dir left inside ~/.claude/skills/ registers as a live duplicate skill (h-mad.bak-* appeared in the skill listing with h-mad's full description). Move skill backups OUTSIDE skills/
- 2026-08-09 · pattern · [0.7] · `skills,install,verification,handoff:2026-08-09-main__h-mad-symlink-install-repair` — Verify a skill install by running its suite through the INSTALL path, not the repo path — diff -rq proves content equality but is blind to links the skill expects outside its own dir
- 2026-08-07 · gotcha · [0.7] · `h-mad,cleanup,handoff:2026-08-07-rpl-shipped-j26-orca-13005` — .h-mad/ is gitignored but .h-mad/invariants.md is TRACKED and load-bearing (Axis B, inlined into every audit prompt). Clean up .h-mad/wires.jsonl by name; rm -rf .h-mad deletes it.
- 2026-08-07 · gotcha · [0.7] · `git,tdd,revert,handoff:2026-08-07-rpl-shipped-j26-orca-13005` — git stash push on an untracked path stashes nothing; git add -N makes it stashable but stash then fails 'Entry not uptodate. Cannot merge'. For a brand-new module the working revert is a file swap to the RED scaffold, asserted.
- 2026-08-07 · gotcha · [0.7] · `h-mad,audit,cross-doc,handoff:2026-08-07-rpl-shipped-j26-orca-13005` — h-mad impl-plan audits inline the paired DESIGN, never the paired plan. Eight clean 5b cycles verified impl-plan vs design while plan.md drifted out of sync, undetectable by any gate. Back-propagate to the plan by hand.
- 2026-08-07 · gotcha · [0.7] · `h-mad,mutation,verification,handoff:2026-08-07-rpl-shipped-j26-orca-13005` — MUTATION: REFUSED measured nothing and is not a pass: a zero-match anchor leaves the guard intact and the suite green, identical to enforced. Write mutation anchors by reading the source, never from memory.
- 2026-08-07 · gotcha · [0.7] · `h-mad,fixtures,unicode,wire-registry,handoff:2026-08-07-rpl-shipped-j26-orca-13005` — Impl-plan template writes U+2192 → while the parser required ASCII ->, so 5b registration silently skipped every wiring task while printing PASS. Survived 96 tests, 6/6 mutations, a wire revert. Check template and parser agree on encoding.
- 2026-08-07 · gotcha · [0.9] · `git,gates,handoff:2026-08-07-gate-blindness-shipped-rpl-at-phase-5` — git show cannot tell an absent path from an invalid sha (both exit 128); git rev-parse --verify --quiet <sha>^{commit} is the clean rc 0/1 discriminator. git ls-files --error-unmatch is also non-zero for an ABSENT path.
- 2026-08-07 · gotcha · [0.9] · `testing,doc-tests,handoff:2026-08-07-gate-blindness-shipped-rpl-at-phase-5` — A doc test slicing a fixed char window makes its scope depend on prose length: a 1707-char section left guards outside a 1600 window, and a substring ban passed only because the warning fell past the cliff. Slice at the real boundary.
- 2026-08-07 · pattern · [0.9] · `h-mad,wiring,regression,handoff:2026-08-07-gate-blindness-shipped-rpl-at-phase-5` — h-mad proves a wire only at creation: 5b, 5d and 5e all fire while it is built and nothing re-checks after 5g. With 4 of 172 impl-plans declaring wiring, almost every wire is unguarded.
- 2026-08-07 · gotcha · [0.9] · `tdd,codex,red,handoff:2026-08-07-gate-blindness-shipped-rpl-at-phase-5` — A broken RED test looks identical to a good one when run: a lowercased haystack vs an upper-case literal, and two mutually unsatisfiable assertions, both showed expected counts and made GREEN unreachable.
- 2026-08-07 · gotcha · [0.9] · `pytest,gates,handoff:2026-08-07-gate-blindness-shipped-rpl-at-phase-5` — One unresolvable pytest node id aborts the whole selection (rc=4, no tests ran); an empty node-id list collects the entire tree. Partition with --collect-only first, then run only what resolved.
- 2026-08-07 · pattern · [0.9] · `h-mad,state,verification,handoff:2026-08-07-gate-blindness-shipped-rpl-at-phase-5` — A schema check cannot verify a write to a non-required field: --strict-only returns PASS on a record where the field is ABSENT. To prove a write landed, read the field back and compare to what you wrote.
- 2026-08-06 · pattern · [0.9] · `h-mad,review,6a-prime,handoff:2026-08-06-gate-blindness-hardening-at-phase-5` — 6a-prime is the only H-MAD pass reasoning OUTSIDE the pinned impl contract — it caught a hardcoded heartbeat nothing else did. Its pane preflight fails in a normal exec-default session: run it headless via exec agy, never skip it.
- 2026-08-06 · gotcha · [0.7] · `h-mad,mutation,handoff:2026-08-06-gate-blindness-hardening-at-phase-5` — h_mad_mutation_harness spec key is 'command' (not 'suite') and 'root' defaults to the SPEC FILE's dir — a spec in a scratchpad yields BASELINE_NOT_GREEN. Pass root explicitly; both refusals are correct and measure nothing.
- 2026-08-06 · gotcha · [0.9] · `h-mad,gates,phase7,handoff:2026-08-06-gate-blindness-hardening-at-phase-5` — h_mad_phase7_preconditions archreview ladder has no else, so a record that never wrote the field returns PHASE7: READY blockers=0 — the architectural review is optional by OMISSION, not just via the documented SKIPPED_NO_PANE escape.
- 2026-08-06 · gotcha · [0.9] · `testing,assertions,handoff:2026-08-06-gate-blindness-hardening-at-phase-5` — 'values == sorted(values)' passes on a CONSTANT, so a hardcoded 'running 0m' heartbeat satisfied its own monotonicity AC. Monotonic assertions need strict growth (values[-1] > values[0]) or they certify nothing.
- 2026-08-06 · pattern · [0.9] · `testing,fixtures,mutation,handoff:2026-08-06-gate-blindness-hardening-at-phase-5` — Test fixtures of short glob-free ASCII make a whole defect class unreachable: a card-corrupting bug passed 1063 tests, 5/5 mutations, 5 wire reverts and a clean arch review. Drive agent-sourced values with markdown/newlines/markers.
- 2026-08-06 · gotcha · [0.9] · `bash,quoting,fixtures,handoff:2026-08-06-gate-blindness-hardening-at-phase-5` — bash ${var%$pat} glob-matches an UNQUOTED pattern; agent markdown ([x](y), **b**) makes the strip silently fail. Quote it: ${var%"$pat"}. Unquoted doubled a worktree comment to 513 spans/38KB, invisible to every tidy-ASCII fixture.
- 2026-08-05 · pattern · [0.7] · `testing,mutation,handoff:2026-08-05-exec-verdict-laundering-fixed` — A first-vs-last-occurrence mutant survives whenever the sought item is last in both regions. To discriminate, build the case where the regions genuinely differ — put the decoys BETWEEN the markers and let the tail be empty.
- 2026-08-05 · gotcha · [0.7] · `h-mad,testing,exec,handoff:2026-08-05-exec-verdict-laundering-fixed` — hmad-dispatch exec codex redirects with > $log, truncating a caller-supplied --log before writing. A test that seeds that file then asserts on it is testing an empty file — mine passed with the guard removed.
- 2026-08-05 · gotcha · [0.9] · `git,h-mad,tree-delta,handoff:2026-08-05-exec-verdict-laundering-fixed` — git -C <subdir> status --porcelain reports the ENTIRE work tree; no pathspec means no scoping. Use '-- .' or a subdir-scoped count silently includes dirt from elsewhere in the repo.
- 2026-08-05 · gotcha · [0.9] · `h-mad,dispatch,codex,verdict-extraction,handoff:2026-08-05-exec-verdict-laundering-fixed` — codex exec '-' echoes the piped prompt into its transcript, so any recovery that greps the whole log reads your own contract block. agy --print does not (prompt is an arg). The two backends need different recovery rules.
- 2026-08-05 · pattern · [0.7] · `h-mad,testing,incident-replay,handoff:2026-08-05-exec-verdict-laundering-fixed` — Replay the original incident artifact against your own fix — it is a different test from the unit tests. J23's boundary slice passed RED and still fabricated a verdict on the real log, exposing a no-boundary fallback hole.
- 2026-08-03 · pattern · [0.5] · `h-mad,e2e,report-file,handoff:2026-08-03-orca-defects-and-preflight-decision` — Give a transport e2e real work as its payload, not a smoke string. Using an adversarial review of my own bug doc as the report-file payload proved the transport AND falsified the doc's central claim; a smoke test would have found nothing.
- 2026-08-03 · pattern · [0.7] · `h-mad,mutation,testing,handoff:2026-08-03-orca-defects-and-preflight-decision` — Mutation-test a guard's CONTENT assertions, not just its exit code. Mutations that keep exit+stream routing and strip only the message text are the ones a returncode-only test survives — and the message is often what the decision rests on.
- 2026-08-03 · pattern · [0.7] · `h-mad,dispatch,orca,design,handoff:2026-08-03-orca-defects-and-preflight-decision` — Do not wrapper-pre-flight a check the runtime already does atomically. Orca refuses --inject on an agentless pane leaving dispatch:null; a pre-flight would only add a TOCTOU window and false-refuse on unreliable identity signals.
- 2026-08-03 · gotcha · [0.7] · `orca,terminal-read,identity,handoff:2026-08-03-orca-defects-and-preflight-decision` — Orca terminal read's empty result is recoverable-forward: the buffer repopulates on new output. Only an IDLE restart-surviving pane reads 0 lines with all cursors '0' — that is the undiagnosable window, not live panes.
- 2026-08-03 · gotcha · [0.7] · `orca,orchestration,dispatch,handoff:2026-08-03-orca-defects-and-preflight-decision` — Orca: worker-abandon AND worker-stop both return dispatch_not_found for a dispatch that dispatch-show returns as status:dispatched. Only task-update --status completed releases the pane; --status ready does not.
- 2026-08-03 · gotcha · [0.7] · `h-mad,handoff,repro,handoff:2026-08-03-orca-defects-and-preflight-decision` — A parked 'clean reproduction' is a hypothesis with a command attached, not evidence. Re-run it before writing it up: two carried repros died to a control run the same day they were recorded.
- 2026-08-03 · pattern · [0.9] · `testing,stubs,tdd,handoff:2026-08-03-agent-identity-and-await-correctness` — A test that PASSES before the fix exists pins nothing. If a stub replays state the real system consumes (e.g. an acked queue), the test re-matches from the stub and stays green either way. Make the stub model the destructive step.
- 2026-08-03 · gotcha · [0.7] · `orca,orchestration,false-completion,handoff:2026-08-03-agent-identity-and-await-correctness` — Orca still DELIVERS a worker_done it lifecycle-rejected (missing_dispatch_id, sender_not_assignee) — payload carries _orcaLifecycleRejection. Matching on taskId alone accepts a report the runtime refused: a false completion.
- 2026-08-03 · gotcha · [0.7] · `orca,orchestration,fanout,handoff:2026-08-03-agent-identity-and-await-correctness` — Orca: ONE delivery carries every pending message for a Run, and `check --ack` destroys them ALL. In a fanout the first await acks siblings' finished reports away. Park each worker_done before acking, or a completed module times out.
- 2026-08-03 · gotcha · [0.7] · `macos,process,liveness,handoff:2026-08-03-agent-identity-and-await-correctness` — `ps -e` can return a PARTIAL process list (measured: 1374 pids on one call, 31 on the next; `ps -p <pid>` finds what `ps -ax|grep` cannot). Use `lsof -a -d cwd -c <name>` for liveness. A pid-count sanity check does NOT validate it.
- 2026-08-03 · gotcha · [0.9] · `jq,bash,guards,handoff:2026-08-03-agent-identity-and-await-correctness` — jq's // treats FALSE as null-ish: `.x.injected // "absent"` yields "absent" for exactly the false you are hunting. Use `has("k")` whenever the value you test can legitimately be false — a guard written with // silently never fires.
- 2026-08-03 · pattern · [0.7] · `testing,docs,mutation,handoff:2026-08-03-orchestration-fixes-skill-reviewer` — A doc-test asserting one literal at one site is a weak guard: the git add -N fix landed at 1 of 4 sites naming the same hazard and the test stayed green. Scope the assertion to every site the rule covers, then mutation-test each.
- 2026-08-03 · gotcha · [0.7] · `git,merge,testing,handoff:2026-08-03-orchestration-fixes-skill-reviewer` — A clean auto-merge says nothing about whether the union is green: a guard on one branch can fire on a file that exists only on the other, so neither branch fails alone. Merge to a throwaway integration branch and run the suite before main.
- 2026-08-03 · gotcha · [0.7] · `orca,review,cli,handoff:2026-08-03-orchestration-fixes-skill-reviewer` — A vendor CLI's guide is not its command surface — run <cmd> --help. Four 'undocumented flag' findings in one review were all real flags the guide omitted; a review prompt naming the guide as ground truth manufactures that whole class.
- 2026-08-03 · gotcha · [0.7] · `verification,review,handoff:2026-08-03-takeover-and-hemasuite-handover` — A conclusion can be right while its reasoning is unsound. Closing a guard because it never fired measures non-exercise, not correctness — keep the guard, kill the reasoning, and get correctness from a discriminating test.
- 2026-08-03 · pattern · [0.5] · `review,integration,handoff:2026-08-03-takeover-and-hemasuite-handover` — Reviewing a DEPENDENCY finds your own untested seams: an orca-cli review returned two Must-fix items, both in our integration rather than theirs, and both in the code path we had never exercised.
- 2026-08-03 · gotcha · [0.7] · `skills,orca,review,handoff:2026-08-03-takeover-and-hemasuite-handover` — Skills under ~/.agents/ are vendor-managed: .skill-lock.json pins a git source plus folder hash, local edits are clobbered on sync, and the real guide comes from the binary. Classify review findings by who can act before spending effort.
- 2026-08-03 · pattern · [0.7] · `testing,mutation,handoff:2026-08-03-takeover-and-hemasuite-handover` — Mutation survivors are three different findings: a weak test (tighten it), an equivalent mutant (the CODE is redundant, delete it — no test can catch it), or a pre-existing weak test. Conflating them produces meaningless tests.
- 2026-08-03 · pattern · [0.5] · `git,merge,handoff:2026-08-03-agy-reviews-mutation-harness` — git merge-tree reporting clean does not mean the union is green. PRs that each merge cleanly to main can conflict once one lands, and were never tested together. Build a throwaway integration branch before merging a batch.
- 2026-08-03 · solution · [0.7] · `orca,agents,computer-use,handoff:2026-08-03-agy-reviews-mutation-harness` — Orca: pin-agents cannot resolve panes Orca does not track in worktree-ps agents[], and terminal read returns 0 rows for ANY full-screen TUI. Working join: terminal switch then get-app-state, unique focused element index per handle.
- 2026-08-03 · gotcha · [0.7] · `skills,review,docs,handoff:2026-08-03-agy-reviews-mutation-harness` — Skill docs here name a hazard precisely then withhold the command to avoid it: 6 of 10 agy findings. git stash push exits 0 on untracked paths, documented for months, while git add -N appeared nowhere. Grep for that shape.
- 2026-08-03 · gotcha · [0.9] · `testing,mutation,h-mad,handoff:2026-08-03-agy-reviews-mutation-harness` — A green test is unverified until a mutation kills it. Six this session passed for the wrong reason, all asserting a MENTION not the actionable thing: prose vs the copyable command, a filename in a list vs the instruction using it.
- 2026-08-03 · gotcha · [0.7] · `h-mad,wire-pin-gate,fail-closed,handoff:2026-08-03-wire-pin-gate-hardened` — H-MAD wire-pin gate matches Task shape against a CLOSED allowlist (new-behaviour|refactor|wiring) and fails closed: any unknown word (typo/synonym) was previously read as 'not wiring' → silent PASS on the task the gate exists to catch.
- 2026-08-03 · gotcha · [0.7] · `h-mad,mutation-testing,test-quality,handoff:2026-08-03-wire-pin-gate-hardened` — A gate-guard test that asserts on an incidental substring passes both pre- and post-fix — mutation-testing (revert the guard, expect RED) is what exposes the dead pin; assert on the operator-visible routing message instead.
- 2026-08-03 · pattern · [0.7] · `h-mad,parser,corpus-sweep,handoff:2026-08-03-wire-pin-gate-hardened` — Before tightening a plan-parser regex, diff old-vs-new parse across the whole shipped corpus: it proved only 2 phantom headings (Task decomposition/outline) dropped and 0 real digit-led ids lost, turning an assumption into evidence.
- 2026-08-02 · solution · [0.7] · `h-mad,orca,dispatch,handoff:2026-08-02-wire-retro-verify-task5-parked` — hmad-dispatch launch <agent> fails under J1: the create response carries no paneKey so nothing is pinned. Use hmad-dispatch pin-agents instead — the Pass-0 agentType join resolves both codex and agy.
- 2026-08-02 · gotcha · [0.7] · `h-mad,state,claim,handoff:2026-08-02-wire-retro-verify-task5-parked` — h_mad_resume_decision.py treats a >2h claim as abandoned and routes enter_autonomous, but h_mad_state_write.py --claim has no staleness allowance and refuses outright. A 19.6h-dead session still blocks; only --claim --force gets through.
- 2026-08-02 · gotcha · [0.7] · `h-mad,wire-pin,phase5,handoff:2026-08-02-wire-retro-verify-task5-parked` — WIREPIN: PASS on an impl-plan whose Task shapes were retrofitted AFTER Phase 5e is a documentation PASS, not enforcement — the gate runs at 5b, pre-dispatch. Plan edited later than the task's GREEN commit means no wire-scoped revert ran.
- 2026-08-02 · gotcha · [0.7] · `h-mad,wire-pin,guards,mutation-testing,handoff:2026-08-02-wire-pin-mislabel-merged` — Closing a one-edit evasion often leaves the two-edit one open: h-mad's wire-pin mislabel check catches a shape demotion with WIRE fields kept, but demoting AND clearing them reads as honest work. Residual, not coverage.
- 2026-08-02 · gotcha · [0.7] · `handoff,git,pr-state,handoff:2026-08-02-wire-pin-mislabel-merged` — A squash-merge commit title ending in '(#N)' is the cheapest proof a handoff's cited PR already landed — check git log for it before trusting a doc's 'PR open, unmerged' claim.
- 2026-08-02 · gotcha · [0.7] · `mutation-testing,regex,handoff:2026-08-02-wiring-task-shape-gate` — Zero failures from a mutation is a finding about the TEST. Relaxing a header regex to \S+ broke nothing because every prose-heading case was multi-word, which the trailing $ rejects anyway; single-word cases discriminate.
- 2026-08-02 · gotcha · [0.7] · `mutation-testing,doc-tests,handoff:2026-08-02-wiring-task-shape-gate` — A mutation harness must assert the literal LANDED. One doc literal spanned a line break, .count() returned 0, and the no-op mutation reported the guard as enforced. Wrap-aware regex + post-write assert.
- 2026-08-02 · gotcha · [0.9] · `h-mad,wiring,phase5,handoff:2026-08-02-wiring-task-shape-gate` — Every H-MAD Phase-5 gate is callee-scoped, but a wiring task's deliverable is the CONNECTION: RED, the whole-module revert, anti-gaming audit and 6a-prime are all blind. Presence is not enforcement; only a wire-scoped mutation sees it.
- 2026-08-01 · gotcha · [0.5] · `hemasuite,git,bkit,handoff:2026-08-01-hmad-dispatch-timeout-pgroup` — HemaSuite tracks .bkit/state/pdca-status.json while ALSO gitignoring it (.gitignore:39 '**/.bkit/state/'). It predates the rule, so it reads dirty forever and 'git add' refuses without -f. Fix is 'git rm --cached', not force-add.
- 2026-08-01 · gotcha · [0.5] · `hemasuite,pytest,test-selection,handoff:2026-08-01-hmad-dispatch-timeout-pgroup` — pytest -k 'h_mad' collects 0 tests in HemaSuite/hematology-paper-writer despite 7 matching files — run them by explicit path. Any workflow selecting them via -k is silently testing nothing.
- 2026-08-01 · pattern · [0.7] · `macos,hmad-dispatch,portability,handoff:2026-08-01-hmad-dispatch-timeout-pgroup` — macOS ships no 'timeout' (GNU coreutils only; brew = gtimeout, not guaranteed). hmad-dispatch --timeout is a real bash impl (_run_with_timeout), not a pass-through; gate/await/report verbs use own poll loops. Zero coreutils dep.
- 2026-08-01 · gotcha · [0.7] · `bash,timeout,hmad-dispatch,handoff:2026-08-01-hmad-dispatch-timeout-pgroup` — A timeout loop counting completed 'sleep 1's drifts LATE — each iteration costs more than its sleep, error accumulates. Use absolute 'deadline=$((SECONDS+secs))' so the arg is a real wall-clock bound.
- 2026-08-01 · gotcha · [0.7] · `bash,hmad-dispatch,macos,timeout,handoff:2026-08-01-hmad-dispatch-timeout-pgroup` — Backgrounded child + 'kill -TERM $pid' kills ONLY the direct child — codex/agy grandchildren orphan past the 124. Use 'set -m' (new pgroup; macOS has no setsid) + 'kill -TERM -$pid'. GNU timeout defaults the same way.
- 2026-07-31 · pattern · [0.7] · `h-mad,incident-replay,prompts,handoff:2026-07-31-tdd-dispatch-verification-discipline-shipped` — A prompt/protocol fix needs a live incident replay, not just a doc-test: assert-literal-present proves the text exists, not that it changes agent behavior. Replay the real historical scenario via exec codex and confirm STATUS: BLOCKED /…
- 2026-07-31 · solution · [0.7] · `h-mad,dispatch,exec,orca,handoff:2026-07-31-tdd-dispatch-verification-discipline-shipped` — Under Orca, prefer hmad-dispatch exec (agy/codex) over the pane path for a whole /h-mad run: pins go stale, pin-agents is worktree-scoped (agents in other worktrees resolve to 0), and launch agy spawns an unauthenticated 1.1.8 that hangs.…
- 2026-07-31 · gotcha · [0.7] · `h-mad,tests,single-source,handoff:2026-07-31-tdd-dispatch-verification-discipline-shipped` — A source-level count assertion over a common token — count('grepping')==1 across whole files — IS the over-constraint anti-pattern: it recruits the implementer into editing unrelated code to satisfy the count. Assert the specific…
- 2026-07-30 · solution · [0.5] · `h-mad,agy,exec,dispatch,handoff:2026-07-30-exec-missing-report-recovery-shipped` — exec agy is less reliable than the pane for long reviews (timed out twice on Gemini capacity); pane agy via report-file transport answered all 8 audit/review dispatches cleanly. Prefer the pane for agy reviews; exec agy only for short…
- 2026-07-30 · gotcha · [0.7] · `h-mad,assemble,audit,handoff:2026-07-30-exec-missing-report-recovery-shipped` — h_mad_assemble_audit.py residual-slot preflight ALSO fires on a slot token that appears in inlined doc CONTENT (spec/plan prose): the doc is substituted after the slot, so a literal <REPORT_FILE_PATH> in prose trips ASSEMBLE: HALT…
- 2026-07-30 · gotcha · [0.7] · `h-mad,review,5e,handoff:2026-07-30-exec-missing-report-recovery-shipped` — A review finding (5e/code-review) can be right about the symptom but wrong about the prescription: verify it against the TESTS/source before applying. An h-mad 5e DRIFT matched the design-doc words but contradicted FR-1.4 + the RED tests;…
- 2026-07-30 · gotcha · [0.7] · `orca,agy,identity,handoff:2026-07-30-dispatch-prompt-size-frontier-92kb` — Orca pane titles are unreliable: a pane titled 'Codex - skills repo' actually ran Antigravity/Gemini (agy); the 'skills' pane was the real Codex. Resolve agent identity by banner/paneKey, never title (H4/H5). A fresh 'launch agy' can also…
- 2026-07-30 · solution · [0.7] · `h-mad,dispatch,exec,prompt-size,handoff:2026-07-30-dispatch-prompt-size-frontier-92kb` — h-mad exec dispatch has NO transport size frontier: codex prompt on stdin is mechanically uncapped; agy --print arg is bounded only by ARG_MAX (~1MB, getconf=1048576). >90KB exec confirmed answered. Only the model context budget binds,…
- 2026-07-30 · gotcha · [0.7] · `h-mad,dispatch,prompt-size,agy,handoff:2026-07-30-dispatch-prompt-size-frontier-92kb` — h-mad pane dispatch answers >=92KB: a 92,055B file-indirection prompt was answered by a live agy pane (Gemini 3.1 Pro) 2026-07-30, falsifying the 61,493B ceiling. Reply fragmented across TUI frames so a tail read silent; --from-start…
- 2026-07-29 · solution · [0.7] · `hmad,verifier,5e,exec,handoff:2026-07-29-skill-upgrades-verifier-parked-paths` — 5e verifier: don't let the codex-exec verify RUN the full suite — the PTY shows only progress dots (no summary), so codex re-runs it, doubling wall-time to a timeout. Orchestrator runs 5f full-suite + passes the number; verifier confirms.…
- 2026-07-29 · gotcha · [0.7] · `hmad,exec,dogfood,sandbox,handoff:2026-07-29-skill-upgrades-verifier-parked-paths` — 5e verifier dogfood via exec: --sandbox read-only breaks pytest (no writable temp/cache → FileNotFoundError); use workspace-write. Full-suite verify under codex needs timeout >700s, and must NOT run while a concurrent agent mutates the…
- 2026-07-29 · gotcha · [0.7] · `hmad,pytest,mutation-test,handoff:2026-07-29-skill-upgrades-verifier-parked-paths` — h-mad doc-tests: pin PY=/opt/anaconda3/bin/python3 (pytest 8.3.5); bare python3 can resolve to homebrew 3.14 without pytest. With set -e a mutation loop then applies edits but never runs tests → zero RED evidence + risk of skipped restore.
- 2026-07-29 · gotcha · [0.7] · `orca,terminal,dispatch,session:2026-07-29` — orca terminal read: scrollback lines live at .result.terminal.tail[] (NOT .rows[]/.lines[]); read-only, --limit <n> pulls more retained rows. jq -r '.result.terminal.tail[]?' to grep a live pane.
- 2026-07-29 · gotcha · [0.7] · `hmad,handoff,resume,session:2026-07-29` — A handoff's 'parked, nothing committed' can be stale: the branch may have merged since. Reconcile merge/branch state (git branch --merged, grep the symbol in main) before assuming parked work is unstarted — Task 3 was already live on main.
- 2026-07-29 · gotcha · [0.7] · `hmad,exec,verify,dispatch,session:2026-07-29` — exec faithfully carried STATUS: DONE while codex content was wrong (reported 21 tests, actual 28). Exit 0 + a STATUS line never means correct work — extractor carries the token, cannot judge truth. Always run independent 5e content…
- 2026-07-29 · solution · [0.7] · `hmad,exec,dispatch,transport,session:2026-07-29` — hmad-dispatch exec (codex) live-validated v0.145.0: read-only headless run returns real rc=0, --log streams transcript live (tailable 33→564 lines mid-run), verdict identical in stdout+--out, h_mad_extract_verdict.py reads STATUS: DONE.…
- 2026-07-28 · pattern · [0.7] · `h-mad,dispatch,exec,handoff:2026-07-28-j17-dispatch-verdict-guard` — H-MAD one-shot 5d/5e dispatch defaults to headless 'hmad-dispatch exec' (hard exit code, no scrape, sidesteps the pane failure class); pane path only for the iterative revision loop. Monitor headless via 'exec --log <file>' + tail -f…
- 2026-07-28 · gotcha · [0.7] · `h-mad,dispatch,wait,handoff:2026-07-28-j17-dispatch-verdict-guard` — hmad-dispatch wait idle is not completion: a pane on 'Waiting for background terminal' (Codex delegated to a bg terminal) is a static frame that satisfies tui-idle + snapshot-stability. Gate on positive evidence (--until-regex verdict +…
- 2026-07-28 · gotcha · [0.7] · `h-mad,dispatch,verdict-extraction,handoff:2026-07-28-j17-dispatch-verdict-guard` — Pane-scrape verdict extractor reads the prompt's own echoed contract as the agent's answer on silence (false STATUS: DONE). Fix: append a fixed boundary line to every dispatch, extract only past its last occurrence (--after-marker); fails…
- 2026-07-28 · gotcha · [0.9] · `orca,pinning,hmad-dispatch,tui` — Orca full-screen TUI (Codex/agy) on the alt-screen buffer: 'terminal read' reports cursor:0 and .preview reads empty even while live mid-audit — an API artifact, NOT a dead/wrong pane. Confirm pin via 'worktree ps'…
- 2026-07-24 · solution · [0.7] · `h-mad,tdd-gate,codex,enforcement,handoff:2026-07-24-skill-candidate-upgrades` — H-MAD Codex-authorship gate: the PreToolUse hook only sees CLAUDE's writes (Codex writes via its own process), so a prod-.py write in step5 IS Claude self-implementing -> block when codex on PATH; fallback=codex_status=exhausted
- 2026-07-24 · gotcha · [0.7] · `agy,antigravity,cli-args,live-verify,handoff:2026-07-24-skill-candidate-upgrades` — agy --print takes the NEXT token as its prompt: must be last, prompt adjacent ('agy <flags> --print "<prompt>"'). A --print followed by a flag ate that flag + dropped the real prompt (agy just greeted). Stub green; live caught it
- 2026-07-24 · gotcha · [0.7] · `bash,stdin,background,codex-exec,live-verify,handoff:2026-07-24-skill-candidate-upgrades` — bash nul's a backgrounded command's stdin (job control off): '"$@" &' starved 'codex exec -' of its piped prompt. Fix: '"$@" <&0 &'. Stub test passed (no --timeout path); only the live codex run exposed it
- 2026-07-24 · solution · [0.7] · `h-mad,ask-verb,dogfood,agy-tui,handoff:2026-07-24-skill-candidate-upgrades` — hmad-dispatch ask dogfooded live: send->wait->read->extract_verdict chain sound (exit 0). agy returned false-DRIFT inventing a nonexistent _cmd_wait idle arg; premise-check caught the bad finding
- 2026-07-24 · solution · [0.7] · `backlog,verification,handoff:2026-07-24-skill-candidate-upgrades` — Reconcile a candidate/registry backlog by checking each row's premise against code, not its label: 3 skill-candidate rows described work already shipped (verified via git log -S / grep) before…
- 2026-07-24 · gotcha · [0.7] · `testing,mutation-testing,handoff:2026-07-24-skill-candidate-upgrades` — A test can pass under mutation because the DATA is undiscriminating, not just the assertion: 6-char words aligned so a naive limit-1 cut also hit a word boundary. Fix the fixture, not only the assert.
- 2026-07-24 · solution · [0.7] · `handoff,learn,ergonomics` — learn.py --trim word-boundary-trims a >200-char kernel in one call (marked …); the plain rejection also prints a paste-ready ≤200 suggestion. Never eyeball-retry the length — it overshoots.
- 2026-07-24 · solution · [0.7] · `h-mad,hmad-dispatch,ask,dispatch` — hmad-dispatch ask = send + wait-idle + read --from-start in one call (the audit scrape dispatch). STDOUT is the reply buffer; send/wait chatter goes to stderr; pipe into h_mad_extract_verdict.py.
- 2026-07-23 · gotcha · [0.7] · `review,h-mad,handoff:2026-07-23-monitoring-registry-drained` — Test a filed fix direction before adopting it. ASSEMBLE: PASS_OVERSIZE would have matched every grep 'ASSEMBLE: PASS' consumer, reproducing the defect it was meant to fix.
- 2026-07-23 · gotcha · [0.9] · `git,workflow,handoff:2026-07-23-monitoring-registry-drained` — Commit before any history-rewriting git command. git reset --hard with uncommitted work destroyed an implementation mid-session; only a stray /tmp mutation-test backup saved it.
- 2026-07-23 · gotcha · [0.9] · `testing,regression,h-mad,handoff:2026-07-23-monitoring-registry-drained` — When a fix breaks an existing test, check if the test PINNED the defect. 3 tests this session asserted the filed bug as an acceptance criterion (J17 selector, J1 handle, J2 pin path).
- 2026-07-23 · gotcha · [0.9] · `bash,cli,hmad-dispatch,argument-parsing` — A shell arg loop ending in '*) shift ;;' silently eats misspelled flags: --timeut made wait block 300s instead of 2s. Reject unknown flags with exit 2 naming the token.
- 2026-07-23 · gotcha · [0.9] · `jsonschema,validation,h-mad,j4` — Draft-07 'format' is annotation-only unless a format checker is passed, and bool subclasses int in Python but is NOT an integer in JSON Schema. Both bite hand-rolled validators.
- 2026-07-23 · gotcha · [0.9] · `testing,mutation-testing,isolation,h-mad,j18` — Mutation-testing a path-resolution branch can disable the test suite's own isolation and overwrite live state while reporting all-green. Snapshot the real target or sandbox the cwd first.
- 2026-07-23 · gotcha · [0.9] · `orca,h-mad,launch,identity,j1` — orca terminal create's .result.terminal.handle is a pre-adoption placeholder the pane never gets. Use .result.terminal.paneKey joined to terminal list tabId:leafId.
- 2026-07-23 · gotcha · [0.9] · `h-mad,orca,agy,tui,measurement,j13` — Tail-grepping a TUI pane for a sentinel reports SILENT for replies that arrived: redraws split tokens across frames. Read --from-start and extract on the sentinel pair.
- 2026-07-23 · gotcha · [0.9] · `h-mad,orca,worktree-rm,destructive-verbs,guards,j17` — A guard inside `if resolve; then...fi` is no guard when resolve fails: for a destructive verb 'cannot check' must mean refuse. J17: orca selectors didn't resolve, so worktree-rm deleted unguarded.
- 2026-07-23 · gotcha · [0.7] · `orca,cli,debugging,handoff:2026-07-23-wave3-wave4a-shipped` — Never 2>/dev/null an Orca CLI probe: orca terminal read takes --limit not --lines, and the suppressed invalid_argument envelope read as an empty pane — i.e. 'the agent is gone'.
- 2026-07-23 · solution · [0.7] · `orca,h-mad,agent-identity,orca-9870,handoff:2026-07-23-wave3-wave4a-shipped` — Orca agent identity: join worktree ps agents[].paneKey (<tabId>:<leafId>) to terminal list tabId/leafId. Title/preview are unreliable; agentType is 'antigravity' not 'agy'.
- 2026-07-23 · gotcha · [0.9] · `h-mad,validation,replay,handoff:2026-07-23-wave3-wave4a-shipped` — Replay a detector against historical data, not just synthetic cases: 14 handcrafted cases passed while the real label 'Working-tree concern:' was rejected, discarding a genuine concern.
- 2026-07-23 · pattern · [0.9] · `h-mad,testing,mutation,vacuous-guards,handoff:2026-07-23-wave3-wave4a-shipped` — Mutation-test every guard: disable it and re-run. A green suite hid two unenforced guards this session — one passed because run() strips HMAD_ORCA_* so the guarded call never fired.
- 2026-07-23 · pattern · [0.9] · `h-mad,orca,fanout,guards,handoff:2026-07-23-wave3-wave4a-shipped` — Guard the irreversible step, not the actor: worktree-rm refusing to delete uncommitted work beat telling fanout workers to commit — both workers were told and neither did.
- 2026-07-23 · gotcha · [0.7] · `h-mad,codex,handoff:2026-07-23-wave2-preflight-shipped` — Verify pinned API against the ACTUAL file, not a sibling module: test_hmad_dispatch.py defines SKILL/WRAPPER/STUBS, not the REPO_ROOT/SKILL_DIR its siblings use. Cost: one wasted dispatch
- 2026-07-23 · solution · [0.9] · `codex,tdd,h-mad,handoff:2026-07-23-wave2-preflight-shipped` — Label GUARDS in refactor-shaped RED dispatches and state expected fail/pass counts. 'Every new test must FAIL' makes Codex bolt on assertions to manufacture failures
- 2026-07-23 · pattern · [0.7] · `testing,h-mad,handoff:2026-07-23-wave2-preflight-shipped` — A test inside a suite can't honestly assert a property OF that suite (e.g. 'counts identical with/without file X'). Verify by measurement and say so in the spec, don't fake it with a test
- 2026-07-23 · pattern · [0.9] · `testing,tdd,handoff:2026-07-23-wave2-preflight-shipped` — Prove a regression test discriminates: revert the fix and confirm it fails. A test that passes against the code it was written to catch is decoration, not coverage
- 2026-07-23 · gotcha · [0.9] · `testing,handoff:2026-07-23-wave2-preflight-shipped` — Guards can pass VACUOUSLY: 'if line.startswith(X): assert ...' is green when no X line exists, and stays green if the impl stops emitting X. Assert the thing EXISTS before asserting what it lacks
- 2026-07-23 · pattern · [0.7] · `tdd,h-mad,handoff:2026-07-23-wave2-preflight-shipped` — RED tests fix the contract — adapt the impl to them. A reviewer moving a cast INTO the tests because the plan said otherwise inverts TDD: the plan is not authoritative over the test
- 2026-07-23 · gotcha · [0.9] · `h-mad,telemetry,handoff:2026-07-23-wave2-preflight-shipped` — A sentinel that is a VALID value can't be told from real data downstream: started_ts defaulting to the epoch read as a parser bug for weeks. Default to now(UTC), not a parseable placeholder
- 2026-07-22 · pattern · [0.5] · `h-mad,audit,handoff:2026-07-22-audit-assembler-agent-resolution` — A degraded prompt does not imply a wrong verdict: an audit that gated clean from a prompt with duplicate rubrics + a raw placeholder reproduced the same verdict on a clean re-run, with more evidence
- 2026-07-22 · gotcha · [0.7] · `tooling,backup,handoff:2026-07-22-audit-assembler-agent-resolution` — Re-running a repair with the same --backup-dir overwrites originals with the prior pass's output; an mtime freshness guard is void once your pass rewrote every file — read age from the backup
- 2026-07-22 · gotcha · [0.7] · `h-mad,audit,handoff:2026-07-22-audit-assembler-agent-resolution` — Never hardcode a needle the project authors: preflight pinned 'H-MAD Project Invariants', HemaSuite writes 'HPW Project Axis B' -> count 0 = 'never inlined'. Derive from head -1 of the inlined file
- 2026-07-22 · gotcha · [0.7] · `hmad-dispatch,orca,handoff:2026-07-22-audit-assembler-agent-resolution` — Disabling a broken matcher exposes what was beneath it: killing Codex title-match let a bare-token preview signature match the coordinator's own pane -> self-dispatch. Match only what a program EMITS
- 2026-07-22 · gotcha · [0.7] · `h-mad,testing,handoff:2026-07-22-audit-assembler-agent-resolution` — h-mad design docs live in docs/02-design/features/, NOT beside spec/plan/impl-plan in 01-plan — a fixture colocating all four passed while the tool couldn't read any real design doc
- 2026-07-22 · gotcha · [0.9] · `bash,shell,handoff:2026-07-22-audit-assembler-agent-resolution` — set -euo pipefail: 'cmd; rc=$?' aborts on non-zero (if-conditions suppress set -e, bare sequences don't) — use 'rc=0; cmd || rc=$?' or the check exits silently
- 2026-07-22 · gotcha · [0.9] · `orca,hmad-dispatch,handoff:2026-07-22-audit-assembler-agent-resolution` — Orca .title = program OSC title else the enclosing TAB title (shared by every leaf); Codex emits no OSC title, so any codex-matching title is inherited and is NOT identity
- 2026-07-22 · pattern · [0.7] · `h-mad,testing,handoff:2026-07-22-orca-agent-resolution-hardening` — test_hmad_dispatch.py run() strips ambient HMAD_ORCA_* (F13) but re-applies the explicit env= param AFTER the strip, so tests still pass pins/pin-file via env={...}
- 2026-07-22 · solution · [0.5] · `h-mad,orca,handoff:2026-07-22-orca-agent-resolution-hardening` — hmad-dispatch pin-agents returning rc=0 on a partial pin was a bug — a run proceeded believing an unresolved agent was addressable; fail loud (rc=1 + name it)
- 2026-07-22 · gotcha · [0.7] · `h-mad,orca,report-wait,handoff:2026-07-22-orca-agent-resolution-hardening` — Dispatching an agent to edit the very wrapper the coordinator polls with races (report-wait hit transient bash syntax error mid-save); poll via a standalone stdlib script, not the wrapper
- 2026-07-22 · gotcha · [0.7] · `orca,h-mad,codex,handoff:2026-07-22-orca-agent-resolution-hardening` — Codex Orca preview banner (gpt-N/OpenAI Codex) scrolls off once the pane works, so preview-based auto-detect only matches a FRESH pane; pin the handle or use launch
- 2026-07-22 · solution · [0.9] · `orca,h-mad,codex,launch,handoff:2026-07-22-orca-agent-resolution-hardening` — orca terminal create --json returns .result.terminal.handle at spawn — the durable way to capture agent identity (hmad-dispatch launch); own the launch, don't auto-detect Codex
- 2026-07-22 · gotcha · [0.9] · `orca,h-mad,codex,identity,handoff:2026-07-22-orca-agent-resolution-hardening` — orca terminal rename sets a tab-UI title NOT surfaced in 'terminal list --json'; .title there is the OSC title the program emits (Codex=cwd basename, agy=agy) — rename can't fix resolution
- 2026-07-22 · solution · [0.7] · `orca,h-mad,report-file,handoff:2026-07-22-orca-skills-hardening` — Orca agent verdicts: report-file transport (agent writes <path>+<path>.done; coordinator report-waits) beats TUI scrape — kills the indent/bullet/idle/sentinel fragility; gate reads the clean file
- 2026-07-22 · gotcha · [0.7] · `review,tdd,handoff:2026-07-22-orca-skills-hardening` — Separate review lane is load-bearing: 4 Claude-subagent reviews each caught 1 real bug green tests missed (stub-envelope regression, gate-wait fail-open, find_latest prefix false-match)
- 2026-07-22 · pattern · [0.7] · `orca,handoff,worktree,handoff:2026-07-22-orca-skills-hardening` — Handoff/learnings under Orca multi-worktree: anchor to canonical main worktree via git-common-dir (not show-toplevel); disambiguate by branch with __ separator so feat can't match feat-ab
- 2026-07-22 · gotcha · [0.9] · `h-mad,gap-analysis,verification,handoff:2026-07-22-h-mad-fourteen-issues-shipped` — Checking code against spec alone cannot separate 'code diverged from design' from 'design diverged from spec' — a deliberate narrowing reads as a defect. Classify before recommending a fix.
- 2026-07-22 · pattern · [0.7] · `h-mad,state,schema,handoff:2026-07-22-h-mad-fourteen-issues-shipped` — Validate-before-write at the seam beats documenting a rule: h-mad state drifted to 38 shapes over 53 keys because nothing sat between an invented key and the file, not because the rule was unclear.
- 2026-07-22 · gotcha · [0.7] · `zsh,git,shell,handoff:2026-07-22-h-mad-fourteen-issues-shipped` — zsh executes backticks and glob-fails on # inside git -m messages, producing a silent no-op that looks like success. Use -F <file>; note git merge -F - does not read stdin though git commit does.
- 2026-07-22 · solution · [0.7] · `git,heuristics,handoff:2026-07-22-h-mad-fourteen-issues-shipped` — Counting a feature's commits: rev-list --count <branch> gives whole history; merge-base..<branch> gives 0 once merged. Only --grep for commits naming the feature survives a merge.
- 2026-07-22 · gotcha · [0.9] · `h-mad,testing,verification,handoff:2026-07-22-h-mad-fourteen-issues-shipped` — A green test suite can certify a 0% spec match rate — tests encode the design, and the design can silently diverge from the spec. Test-passing and requirement-satisfying are independent axes.
- 2026-07-22 · gotcha · [0.9] · `h-mad,dispatch,silent-failure,handoff:2026-07-22-h-mad-fourteen-issues-shipped` — Halt conditions phrased as halt-on-bad-value fail open: 'halt on VERDICT: DRIFT' means a scrape with no verdict greps clean, so agent silence reads as approval. Extraction must fail closed.
- 2026-07-21 · gotcha · [0.7] · `orca,automations,worktree,handoff:2026-07-21-orca-arc-complete-hemasuite-wiring` — Orca automations: valid --provider = claude|codex|gemini (NOT agent); worktree create needs --repo; --trigger and --schedule/--cron are mutually exclusive.
- 2026-07-21 · pattern · [0.9] · `orca,e2e,verify,handoff:2026-07-21-orca-arc-complete-hemasuite-wiring` — Stub-green + N doc-audits can't catch schema-extraction bugs; only a live-runtime e2e does. Reconcile verb output KEYS vs the real runtime, not just argv.
- 2026-07-21 · gotcha · [0.7] · `orca,automations,worktree,handoff:2026-07-21-orca-arc-complete-hemasuite-wiring` — Orca automations are agent-driven: valid --provider is claude|codex|gemini (NOT agent); worktree create requires --repo/targeting; --trigger and --schedule/--cron are mutually exclusive.
- 2026-07-21 · pattern · [0.9] · `orca,e2e,verify,handoff:2026-07-21-orca-arc-complete-hemasuite-wiring` — Stub-tested-green + N doc-audits cannot catch schema-extraction bugs; only a live-runtime e2e does. Reconcile verb output-KEYS against the real runtime, not just the input argv.
- 2026-07-20 · solution · [0.7] · `orca,orchestration,handoff:2026-07-20-orca-adaptation-tiers` — Tier-2 orchestration worker can't read coordinator handle from its own shell env — task-create injects it into the task spec (prepended [H-MAD] line) and enforces the pin
- 2026-07-20 · gotcha · [0.7] · `zsh,shell,handoff:2026-07-20-orca-adaptation-tiers` — zsh no-wordsplit: 'env $PINS bash' passes one arg — use prefix-assignment 'VAR=v bash'. Shell exports don't persist across Bash tool calls; inline pins each call
- 2026-07-20 · gotcha · [0.7] · `hmad-dispatch,cmux,handoff:2026-07-20-orca-adaptation-tiers` — hmad-dispatch alive string-matches surface in cmux tree -> false positives; panes may not be at default surface:5/:2 (was Codex=4 agy=5). Pin HMAD_CMUX_*_SURFACE + read pane to confirm REPL
- 2026-07-20 · gotcha · [0.7] · `orca,hmad-dispatch,handoff:2026-07-20-orca-adaptation-tiers` — Reconcile Orca CLI usage vs 'orca agent-context --json' at build time — original hmad-dispatch guessed syntax, shipped broken wait (real: --for tui-idle --timeout-ms) + tail-piped read (real: --limit)

- 2026-07-20 · gotcha · [0.7] · `orca,hmad-dispatch,handoff:2026-07-20-orca-adaptation-tiers` — orca terminal list --json nests terminals under .result.terminals[].handle (no .id/.command) — guessed .[]|select(.id) never matches live Orca; identity = handle pin not command-match
