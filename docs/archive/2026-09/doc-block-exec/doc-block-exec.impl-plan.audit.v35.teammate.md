## Summary

Read the assembled prompt in full, then re-derived every claim the impl-plan makes *about the tree*
against HEAD `a8e0372`. Both carried discrepancies are **REFUTED — already fixed**: the impl-plan
carries `80 + 1` at `:1327` (and the design's own helper matrix is exactly 81 data rows with exactly
1 naming `SKILL.md`, derived here), and the 5f bound reads `383 s` at `:1887`; neither `79 + 2` nor
a live `397 s` survives outside the Version History. The two Must-fixes below are new and both are
tree-verified: a pre-existing parametrised guard makes AC-6.4's "Seven is exact" wrong by two nodes,
and the 5f note routes a debt to the spec author that spec v1.54 already discharged.
Evidence: 20 files opened, 55 greps run. (Plus 2 `pytest --collect-only` runs and 2 `python3` set
sweeps; commands are quoted inline so each figure can be re-derived.)

## Must-fix
- AC-6.4's seven-node tuple is short by two, and the `+ 7` constant with it, because creating `h-mad/scripts/h_mad_doc_block_exec.py` in Task 1 adds two **parametrised** node IDs to a pre-existing test file. `h-mad/tests/test_h_mad_portable_timeout.py:40` binds `SKILL = Path(__file__).resolve().parent.parent` — this repo's `h-mad/`, not the installed skill — and `:160` puts `*sorted((SKILL / "scripts").glob("*.py")),` into `_SCANNED`; `:165` and `:295` each carry `@pytest.mark.parametrize("path", _SCANNED, ids=lambda p: p.name)`. Verified: `python3.11 -m pytest h-mad/tests/test_h_mad_portable_timeout.py --collect-only -q -p no:cacheprovider` collects 160 nodes, 58 of them `test_no_document_or_script_emits_a_bare_timeout_command[...]`, one per `_SCANNED` entry (58 = 4 named docs + 12 `references/*.md` + 3 `scripts/*.sh` + 37 `scripts/*.py` + 2 `hooks/*.sh`). Task 1 makes that 59, so the feature adds `test_no_document_or_script_emits_a_bare_timeout_command[h_mad_doc_block_exec.py]` and `test_no_document_or_script_rests_on_an_unconditional_absence_claim[h_mad_doc_block_exec.py]` to `h-mad/tests/test_h_mad_portable_timeout.py`. Both are expected to **pass** on the specified module, so this is not a failing test — it is precisely the silent weakening AC-6.4 names as its own reason to exist: the assertion is `full_collected >= 2748 + new_module + 7` while the true addition is 9, so the floor tolerates two invisible deletions. The 5c re-measure does not absorb it, because the baseline is taken before the feature lands and the `+ 7` is not re-measured. Prescription: `+ 9`, with both node IDs added to the asserted tuple. **The same correction is owed to both siblings**, so this is back-propagation, not an impl-plan-local edit: `docs/01-plan/features/doc-block-exec.plan.md:908-912` states "**exactly these seven**" and asserts `full_collected >= 2748 + new_module + 7`, and `docs/01-plan/features/doc-block-exec.spec.md:466` states "a fixed tuple of the named new node IDs added to existing files — the **seven** enumerated in the plan". **Close the class** — the axis is *a pre-existing `parametrize` whose argvalues come from a filesystem glob this feature writes into*; the rule is to enumerate those globs before fixing the constant and add one node per (parametrised test × added file). Residual, derived at `a8e0372`: `h-mad/scripts/` is globbed only by `_SCANNED` (two parametrised consumers) and by `h-mad/tests/test_h_mad_collect_report.py:287`, which filters to `writer_modules = ("h_mad_audit_cycle.py", "h_mad_collect_report.py")` at `:285-289` and so adds nothing; `h-mad/tests/mutation-specs/` is globbed by `h-mad/tests/test_h_mad_mutation_harness.py:81`, reached only from the **non**-parametrised `test_committed_mutation_specs_are_not_drifted` at `:313-316`, so `doc_block_exec.json` and `doc_block_exec_wire.json` add no node while still being subject to its exact-once anchor sweep; `h-mad/tests/test_hmad_dispatch_audit_cycle.py:250` globs a `tmp_path` script dir, not the repo's. Nothing else under `h-mad/tests/` globs a directory this feature writes into.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `Seven is exact: those are the only node IDs this feature adds to pre-existing files`
- The 5f note flags a back-propagation debt against the spec that the spec has already paid, so the impl-plan now asserts something false about a sibling and would send the spec author on a no-op edit. The spec is at v1.55; its single `git rev-parse --show-toplevel` hit is `docs/01-plan/features/doc-block-exec.spec.md:474`, whose inline comment reads `# from the REPOSITORY ROOT: the 2748 baseline is the root count; from h-mad/ the same command collects 2486` — the corrected pair — and the spec's own `- v1.54` entry records that fix ("AC-6.4's embedded Phase-5f gate command still carried the pre-fix 2747/2485 pair in its comment"). The three line pins in the same paragraph are stale with it: the subshell has moved from `:462` to `:474`, `spec.md:451` is now `hand-rolled \`re.findall\` and \`run_recipe\`'s inline \`subprocess\` are both gone` and `spec.md:458` is `migration. Both the delivered and the missing paths are driven **through the preamble boundary`. This is the fourth recurrence of the class this document named at v1.35 ("any note asserting what a sibling currently says") and superseded twice at v1.36; the note is now a stale clearance in the opposite direction. Prescription: drop the "owed upstream" sentence and the three `spec.md:` numbers, keeping only the by-name `grep -n 'git rev-parse --show-toplevel'` locator that already sits two lines above it and already returns exactly one hit (verified).
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `**One thing is therefore owed upstream**: that comment needs 2747→2748 and 2485→2486`

## Should-fix
- Three sibling line pins survive the v1.36 sweep and all three have drifted at HEAD, while their claims remain true — so the citations mislead without falsifying. At impl-plan `:1150` `design.md:1005` is cited as rendering `BAD_INFO key="<k>"`; the real line 1005 is `` `NOT_FOUND` line parses under that grammar to exactly `heading` (`== "x rc=0"`) with no `rc` `` and the verdict-table row is now `design.md:1039`. At `:1152` `design.md:1002` is cited as rendering `overlap: "<a>" "<b>"`; line 1002 is `quoted-string grammar recovers every field. The rule is what keeps the one-line, one-token-per-field` and the row is now `design.md:1036`. At `:1154` `design.md:1435` is quoted as the v1.81 history entry; line 1435 is the **v1.17** entry and the v1.81 entry is now `design.md:1499`. All three were correct at `1861157` (`git show 1861157:...` confirms) and moved by design v1.93. `design.md:1290` and `:1291` still resolve exactly, so the fix is not "all pins are wrong" but "pins into a document under active revision expire in under a day". Prescription and class: replace all five with the by-name `grep` locators this document already uses elsewhere — for the two that still resolve, `grep -n 'guard it removes'` reaches the matrix (verified one hit) and the row names are unique within it; for the three that drifted, cite the verdict-table rows and the v1.81 entry by their text.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `` `design.md:1005` renders the head as `BAD_INFO key="<k>"` ``
- The justification for leaving `docsections.json`'s whole-file `command` narrow miscounts its own set: it names "three of the eight killers", but the third item is the first item's killer, so the true count is two. `docsections-syspath-setup-removed`'s `test` key **is** `tests/test_h_mad_doc_block_exec.py::test_docsections_imports_from_an_unrelated_cwd` — stated at impl-plan `:332-337` ("Add a sixth row `docsections-syspath-setup-removed`" … "killed by `tests/test_h_mad_doc_block_exec.py::test_docsections_imports_from_an_unrelated_cwd`") and again in the AC-1.8 bullet — so listing the row and the binding as separate members double-counts. Enumerated against the shipped spec (`h-mad/tests/mutation-specs/docsections.json`, whose four rows carry `_killed_by` values all under `tests/test_docsections.py::`) plus the four rows Task 1 adds, exactly two of the eight killers live in `test_h_mad_doc_block_exec.py`: `test_docsections_imports_from_an_unrelated_cwd` (row 6) and `test_docsections_has_no_second_bounder` (row 8). The conclusion survives at two, so nothing mechanical breaks — but a 5d implementer who counts finds two and reads the gap as a missing row. Prescription: "two of the eight killers — `docsections-syspath-setup-removed`'s and `docsections-local-bounder-restored`'s".
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `so `command` never selects a killer, and three of the eight`
- AC-6.1's "Re-run to check" states three absolute integers that are already wrong one commit later, which is the class this document has been closing all session. The paragraph honestly stamps its measurement at `1861157`, but the re-run instruction itself is unconditional. Re-run at `a8e0372` from the repository root: the dot-excluded command prints **30**, not 25; dropping the dot clause prints **35**, not 30; `p.parts` still prints 0. `git ls-files -- h-mad handoff | grep '\.md$' | grep -v '/archive/' | wc -l` is likewise **30**. The cause is this session's own five `h-mad/agents/*.md` commits, and the plan (`:727`, `:747`, `:750`) and design (`:177`, `:317`) carry the same 25/30 pair, so the impl-plan is not out of step with its siblings — the numbers are just growing-tree values written as constants. What is invariant, and what I verified still holds at HEAD, is the **relation**: the dot-excluded set and the `git ls-files` set are identical (symmetric difference empty, both 30), and the globbed set is exactly those plus the five named `.pytest_cache/README.md` paths (35 = 30 + 5, and the five dropped are still exactly `h-mad/.pytest_cache/README.md`, `h-mad/scripts/.pytest_cache/README.md`, `h-mad/tests/.pytest_cache/README.md`, `handoff/.pytest_cache/README.md`, `handoff/tests/.pytest_cache/README.md`). Prescription: state the relation as the check and keep the three integers only as a dated example. The same treatment is owed to Task 1's guard-narrowing paragraph at impl-plan `:254-261`, which carries the same 25/30 corpus figures beside the transcribed 263/268 differential and ends with its own unconditional re-run (`… | wc -l` → 25, the glob → 30), both of which now print 30 and 35.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `Re-run to check`

## Nit
- The class Task 1 closes — a second, fence-blind heading grammar picking a section start independently of the scanner — has a third live instance the document does not name as residual: `h-mad/tests/test_h_mad_context_budget_docs.py:69` defines a local `_titled_section` that bounds on `text.index(title)` and `text.find("\n## ", start + 1)` with no fence state at all, and eight call sites at `:301-372` run it over `h-mad/SKILL.md`. It violates no stated invariant — the Conventions scope the single-source rule to marker-run recognition, which that function does not perform, and `test_docsections_has_no_second_bounder` is explicitly scoped to `docsections.py` — so this is not a defect, only an unstated residual of a class the document otherwise closes carefully.
- Everything else I set out to falsify held. Verified exact-once or exact-value at `a8e0372`: `docsections.py:31` `_fence_aware_end`, its docstring at `:32`, its body `:33-42`, `titled_section`'s docstring `:46-52`, the inline `re.search` at `:53`, `section_from`'s docstring `:60-65`, `grep -c 'def _find_heading'` → 0, and imports limited to `from __future__ import annotations` + `import re`; the shipped `docsections.json`'s `command`, the two current anchors (`startswith("```")` and the `re.match` heading test) and `missing-heading-returns-empty-instead-of-failing`'s `find`/`replace` pair, all exactly as quoted; consumer pins `:8`, `:10-13`, `:22`, `:267-273`, `:281`, `:304`, `:309`, `:312`, `:340`, `:346`, `:368`, `:403`, `:412`, `_second_surface` at `:49`, `grep -c` on `exec-scan-executes`'s assert anchor → 1, and `re.findall` appearing at exactly `:270` and `:412`; `h_mad_mutation_harness.py:606-607`, `:609-623`, `:645`, `:660-670`, `:679`; `h_mad_assemble_tdd.py:230` and `:238-243`; `h_mad_collect_report.py:102-111` returning 0 on `MISSING`; the SKILL.md locator (heading at `:2179`, four `^```bash` openers in the window, the awk resolver printing exactly one line, `2220`, which is also the last of the four, and one `hmad-dispatch exec codex` fence); `grep -rn 'hmad:exec' h-mad/ handoff/` → 0; all seven `grep -n` locators returning exactly one hit; and the internal counts — 81 = 25 + 5 + 24 + 27, 23 `VERDICT_TABLE` heads split 17 subprocess + 6 in-process, 15 head fields, 11 `DETAIL_KEYS`, 26 slots = 7 bare + 19 quoted, `__all__` 25 → 29, 19 exception subclasses as 6 + 3 + 4 + 5 + 1, Task 2's nine tests, Task 3's twelve injecting tests split 8 module-seam + 4 instance-wrapper with five `real_rmtree` binders, Task 5's eight wire rows over six distinct pins, and AC-6.4's six consumer tests + one docsections test.
