## Summary
Audited impl-plan v1.41 at the freeze sha `6f0ee85`, re-deriving every figure rather than carrying one, and re-reading the delta `35698f9..6f0ee85` before the whole document. Almost everything v1.41 claims reproduced exactly at HEAD: the bare-`.py:N` shape grep returns 0, `grep -c 'SKILL.md:[0-9]'` is 0, all 13 `docs/`-sibling locators return exactly one hit against the *moved* siblings (design v1.97 / spec v1.59 / plan v1.92 at HEAD), the corpus scan prints `files 30 space-arm 29 in 4 files tab-arm 0 declined-inside-fence 2` verbatim, residual (a) prints 22, residual (b) prints 7 bodies, the AST absence screen prints 2, the GNU/BSD sweep returns 3 lines, the `30 0` command prints `30 0`, the glob sweep prints `module-level 7` / `in-body 21`, the row totals re-count to 25+5+24+27 = 81 with the design's own matrix at HEAD holding 81 data rows and exactly 1 naming `SKILL.md`, the wire spec enumerates 8 rows and `docsections.json` 8, and every tree pin I opened (`h_mad_mutation_harness.py` :482/:606-607/:609-623/:645/:660/:670-671/:679, `assemble_tdd.py` :174/:230/:238-243, `collect_report_docs.py` :22/:40/:267/:268/:270/:272/:273/:294/:309/:403/:412, `portable_timeout.py` :151/:153/:211, `docsections.py` :31/:45/:46/:53, `test_suite_collection.py` :81 in `test_no_declared_skill_has_a_test_directory_left_out` :49, `audit_cycle.py` :15/:18/:19/:23/:1672) is the line the document says it is, with its enclosing symbol correct. Two claims did not survive: the closure evidence for DECISION C publishes a before-figure of `3` where its own published command returns 22, and the `docsections.json` paragraph asserts a single consumer of the harness's `command` where the source it pins has four.
Evidence: 24 files opened, 41 greps/sweeps run.

## Must-fix
- The DECISION C closure publishes a **before**-figure its own command contradicts — the bare-filename shape grep run verbatim against v1.40 at `35698f9` returns **22 occurrences across 19 lines**, not 3, so the one number a reader can re-derive to check that the class was closed is wrong by ~7x and the closure reads as unearned. Re-derived: `git show 35698f9:docs/01-plan/features/doc-block-exec.impl-plan.md | awk '/^## Version History/{exit}{print NR": "$0}' | grep -oE '(^|[^/A-Za-z0-9_.-])[A-Za-z0-9_]+\.py:[0-9]+'` → 22 hits, 19 lines, over 8 distinct files (`h_mad_mutation_harness.py` ×9, `docsections.py` ×4, `test_h_mad_portable_timeout.py` ×2, `test_h_mad_collect_report_docs.py` ×2, `h_mad_assemble_tdd.py` ×2, `test_suite_collection.py`, `test_h_mad_context_budget_docs.py`, `test_h_mad_audit_cycle.py`). The **after**-figure of 0 is correct and I reproduced it at `6f0ee85`, so the fix landed; it is only the evidence that is wrong, which is the base-invariant failure ("Behavioural premises carry their command" — the command is present and does not produce the stated output). This is also the paragraph's own argument and the reason to state it rather than patch the digit: the same paragraph reports the two audit surfaces as having found **7** counter-instances, and 22 measured against 7 reported is exactly why the shape grep is the right closure and a value sweep is not — say so, instead of leaving a reader to reconcile 3, 7 and 22. Prescription: replace `3` with the two re-derivable numbers (22 occurrences / 19 lines) beside the `git show 35698f9:<doc> | awk … | grep -oE …` form that produces them, note that the reported 7 was the surfaces' yield and not a census, and correct the identical `returned 3 hits before this revision and **0** after` in the v1.41 Version History entry — Version History is exempt for line pins only, never for factual claims (the precedent the paired design set at its own v1.96).
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `returns **nothing** at `35698f9` after this revision — 3 hits before it.`
- The `docsections.json` narrow-`command` justification rests on a false behavioural premise about the harness: `command` has **four** consumers in `run_spec`, not one. Verified by reading `h-mad/scripts/h_mad_mutation_harness.py`: `:562` `if not _suite_is_green(command, root):` → `result["verdict"] = "BASELINE_NOT_GREEN"` (the precondition gate for the whole run), `:679` the survivor-branch diagnostic the document names, `:694` `suite_green, suite_output = _run(command, root)` (the scoring path for any row with **no** `test` key), and `:721` `result["baseline_green_after"] = _suite_is_green(command, root)` → `RESTORE_FAILED` (the post-run read-back). One command re-derives it: `grep -n 'command' h-mad/scripts/h_mad_mutation_harness.py`. This breaks the base "Assumption verification" and "Mutation verification" rules — the conclusion drawn ("costs nothing") is asserted from a reading of the source that the source refutes, and the two consumers omitted are precisely the run's own state re-reads. Scoping it honestly, because the prescription must name the real cost and not the one I first assumed: `:694` is moot once Task 1 gives all eight rows a `test` key as planned, and the already-red-killer hazard is separately caught per row by the pre-mutation check at `:630-641` (`named test … was already failing … so a kill would measure nothing`), which runs `scoring_command`, not `command`. What genuinely remains is `:562` and `:721` — with `command` scoped to `tests/test_docsections.py`, neither the baseline gate nor the "prove the tree is back" re-run ever executes `tests/test_h_mad_doc_block_exec.py`, the file holding two of the eight killers and the file two of the eight rows will be re-anchored into. Prescription: strike "the only consumer", enumerate the four with their line pins and enclosing symbol (`run_spec`, `:482`), and re-state the decision on its actual ground — either widen `command`, or say explicitly that the baseline and restore verification are deliberately scoped to `test_docsections.py` and name what that leaves unverified.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `The only consumer of `command` is the survivor-branch diagnostic`

## Should-fix
- Locator 13 of the 13 is not runnable as written: it names a needle but no target file, so the hard condition the same bullet imposes ("returns **exactly one hit at the commit the revision that writes it ships against**") cannot be evaluated by a reader without inferring the sibling. I supplied the target and it does return exactly one hit at `6f0ee85` (`grep -c 'guard it removes' docs/02-design/features/doc-block-exec.design.md` → 1), so the property holds — the defect is that the published command does not express it, which is the same class as the bare-filename pins DECISION C just closed. Prescription: write the path, as the other 12 do.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › ``grep -n 'guard it removes'` — count those whose mechanism column names`
- Class note, not a new instance: the symbol half of the tree-pin rule has no detector, and the only way to know it holds is to read every pin. I read all of them at HEAD and found no counter-instance — including the three that read oddly and are in fact correct (`:272`/`:273` carry their `assert` verbatim with `_gate_bash_block` named in the same sentence, `:309` names the enclosing `test_documented_gate_recipe_halts_instead_of_gating_an_empty_path` `:294`, `:40` quotes the module-level `SKILL = Path(__file__).resolve().parent.parent` statement instead of a symbol, which residual (i) already licenses as the stronger anchor). Recording this so the next cycle does not re-derive it, and so a later reviewer does not read the absence of a detector as an absence of a check.

## Nit
- `returns **nothing** at `35698f9` after this revision` stamps the *pre*-revision sha for a post-revision state. The revision ships at `6f0ee85`; `35698f9` is the base it was authored against and the sha the before-figure belongs to. Stamping both — before at `35698f9`, after at the shipping commit — removes the only ambiguity in an otherwise fully re-derivable paragraph.
- The header's three sibling pins (design v1.96 / spec v1.58 / plan v1.91) are each one behind at `6f0ee85` (v1.97 / v1.59 / v1.92, re-derived with the header's own command). This is the drift the header declares as expected and is explicitly not a finding; noted only so the next reviewer does not spend a cycle on it.
