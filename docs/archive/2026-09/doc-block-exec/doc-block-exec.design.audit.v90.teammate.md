## Summary
Gating pass on design v1.99 at freeze `7982c18`. I re-ran every figure v1.99 publishes: the 6-gram
carry screen reproduces `BODY: 8 runs, 3 of them figure-bearing` / `VERSION HISTORY: 15 runs, 8 of
them figure-bearing` exactly, the dottedness clause really does print twice (`2`), the 36-candidate
sweep is `36` at `cf3a862`, the AC figures are `7` of `49` and `15`, the tail ordinal count is `8`
at `35698f9`/`cf3a862`/working, the seam-enumeration command prints the identical eight-member set
three times at `(sites=1)`/`(1)`/`(2)` at **both** `cf3a862` and `7982c18`, and the sibling-ORDER
correction is TRUE as measured (design/spec/impl canonical sentences agree on the seven module
seams; design and impl transport sentences agree on the same other order). The three defects below
are all in what v1.99 *added*: an absence-site set that is under-derived inside its own needle, a
new span-ordinal control that has no control and cannot see this document's own idiom for a span
ordinal, and a "both sides are committed blobs" claim that the shipped code does not honour.
Evidence: 12 files opened, 55 greps/commands run.

## Must-fix
- The absence-site set is under-derived *inside the sweep's own needle*, so the coverage claim "every one of the four is labelled" rests on a wrong denominator — the `census()` arm-(1) and arm-(2) zeros are absence claims about a corpus, they sit on candidate lines the sweep raises (`716`, `721`, `729` at `cf3a862`, all matching `` `0` ``), and the document already labels them with the rule's own vocabulary two paragraphs earlier, yet neither is in the four. That is decision G's failure mode with the rule's own instrument: the residual v1.99 added only excuses misses *outside* the needle, so it does not cover these. A probable third is the `^ {4,}` fence-bound measurement (line `1809`, `returns `0` at `6f0ee85` and `0` on the working file`), an absence claim about this document that carries its reason in prose but none of the three labels — I flag that one as a reading, not a measurement. Prescription: state the site count as **six** (or seven), or narrow the sweep's stated scope to sites the four-way classification actually partitions, and add "a candidate line whose zero is *about a corpus but printed by a control harness*" to the residual.
  quote: docs/02-design/features/doc-block-exec.design.md › `being written. At `cf3a862` it raises **36** candidate lines, of which four sites are absence`
  quote: docs/02-design/features/doc-block-exec.design.md › `carrying an info string, so its `0` is *vacuous* — correct today and worth nothing the moment a`
  quote: docs/02-design/features/doc-block-exec.design.md › `**That is a property of the corpus`
- The span-ordinal control v1.99 introduced ships with **no control at all** and returns `0` on this document's own canonical way of writing a span ordinal, so its `1` cannot carry the conclusion drawn from it. Measured at `7982c18`: the composite returns `1`; per branch `index`→`0`, `block`→`0`, `blocks`→`1`, `--index [0-9]+`→`0`, so three of four branches have never fired (decision O exactly — the healthy `blocks` branch is the whole result). And it is a false negative on the document's own idiom: `printf 'the gate block is 4\n' | grep -cE '\b(index|block|blocks)[ =]+[0-9]+|--index [0-9]+'` → `0`, and the body writes precisely that shape at `:1226`. `the second block`, `the exec codex block is 2` and `struck the fourth block` all score `0` too. So a §Version History entry that stated a span ordinal without its base would be invisible, and "the rule is satisfied there rather than merely asserted" is still an assertion. This is an **instance of: the class of span-ordinal addresses written as word-ordinal-plus-noun**, which the `$O`/`$G` machinery two screens up already handles — the rule over the class is to run that machinery with a span-noun set (`row|line|block|entry|element|position|column`) over the tail, and the residual is nouns outside that set. Prescription: ship a positive control (a two-line fixture the needle raises) and a true-negative that shows it declines a based ordinal, or replace the needle with the `$O`-based form.
  quote: docs/02-design/features/doc-block-exec.design.md › `**output** — a printed count, not an index into a span — so no entry states a span ordinal`
  quote: docs/02-design/features/doc-block-exec.design.md › `   without its base and the rule is satisfied there rather than merely asserted.`
  quote: docs/02-design/features/doc-block-exec.design.md › `   them): the gate block is **4** and the `exec codex` block is **2**. Over the`
- The carry screen's immunity claim is false about the shipped code: only **one** side is a committed blob. `git diff {base} {head} -- {doc}` reads two trees, but the report side is `open(rep, ...)` — a working-tree read of a path, with no `git show`. The document knows how to read a blob and does it four paragraphs later (`case $f in *design.md) src=$(cat "$f") ;; *) src=$(git show "$SHA:$f") ;;`), which makes the omission here a slip rather than a convention. Decision N is exactly the check that a control which *says* it reads committed blobs really does, and this one does not: its immunity rests on the report file happening to be tracked-and-unmodified, a property nothing in the fence enforces and no reader can see. The output is correct today — I reproduced 8/3 and 15/8 — so this is a false claim, not a false figure. Prescription: read `$R` with `git show <sha>:$R`, or drop "BOTH" and state that `$R` is a working-tree read of a tracked file and is immune only while that holds.
  quote: docs/02-design/features/doc-block-exec.design.md › `# BOTH sides are committed blobs, so nothing written into the working file can move this output.`
  quote: docs/02-design/features/doc-block-exec.design.md › `seen = tok(open(rep, encoding='utf-8', errors='replace').read())`

## Should-fix
- `NUM`'s word list does hide figures, so "never hides them" is a one-sided property the list does not have. Verified by fixture against the shipped tokeniser and regex: `'fifteen runs'` → not figure-bearing, `'forty runs'` → not figure-bearing, and `'twenty-two runs'` → not figure-bearing because `-` is inside the token class so `twenty-two` is one token that `NUM` never matches, even though `twenty` is listed. `'22 runs'` and `'twenty runs'` are caught. The gap is unexercised on the published input — I grepped the added lines of `35698f9..6f0ee85` for `fourteen|fifteen|…|ninety|hundred|twenty-*|thirty-*` and found none — so no published figure moves. Close the class: `WORDS` is a composite whose uncovered members are `fourteen`–`nineteen`, `forty`–`ninety`, `hundred`, and every hyphenated compound; name them rather than asserting the safe direction.
- The widened `$N` alternation ships with no per-branch control, which is decision O on the alternation this revision itself edited. The two positive controls fire only `seam` and `injection`, both singular: over the v1.95 blob, `$N=(seam)` → `4`, `(injection)` → `1`, `(seams)`/`(injections)`/`(primitive)`/`(primitives)` → `0` each, and the composite prints the same `4` under the plural and singular forms, so the widening moved nothing anywhere in the document's own evidence. I built the missing fixtures and the branches *do* fire — `the third seams`, `the third injections`, `the third primitives`, `the third injection`, `the third primitive` each score `1` — so the screen is not broken and the defect is the missing control, not a wrong claim. Publish that per-branch row beside the `0`.
- The `` ^ {0,3}(`{3,}|~{3,}) `` fence alternation is controlled on one branch only, and the other branch is unexercised everywhere it is used. Both census control fixtures build their fences from `B = '`' * 3`, and the tracked corpus contains **0** lines matching `^ {0,3}~{3,}` across the 30 `*.md` files under `h-mad`/`handoff` with `archive/` excluded. So `$STRIP`, `FENCE` and `SHIPPED` all ship a tilde branch that no control and no corpus has ever moved — a healthy backtick branch masking an untested one. A `~~~` variant of `c1.md` is a two-character control.
- Two figures new in v1.99 read a **sibling** from the working tree with no sha, four paragraphs after a command that stamps its siblings on purpose. `comm -23 <(grep -oE 'AC-[0-9]+\.[0-9]+' docs/01-plan/features/doc-block-exec.spec.md …)` and the table-scoped `grep` both name paths with no `git show`, so `7` and `15` move if the spec is edited — standing E and F. I re-ran both: `7` of `49`, and `15`, hold at the freeze, and I additionally expanded the design's 11 en-dash ranges and confirmed all seven (`AC-1.2`, `AC-2.3`, `AC-3.2`, `AC-3.4`, `AC-3.5`, `AC-4.4`, `AC-6.3`) are range-covered, so the *claim* is sound. Stamp the spec side.
  quote: docs/02-design/features/doc-block-exec.design.md › `SHA=cf3a862   # the two siblings are read at a sha; this document is read as the working file`
- The *incidental* label v1.99 added to `grep -c parametrize` → `0` is applied at one of the three sites that state it; the same figure is restated twice more under §Test Plan with no label, in a revision whose own carry-screen fix was explicitly a three-site sweep. Same figure, same rule, different treatment two hundred lines apart.
  quote: docs/02-design/features/doc-block-exec.design.md › `h-mad/tests/test_h_mad_portable_timeout.py` → `2`, and `grep -c parametrize` → `0` on both`
- `$R` is named only as "the v87 teammate report" — no path — so the published `8`/`3` and `15`/`8` are not re-derivable without guessing. I guessed `docs/02-design/features/doc-block-exec.design.audit.v87.teammate.md` and got the published numbers exactly, so the figures are sound and this is a reproducibility gap, not a wrong figure. One path in the `# $D … $R …` comment line closes it.

## Nit
- The disposition of the 8 body runs sums to 8 only on a non-obvious parse. "Two are commands, three are fixture descriptions and one fixture's own printed result, two are phrasings of a rule and of its residual, and **one** is the dottedness clause" reads at first pass as 2 + 3 + 1 + 2 + 1 = 9; it works only if "three are fixture descriptions and one fixture's own printed result" is one group of three. A semicolon or "three, of which one is" fixes it. The classification itself is right — I printed the eight run texts and the three figure-bearing ones are exactly the dottedness clause, the `1 where commonmark says 0` fixture result, and the pronoun `one`, as stated.
- "`$R` is a report committed at `cf3a862`" reads as "committed *in* `cf3a862`"; `git log --diff-filter=A` shows that file was added at `6f0ee85`. Under the intended "committed as of" reading it is true, and the immunity argument only needs that reading, but the two v88/v89 teammate reports *were* added at `cf3a862`, which makes the ambiguity live in this exact paragraph.
- For the record at the freeze sha: the candidate sweep is `42` at `7982c18` against the `36` stamped at `cf3a862`. The document explains why it does not restate a working-file value and I agree with the decision; a reader re-running at the freeze still gets a different number with nothing to reconcile it against, and "the delta is the labels this revision wrote" would cost one clause.
- Figures I did **not** challenge this pass — unchallenged is not verified, and this list is the register: the Setext-census harness output in full (`control arm1/arm2 shipped 1 repaired 0`, `tracked files 30 … arm2 8 marker lines / 4 fences / 2 files … 9 body lines … 0 below 4 columns`) — I re-derived only the corpus size and the tilde reach, not the census run; the `5` dotted-seams `tee /dev/stderr` derivation; the dotted-form two-line fixture printing `1` against the predecessor's `0`; the line-scoped predecessor printing `3` on the v1.95 blob; the property-(ii) run where dropping `$STRIP` turns `0` into `1`; the named-anchor block census (`lines 159 blocks 7 gating 1`, gate `4`, `exec codex` `2`) and the `1861157` pair; both line-pin blind-form sweeps at `:1118` and `:1152`; the 14/14 markdown-it grammar oracle and the `files=25`/`30` Setext transcript, both of which the document itself marks non-re-derivable; and the whole of §Error Handling Strategy, §Test Plan's AC table and the helper mutation-spec table, which I read for cross-document contradictions but did not re-derive against the tree. I also did not verify any claim about tests that do not exist yet, since the feature is unimplemented.
