AUDIT-doc-block-exec-design-v86-BEGIN

## Summary
I audited the v1.94→v1.95 repair first, then swept the document. Of the seven self-reported claims
the brief asked me to check, five reproduce exactly — I extracted all three edited bash fences
verbatim from the shipped file and ran them (differential, eleven-shape ATX proxy, awk line-pin
check), plus the ast symbol one-liner and the block census, and every printed field matches the
document byte-for-byte; the two-`<h1>`-one-empty render, the `## Text\t##` oracle, the ordinals
(gate 4 / exec-codex 2 over both spans), the parametrize censuses and the softening-table counts
all hold. Two defects survive: the one published command whose stated output is **wrong** happens
to be the sha-equality premise every other figure hangs from — and it trips the document's own
trip-wire — and the seam-ordinal rule closed one member while leaving two live ordinals in the
very section it names as governed, both wrong under both of the document's own enumerations.
Evidence: 15 files opened, 27 greps run.

## Must-fix
- The published sha-equality command's stated output is false, and the document's own trip-wire fires on its own tree — `git diff --name-only a8e0372 335f535` names **13** files, of which **11 are `.md`**, not "two files, both `.py`". The document immediately instructs a reader to re-run that exact command and, "if it ever names an `.md`", to distrust *every* `a8e0372` figure below — so a reader who obeys the sentence written directly above is told to discard the whole measurement set. This is the load-bearing premise for every remaining `a8e0372` figure in the document, and §"Assumption verification" is explicit that a command's output shape must be executed and the observed output cited; this one asserts an output that one execution refutes while claiming it was "checked rather than assumed". The *conclusion* is still true, but for a reason the sentence does not state: all 11 `.md` are under `docs/`, outside the two roots the corpus is drawn from. Prescription: scope the command to the roots it is reasoning about — `git diff --name-only a8e0372 335f535 -- h-mad handoff`, which I ran and which names exactly `h-mad/scripts/h_mad_assemble_audit.py` and `h-mad/tests/test_h_mad_assemble_audit.py` — and restate the trip-wire against that scoped form. The identical false claim also sits in the v1.95 Version History entry; §Version History is exempt for *line pins* only, by this document's own words, never for factual claims, so both sites need the fix.
  quote: docs/02-design/features/doc-block-exec.design.md › `names two files, both `.py``
  quote: docs/02-design/features/doc-block-exec.design.md › `an `.md`, every count carrying `a8e0372` must be re-derived rather than re-read.`
  quote: docs/02-design/features/doc-block-exec.design.md › `names two files, both .py`

- The seam-ordinal rule was applied to one member and not closed as a class — two live ordinals survive in §Error Handling Strategy, and both are wrong under **both** published enumerations. §Test Strategy now states the rule at its head and says in terms that it governs "every seam mentioned in §Error Handling Strategy above". It does not: `os.unlink` is addressed there as "the eighth named seam, Test Strategy", and `_final_write` as "the fifth named injection". Under §Test Strategy's list (`os.killpg`, `shutil.rmtree`, `tempfile.mkdtemp`, `os.chmod`, `os.unlink`, `_final_write`, `_close_stream`, + the `Popen` wrapper) `os.unlink` is **fifth** and `_final_write` is **sixth**; under the `main(argv)` list (`_final_write`, `_close_stream`, `tempfile.mkdtemp`, `os.chmod`, `shutil.rmtree`, `os.killpg`, `os.unlink`, + the wrapper) they are **seventh** and **first**. Neither ordinal is right under either — `_final_write`'s "fifth" is the stale v1.23 ordinal, which is precisely the drift the new rule exists to stop. This is a self-contradiction between a rule and the section it names, and it is the exact defect v1.95 struck for `_close_stream` ("the sixth" → seventh): member N was fixed and members N+1 and N+2 were left standing. **The rule over the axis**: no member of the eight-seam set may be addressed by ordinal anywhere outside §Version History; strike both, naming the seam instead. **The check**, which the document should publish beside its awk one: `awk '/^## Version History$/{v=1} !v' <doc> | grep -nE '\b(first|second|third|fourth|fifth|sixth|seventh|eighth|ninth)\b[^.]{0,60}\b(seam|injection|primitive)\b'` — it returns exactly these two plus one more today. **Residual, exactly**: that third hit, "without a ninth seam", is a *cardinality* reference (a hypothetical additional seam) rather than an address of a member, and so is "add no sixth" once the "fifth" beside it is struck. Those are stable while the set has eight, but the rule as worded ("named, never numbered") does not say whether they are in or out — say which, or the next author strikes them wrongly or leaves the next real one.
  quote: docs/02-design/features/doc-block-exec.design.md › `(the eighth named seam, Test Strategy)`
  quote: docs/02-design/features/doc-block-exec.design.md › `injection** — the `_final_write` seam — and add no sixth`

## Should-fix
- The Setext census is the same shape as the "14 of 14" premise SHOULD-3 just closed, and got none of the same treatment — instance of: the class "a cited measurement whose script is a throwaway that is not in the tree". The document cites the plan's §Measurements "Setext census" and adds a re-run "at `a8e0372` over the 30 tracked files, and `0` again over the 35-file glob". The plan's §Measurements publishes a transcript at a *different* sha (`1861157`, `files=25` / `files=30`), so the cited locator does not contain the re-run this document claims; and the script it names, `heading_differential.py`, is untracked and absent from disk (`git ls-files | grep -cE 'heading_differential|grammar_corpus'` → `0`), so neither the cited run nor the claimed re-run is re-derivable — which is exactly the condition that made this same revision declare "14 of 14" not re-derivable, publish a proxy, and state a residual. **The figure itself is correct**: I re-implemented the census independently (CommonMark §4.3 underline after a paragraph line, fence-aware, YAML front matter skipped, list/table/blockquote/indented-code excluded) over the 30 tracked files at the frozen tree and got `setext_headings=0`. So this needs the honesty treatment and a runnable command, not a correction. **The rule over the axis**: every measurement this document cites must either publish its command inline or name a script that `git ls-files` can find. **Residual**: the census above closes it for these two scripts; any newly cited script needs the same check.
  quote: docs/02-design/features/doc-block-exec.design.md › `(re-run at `a8e0372` over the 30 tracked files, and `0` again over the 35-file`

- The published awk line-pin check certifies a class, but its own residual is unstated — the document carefully states the residual of the enclosing-symbol *locator* (two `def`s of one name; silent staleness on rename) and then stops, leaving the *detector* that proves the class closed with no residual at all. Its pattern matches only `<name>.<py|md|json|sh>:NNN` and a backticked `` `:NNN` ``. It would not see `line 412`, `lines 403-421`, `L412`, or a bare `:412` not preceded by a filename-shaped token — so a regression written in any of those forms passes the check the document offers as proof. I swept the body for all four forms and found none, so the class **is** closed today and the `0` is honest; the gap is that the published check does not prove what it is offered as proving. Prescription: one clause naming the forms the pattern matches and the forms it does not, in the same style as every other residual here.

## Nit
- The claimed verbatim output block under the differential fence shows `30` and `35` flush-left, but the fence's `… | wc -l` right-pads on this platform (the run prints `      30` / `      35`). Every other line in that block matches byte-for-byte, so a reader diffing the block against a real run sees two spurious differences in a block whose whole point is that it is read off a run. Either use `wc -l <` / pipe through `tr -d ' '`, or show the padding.
- The AC-6.1–6.6 Test Plan cell is now a single ~1,100-word paragraph inside one table cell, and this revision roughly doubled it. Every sentence in it is load-bearing and I verified its claims, but a table cell that renders as one unbroken block is the least reviewable shape available for the document's densest reasoning; the floor-tuple evaluation in particular would read as prose beneath the table with the cell pointing to it.

AUDIT-doc-block-exec-design-v86-END
