## Summary
All four orchestrator claims I was asked to verify independently hold, and every published figure I
re-ran from the diff reproduces exactly — the 13 sibling locators (one hit in target at `7d8e797`
**and still at `1cbddb7`**, with the one known off-target intact), the stamp screen at 11/5/22 and
0/13, the `4e4a00c..7d8e797` sibling diff naming the plan and design but not the spec (still true at
`4e4a00c..1cbddb7`), and the MUST's class repair, which holds on both halves: no register figure is
described as `verified` anywhere in the body, and the register's stated cardinality of three matches
its three members. **One repair broke a screen elsewhere in the document**: the NIT-4 fix wrote the
literal token `**(4)**` into the body, so the marker screen — whose reading the same section
publishes as 5 and claims was re-run after the edits landed — now returns 6.
Evidence: 7 files opened, ~135 greps and commands run.

## Must-fix
- The NIT-4 repair injected a sixth bolded marker into the body and falsified the marker screen's own published reading three paragraphs above it. The repair at `:400` quotes the failing `sed` pattern verbatim, and that quotation contains the literal `**(4)**`. Run on the shipped tree, `M='\*\*\([0-9]\)\*\*'; awk '/^## Version History/{exit}{print}' <this file> | grep -oE "$M" | wc -l` gives **6**, whole-file gives **6**, and the per-marker reading prints `**(1)**` 1, `**(2)**` 1, `**(3)**` 1, **`**(4)**` 2**, `**(5)**` 1 — precisely the arm-(a) signature this document publishes as "both arms fire". The same command gives **5** at `6dcb70f`, **5** at `7d8e797` and **6** at `1cbddb7`, so this revision introduced it. The screen's stated target is `len(list)` = 5, so this is a fail and not a target drift; and the decision-K sentence asserting the screen was re-run *after* the edits is falsified by the reading itself — had it been re-run it would have printed 6. This is the second time in three revisions that this screen's reading has been published wrong (v1.44 published 5 unrun and it was 6, impl-plan audit v42; v1.45's 5/5/five-at-1 was correct and I re-confirmed it at `6dcb70f`). **The class, not the instance**: any repair that quotes a mutation string, an error message, or a diff hunk verbatim re-introduces the token the screen counts, and the screen cannot distinguish a quotation from a member. `instance of:` the class *a screen whose needle can be written literally inside prose that describes the screen*. The rule that would close it: **no screen's needle may appear literally anywhere in the scope that screen counts** — a quoted needle is paraphrased, e.g. `s/<the bolded member-4 marker> the 5f/…/`, which reproduces the mechanism without reproducing the token. Residual: the rule covers only needles that are literal strings; a screen whose needle is a regex class could still be matched by prose that does not contain it literally, and nothing here detects that.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `` `sed: 1: "s/**(4)** the 5f/(4) th ...": RE error: repetition-operator operand invalid` ``
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `On the tree this revision ships, re-run **after** this revision's own edits landed rather than before them (decision K): body **5** occurrences, whole file **5** occurrences, and the third reading prints **five lines, each with count 1**.`

## Should-fix
- The residual naming which stamps are **not** certified is under-inclusive, though its own numbers are exact. I re-ran the screen and got `4e4a00c` 11, `68a70d6` 5, `cf3a862` 4, `335f535` 19, `1861157` 6 — all five right, 45 readings. But `awk '/^## Version History/{exit}{print}' <this file> | grep -oE 'at \`[0-9a-f]{7,40}\`' | sort | uniq -c` shows the body stamps **fourteen** distinct shas: the five named, plus `7d8e797` 22 (the certified set), plus **eight unnamed** — `35698f9` 12, `b7d0d77` 9, `74e126f` 6, `e8eaf6f` 5, `6f0ee85` 4, `6b4df35` 2, `8909ec4` 1, `0aac0b7` 1 = **40 further readings** this revision did not re-run and does not name. One of them is a `verified at` stamp: `grep -n '^## Run-context ceiling' h-mad/SKILL.md`, "one hit, verified at `35698f9`". It is legitimately outside the 13-needle sweep, which is scoped to the three sibling documents while this needle targets a tree file, and it still holds — `git diff --name-only 35698f9 1cbddb7 -- h-mad/SKILL.md` is empty and the grep returns 1 today. So it is unenumerated, not stale; the point is that the residual reads as the full set of surviving stamps and covers 5 of 13. By this revision's own words at AC-6.4 ("an enumeration with a member quietly dropped is not an enumeration"), the residual should either name all thirteen non-`7d8e797` shas with their counts or publish the screen as `sort | uniq -c` over all shas, so the set is derived rather than typed.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `it says nothing about the `4e4a00c`, `68a70d6`, `cf3a862`, `335f535` and `1861157` stamps that remain because this revision did **not** re-run what they cover (11 / 5 / 4 / 19 / 6 readings respectively)`
- A carried sentence at `:519–521` contradicts the hunk this revision inserted three lines above it in the same paragraph. The revision added "**0** again at `7d8e797`, the sha v1.46 is authored against and stamps its re-runs at" at `:517`, and left standing a parenthetical that still describes this as v1.44 and asserts a stamp closure the revision's own screen refutes: `4e4a00c` carries 11 body stamps against `7d8e797`'s 22, so "every figure here names `4e4a00c`" is false under any reading of "here" wider than the sentence. This is the same closure-assertion class the SHOULD-1 repair was written to remove, surviving twelve lines away from the repair.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `it is the base v1.44 is written **against**, and the shipping commit of this revision does not exist while it is being written, which is why every figure here names `4e4a00c` and not a promise.`
- The new Version History "owed to a sibling" note states what the design **currently says**, which this document's own Conventions rule forbids, and it is false at the commit it ships in. `git show 1cbddb7:docs/02-design/features/doc-block-exec.design.md | grep -c 'four re-anchored originals'` is **0** (it is **1** at `7d8e797`); the design at `1cbddb7:2571` now reads "**the four originals**" and explains that the adjective was deliberately dropped. So the collision the note reports to the orchestrator as owed is already resolved unilaterally on the design side, and an orchestrator acting on the note would re-dispatch a harmonisation that no longer has two sides. This is exactly the failure the rule at `:351` names — a statement about a sibling that outlives the cycle it belongs to.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `the design's audit raises a term collision with this document on "re-anchored" (design: "the four re-anchored originals"; here at Task 3's `docsections.json` row list: "the four existing (two re-anchored)")`
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `**No sentence in this document states what a sibling document currently says.**`
- AC-6.1 now lists **four** datings but still tells the reader the conclusion is unmoved by "which of the three you get". The revision rewrote the immediately preceding clause to "which **one** you get" and extended the list with `2088`, and did not move the cardinal in the next sentence; `sed -n '2403p' <this file>` carries both spans on the same line. By this document's decision H (a count in a tuple carries its identity), the cardinal should go — "which of them you get" — rather than be re-typed as four, since the list is by construction open-ended.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `Datings, kept as illustration only: 2075 at `4e4a00c`, **2076** at `68a70d6`, **2079** while v1.45 was being written, and **2088** on the working tree while this revision was being written, one round later.`
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `The control's conclusion is unmoved by which of the three you get`

## Nit
- The sixth of AC-6.4's eight falsification readings is the only one whose command is not reconstructible from the text. Seven reproduce verbatim from "counting `def test_` across every `test_*.py`" plus the stated qualifier; "unique names via `sort -u` **2691**" does not, because `sort -u` over *lines* gives 2697 (unanchored) or 1446 (anchored). It reproduces only as an extraction: `git grep -hoE 'def (test_[A-Za-z0-9_]*)' e8eaf6f -- '*test_*.py' | sort -u | wc -l` = **2691**. Naming the `-o` extraction would make the member re-runnable like its seven siblings.
- The 5f gate paragraph at `:2835–2836` states `2748` and `2486` as "the pair this document uses everywhere" with no local label; the register that labels them sits eighteen lines below at `:2853–2861`. The class rule this revision writes is scoped to "every **AC** site that uses it", which does not reach this site — and this is the paragraph a 5f operator reads, not AC-6.4. Low risk because both sit inside §Verification (Phase 5f), but the rule's stated scope is narrower than the sites that matter.
- `:2845` reads "the predicate behind 2675 is not published here, so the **invariance** is verified and the absolute is not." AC-6.4 now *does* publish a predicate and reports it falsified for 2675; "here" is carrying scope work a reader can take as a claim about the document rather than about the paragraph.
