## Summary

This pass is GATING and I read the tree rather than the inlined copy. The design's executable
self-measurement is in very good shape: I extracted and ran the heading differential, the ATX-shape
census, the committed `heading_differential`/`setext_census` probes, the AC-coverage expansion, the
intersecting-span probe, the 13,104-case exhaustive search, the NUL and `umask` probes, the
seam-ordinal and line-pin screens, the AST slicer sweep and both compliance walks, and every one
printed byte-identically what the document publishes (`132`/`71`, `spec 49 covered 49 uncovered []
not-in-spec []`, matrix 86 rows / 86 distinct names / 1 `SKILL.md` target, `13104`/`194`,
`files 30 both 292 old_only 82 new_only 0`, slicer sweep 23, precheck `PASS issues=0`). What is
wrong sits in the stamping layer around those runs: the document's own scoped-diff trip-wire has
FIRED at the measurement commit and at HEAD and no reading records it, the prose asserts the
opposite about the commit that fired it, one owed-elsewhere debt was never routed and the sibling
now contradicts the design and the spec on a diagnostic three documents must spell identically, and
one of the four verify-only self-counts is off by one in the direction its own clause warns about.
Evidence: 13 files opened, 46 greps run.

## Must-fix
- The document's own scoped-diff trip-wire has FIRED and no reading in the document records it — the published invariant carries `# expect 0`, its newest stamp is `cf3a862`, and it returns **8** at the measurement commit `cac6edc`, at the batch commit `ccd8ebd` and at HEAD `093c3ee`. The eight are `h-mad/SKILL.md`, the five `h-mad/agents/*.md` and the two `h-mad/references/*.md`, all changed by `b39d9dc`; the scoped form `git diff --name-only a8e0372 cac6edc -- h-mad handoff | grep '\.md$'` names the same eight. The five readings the prose enumerates are true (I re-ran all five and got 0), so this is an unstamped invariant rather than a false one — but the document states the stamping rule for itself ("The invariant is stamped at each new sha instead") and this revision did not apply it, which is exactly how a trip-wire stops being one. Prescription: stamp it at the batch with its real value 8, name the eight paths and `b39d9dc`, and state which `a8e0372`-stamped figures were re-derived. Class, and the residual: every `expect 0` screen this document publishes must be re-run at the commit the revision ships; I ran the other three (line-pin strict at `awk … | grep -nE '…\.(py|md|json|sh):[0-9]+…'` → 0, its space-tolerant folded twin → 0, the seam-ordinal fold-and-strip → 0, with the indented-fence bound → 0) and all three are honest, so this is the one member that moved.
  quote: docs/02-design/features/doc-block-exec.design.md › `git diff --name-only a8e0372 <sha> | grep '\.md$' | grep -vc '^docs/'   # expect 0`
  quote: docs/02-design/features/doc-block-exec.design.md › `if *it* ever names an `.md`, every count carrying `a8e0372` must`
- The sentence that grounds the "a corpus specimen is a measurement" rule is inverted about the commit it names — `b39d9dc` did **not** pass either scoped census predicate. Measured: `git diff --name-only b39d9dc^ b39d9dc -- h-mad handoff` names 13 files, one of them `h-mad/SKILL.md`, the very file whose line moved; `git diff --name-only b39d9dc^ b39d9dc -- '*.py'` names 4 (`h_mad_assemble_audit.py`, `test_h_mad_agent_definitions.py`, `test_h_mad_assemble_audit.py`, `test_hmad_dispatch_exec.py`). So the guards this feature publishes did see the commit and nothing read them, which is a stronger and more useful finding than "a commit that touches nothing the document guards". The fixture decision the paragraph reaches is right and should stand; only its stated ground is false. `instance of:` the same class as the bullet above — a published trip-wire that was never read at the batch.
  quote: docs/02-design/features/doc-block-exec.design.md › `since `b39d9dc` passed every scoped census predicate this`
- The `<offset>` definition — which this document declares itself the owning document for — was reworded here and in the spec and the debt was never routed, so at the gating tree the impl-plan contradicts both. Measured at HEAD: `grep -cF 'shared by *any* intersecting span pair'` returns 1 in the design and 1 in the spec and **0** in the impl-plan, while `grep -cF 'the two spans SHARE'` returns **3** in the impl-plan, two of them body sites (`docs/01-plan/features/doc-block-exec.impl-plan.md:2094`, the `pairs` construction comment, and `:2463`, the AC-2.7 field rule) and the third inside the v1.53 Version History entry at `:4146`; it was 2 at `cac6edc` — the r18 batch *added* a site of the retired single-pair wording while the design and spec were retiring it. The v1.110 owed-elsewhere list names four impl-plan debts (86 rows, single-field `OverlappingSubstitution`, the `__suppress_context__` assertion, the `LaunchFailed` annotation) and I confirmed all four are discharged at HEAD; this fifth one is missing from the list. Class, with its residual: a shared definition this document owns and rewords carries an owed-elsewhere entry naming every sibling site by command, and the residual is the two other owned field lists the document already names, `StreamPathUnwritable.leftover` and `StreamWriteFailed.verify`.
  quote: docs/02-design/features/doc-block-exec.design.md › `sibling is re-measured before it is restated, because the sibling's authors read these bullets`
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `(ii) `offset` is the **smallest character`
- The fourth of the four "verify-only" readings in the v1.110 entry is wrong by one under the convention its own three siblings use, and its qualifier is false for three of the five sites. The other three readings in that clause are whole-file counts and all three reproduce exactly (`collect-only` 10 at `cac6edc` / 12 shipped; `wire-unconditional` 2 / 3; the four suite tokens 0 / 4). Under the same command shape `tr '\n' ' ' < $D | grep -oF -- 'intersections' | wc -l` the shipped file returns **5**, not 4 — 2 in the body (`:1436`, `:2955`) and 3 inside the v1.110 entry itself — and only the 2 body sites are "in the new prose forbidding a second field". The `cac6edc` half (0) is correct. This is the self-reference the same clause records against its own first draft, surviving in the one member the correction did not re-run, and the document's own rule denies Version History any exemption here.
  quote: docs/02-design/features/doc-block-exec.design.md › `and intersections, at cac6edc 0, on this body 4, every one in the new prose forbidding a second field`

## Should-fix
- The import-convention census is stamped at a sha but published with a working-tree command, which is the exact correction this document applies 440 lines later to the `grammar_corpus` citation. The prose reads "Measured at `fbc2ea0`" for 88/13/14/48; run verbatim on the shipped tree the fence prints **89**/13/14/48, because `h-mad/tests` holds 89 `test_*.py` at `cac6edc` against 88 at `fbc2ea0` (`git ls-tree -r --name-only <sha> -- h-mad/tests | grep -cE '(^|/)test_[^/]*\.py$'`). Three of the four figures still hold, so nothing downstream is wrong — but a reader re-deriving gets a mismatch on the first line and has no way to reach the stamped one, and the plan has already re-stamped its own denominator to 89. Prescription: respell the first arm `git ls-tree -r --name-only <sha> -- h-mad/tests` and the per-needle arm through `git show <sha>:"$f"`, or publish both readings side by side as this document does for the differential fence.
- The AC-coverage fence reads the AC set out of the spec blob at `cac6edc` while the batch ships spec v1.64, so the derivation is pinned one commit behind the tree it is published on. It happens not to matter — I differenced the two AC sets and they are identical, 49 each with an empty symmetric difference — but the fence as written cannot show that, and the same fence run against a spec whose ACs moved inside a batch would print a clean `uncovered []` about the wrong blob. Prescription: run the expansion against the shipped spec file as well as the blob, exactly as the design-matrix derivation now carries both a `git show` arm and a working-tree arm.

## Nit
- The trip-wire fence is written with a `<sha>` placeholder while the sentence beneath it enumerates five concrete shas, so the one command in this document a reader is told to re-run before trusting anything below is the one they cannot copy and execute. Naming the newest concrete sha inside the fence, with the placeholder form beside it, would make it self-running like the neighbouring census blocks.
