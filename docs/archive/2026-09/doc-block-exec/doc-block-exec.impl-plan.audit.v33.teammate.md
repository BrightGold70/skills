## Summary

Opened every existing `path:symbol` the document names (`h-mad/tests/docsections.py`, `test_docsections.py`, `test_h_mad_collect_report_docs.py`, `h-mad/SKILL.md`, `h_mad_mutation_harness.py`, `h_mad_assemble_tdd.py`, `h_mad_collect_report.py`, `mutation-specs/docsections.json`, `conftest.py`) and re-ran the two collect-only baselines. Every cited existing **test name** exists; the harness line citations (`:606-607`, `:609-623`, `:645`, `:660-669`, `:679`), the assembler's wiring rule at `:238-243`, the consumer's `:8/:10-13/:22/:49/:267/:270/:281/:304/:309/:312/:340/:346/:368/:403/:412/:415` and every `docsections.py` offset (`:31`, `:32`, `:33-42`, `:46-52`, `:53`, `:60-65`, `def _find_heading` → 0) are all correct, as are the five row-count arithmetics (25+5+24+27=81, 23 verdict heads, 15 head fields, 7 bare + 19 quoted = 26 slots, 11 `DETAIL_KEYS`, 8 fault injections, 12 injecting Task 3 tests, 5 `real_rmtree` tests, 7 floor node IDs). Three defects block: every `h-mad/SKILL.md` line number in Task 5's WIRE target is stale by 93 lines; AC-6.1's tree sweep lost the `*.md` scope its own cited census carries, so it cannot pass at Task 5 GREEN against the feature's own fixtures; and one re-pointed `docsections.json` row's `replace` still names a variable the migrated source no longer binds, so that mutant is scored a kill on a `NameError`.

## Must-fix

- **Every `h-mad/SKILL.md` line number in Task 5's WIRE target is stale by 93 lines.** The document is what 5d executes literally and this is the one production edit of the task (tagging the gate fence). Measured at base `e8eaf6f`: `## Second surface — the codex leg` is at **`:1897`**, not `:1804`; the section's four ```` ```bash ```` fences open at **`:1902`, `:1915`, `:1925`, `:1938`**, not `:1809/:1822/:1832/:1845`; the gating block's `h_mad_audit_gate.py` line is at **`:1943`**, not `:1850`, and that block opens at **`:1938`**, not `:1845`. Every *structural* claim in the same parenthesis verified true — four `​```bash` fences in the section, exactly one containing `h_mad_audit_gate.py`, it is the last of the four, and the `exec codex` block (opening `:1915`) stays untagged — so the repair is six numbers, not a re-derivation. Left as-is, the implementer is pointed at a fence 93 lines away from the one the task means, in the one place the task changes production text.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `the heading is at \`h-mad/SKILL.md:1804\``

- **AC-6.1's sweep dropped the `*.md` scope of the census it is derived from, so `test_exactly_one_tagged_fence_in_the_tree` cannot pass at Task 5 GREEN — and its stated RED reason is also false.** The AC counts openers over `h-mad/` and `handoff/` with no file-type restriction, using `_fence_events`. The plan's census that AC-6.1 is bound to (design §Test Strategy: "the plan's census sweep asserting cardinality 1") globs **markdown only**. But by Task 5, `h-mad/tests/test_h_mad_doc_block_exec.py` has landed (Tasks 1–4) under `h-mad/`, and its fixtures carry ` ```bash hmad:exec ` as a column-0 line inside triple-quoted strings — AC-1.1 ("one tagged and one untagged fence"), AC-1.7's duplicate-heading fixture, AC-3.7's `shell=fish`/`hmad:exec hmad:exec` fixtures, AC-1.5's section fixtures. Column 0 is the house style for markdown fixtures in this tree (`h-mad/tests/test_docsections.py:27` is a bare `​```bash` at column 0), and by the stated grammar a 0–3-space marker run **is** an opener regardless of the enclosing file's suffix. So the sweep returns well over 1 at GREEN, and Task 5's RED prediction "zero tagged fences" is wrong for the same reason — those fixtures exist before Task 5 begins. Either restrict the sweep to `*.md` (matching the census) or state the exclusion of the test module explicitly. The design shares the omission, so this needs back-propagation.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `opening fences carrying \`hmad:exec\` across \`h-mad/\` and \`handoff/\` excluding any \`archive/\` path, counted with the module's own \`_fence_events\`, equal exactly 1.`
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `\`test_exactly_one_tagged_fence_in_the_tree\` fails (zero tagged fences)`
  quote: docs/01-plan/features/doc-block-exec.plan.md › `for p in sorted(Path('.').glob('*/**/*.md')):`

- **`docsections.json`'s `missing-heading-returns-empty-instead-of-failing` is re-spelled on its `find` only; its existing `replace` names `match`, which the migrated `titled_section` no longer binds — the mutant becomes a `NameError` and the row is scored `ALL_CAUGHT` while measuring nothing.** The Task 1 delta rewrites the lookup as `found = _dbe.find_heading(text, heading)` / `assert found, f"missing section {heading!r}"`; there is no `match` in the delegating body. Applying the row's unchanged `replace` therefore lands `if not match: return ""` in a function whose only names are `found`, `start` and `level`, so every `titled_section` call raises `NameError` instead of returning `""`. The harness still counts it: the named test goes red, `ran_and_failed` is satisfied at `h_mad_mutation_harness.py:660-670`, and `ALL_CAUGHT` is reported for a row whose `_mechanism` claims to prove the loud `assert` (the vacuous-`not in` guard) — it proves nothing of the kind. This is the same class v1.11 already fixed once for `docsections-heading-lookup-reverted` ("the restored regex would have raised NameError — a fix-introduced defect"); the re-spelling sentence needs to carry the `replace` too, e.g. `if not found: return ""`.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `\`missing-heading-returns-empty-instead-of-failing\` finds \`assert found, f"missing section {heading!r}"\``
  quote: h-mad/tests/mutation-specs/docsections.json › `"replace": "    if not match: return \"\"",`

## Should-fix

- **The suite-floor baselines are stale by one in both directions at the base commit.** Re-measured at `e8eaf6f` (working tree clean apart from untracked `*.md.done` files): `python3.11 -m pytest --collect-only -q -p no:cacheprovider` reports **2748** from the repository root and **2486** from `h-mad/`, not 2747 / 2485. The `≥` floor still holds, so this does not break AC-6.4, and the 262-test delta the 5f note relies on is unchanged (2748−2486 = 262). But the document states both as measured facts and re-asserts 2747 four times, including "**`2747` is the repository-root count and only the repository-root count**"; a reviewer re-running the command at base gets a different number and cannot tell a stale citation from a wrong cwd — the exact failure mode the paragraph exists to prevent. The design and spec carry 2747 too, so this is a three-document sweep.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `\`2747 tests collected\`, re-measured 2026-09-03; the same command from \`h-mad/\` reports 2485`

- **Task 5's RED step 0 is self-contradictory about whether the six new tests are part of it, and the RED gate depends on the answer.** Step 0 is declared "a pure refactor … suite green", and the RED gate says to run "after RED step 0's refactor has landed and the suite is green again" — but the same paragraph says step 0 also adds the six new tests, and RED step 1 (the very next paragraph) has four of those six failing (`test_gate_block_resolves_through_doc_block_exec`, `test_recipe_runs_through_run_block`, `test_gate_block_refuses_an_untagged_recipe`, `test_only_the_exec_scan_hand_rolls_extraction`). The suite cannot be green after a step that adds four failing tests. The sequencing is also unstated: step 0 edits `h-mad/tests/test_h_mad_collect_report_docs.py`, which Task 5 lists as a **Production file**, while `h_mad_assemble_tdd.py --phase red` prints "Write failing tests only. Do not modify production code." (`:230-231`) into the same dispatch. Say explicitly whether step 0 is a separate commit landed *before* the 5d RED dispatch (refactor only, suite green, no new tests) or the first half of the RED commit (tests included, suite not green) — the two readings give different RED gates.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `**RED step 0 — a pure refactor, no \`dbe\` call, suite green.**`
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `since no \`dbe\` **call** exists yet — plus the six new tests.`

- **The `__all__` comment's exception count contradicts its own list and the rest of the document: 5 + 19 ≠ 25.** Task 1's `__all__` is `Block, extract, select, fence_aware_end, find_heading` (5) plus **20** exception classes — `DocBlockError` and its 19 subclasses (6 + 3 + 4 + 6), which the same code block enumerates and which the description states correctly as "`DocBlockError` and its 19 subclasses (20 exception classes)". The parenthetical total 25 is right; "the 19 exception classes" is the wrong half and should read 20, matching the 29-name sentence two lines below ("+ 20 exceptions (BadArgs included)"). Left as-is, the one place an implementer counts `__all__` from disagrees with the list directly beneath it.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `Task 1 lists Block, extract, select, fence_aware_end, find_heading and the 19`

## Nit

- The Task 1 `docsections.py` delta annotates its import line with "`os` is no longer needed: `resolve()` does the absolute-path work `os.path.abspath` did". Today's `h-mad/tests/docsections.py` imports only `re` — there is no `os` in the file being edited. The sentence is about a superseded draft of the delta, not about the source, and reads as a claim about the tree.
- `exec-scan-executes` says its anchors were "verified at HEAD `8599e28`". HEAD is now `e8eaf6f`; both anchors are still exact-once (`grep -c` → 1 each), so the claim holds, but the commit pin is stale.
- The Conventions payload-timing bullet says impl-plan audit v20's must-fix "asked for all 76 payloads here" while the bullet now governs 81 rows. Historically accurate, but the two numbers sit in one sentence in a live (non-history) section.
- AC-6.2 asserts that a recording pass-through over `dbe.subprocess.run` stays empty. The helper spawns through `subprocess.Popen`, never `subprocess.run`, so that half of the assertion cannot be tripped by `exec-scan-executes`; the `dbe.run_block` spy carries the whole kill. Harmless as a belt, but worth saying which recorder is the discriminator.
