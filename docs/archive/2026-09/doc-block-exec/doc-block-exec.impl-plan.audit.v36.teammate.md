## Summary
Audited impl-plan v1.38 at the frozen tree `335f535`, delta-first (`a8e0372..335f535`). Every re-derivation the revision claims held on re-measurement: the nine tuple members, the `_SCANNED`/`REAL_AUDIT_REPORTS` AST residual, all eight `grep -n` locators at exactly one hit, the 30/30/35/0 corpus relation with empty symmetric difference and the five named `.pytest_cache/README.md` extras, the 81-row/`80 + 1` design matrix split, the two-of-eight `docsections.json` killer count, `_titled_section` at `:69` with eight call sites, and the portable-timeout collection (160 nodes, 58 per parametrised test, 37 `h-mad/scripts/*.py`). Two Must-fixes are in the new text itself: the sentence "no total is written … here or anywhere" is false against the paired plan at this same commit, and the `##`-slicer residual claims one live slicer where three exist and the design it cites by locator says three.
Evidence: 19 files opened, 52 greps/collect runs.

## Must-fix
- AC-6.4's "**No total is written for that tuple, here or anywhere.**" is false at the frozen commit, and it is itself the class the same revision's new Conventions rule forbids — a present-tense claim about what siblings contain. `docs/01-plan/features/doc-block-exec.plan.md` writes the total twice at `335f535`: at `:1001` and again at `:1046` (both opened, both quoted below). The claim's scope word is "anywhere", so it is not a claim about this document only. This matters beyond pedantry: the whole point of v1.38 was that a written total is a second authority that drifts, and the reader who checks the assertion is sent looking for a total that the document says does not exist and the plan does supply — with a value (`nine`) that is correct today and goes stale on the next script. Prescription: scope the sentence to this document ("this document writes no total for that tuple") and say nothing about the siblings, per Conventions form (b); route the plan's two totals to the plan author as cycle content, not as a sentence here.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `**No total is written for that tuple, here or anywhere.**`
  quote: docs/01-plan/features/doc-block-exec.plan.md › `the floor is written `len(tuple)` there. **Evaluated at `a8e0372` the rule yields nine**, and the`
  quote: docs/01-plan/features/doc-block-exec.plan.md › `stale when the enumeration above is re-derived; `len(tuple)` is **9** at `a8e0372` — from a`
- The new Single-source residual files one member of an open class and stops, and contradicts the design it cites by locator in the same sentence. It names `_titled_section` as the only live hand-rolled `##`-slicer; **three** exist, all verified in the tree at `335f535` and all fence-blind: `_titled_section` (`h-mad/tests/test_h_mad_context_budget_docs.py:69`, opened), `section_text` (`h-mad/tests/test_h_mad_batch_doc_rules.py:26`, opened — bounds on `lines[end].startswith("## ")`, no fence state) and `_section` (`h-mad/tests/test_h_mad_collect_report_docs.py:40`, opened — bounds on `text.find(start)` / `text.find(end, …)`, no fence state). `grep -n 'section_text\|test_h_mad_batch_doc_rules' docs/01-plan/features/doc-block-exec.impl-plan.md` returns **0** hits, and `_section` is never named as a slicer anywhere in the document. This is not a cosmetic count: the paragraph's stated purpose is that "a 5d or 5e reader who greps for `## `-slicing does not read its survival as an oversight", and two of the three survivors such a grep finds are exactly the ones the residual does not cover — one of them living in `h-mad/tests/test_h_mad_collect_report_docs.py`, the consumer file Task 5 edits, reached through `_second_surface()` which the document's own Task 5 code at `:1610`/`:1696`/`:1715` keeps calling. Prescription: state the class ("test-local `##`-slicers that perform no marker-run recognition"), enumerate all three with the same treatment `_titled_section` gets, and give the residual as a command a reader can re-run rather than a cardinality.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `is live and this document names it rather than leaving it to be re-found: `_titled_section` at`
  quote: docs/02-design/features/doc-block-exec.design.md › `and the scope is stated because the tree holds three further hand-rolled `##`-slicers that the`

## Should-fix
- The glob residual's enumeration is presented as exhaustive and is not. Its conclusion is correct — I re-derived it independently by AST over every `test_*.py` under `testpaths` (`pytest.ini:14` → `h-mad/tests handoff/tests handoff/scripts`) and found exactly two module-level assignments whose value contains a `.glob`/`.rglob` call: `REAL_AUDIT_REPORTS` (`h-mad/tests/test_h_mad_audit_cycle.py:15`) and `_SCANNED` (`h-mad/tests/test_h_mad_portable_timeout.py:153`). But the sentence quoted below is followed by a list of eight sites, and a full walk of the same three trees finds **13 further** `.glob`/`.rglob` sites the list does not name: `test_h_mad_advisor_warn.py:265`, `:288`, `:295`; `test_h_mad_audit_gate.py:173`; `test_h_mad_audit_surface_discovery.py:139`; `test_h_mad_context_budget.py:274`; `test_h_mad_cycle_counts.py:213`, `:230`; `test_h_mad_prepush_anchor_hook.py:356`; `test_hmad_dispatch.py:70`, `:75`, `:5787`; `test_hmad_dispatch_audit_cycle.py:1388`. Every one is inside a function body (I opened `test_hmad_dispatch.py:60-77` and `test_h_mad_audit_gate.py:170-176` to confirm the two that read most like module-level constants), so nothing about the `+ 2` moves — but a 5c/5d reader who re-runs the sweep finds 13 sites and cannot tell whether they were missed or deliberately excluded, which is exactly the ambiguity this residual exists to remove. Prescription: either state the enumeration as *the module-level glob-fed parametrize sources* (which is what was actually swept, and is complete) and give the reproducing command, or drop the eight-site list; do not present a partial list under "Every other".
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `Every other `.glob`/`.rglob` in those trees is inside a function body and so adds no collection node:`
- The 5f note labels itself Conventions form (a) while stating a constraint on the **spec**, not a value this document must carry — and then denies flagging a debt two sentences later. Form (a) as the rule defines it is "a value **this** document must carry, stated as this document's own constraint"; "the same root-pinned subshell **must appear in the spec's** AC-6.4 gate command, and **its inline comment must carry** 2748 … and 2486" is a requirement imposed on a sibling, i.e. a debt in modal dress. It escapes the rule's text only because the rule forbids "a claim in the **present tense**", and this one is modal — so the rule is stated one notch too narrowly for the recurrence it is closing (the fourth recurrence was itself a *debt*, not a description). The locator is sound and I verified it: `grep -n 'git rev-parse --show-toplevel' docs/01-plan/features/doc-block-exec.spec.md` returns exactly one hit, `spec.md:514`, whose inline comment does carry 2748 and 2486 — so nothing is presently unpaid; the defect is the rule's axis and the mislabel, not the content. Prescription: either add a third admissible form for a cross-document requirement, with its own residual (it has no detector either), or restate the note as this document's own constraint — "this document's 5f gate uses the root-pinned subshell and the 2748/2486 pair" — and let the locator stand alone.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `for pytest, not the subshell's `cd`. **The constraint this document states is its own**: the same`
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `root-pinned subshell must appear in the spec's AC-6.4 gate command, and its inline comment must`
- The two portable-timeout guards are stated for Task 1's new module only, but `h-mad/SKILL.md` is already a member of `_SCANNED` and Tasks 4 and 5 both write to it. `h-mad/tests/test_h_mad_portable_timeout.py:154` puts `SKILL / "SKILL.md"` into `_SCANNED` (opened, lines 153–162), so `test_no_document_or_script_emits_a_bare_timeout_command[SKILL.md]` and `test_no_document_or_script_rests_on_an_unconditional_absence_claim[SKILL.md]` already exist and already constrain any text added there. Task 4 adds a Helper-scripts registry entry that "states the CLI contract and carries a table with one row per emittable line" — including a `BAD_TIMEOUT` row with a remedy column — and Task 5 retags the fence. Neither task says the guards apply. This is the adjacent half of the class v1.38 just closed: the closed half is *a file this feature ADDS enters a glob* (changes the node count); the open half is *a file this feature EDITS is already in the glob* (turns an existing node red, with no count change, so AC-6.4 cannot see it). Class rule: any file this feature writes to that is a member of `_SCANNED` must satisfy `_TIMEOUT_CMD` and `_ABSENCE_CLAIMS`. Residual, derived from `_SCANNED`'s eight sources against this feature's file list: `h-mad/SKILL.md` is the **only** such file — `h-mad/tests/docsections.py`, `h-mad/tests/test_*.py` and `h-mad/tests/mutation-specs/*.json` are all outside `_SCANNED`, and the feature adds nothing under `references/`, `hooks/` or `scripts/*.sh`. The bare `timeout <n>` form is the live risk (`--timeout 60` is safe, the leading `-` is excluded by the regex; `timeout 60` in a remedy sentence is not), and the AC-6.1 no-tagged-opener note at Task 4 is the natural place to state it beside.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `**Task 1 therefore inherits two guards it did not have before, and both must be satisfied by the module's source**`

## Nit
- The same revision that forbids a sibling **line number** because "a pin expires in under a day" adds ten fresh line pins into the tree — `h-mad/tests/test_h_mad_portable_timeout.py:40`, `:151`, `:160`, `:165`, `:211`, `:295` and `h-mad/tests/test_h_mad_context_budget_docs.py:69`, `:301`–`:372`. All ten are correct at `335f535` (I opened every one). They are defensibly different in kind, because each carries the symbol name beside it (`SKILL`, `_TIMEOUT_CMD`, `_SCANNED`, `_ABSENCE_CLAIMS`, `_titled_section`), so a drifted pin self-repairs under one grep — but the document does not say that, and a reader who has just been told line pins are never admissible will read these as the rule being broken in its own paragraph. One clause saying the pins are symbol-anchored and re-derivable, and that the rule's axis is *sibling documents under concurrent authorship*, would close it.
