## Summary
I ran the v1.90 shape screen myself at `74e126f` and its headline numbers are true: screen one returns exactly 4 lines (531, 548, 563, 698), screen two exactly 2 (450, 608), and re-running both against `git show 335f535:` reproduces the stated before-pair (6 hits / 4 real, 3 hits / 1 real) with the members at the stated lines — so the before/after evidence is real, not asserted. I also re-derived, unchanged and correct at the frozen tree: 3 importers, 3 `_gate_bash_block()` call sites in the three named enclosing functions, `returncode` 0, `ls`/`git ls-files h-mad/scripts/*.py` 37/37, parametrise 2, second-surface 7 blocks with the gate block unique at 4 and `exec codex` unique at 2, fence census 73 across 10 files with control 88, corpus 30 tracked / 35 glob with the surplus exactly the five named `.pytest_cache/README.md`, doc-auditor's four in-fence headings (`## Summary`, `## Must-fix`, `## Should-fix`, `## Nit`) with the other four agent documents at zero, AC count 49 at spec v1.57, extractor census 6 with controls 24/4, and the codex-leg ceiling at v72. Decisions A, B and D are applied as prescribed. What the screen cannot see is the finding: it misses a surviving member of its own class at `:409`, one of the three markers its rule names is inert in awk, and the paired spec implements the same rule at the same commit with a strictly stronger enumeration.
Evidence: 18 files opened, 44 greps run.

## Must-fix
- plan:234's premise is false as written against the tree, and the conclusion it carries survives only for a reason the plan never states. Two of the three importers DO put `h-mad/scripts/` on `sys.path`: `h-mad/tests/test_h_mad_review_evidence.py:20-21` is `SCRIPT = REPO_ROOT / "h-mad" / "scripts" / "h_mad_review_evidence.py"` then `sys.path.insert(0, str(SCRIPT.parent))`, and `h-mad/tests/test_h_mad_wire_registry.py:14-15` is `SCRIPTS = Path(__file__).resolve().parents[1] / "scripts"` then `sys.path.insert(0, str(SCRIPTS))`. Read universally the sentence is false; read restrictively ("[those] test files that never touch...") the premise is true only of `test_docsections.py:16` and the "so ... fails at collection" no longer follows for the other two — either way the argument is broken. What actually holds is ORDER, not absence: in both files the `from docsections import` line precedes the insert (`:15` before `:21`; `:7` before `:15`), so `docsections` executes while `scripts/` is still un-importable. This is an §"Assumption verification" violation — a load-bearing assumption about the tree written with no command and no sha, where two `grep -n` calls were the evidence — and it is load-bearing because it is the whole justification for the delegating-import shape Task 1 must implement. Prescription: state the guarantee as import order, cite the two `path:line` pairs above with the sha, and name the residual — an import-block reorder (isort, an autoformatter, a hand edit) silently removes it per file, so AC-1.8's isolated `python3 -c "import docsections"` pin is the only thing that would catch it and should be cited here as such.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `top-level module by test files that never touch `sys.path` for `scripts/`, so a bare`
- plan:409 is a fifth surviving surface of the very class v1.90 declares closed at four, and it carries the exact marker the new rule forbids. `.returncode` is stated absent with no command, no sha, and the word "today" — while the same claim at plan:377 was repaired in this revision and now carries `grep -c returncode h-mad/tests/test_h_mad_collect_report_docs.py` → **0** at `335f535`. The claim is factually TRUE at `74e126f` (I ran it: 0), so this is a provenance defect, not a wrong number — but the rule at plan:546 is declared "binding on this whole document", and the Version History asserts completeness ("All four members fixed at `335f535`"), which this line falsifies. This is the same "sweep by claim, not by phrase" failure the plan itself names at plan:77 for the `--help` carve-out: the fix landed on one surface of a claim that has two. Class, rule and residual: the axis is *one claim, several surfaces* — before declaring a member fixed, grep the claim's SUBJECT (`returncode`, `_gate_bash_block`, `from docsections import`) across the whole body and provenance every surface; the residual is that a claim restated in different words than its subject (e.g. "nothing maps to `.rc`") is not reachable by that grep and must be caught by the enumeration below.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `` `.returncode` is not read today so ``
  quote: docs/01-plan/features/doc-block-exec.plan.md › `All four members fixed at 335f535`
- The screen shipped as the rule's enforcement is narrower than the rule on three independent axes, and one axis is dead code. (a) `\b` is NOT a word boundary in awk — it is a backspace escape. On the platform this repo runs (`awk version 20200816`, the macOS default) I probed a two-line file: `awk '/\btoday\b/'` on a line reading `a plain line saying today with no sha` matches NOTHING, while `awk '/today/'` matches it. So `today` — one of the three markers plan:549 explicitly names as insufficient — is unenforceable by screen one. This is not theoretical: at `335f535` the line `h-mad/scripts/*.py` is 37 files today` contains "today" and no sha, yet it does not appear in screen one's before-output (it was caught only by screen two), which is independent proof the term contributes nothing to either stated count. (b) Screen two's number token is `[0-9]+`, so every spelled cardinal is invisible — and this document states most of its counts as words. (c) Screen two is not bold-tolerant and requires a single space, so `**3** files`, the document's house style, cannot match. The rule over the class: the enumeration must key on the SHAPE the document actually writes — bold-tolerant, digits AND spelled cardinals, POSIX-portable regex — not on the shape a digits-only reading of it assumes. The correcting evidence is in the repository at the same commit: the paired spec already implements exactly this, and says why.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `$ awk '/^## Version History/{exit} /\(measured\)|, measured[,)]|measured with|\btoday\b|this session/ && !/[0-9a-f]{7}/{print NR": "$0}' \`
  quote: docs/01-plan/features/doc-block-exec.spec.md › `so a digits-only enumeration would miss the majority of its own members`
- Cross-document (rule 7): the plan and the spec implement ONE class rule as TWO independently-worded checkers at the same commit `74e126f`, and the plan's is strictly weaker — the exact hazard the plan itself names at plan:1071 ("two independently-worded versions of one rule is how the corpus contradiction above started"). spec:695 runs `awk … | grep -Ei` with `(\*\*)?` bold tolerance, a full spelled-cardinal alternation through `ninety-nine`, and the noun set `blocks|headings|hits|files|fences|openers|nodes|seams|consumers`; plan:563-566 has none of that and the noun set `files|hits|blocks|call sites|importers|node IDs|fences`. I applied spec:695's enumeration to the plan body with a sha filter: it returns ~40 lines to the plan screen's 2, and it is what surfaces plan:554. The two documents will now drift apart on what "the class" means. Prescription: single-source it — the plan should cite spec §"How the members are found" and run that enumeration verbatim (adding `call sites|importers|node IDs` to the shared noun list, which the spec's stated residual (1) already anticipates as maintenance cost), rather than carry a second wording. Residual after that: the spec's own two — a counted noun outside the alternation, and a cardinal ≥ one hundred written as words — plus the plan's existing same-line residual for a sha that wraps.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `independently-worded versions of one rule is how the corpus contradiction above started`

## Should-fix
- plan:264's command does not reproduce its own number, which is the precise failure the new provenance rule exists to prevent ("a sha with no command is uncheckable, because two readers measuring 'the same' thing run different commands"). I ran it: `grep -n 'titled_section\|section_from' h-mad/tests/test_docsections.py` returns **8** lines at `74e126f` — `:16` the import, `:68` the `def test_section_from_bounds_an_offset_anchored_pin` name, and six actual call sites at `:47 :53 :58 :65 :69 :87`. "six call sites beside the import" accounts for one of the two non-call lines; a reviewer re-running subtracts the import, gets seven, and reads a contradiction. The substantive claim (six call sites, none comparing bytes) is correct — only the command/number pairing is wrong. Prescription: state the grep as returning 8 lines and name the two non-call lines, or narrow the command to `grep -n 'titled_section(\|section_from('`, which I ran and which returns exactly the six.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `` `grep -n 'titled_section\|section_from' h-mad/tests/test_docsections.py` returns six call sites ``
- plan:553-554, the paragraph explaining why the class survived, is itself an unprovenanced member of the class: it restates three tree counts in the present tense and asserts they are "still arithmetically correct" with neither a command nor a sha on that surface. All three are in fact true at `74e126f` (I ran all three: 3 importers, 3 call sites, 0 `returncode`), so this is provenance rather than accuracy — but the rule at plan:546 admits no carve-out for explanatory prose, and the spelled cardinals make it invisible to the plan's own screen while spec:695's enumeration finds it immediately. Prescription: append the sha, or restate as a historical claim about `a8e0372` explicitly.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `files, three `_gate_bash_block()` call sites, zero `.returncode` reads, all still arithmetically`
- Decision D ("seams named, never numbered") is applied to the injection seams and the four docsections connection rows but not to the floor tuple, where plan:1090-1091 still address members as "A seventh" and "The eighth and ninth" over an enumeration the same paragraph says will move ("re-derive it at 5c … a second new script, or a third glob-parametrised test over that directory, changes it"). The self-granted licence "numbered within this enumeration of nine and nowhere else" localises the restale but does not remove it: an added or removed source-(1) node restales both ordinals. The class is *ordinals over a list the document predicts will change*, and the rule that closes it is the one plan:296 already states — key by source, not by position. The fix is nearly free, since the text already names them as "source (2)'s two members".
  quote: docs/01-plan/features/doc-block-exec.plan.md › `**The eighth and ninth are written by nobody** — they are source (2)'s two members:`

## Nit
- plan:577's residual says the command block "matches permanently, because it contains its own patterns", but only screen one's command line self-matches. Screen two's command line contains no digit followed by a space and a listed noun (its literal text is `[0-9]+ (files|…`, where the digit `9` is followed by `]`), so it does not self-match — which is why screen two returns 2 and not 3. The sentence is right about the observed counts and wrong about the reason for one of the two blocks.
- plan:668 reads "inline fixture strings" (plural) for `h-mad/tests/test_docsections.py`, which carries exactly one hit — `test_docsections.py:27` — in the six-hit census I re-ran. The arithmetic (1 + 2 + 1 = 4 non-extractors) is correct; only the plural is loose.
- plan:623's corpus-invariance sentence provenances one half and not the other: the agent-document half carries `grep -c '^```bash' h-mad/agents/*.md` → `0` at `a8e0372`, while "the five `.pytest_cache/README.md` artifacts carry no fence at all" carries no generating command. Both are true at `74e126f` (I walked both corpora); the asymmetry is the nit.
