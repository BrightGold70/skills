AUDIT-doc-block-exec-plan-v83-BEGIN
## Summary
I re-ran every claim v1.96 added and every figure the document publishes that a moved sibling or a moved tree could have staled, at `6dcb70f`. All five `awk` boundary legs reproduce exactly as published on `awk version 20200816`; the `audited` sweep returns 8 word-form / 1 phrase-form at `68a70d6` and its three categories account for all eight line-for-line; the closure's four `git diff` commands, the 60 `4e4a00c` occurrences, the `06ef40f`≡`68a70d6` identity, the spec enumeration's `lines?|pins?` growth, and the six-leg screen-two walk all hold; and no tree-scoped figure in this document is arithmetically wrong (73/10/88, 30/35, 2, 6, 24, 4, 411, 3 importers, 3 call sites, 0 `returncode`, 37/37, 49 AC anchors with the duplicate check silent, 21 openers over 11 tokens — every one identical at its own stamp and at the freeze). Two things do not hold: the population-short-by-N members list misattributes two of its three members by one revision, and the codex-leg ledger was carried at `4e4a00c` past the freeze the same sentence says it must be re-derived at, where its teammate half has moved 81→82. DECISION Q for v1.96: **11 property claims shipped, 10 executed and reproduced, 1 unexecuted** — and that one unexecuted claim is Must-fix 1, exactly the shape the standing metric predicts.
Evidence: 25 files opened, 131 greps run.

## Must-fix
- The population-short-by-N members list attributes two of its three members to the wrong revision, and the tree refutes both — the paragraph four lines above gets it right ("v1.94's register said 'the three OS probes'"), so the list contradicts its own prose. Re-derived: the five-row carve-out table is **v1.93's**, not v1.94's — `git show 8909ec4:<doc> | grep -n 'Five members'` → line 974 `**Five members at `6f0ee85`, all five checked with that command**:`, and v1.94 is the revision that *repaired* it (`git show 7982c18:<doc> | grep -n 'Seven members'` → line 1132). The "three OS probes" register is **v1.94's**, not v1.95's — `git show 7982c18:<doc> | grep -n 'three OS probes'` → line 705, while at `06ef40f` and `68a70d6` (the v1.95 body) line 762 already reads "**five** OS- or runtime-determined probes". Only the third member, the screen-two clause, is correctly labelled (`git show 68a70d6:<doc>` line 772: "the four screen-two legs"). This is the one **unexecuted** property/population claim v1.96 ships, in the paragraph whose entire subject is that such populations must be derived by walking and never by recall — the list self-describes as "recall-driven" and the recall is wrong. **Close the class, do not patch two labels**: the axis is *which revision a defect was shipped in versus which repaired it*, and the rule is that every member of this list is addressed by the revision whose **body carries** the short cardinal, verified with one `git show <that revision's landing commit>:<doc> | grep -n '<the cardinal>'` per member. Its residual: the landing commit of a revision is not derivable from the revision number without the log, so each member needs the commit written beside it (v1.93→`8909ec4`, v1.94→`7982c18`, v1.95→`06ef40f`). The cardinal *three* survives the repair; only the labels move.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `was seven), v1.95's register (said three OS probes, the table listed five) and v1.95's screen-two`
- The codex-leg ledger was carried at `4e4a00c` through v1.96 in the same sentence that forbids carrying it, and its teammate half has drifted. Both halves are re-derivable in one command and I ran it at three shas: at `4e4a00c` codex `72` / teammate `81` (both reproduce, so nothing published is arithmetically wrong); at `68a70d6` — the commit the document itself names as "the commit v1.96 was audited against" — and again at `6dcb70f`, codex `72` / teammate **`82`**. The document's own definition makes the published `81` not the operative figure: "the number that matters is the one the command returns at the freeze". **This is an instance of a class v1.96 created**: by pinning *the freeze* to `4e4a00c` and separately naming `68a70d6` as its measurement commit, every "re-derived at the freeze" obligation in the body detached from the round's actual measurement commit. I enumerated and re-derived the whole class at `6dcb70f` — the design `seven-plus-two-plus` locator (1), the design mutation-target derivation (1), the 49 AC anchors with the duplicate check silent, AC-6.1's two spec greps (1 and 0), spec `len(tuple)` (2), the spec `^  $ awk ` locator (1) with design and impl-plan (0 each), the "Residual on the enumeration itself" needle (1), the spec opener census (21 openers over 11 tokens, distribution unchanged), and the impl-plan's `found = _dbe.find_heading(text, heading)` (4) — **every one is byte-for-byte identical at `4e4a00c` and `6dcb70f`, so the codex ledger is the only member that moved**. The prescription is therefore one of two, not eleven edits: re-derive the ledger at the round's measurement commit and write `72`/`82` with that sha, or enter it in the inherited-unverified register. Residual: the eleven are the members as of `6dcb70f`; a twelfth added later is caught only by re-walking the `docs/`-scoped figures, which no shape screen can do.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `at every freeze rather than carried — through v1.94 it was stamped `6f0ee85` with nothing`

## Should-fix
- The register does not name the `docs/`-scoped figures as inherited-unverified at `68a70d6`, which the document's own rule says makes it a defect in the register rather than a licensed gap. Two populations sit outside both the closure and the register: (a) the ten sibling-derived figures listed in Must-fix 2 — I re-derived all ten at `6dcb70f` and **none moved**, so this half costs nothing today and is a completeness gap only; (b) the body-scoped `74e126f` self-counts, which are handled by an inline "Residual, stated because it is not closed" sentence instead of a register entry, and which **have** moved: whole-file `30` / body `26` / narrowed `11` at `4e4a00c` reproduce exactly, but the same three commands return `34`/`29`/`10` at `68a70d6` and `36`/`30`/`10` at `6dcb70f`. Either the register's population statement should say that an inline residual discharges the obligation, or these two populations belong in the register.
- **On the deliberately unpublished `74e126f` triple: the restraint is right, and I checked it rather than agreeing with it.** I reproduced v1.96's unpublished measurement exactly — 29 body / 34 whole / 10 narrowed at `68a70d6` — and then looked for a surface where the figure is load-bearing. There is none: the paragraph states in as many words that "no integer in this sentence is the number of covered figures" and that "the two `git diff` commands are the whole evidence", and the demonstration the triple exists for (a body count cannot be narrowed to the closure's subject by co-occurrence, because the document hard-wraps) is already carried by the `4e4a00c` triple's own 26-versus-11 gap, which the `68a70d6` reading's 29-versus-10 only repeats. Publishing a fourth stamped triple would add three maintained figures and no argument. The only thing the restraint costs is the register entry named in the bullet above.
- The screen-two walk is complete at six and I confirmed the boundary the cardinal turns on, but the criterion is stated only obliquely. Walking §Measurements at `6dcb70f`, the six executed legs are the `335f535` positive, the two-declined-sentences true negative, the blind form, the over-reach, the multi-word-gap bullet and the cardinal-alternation bullet. The third residual bullet ("The line-break miss stands") is not a seventh leg — it carries no command and no fixture, and it names the *same member* as the multi-word-gap bullet (the "three importing test files" sentence), which the closing line "One member missed in three different ways at once" makes explicit. That is the right call, but a reader re-walking has to reconstruct it; one clause saying "the third residual is a third reason for the first bullet's member, not a leg" would make the count checkable without inference.

## Nit
- The v1.96 Version History entry's account of the agy leg is accurate on every component I could check, and the surface a reader meets first still carries the retracted claim. Verified: `docs/01-plan/features/doc-block-exec.plan.audit.v82.p1.md` is tracked (`git ls-files`) and carries `## Must-fix`/`## Should-fix`/`## Nit` all `None`; the run log records `"thinking_tokens":39906` and `"num_turns":1`; and `tools=113` reconciles exactly — 222 `"name":"run_command"` plus 4 `"name":"view_file"` occurrences in the log, two per call, is 111 + 2 = 113, with zero `"is_error":true`. So the leg was a substantive read of the tree that found nothing, which is the datum the entry claims. But commit `68a70d6` is titled `docs(doc-block-exec): round ten audit -- DECISION Q measured; agy produced no usable verdict`, so `git log` shows the false premise and only the document carries the correction. History is immutable, so the remedy is a pointer in the entry naming the commit whose subject line it retracts.
AUDIT-doc-block-exec-plan-v83-END
