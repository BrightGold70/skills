## Summary
Every one of the twenty property claims v1.101 enumerates was re-executed here at the freeze sha and every one reproduced byte-for-byte: both rows of the blob-immunity table (229 lines → `BODY: 1 runs, 1` / `VERSION HISTORY: 49 runs, 17`; 161 lines → `13`/`7` and `15`/`8`; the `git show` form unmoved at `8`/`3` and `15`/`8` under both, `RESTORED` after each), the `1861157` differential harness (`tracked files 25 headings shipped 0 arm1 0 arm2 0 | reached: arm1 0 lines, arm2 8 marker lines / 4 fences / 2 files, whose 9 body lines are scanned as prose, 0 of them below 4 columns`), the candidate sweep at all five shas (36/42/42/50/50), the `$STRIP` tilde control with real mutual discrimination (`shipped 0 no-STRIP 1 TILDELESS 1` vs `shipped 0 no-STRIP 1 TILDELESS 0` on awk 20200816), and `$W`'s fifteen branches, two positives, true negative, head 6 at `cf3a862` and tail 6. The `$W` withdrawal is complete, not partial: the body site now reads `the two rows keyed`, the tail entries are left as dated records, and `docsections.json`'s `mutations` array is `4` at `68a70d6`. The two must-fixes below are both DECISION Q recursing one level further out — a property claim the revision's own edit changed and did not re-run, and an absence claim on a raised candidate line that carries a command and a sha but no label. PROPERTY CLAIMS SHIPPED VERSUS EXECUTED: **21+ changed, 20 executed, at least 1 asserted** (the document's own count of "TWENTY / TWENTY / ZERO" is one short in the numerator and wrong in the last term).
Evidence: 7 files opened (the design also read at 8 blob shas), ~75 greps and derivation commands over 22 shell invocations, 2 scratch clones.

## Must-fix
- The NUM-residual's "unexercised on the published input" figure is stated as `0` and is `1` on the bytes v1.101 ships — the same revision added `zero` to the name set that grep runs over and did not re-run the grep. Executed both ways at `6dcb70f`: with the set as the document now names it (`zero|fourteen|fifteen|sixteen|seventeen|eighteen|nineteen|forty|fifty|sixty|seventy|eighty|ninety|hundred|thousand`) `git diff 35698f9 6f0ee85 -- $D | grep '^+' | grep -cEi` returns **1**, the hit being the added line `+as prose, never hide a heading — and the corpus exercises it zero times. (2) It matches a fence`; with `zero` removed it returns `0`. This is the exact shape the paragraph four above it names as the worst a control can ship in — an unexecuted property claim reading like a verified one — and it is decision K as well: a figure the revision's own edit moved and then restated unchanged. The stated *conclusion* ("so no figure above moves") does survive, but by a route the document does not give: I re-ran the carry screen at `68a70d6` with `zero` prepended to the shipped `WORDS` list and it still prints `BODY: 8 runs, 3 of them figure-bearing` / `VERSION HISTORY: 15 runs, 8 of them figure-bearing`, so the token is present in the added lines but does not land inside a shared six-gram run. Prescription: publish `1`, name the line, and carry the run-level differential as the evidence that no figure moves — a name-set hit and a figure-bearing run are different objects and only the second one moves a number.
  quote: docs/02-design/features/doc-block-exec.design.md › `` `git diff 35698f9 6f0ee85 -- $D | grep '^+' | grep -cEi` over that name set returns `0` — so no ``
  quote: docs/02-design/features/doc-block-exec.design.md › `members named exactly: **`zero`**, `fourteen` through `nineteen`, `forty` through `ninety`,`
- A ninth absence site sits unlabelled on a candidate line the sweep itself raises, and it is neither of the "exactly two ways to be missed" the residual closes over — so the eight-site denominator is under-derived for the third round running, by a mechanism the residual says does not exist. The site is the provenance probe `git show 35698f9:$D | grep -cF "tr '\n' ' '"` → `0`: it is an absence claim about a corpus (this document at a sha) in the rule's own vocabulary, it carries a command and a sha and no label — verbatim the defect v1.101 just repaired for the `1861157` restatement — and it is *not* a screen's expected output on this document, so the rule's scope carve-out does not reach it. It is inside the needle: at `cf3a862` it is one of the 36 raised lines (line 1107 of that blob, line 1195 of the working file), yet it is neither "read off a control harness" nor "outside the needle". I swept all 36 raised lines at `cf3a862` against the eight-site list; this is the only one outside both the eight and the stated carve-outs, so the class is closed here rather than left open: *a prose absence claim about a **blob**, used as provenance for why a figure is stamped at the working file rather than at a sha.* Prescription: either label it (denominator nine) or state the carve-out for provenance probes explicitly — and in the latter case the residual needs a third way to be missed, because "exactly two" is then false.
  quote: docs/02-design/features/doc-block-exec.design.md › ``(`git show 35698f9:$D | grep -cF "tr '\n' ' '"` → `0`, against `1` at `6f0ee85`), so a bare``
  quote: docs/02-design/features/doc-block-exec.design.md › `the sha it was run at, and **the reason the zero is zero, marked with one of three labels**`
  quote: docs/02-design/features/doc-block-exec.design.md › `eight-site classification is not, and there are exactly two ways to be missed by it.`

## Should-fix
- The DECISION Q metric the v1.101 entry publishes is itself an unexecuted property claim in its last term. I reproduced all twenty enumerated items and every one is genuinely executed and genuinely published — that half of the claim is sound and is the strongest thing in this revision. But the first must-fix above is a twenty-first new-or-changed property claim, it was not executed, and it is false on the shipped bytes, so "ZERO asserted" is wrong and the denominator is at least twenty-one. The metric is the standing gate now, so its own derivation has to be as re-derivable as the items it counts: the enumeration is what makes the count trustworthy, and a claim that changed inside a paragraph the revision was already editing is exactly the member an enumeration built by listing *new fences* will miss.
  quote: docs/02-design/features/doc-block-exec.design.md › `TWENTY new or changed property claims, TWENTY executed and published, ZERO asserted`
- Cross-document term collision on "re-anchored" over `docsections.json`, with different cardinalities on each side — **unverified as to which is wrong**, because the two sentences may be using the word on two different axes and I could not settle it from the tree (the file has 4 rows today; both sentences describe the post-5e state). The design calls all four originals re-anchored; the impl-plan says two of the four existing rows are re-anchored, and names them (`fence-tracking-removed`, `section-no-longer-owns-its-subsections`) as re-anchored into `h-mad/scripts/h_mad_doc_block_exec.py`, with the other two originals keeping `"file": "tests/docsections.py"` and only their anchor *text* rewritten. The two "six" figures are also different sets — the design's six is partitioned by which test kills the row, the impl-plan's by which file the `find` anchor lives in — which is legitimate but reads as the same partition on a first pass. A reader reconciling the two documents cannot tell whether "re-anchored" means "given a new anchor *file*" (two rows) or "given fresh `find`/`replace` payloads at 5e" (four rows). Prescription: say which axis the design's adjective is on, in the same clause.
  quote: docs/02-design/features/doc-block-exec.design.md › `the four re-anchored originals`
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `the four existing (two re-anchored) plus`

## Nit
- "all four re-derived" trails a list of five shas (`cf3a862`, `7982c18`, `4e4a00c`, `06ef40f`, `68a70d6`). The intended reading is presumably the four later than the stamped one, and all five values reproduce exactly, but the sentence opens with "A reader re-running the sweep at a later sha" and then includes `cf3a862` in the list, so the antecedent has to be worked out rather than read. Naming the count as "the four later shas" costs two words.
- The new tilde absence claim names the working file but not the entry it was run after, which is half of the rule this document states for a document-self figure. `grep -cE '^ {0,3}~{3,}' "$D"` → `0` is stamped only as "on the working file this revision ships"; the sibling `^ {4,}` bound two paragraphs above pairs the working file with a blob (`0` at `6f0ee85`), and the seam-ordinal `8` names its entry explicitly. Both are `0` and both reproduce; this is a stamping-convention gap, not a wrong figure.
