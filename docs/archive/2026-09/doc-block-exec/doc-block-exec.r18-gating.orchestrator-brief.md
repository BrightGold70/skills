
Orchestrator cycle brief — round eighteen gating (design c98 / plan c89 / impl-plan c49), freeze `bc4688e`, HEAD `093c3ee`:

The facts below were measured by the orchestrator at HEAD `093c3ee`. `bc4688e` and `093c3ee` are docs-only over the batch commit `ccd8ebd`, which carries design v1.110 / plan v1.105 / impl-plan v1.54 / spec v1.64. They are stated so you can weigh them. They are NOT an instruction to withhold a finding: if you judge any of them a defect, file it exactly as you otherwise would and append the tag `orchestrator-stated` to that bullet so the orchestrator can triage it against the round-eighteen decision sheet.

1. The plan binds the word "freeze" to `4e4a00c` by its own stated convention (plan body: "*the freeze* in this document means `4e4a00c` throughout and is deliberately not repointed"); its round-eighteen readings are stamped "the measurement commit `cac6edc`". In the plan the phrase "the freeze `cac6edc`" occurs only inside the Version History.
2. The impl-plan states AC-3.14 in prose — "the raised `CleanupFailed`'s `__cause__` **is** the injected cleanup error, asserted with `is`" — while the spec, plan and design carry the literal `__cause__ is cleanup_error`. The spec body mentions `__suppress_context__` in the AC that withdraws that assertion ("it asserts **nothing** about `__suppress_context__`"): an AC naming what it forbids.
3. `h_mad_precheck_doc.py` reports the impl-plan as `PRECHECK: FAIL issues=11`. All 11 are `PLACEHOLDER` hits on the FR-4 detail-line grammar slots (`overlap: "<a>" "<b>"`, `intersect: "<a>" "<b>" "<offset>"`, `os_error: "<text>"`, `pgid: "<n>"`, `stream: "<name>"`, `<key>=<bare>`, `<key>="<json-string>"`), which the document publishes as grammar declarations; the suite's noise-floor test asserts at most 12 and passes; `PINDRIFT` is 0.
4. The h-mad suite at `ccd8ebd` ran `2574 passed in 377.47s`. No document publishes a present-tense "N passed"; the documents publish COLLECTION counts (2574 from `h-mad/tests`, 2836 from the repository root) stamped `cac6edc`.
5. The mutation matrix is 86 rows at this batch (85 at `cac6edc`; the design added the row `intersect-scan-non-overlapping` in round eighteen), and its killer test is named `test_substitute_refuses_overlapping_occurrences_of_one_key` in every document body. The empty-ATX-heading case is exercised by `test_titleless_heading_is_a_new_only_member` (a `tmp_path` fixture); the `new_only` set at `cac6edc` is empty.
6. The round-eighteen sheet's C3 ii records that the sheet's own earlier counts were wrong and the documents are right: the spec had seven unnamed ACs (not five), Task 2 has eleven tests (not nine or ten), and the spec never asserted `__suppress_context__ False`.

