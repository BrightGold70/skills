## Summary
GATING pass on impl-plan v1.42 at freeze sha `8909ec4`. I re-derived every figure the orchestrator named at `8909ec4` rather than trusting the document's `6f0ee85` stamps: the DECISION C before-figure (22 occurrences / 19 lines / 8 distinct files, per-file breakdown exact), the after-figure (0 bare-filename, 49 path-qualified occurrences across 19 folded paragraphs, moved from 47), the four `command` consumers in `h_mad_mutation_harness.py`, all 13 sibling locators (1 hit each at `6f0ee85` **and** at `8909ec4`, so they survived the siblings moving in the shipping commit), the 81-row split 25+5+24+27 and the design matrix's 81 data rows with exactly 1 naming `SKILL.md`, all three residual sweeps (22 helpers, 7 fence-state bodies, 2 behavioural guards), the corpus fence scan (`files 30 space-arm 29 in 4 files tab-arm 0 declined-inside-fence 2`), every `path:line` pin's file and line range, `grep -c 'SKILL.md:[0-9]'` = 0, and that no Version History entry was rewritten (word-level diff: the only VH change is a bracket insertion). Two defects survive: the recurrence ledger still contradicts itself between "five" and "four" inside one bullet, and the positive control that is the sole evidence the `tr -s ' '` fold repair is a repair does not reproduce at the shipping commit in either unit.
Evidence: 16 files opened, 74 greps run.

## Must-fix
- The recurrence-ledger ordinal contradicts itself at two live sites in the same Conventions bullet — `:335` says the class "recurred **five** times" and pins the count to `len(list)` with enumeration markers (1)–(5), `:474` says "the list has since grown to five", and `:480`, six lines later, still says the list "stands at **four**". This is exactly the class impl-plan audit v38 caught at v1.40 ("contradicting itself at three sites over the same ordinal", the document's own words at `:471-472`); v1.42 repaired two of the three sites and left the third, so the round-six shape is reproduced rather than closed, and a 5d/5e reader who counts the enumerated members finds five and reads `:480` as a missing member. Prescription: `:480` reads "The list above stands at **five**"; re-run `grep -n 'stands at\|recurred \*\*' <doc>` after the edit — those two lines and `:2602` (v1.41's Version History, correctly historical, leave it) are the whole population.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `recurred **five** times and every recurrence cost a full dual-surface audit cycle. The list, and`
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `above stands at **four**, and what decides membership is a **content predicate, not the`
- The positive control published for the `tr -s ' '` fold repair scores 1/1, not 0/1, at the shipping commit — it is falsified by the sentence that publishes it, and it is the only evidence offered that the repair is a repair. Measured at `8909ec4` against the body (`awk '/^## Version History/{exit} /^$/{print b;b="";next}{b=b" "$0}END{print b}'`): the previously-published fold returns **1** on `never a census` by `grep -c` and **1** by `grep -o | wc -l`; the `tr -s ' '` form returns **1** by `grep -c` and **2** by `grep -o | wc -l`. No reading yields the published 0. The cause is an observer effect the revision created: the wrapped instance is at `:420-421`, and the control sentence at `:446` writes the needle a second time, literally and unwrapped, so the "0" the control depends on cannot exist in any document that states the control. This also breaks the revision's own DECISION H — neither the 0 nor the 1 carries a unit, and the two units disagree. The same false claim is repeated in the new v1.42 Version History entry at `:2603` ("which the old form scores 0 on and the repaired form 1"); that entry is this revision's own claim, not a protected historical record, so it moves with the body. Prescription: publish the unit and both true integers — old fold **1 occurrence**, `tr -s ' '` fold **2 occurrences**, the difference being the wrapped instance at `:420-421` — or pick a needle the control sentence does not itself reproduce (write it split, e.g. the phrase ending `never a` and continuing `census`), and state which of `grep -c` / `grep -o | wc -l` the figures are in.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `` `never a census` is wrapped between `a` and `census`, and the fold **as previously published``
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `scores 0 on it while the `tr -s ' '` form scores 1**. The bare-filename figure is unaffected`

## Should-fix
- The bare-filename half of the DECISION C closure has no published folded command, and the reason given for its immunity to the wrap names the wrong regex — so the `tr -s ' '` repair does not reach it. The bare-filename screen is written once, at `:400`, as `grep -oE '(^|[^/A-Za-z0-9_.-])[A-Za-z0-9_]+\.py:[0-9]+'`, which allows **no** space; the folded variant residual (ii) publishes is `grep -oE '[A-Za-z0-9_./-]*\.py: ?[0-9]+'`, the **path-qualified** regex, which is the one with the single optional space. So "both screens return **0** bare-filename occurrences" asserts a measurement whose command is nowhere in the document, against DECISION G. Verified with a constructed probe (`some prose reaching h_mad_mutation_harness.py:` / newline / `  645 in run_spec`): the line-scoped bare screen returns 0, the folded `tr -s ' '` bare screen returns 0, and the published folded path-qualified regex returns **1** — i.e. a bare-filename pin that wraps between its `.py:` and its digits is invisible to both compliance screens *and* is silently absorbed into the 49-strong compliant population. This is DECISION I: the wrap fix was applied to one variant and not its sibling alternative. I re-ran the folded bare-filename screen against the real document at `8909ec4` and it returns 0, so there is no live counter-instance — the defect is in the published closure argument, not in the tree. Prescription: publish the folded bare-filename command explicitly (same `awk … | tr -s ' '` prefix, the `:400` regex with ` ?` inserted after the colon) and re-state the residual against that regex rather than the path-qualified one.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `return **0** bare-filename occurrences, and their path-qualified populations are identical. That`
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `this document happens to wrap between its colon and its digits, and the regex allows only **one**`
- The DECISION C after-figures are stamped at a commit that does not contain the document they measure, which makes the measurement unreproducible as written even though every figure is true. `6f0ee85` holds impl-plan **v1.41**; the "0 occurrences … against this document at `6f0ee85`" and the "**49** occurrences across **19** folded paragraphs … at `6f0ee85` *after* this revision" are measurements of the v1.42 working tree, not of anything at that sha. The parenthetical explains why the shipping sha could not be named, which is fair, but a reader running `git show 6f0ee85:<doc> | …` gets **47**, not 49 (I ran it), and has no way to tell a stale figure from a base-vs-tree mismatch. All four figures do hold at the shipping commit — re-derived at `8909ec4`: bare-filename 0 line-scoped and 0 folded, path-qualified 49 occurrences, 19 folded paragraphs, and the line-scoped and folded populations are byte-identical (`diff` of the two `sort | uniq -c` outputs is empty). Prescription: label them "measured on the revised working tree, base `6f0ee85`, re-derivable at the commit this revision ships in", exactly as the before-figure is labelled against `35698f9`.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `returns **nothing — 0 occurrences** — against this document at `6f0ee85`, the freeze sha this`

## Nit
- The pre-mutation check's published span stops one line short of the branch it names: `h-mad/scripts/h_mad_mutation_harness.py:630-641` covers the condition and the `result["refused"].append(...)` call, but the `continue` that makes it a refusal rather than a fall-through is at `:642` (read at `8909ec4`; `:630` is `if scoring_command is not None and mutation.get("test"):`, `:641` is the closing `)`, `:642` is `continue`). The neighbouring spans in the same paragraph — `:609-623`, `:660-669`, `:670-671` — all include their terminator. `:630-642` is the consistent form.
- `git rev-parse --show-toplevel` is counted among the **7** locators that satisfy the selection preference, which the same bullet defines as "a backticked identifier, a verdict token, or an anchored table-row prefix". It is a backticked *command*, none of the three. The needle is lexically specific and returns exactly 1 hit at both `6f0ee85` and `8909ec4`, so nothing is wrong with it — the preference's own enumeration is what is one category short.
