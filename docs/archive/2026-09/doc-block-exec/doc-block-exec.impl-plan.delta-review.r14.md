## Summary
ADVISORY delta review of `git show 8c6539a -- docs/01-plan/features/doc-block-exec.impl-plan.md` (v1.47 → v1.48). Every published integer I re-ran reproduced exactly — the self-reference screen at **0**, the marker screen **5 / 5** with five per-marker lines of count 1, the four `.py:` screens **49 / 49 / 0 / 0** at **20** folded paragraphs, both `SKILL` screens **0 / 0**, the restated-cardinal pair body **0 / 0** and whole file **2** lines / **3** occurrences at `700c599` and on the shipped tree alike, the precheck probe `flat 1 / wrapped 0 / unedited 0`, the six-token divergence sweep **3**, the sibling sweep **25** with `design.md:` 1 and `spec.md:` 1, the stamp table **115** readings over **16** shas with every per-sha count matching the published table, the span `74e126f..700c599` at **16** commits with the fifteen below the endpoint exactly the fifteen named, `git diff --name-only 74e126f 700c599 -- h-mad handoff` and the working-tree form both empty, `git diff --stat 335f535 74e126f` 9 files all under `docs/`, the collect-only pair **2809 / 2547** divergence **262** run live, AC-6.1's control **2097** without the root clause and **30 = 27 h-mad + 3 handoff** with it, the design churn **127 insertions / 25 deletions** across `1cbddb7..700c599`, the 81-row derivation (**81** data rows, **81** distinct names, exactly **1** naming `SKILL.md` — `registry-row-removed` — **0** missing from this document), and all **13** sibling locators at one hit each with the 9/2/2 split and the single `git rev-parse --show-toplevel` caveat. All five audit v44 findings are genuinely closed at their sites, not just at the instances named. What did not survive is the paragraph the round's second must-fix rewrote: its closing partition raises "Four of the six" to "Five of the ten" with no fifth rule-applied member, and its residual (a) states a false mechanism for how the walk reached the two `SKILL` screens.
Evidence: 17 files opened, 96 greps/commands run.

One premise in the dispatch does not hold, stated plainly because orchestrator claims have been refuted in each of the last two rounds. The dispatch says the revision "corrected its own auditor: the sweep returns **25** body hits, not the 23 the audit report stated". It does not, and the auditor was not wrong. `git show 700c599:<this file> | awk '/^## Version History/{exit}{print NR": "$0}' | grep -cE 'owed|spec\.md:|design\.md:|plan\.md:'` returns **23**; the same screen on the shipped body returns **25**. The auditor's 23 was correct at the sha it audited, and v1.48's own edits added the other two (both substring confounds: 16 → 18). The document itself gets this right and stamps the 23 to `700c599`. What the revision actually corrects is the *exposure* classification, and that correction is sound: classified line by line at `700c599`, the exposure there is **6** (four lines of the rule's own text plus two sibling-filename-colon prose lines), not the three the report gave. I verified the whole classification, which the dispatch says neither side had: at HEAD the 25 split **18** substring-only / **5** standalone `owed` / **2** sibling-colon, disjoint, and the five standalone are `:957`, `:968`, `:973`, `:975` (the rule's own text) plus `:3092` (the one live use) — exactly the 6 / 1 / 18 / 0 the document publishes. The confound words present are `followed` (14), `allowed` (3), `narrowed` (3), so "wider than the one participle it names" holds.

The "0" on the self-reference screen is a real absence, not a screen that cannot see. I swept the body for eighteen further wordings (`shipping revision`, `this pass`, `this batch`, `present revision`, `present pass`, `this round`, `current revision`, `this version`, `this edit`, `revision being read`, `we ship`, `now ships`, `this document ships`, `this draft`, `the revision you are reading`, `as of this`, `this tree`, `as shipped`). The only non-zero returns are generic statements of the rule or descriptions of the past defect (`:690`, `:993`, `:1000`, `:1002`, `:1006`, `:1035`, `:2435`), code comments (`:2601`, `:2846`, `:2873`), and `:1751`'s "every runnable command this document ships", none of which identifies the shipping revision by a relative phrase. The exempting sentence does not bite in the shipped state: the screen reads 0 on both the working tree and `git show HEAD:<file>`.

Not checked, declared rather than implied: AC-6.4's eight falsification readings at `e8eaf6f` (I did not check out that commit), the `2748`/`2486` historical pair at `b7d0d77`, and the `bash`-vs-`zsh` byte-identity claims. Task 2, Task 3 and Task 4's prose outside the spans the diff touches were read only through the sweeps above.

## Must-fix
- The closing partition of the exposure enumeration sums to ten but assigns member **(vii)** to a bin whose defining property (vii) explicitly does not have, and the raise from four to five is the whole of the change — v1.47's removed line read `Four of the six had the rule applied before it was named, one is the regex-class residual, and one is the instance that named it`, and v1.48 added no new rule-applied member. Working the partition backwards: "the instance that named it" is **(i)**, "the regex-class residual" is **(ii)**, "written under the rule" is **(x)**, "two found by the walk and by nothing else" are **(viii)** and **(ix)**, which leaves **(iii)(iv)(v)(vi)(vii)** as the five said to have had the rule applied before it was named. But (vii) is the member the round-13 audit filed as a must-fix, and its own disposition four lines above says the opposite — its needles are literal and present in the scope it counts, and the exposure is tolerated only because it publishes no reading. So the true partition is 4 rule-applied + 1 regex-class residual + 1 instance that named it + **1 found by the auditor** + 2 found by the walk + 1 written under the rule = 10, and (vii) needs its own bin. This matters under this document's own decision G: a published partition of an enumeration is a derived measurement, and this one was recalled from v1.47's sentence with the cardinal bumped. **The VH/body half of the same defect**: the v1.48 Version History entry writes "The three the walk added: **(vii)** the pre-dispatch sibling-claim sweep, ..." while the body says only two were found by the walk and by nothing else — the same member attributed two ways in one commit. `instance of:` the class "counts and partitions restated from a previous revision with only the cardinal updated", which is the class both of this round's must-fixes are members of. Commands: `git show 8c6539a -- docs/01-plan/features/doc-block-exec.impl-plan.md | grep -n 'of the six had the rule applied'` shows the removed line; `awk 'NR>=478 && NR<=490' docs/01-plan/features/doc-block-exec.impl-plan.md` shows the shipped one.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `Five of` … `the ten had the rule applied before it was named, one is the regex-class residual, one is the`
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `**(vii)** the pre-dispatch sweep in the sibling-claim bullet below, whose four needles — three`
- Residual (a) of the walk states a mechanism for how the two `SKILL` screens were reached that is false on the tree, and the blindness it infers from that mechanism does not follow. The binding `D=docs/01-plan/features/doc-block-exec.impl-plan.md` is at `:803`, inside the fence that opens at `:802` and closes at `:817`; the two `SKILL` screens are at `:827`–`:829`, inside a separate fence opening at `:826` and closing at `:830`. Opened with `awk 'NR>=795 && NR<=835 {printf "%d: %s\n", NR, substr($0,1,120)}' docs/01-plan/features/doc-block-exec.impl-plan.md`. They are not the same fence, and the screens' fence binds nothing. More consequentially, the walk's own second signature is "the shell variable the fenced screens bind it to" — that is `"$D"` itself, and `grep -n '"\$D"'` returns `:382`, `:383`, `:384`, `:513`, `:514`, `:805`, `:814`, `:827`, `:828`, `:1017`, so `:827` and `:828` are hit directly and would be hit from any paragraph. The inference "a screen written in a later paragraph against a variable bound in an earlier one would be invisible to both signatures" is therefore wrong as written: the genuinely blind case is a screen using a *different* variable name (`F=…; grep … "$F"`), which is not what (a) describes and which the walk really cannot see. Since this residual is the stated reason category (a) is judged already-covered here, the error understates what the walk misses. **The rule over the class**: state each residual category in terms of the signature that fails, not of an example that the signature in fact catches; **the residual after that**: the `$D` signature is a *name* convention, so the enumeration's completeness rests on no later screen inventing a second variable name for this file, which nothing detects. Prescription: replace the SKILL-screen example with one built on a second variable name, or say plainly that the SKILL screens are caught by the `"$D"` signature and that category (a) has no live member in this file.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `the two `SKILL` screens are exactly that shape, and the`
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `walk sees them only because the fence binding the variable is the same fence.`

## Should-fix
- The body site that publishes the sibling sweep's composition carries no stamp, and it is precisely the kind of figure that moves. The reading is **25** on the shipped tree and **23** at `700c599`, so it is a working-tree measurement, yet the bullet writes "The composition of its 25 body hits" with no "on the tree v1.48 ships" and no sha, while stamping only the 23 it is contrasting against. Every other reading in this revision carries one, and the v1.48 Version History does stamp this one. Two lines below, the same document writes the rule it is breaking here. Fix: add the same "on the tree v1.48 ships" clause the marker, `SKILL` and restated-cardinal screens carry. `instance of:` the class "a derived integer published in body prose without the tree or sha it was taken on", which the round-12 and round-13 rounds both filed instances of.
- Residual (c) names the standing control as `tests/test_h_mad_precheck_doc.py::test_control_todays_impl_plan_dropped_the_six_stale_SKILL_pins`, and that node path does not resolve from the repository root. `ls tests/test_h_mad_precheck_doc.py` fails; the file is `h-mad/tests/test_h_mad_precheck_doc.py` and the test is at `:299`. The substance of the claim holds and I checked it end to end: `IMPL_PLAN = "docs/01-plan/features/doc-block-exec.impl-plan.md"` at `:261`, the test runs the shipped precheck over `REPO / IMPL_PLAN` with `--phase design --root REPO` and asserts `"SKILL.md:" not in joined`, and `python3.11 -m pytest h-mad/tests/test_h_mad_precheck_doc.py::test_control_todays_impl_plan_dropped_the_six_stale_SKILL_pins -q` prints `1 passed`, so the "re-runs green on the shipped body" claim in the Version History is true. The form is pre-existing and pervasive rather than fix-introduced — `grep -on '[^/A-Za-z]tests/test_h_mad_[a-z_]*\.py'` returns **94** lines, and this document also writes the repo-relative form at `:2631` — so this is a class, not a member: **the rule** would be one path convention for test node paths, repo-relative like every `path:line` pin in this document, with the pytest-rootdir form used only where the document is quoting a command run from `h-mad/`. v1.48 added two more instances of the non-resolving form (`:499` is one).

## Nit
- The enumeration's lead sentence says the walk "returns **ten** instruments" and then, three sentences later, that the tenth "did not exist when the walk was run". The walk returned nine. One word — "It returns **nine**, ten with the screen this revision adds" — removes a self-contradiction in a paragraph whose whole subject is enumerations published as more complete than they were derived.
- "**v1.47 published this enumeration as six … and it was short by three** (impl-plan audit v44)" attributes the figure three to the audit, which named one member and said in its own residual that the enumeration "stays a hand-count and its completeness is re-established by reading, not by a grep". The three is v1.48's own derivation and is the better result; the parenthesis should sit on the *finding* ("published as complete and was short — impl-plan audit v44 — and re-derived here at three"), not on the number.
- The design and the plan moved in the same commit as this revision, so the header's design **v1.103** / plan **v1.98** pins are one behind at `8c6539a`. That is the drift the header itself declares and not a finding; I re-ran all 13 sibling locators against `git show HEAD:<sibling>` as well as `700c599` and every one still returns exactly one hit, with `git rev-parse --show-toplevel` the only needle reaching two siblings. Recorded so the gating cycle does not have to re-derive it.
