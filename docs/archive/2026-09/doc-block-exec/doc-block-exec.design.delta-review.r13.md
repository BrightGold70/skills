## Summary
Delta review of `git show 1cbddb7 -- docs/02-design/features/doc-block-exec.design.md` (design v1.101 → v1.102), ADVISORY, against the tree at `1cbddb7`. Every executable claim the diff introduces was re-run rather than read: MUST 1's differential (`1` full / `0` control, hit line verbatim, carry screen `BODY: 8 runs, 3` and `VERSION HISTORY: 15 runs, 8` identical under shipped and `zero`-prepended `WORDS`), MUST 2's needle (`36` raised at `cf3a862`, the `35698f9` provenance probe present in that set at blob line 1107, probe `0`/`1` at both shas, denominator swept to nine at all three present-tense sites with the surviving "eight"s correctly dated as history), the decision-K `$STRIP` correction (`2/1/1/2/2/3/3/6` across the eight shas, `6` on the working file, all six hits identified and matching the published disposition exactly), the "re-anchored" parenthetical (impl-plan text identical at v1.45/`7d8e797` and v1.46/`1cbddb7`; all four `docsections.json` rows carry `"file": "tests/docsections.py"`), and both sibling directions (impl-plan's 13 locators return exactly one hit at `7d8e797` **and** at `1cbddb7`; the AC `comm` figure is unmoved at `7`). Three findings: one document-self figure the revision's own class-closure claim says was re-stamped and was not, one partition sum I could not reproduce under either of its two published readings, and a third axis for "re-anchored" living in the plan that the repair's collision statement does not account for. No must-fixes: nothing the diff asserts was falsified on the bytes.
Evidence: 9 files opened (design read at 10 shas, impl-plan at 2, plan at 2, spec at 2, `docsections.json` at 1), ~60 greps and derivation commands over 24 shell invocations, 2 extracted-and-executed fences.

## Must-fix
None

## Should-fix
- The revision's NIT-2 class closure is short by exactly one member, and it is the one member the stated residual cannot reach. `docs/02-design/features/doc-block-exec.design.md:2234` still stamps a document-self figure on the **v1.101** entry while the document ships v1.102; the entry's own account of the repair asserts closure over the whole class and then enumerates five sites, none of which is this one. I swept the head for every site naming the working file (`awk '/^## Version History$/{exit} {print}' $D | grep -n 'working file'` → 14 hits at 789, 835, 1216, 1229, 1243, 1906, 1947, 1950, 2191, 2234, 2235, 2289, 2416, 2469): 1216, 1229, 1906, 1947, 2191 name v1.102; 2289 names "the entry recording this revision"; 789/835/1243/1950 are prose about the rule, and 2416/2469 are fences whose figures I re-ran unmoved (`7` of `49`). **2234 is the only stale member**, so the class is closed here rather than left open. The figure itself is correct and nothing downstream moves — the published line-scoped form `awk '/^## Version History$/{v=1} v' "$D" | grep -cE "\b$O\b$G\b$N\b|\b$N\b$G\b$O\b"` returns `8` at `35698f9`, `6f0ee85`, `cf3a862`, `06ef40f`, `68a70d6`, `7d8e797`, `1cbddb7` and on the working file — so this is a stamp, not a number. The residual as the entry states it screens for a document-self figure naming *no* entry, by grepping the phrase "working file"; that screen does not reach a figure naming a *previous* entry, which is what this is. Prescription: re-stamp 2234/2235 on v1.102 with the re-run stated in the shipped order, and state the residual's screen as `grep -n 'after the v1\.[0-9]* entry'` over the head with each hit checked against the current entry — the rule over the class is that a document-self stamp names the *current* entry, so an entry bump is itself the trigger to re-run every member.
  quote: docs/02-design/features/doc-block-exec.design.md › `` `8` on the working file **after the v1.101 entry**, which records its changes without quoting an``
  quote: docs/02-design/features/doc-block-exec.design.md › `Closed as a CLASS across every document-self figure rather than at the named instance: the three-blind-form re-sweep, the fourth-blind-form fence, the`
- The `13 + 15 + 8 = 36` partition does not reproduce for me under **either** of its two published readings, and I could not settle which line I am missing — **unverified**, filed as a should-fix and not a must because the assignment is explicitly a reading and the document says so. I dumped all 36 lines the needle raises at `cf3a862` (`git show cf3a862:$D | grep -nE '[`]0[`]|expect[ ]0'` → exactly 36, blob lines 206 209 370 551 608 610 616 617 633 634 635 716 721 729 754 757 1082 1096 1107 1116 1119 1167 1738 1759 1770 1772 1776 1785 1847 1848 1892 1899 1901 1970 1998 2017) and walked them against the nine-site list. I get **12** site-lines counting the census arm-(2) line (206+209 for the `.md` invariant, 370, 551, 610, 721, 729, 1107, 1770+1772, 1998+2017), i.e. only **three** sites stating their figure on two lines, not the four the document says; the 8-group reproduces exactly as published (633/634/635 in the census arm-differential paragraph, 754/757 for the `index` contract, 1970 for the AC row, and two of 1892/1899/1901 in the seam-ordinal check's) and the remainder falls to the 15. So the published pair is 13/12 for the two readings and I get 12/11 — one short under both, not off by the arguable assignment. The check the paragraph rests on ("no raised line is left over", "the sum is the check") is exactly the thing a reader cannot perform, because the per-line assignment is unpublished: a sum whose addends are asserted is not a walk, which is the same shape as the site list it replaces. Prescription: publish the 36 blob line numbers at `cf3a862` with their dispositions — the walk becomes re-runnable and the discrepancy resolves itself in one direction or the other.
  quote: docs/02-design/features/doc-block-exec.design.md › `The **13** are the nine sites above —`
  quote: docs/02-design/features/doc-block-exec.design.md › `**Zero raised lines sit outside the three parts**, and the sum is the check`
- The "re-anchored" collision has a **third** axis, in the plan, and it is the one most likely to mislead: the plan uses the word with the same cardinality as the impl-plan (two) over the **disjoint** pair of rows. `git show 1cbddb7:docs/01-plan/features/doc-block-exec.plan.md | sed -n '322p'` reads `re-anchored in place: \`offset-anchored-bound-runs-to-end-of-file\` mutates \`section_from\`'s call,` — i.e. anchor-*text*-rewritten-in-place, naming `offset-anchored-bound-runs-to-end-of-file` and `missing-heading-returns-empty-instead-of-failing`, precisely the two the impl-plan says are *not* re-anchored. The design's withdrawal is still correct and still owes the impl-plan nothing; what is wrong is the repair's own account of what it withdrew from. A reader reconciling plan and impl-plan finds "two re-anchored rows" in both and two different pairs, which no amount of the design giving up the word repairs. Prescription: say "three axes across two siblings" and name the plan's, or drop the enumeration of axes and keep only the withdrawal — the axis count is a claim about sibling bytes and is currently false on them.
  quote: docs/02-design/features/doc-block-exec.design.md › `One word on two axes with two cardinalities is a collision; the design gives up the word rather than redefine it, so nothing is owed to the sibling by this repair`

## Nit
- The parenthetical retained at line 2206 ends `hoisting the alternation into a shell variable is why one copy remains`, which reads as present tense about this document and is not true of it: `grep -cF '(first|second|third' docs/02-design/features/doc-block-exec.design.md` returns **3** on the working file (line 1884, the `O=` definition; line 2206, the probe quoting its own needle; line 2963, the v1.98 entry), and 2 over the head. The clause is defensible as a dated statement about `6f0ee85`, where the count is genuinely `1` — but it sits three sentences after the hunk whose whole point is that an unstamped document-self figure drifts, so it is worth the same stamp the rest of the paragraph now carries.
