## Summary
I read the assembled prompt at `/tmp/audit_doc-block-exec_design_cycle93_teammate.txt` in full (7370 lines, including the base + project invariant layers) and confirmed its inlined design is byte-identical to `docs/02-design/features/doc-block-exec.design.md` at HEAD `700c599`, which is clean — so every line number below is the real file's. I then executed the document's own screens rather than reading them: the seam-ordinal pipeline (0 stripped / 6 unstripped, and the eight-sha progression 2·1·1·2·2·3·3·6), the candidate-sweep partition at `cf3a862` (raised 36 / sites 12 / noclaim 8 / both 0 / neither 16, every anchor one line except `grep -c parametrize`), the 6-gram carry screen (BODY 8/3, VERSION HISTORY 15/8), the heading differential (30 files, both 292 / old_only 82 / new_only 1 on *both* old guards), the Setext census and its arm differential, the tilde control, the `$P`/`$W` span-ordinal screens (head 14/6 at `cf3a862`, tail 12/6), the eight-seam sibling enumeration, the five-kinds `os.open` probe, the eleven-shape ATX render, the AC comm (7 of 49, 15 table-scoped), the SKILL.md block census (159/7/1 and 50/4/1, ordinals 4 and 2 under both spans), and the three controls the advisor flagged as unexecuted (singular-`N` differential 0, v1.95-blob positive 4 folded / 3 line-scoped with per-branch seam=4 injection=1, dotted-gap fixture 1-vs-0). **Every one reproduced exactly.** Two findings survived: a pair of definite descriptions at the candidate-sweep sha list that were true at v1.101 and are false at v1.103, and a class of four fenced measurement outputs that carry no command anywhere in the four-document set while §Invariant Compliance asserts the opposite. Standing caveats I am obliged to state: this pass is gating, I have never been scored against a labelled corpus, and I share a model family with the authoring surface — a real codex round on this tree is still owed before anything I gate is treated as settled.
Evidence: 16 files opened, ~240 greps and scripts run.

## Must-fix
- **`§Scanning`, L882: both appositives on the candidate-sweep sha list are false at this revision, and the document's own re-stamping rule (L2309–2313, "an entry bump is itself the trigger to re-run and re-stamp *every* document-self figure") is what they violate.** (i) `68a70d6` is *not* "the freeze sha" — it was round eleven's; v1.102 was answered at `6dcb70f` and v1.103 at `1cbddb7`, and this gating round is at `700c599`. (ii) `7982c18`, `4e4a00c`, `06ef40f`, `68a70d6` are *not* "the four shas later than the stamped one": I re-ran the bracketed needle (`` RAISE='[`]0[`]|expect[ ]0' ``) over `git show <sha>:$D` at every later sha and got **53 at `7d8e797`, 61 at `1cbddb7`, 63 at `700c599`** — three later shas the sentence excludes, two of them the shas v1.102 and v1.103 were authored against. The five *published* values are all correct (36/42/42/50/50 reproduced exactly), and the paragraph's conclusion — that the delta is the labels each revision writes, which is why the figure is stamped at a blob — survives untouched; what is false is the description, and a reader takes from it that the sweep has been re-derived at every sha since the stamp when at the sha under audit it has not been. **Instance of: the class of definite descriptions of shas and rounds** ("the freeze sha", "the four shas later than", "the audited commit") — the entry-naming screen at L2320–2323 closes the `after the v1.1NN entry` form (I ran it: 9 hits, all `v1.103`), and nothing screens this one. Prescription: relabel `68a70d6` as round eleven's freeze, and either drop the definite description or extend the progression with `53` at `7d8e797` and `63` at `700c599` — `700c599` is a committed blob, so stamping it there does not trip this paragraph's own "a working-file value would be a number this paragraph moved by being written" objection.
  quote: docs/02-design/features/doc-block-exec.design.md › `` **50** at `06ef40f` and at the freeze sha `68a70d6` — the four shas later than the stamped one, ``
- **Four fenced measurement outputs are published with no command and no locator, and §Invariant Compliance asserts they do not exist — a breach of the base invariant "Behavioural premises carry their command" ("An output recorded without its command is not re-derivable"), which is auto-Must and cannot be downgraded.** Enumerated by walking every fenced opener in the head (`` awk '/^## Version History$/{exit} {print NR": "$0}' $D | grep -E ':\s*`{3,}' ``, 82 fence-marker lines in the head) and checking each unlabelled block for an adjacent command or citation. The four with neither: (1) **L30–33**, the unbalanced-four-backtick `_fence_aware_end` differential prefaced `Measured on the real helper:` — this is the whole justification for the headline architectural decision (why a differential test "is not achievable" and why this module owns the bounder under the Single-source contract's first branch); its identifying tokens `UNBALANCED 4-backtick` and `even toggle count masks` occur **0** times in the plan, spec and impl-plan. (2) **L992–997**, the `getpgid`-vs-`killpg(proc.pid)` probe prefaced `Measured this session,` — the justification for `killpg(proc.pid)` over `killpg(os.getpgid(pid))`; tokens `alive_after`, `grandchild`, `38030`, `38032` occur **0** times in all three siblings. (3) **L2821–2824**, the setsid-escape probe prefaced `Measured:` — the plan *does* carry a runnable probe with output at `docs/01-plan/features/doc-block-exec.plan.md:1930–1941`, but the design gives no locator to it and the block's wording is not the plan's (`descendant in the group -> survived killpg? False` vs the plan's `in-group descendant 90513: survived killpg? False`). (4) **L2840–2844**, the AC-5.2 reaping claim and its discriminating control, prefaced `Run this session, with a control that discriminates:` — tokens `alive_after` and `VERDICT: CONFIRMED` occur **0** times in the siblings. Three of the four are additionally dated only as "this session", which by this document's own naming rule (L2385–2387, a self-referential locator "stops resolving from the bytes the moment a later entry exists") names no corpus at all. **The rule that closes the class:** every measured output in this document carries either the inline runnable command that produced it — as §Scanning's census, the heading differential, the five-kinds `os.open` probe and §Test Strategy's screens already do — or a locator to the sibling section that does, as the `ENXIO`, `rmtree(…, ignore_errors=True)` and naturally-emptied-group probes already do. **Residual, exactly:** the enumeration above is over *fenced* blocks, so a measured claim written as prose is outside it; the live instance is the umask probe at L969–970 (`` probed: under `umask 0777` it yields mode `0o0` ``), whose only sibling restatement — spec L286–288 — also carries no command, and the `exit_on_error` five-shape table at L1541–1549, which siblings discuss but whose producing command I did not find. Prescription: publish the three design-only probes as runnable fences (each is a short `subprocess`/`os` script), and replace the setsid block with a locator to the plan's §Measurements fence plus that fence's own output text.
  quote: docs/02-design/features/doc-block-exec.design.md › `commands with their observed output, and the design adds no uncited measured claim. The two`
  quote: docs/01-plan/features/doc-block-exec.plan.md › `os.setsid() descendant %d: survived killpg? %s   (want True: escapes the group)" % leg(True))`

## Should-fix
- L2821–2824's block is a *re-rendering* of the plan's measured output rather than the run's own text, which is the exact hazard this document names at L2232–2234 ("a figure retyped in different words — the same number reached by paraphrase — passes it in silence"). Even once a locator is added under the Must-fix above, the fix should quote the plan's fence output verbatim or re-run the probe and publish its own bytes; a paraphrased output cannot be checked against anything.
- L2550 names `test_skill_candidates_census.py` as the shape precedent for the subprocess CLI transport without a path. It resolves to `handoff/tests/test_skill_candidates_census.py` (verified present, and its line 46 is `subprocess.run([sys.executable, str(SCRIPT)], capture_output=True, text=True)` — the shape claimed), which is the only test this document names outside `h-mad/tests/`; a bare filename sends a reader to the wrong root. This is the enclosing-symbol rule's sibling case: name the file by path.
- The AC-6.4 floor literal `2748` is stamped at `e8eaf6f` and the document already says it "travels with the commit it was measured at and is re-measured at 5c branch time" — so this is not a defect, but for the record: `python3.11 -m pytest --collect-only -q -p no:cacheprovider` from the repository root at `700c599` collects **2809**, so the floor holds with 61 tests of slack today. Worth recording in the Version History entry that closes 5b so the 5c re-measure has a dated predecessor.

## Nit
- The Architecture Overview diagram's `select` line reads `POLICY: index<1 ──► BAD_INDEX ; 0 ──► NOT_FOUND ; >1 no index ──► AMBIGUOUS` (L66). The `0` and `>1` are *candidate counts* while `index<1` is an *argument value*; beside each other the `0` reads as `--index 0`, which the same line routes to `BAD_INDEX`. The API docstring at L1421–1425 is unambiguous; the diagram is the only place the two axes are typeset alike. Suggest `0 candidates ──► NOT_FOUND ; >1 candidates, no index ──► AMBIGUOUS`.
- The verdict table's `BAD_ARGS` row (L1833) gives its "when" as "an unknown option or a missing option value", where L1551 closes the class at five grammar shapes (adding missing required option, missing positional, abbreviation) and the `allow-abbrev-restored` mutation row at L2747 requires the abbreviation shape to be a `BAD_ARGS` verdict. The table cell reads as the exhaustive list of what reaches the override and is not; two of the five are named nowhere in the table.
