## Summary

Subject: the diff `git show 8c6539a -- docs/01-plan/features/doc-block-exec.plan.md` (plan v1.98 → v1.99), nine hunks. ADVISORY, not a gate. I re-executed every published probe and every published figure the diff adds rather than reasoning about them: all five stdlib probes reproduce **byte-for-byte** (set-equality `True`, 35/35 literal, 3 breakers, DEL → 1 line, 1-of-32 C1; composite decomposition 4/4/3/3/3; `match.end()` 4 and 5; `st_size` 0 → 100 with hard-link `True` / symlink `True` / unrelated `False`; `TypeError: cannot unpack non-iterable NoneType object`), the surface screen reproduces at all three trees (3 at `700c599`, 5 line-scoped, 6 at `8c6539a`) with per-branch ordinals **9 11 19** / **9 11 19** / **9 11** / **19** / **11** and both controls exact, the stamp driver reproduces at **15/11/8/32** and **28/13/8/47**, and the twelve-added-lines partition is exactly the twelve lines the document assigns by hand — I recomputed the multiset difference of driver-matched lines between `700c599` and the working tree and it is 5 table rows + 5 output stamps + 1 class-paragraph line (python) + 1 controls stamp (awk), then +2 python / +1 awk for the resolution prose. The hand count is right: 13 rows, 12 exempt, 15 = 13 + 2 headers, `74e126f` in 0 of them. The two Must-fixes are both **provenance inside the fix**: the reason given for hand-counting is refuted by a row selector this same document publishes, and the two sibling greps in the composite bullet read lines that are not the lines they are attributed to. All six `quote:` spans below are single source lines and each returns **1** under `grep -cF`.

Evidence: 9 files opened, 70 greps run.
That grep figure is an approximate tally of the `grep`/`awk` invocations I issued, not an exact count; alongside them I made roughly 20 `git` plumbing calls and re-executed 6 python probes. On the dispatch's premises: the one about the escaper repair being unharmonised **holds as stated** — the two documents were repaired in parallel — but the disagreement it anticipated did not materialise. They agree on every figure and on the two-reason split; what I found instead is a coverage asymmetry, filed as Should-fix rather than as a contradiction.

## Must-fix

- The stated reason for hand-counting "thirteen rows" is refuted by a row selector **this document itself publishes**, and one of the two properties the sentence claims for its two published readings is false. Run at `8c6539a`: `` awk '/^## Version History/{exit}{print}' <plan> | grep -c '^| .*`git ls-files ' `` returns **13** — exactly the row count, headers excluded — and that selector is not hypothetical, it is the one printed at plan line 968 inside the closure paragraph's own command. So a row selector this document publishes does separate a row from anything not at line start. Second: `grep -nE '^\| .*git ls-files' <plan>` returns lines **1734, 1736–1743, 2561, 2563–2567** — two headers and thirteen rows, **no prose line at all** — so "both readings count this sentence's own mentions" is false of the `^\|`-anchored reading; only the backtick form self-matches, hitting its own line **2579**. The sentence's own next clause ("the thirteen rows plus the two header rows") already shows the excess is headers, and headers are separable. This matters because the paragraph's purpose is to say a number is unscreenable, and at this revision it is screenable. Prescription: publish the line-968 selector beside the thirteen with its own residual (a row quoted at line start inside a fence would be counted, and none exists at this revision), and either drop the "both readings" clause or restrict it to the backtick form.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `no row selector this document could publish`
  quote: docs/01-plan/features/doc-block-exec.plan.md › `and stated as such because both readings count this sentence's own mentions and`

- The two sibling greps in the composite bullet are misassigned, and one of them counts a line the document elsewhere warns is exactly the thing that inflates a prose-needle count. `instance of: a published command attributed to a site it does not read.` (a) **impl-plan**: at `700c599` the three hits are line **2165**, a line inside the `_field` **docstring in a fenced code payload**; line **2189**, the AC-4.1 checklist row (correct); and line **2995**, which is a **Version History entry** (`- v1.25: Impl-plan audit v24 …`). `## Version History` begins at line **2969**, so one of the three is outside the body and the body-scoped count is **2**, not 3 — and the plan states this precise hazard against itself at line 1449 before walking into it here. The hand assignment "two prose restatements" is wrong on both non-checklist members: one is a code payload, one is a Version History entry. (b) **design**: at `700c599` the single hit is line **1809**, a prose sentence about what `test_unicode_line_separators_cannot_split_a_verdict_line` asserts. The `c1-escape-removed` mutation-matrix row is line **2752** and reads ``| `c1-escape-removed` | `_field`'s second pass is removed, so DEL, C1 controls (U+0085) and U+2028/U+2029 stay literal …`` — which the published pattern `'U+2028, U+2029 and DEL'` does not match. The row *does* name the same set, so the conclusion survives; the command does not read the row. Not a finding, stated so it is not mistaken for one: the bare `grep -c … <path>` form beside a sha is this document's convention for sibling greps (plan lines 1451 and 1571 use it), so the missing `git show <sha>:` is **not** a convention violation and I do not file it. Prescription: body-scope the impl-plan grep and restate its assignment as checklist row plus code-payload docstring; and either re-point the design grep at line 2752's actual wording or change the sentence to name what line 1809 is.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `→ **3** at `700c599`, the AC-4.1 checklist row plus two prose restatements of it, assigned by`
  quote: docs/01-plan/features/doc-block-exec.plan.md › `reading the three; and the design's `c1-escape-removed` row names the same set,`

## Should-fix

- Cross-document staleness introduced by the round rather than by the paragraph: the plan says the design defect is "reported to the design's author, not edited here" (plan line 2459), but the design was repaired **in the same commit** — `git show 8c6539a --format="" -- docs/02-design/features/doc-block-exec.design.md | grep -c '^+.*The second pass covers one set for two different reasons'` returns **1**, and design v1.104's repair sits at lines 1957–1996. A reader at `8c6539a` reads a routing note for work already done at that commit. Prescription: say the design was repaired jointly at this commit and name design v1.104, the way the closure paragraphs name their commits.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `and this document cannot repair it** — reported to the design's author, not edited here.`

- Harmonisation asymmetry — the thing the dispatch expected to be a contradiction and is not. The two documents agree on every figure (35 of 67 left literal; exactly three breakers U+0085/U+2028/U+2029; the other 32 — "DEL and the other 31 C1 code points" in the plan, "DEL and the rest of C1" in the design — break nothing; AC-4.3's one-physical-line invariant turns on the three) and on the two-reason split. What differs is coverage, in both directions: the design names a residual the plan does not (`grep -c 'Cf' <design>` → **2**, `` grep -c '`Cf`' `` `<plan>` → **0** — a bidi control outside `Cc`/`Zl`/`Zp` is neither escaped by the second pass nor screened by the census), and the plan publishes the set-equality check the design does not. A reader of either alone gets a different account of what the census covers. Prescription: mirror the `Cf` residual into the plan's bullet 1 and the set-equality line into the design's run, or state at one site that the split is deliberate.

- The sibling still carries one **unexecuted** decomposition figure of exactly the class the plan's bullet 2 closed on its own side: the design asserts in prose that the composite "returns **4** lines with DEL removed and **1** with DEL alone", but its published output block prints neither — it prints only the census line and six per-code-point lines. I executed both: `4` and `1`, so both figures are **true** and this is provenance only. Prescription: extend the design's fenced run by two prints, or mark the two figures as the plan's measurement and cite it.

- Row 4 of the new carve-out block covers **two** distinct premises — buffered `TextIOWrapper` deferral and the `(st_dev, st_ino)` alias key — in one row, inside a section whose stated argument is that a single row covering several probes is the composite the section exists to argue against. The probe itself runs and publishes both separately with a negative control, so the masking hazard does not materialise; but the row is an exception to a rule stated two sentences above it and is not labelled one. Note also that "five members", "five probes" and "five rows" are three different sets of the same cardinality here — member 1 has two bullets, members 3 and 4 share one row — and the 5 × 2 = 10 arithmetic is over bullets, which happens to be right. Prescription: say in the row that it carries two premises and why, or split it and re-derive the ten.

## Nit

- "paragraphs 9 and 19 are surfaces one and three, and paragraph 11 is this one" sits, at `8c6539a`, inside **paragraph 14**. The freeze reading it labels is exactly right — I confirmed paragraph 11 at `700c599` carries both surface two's wrapped carve-out (lines 78–79, "there is no" / "non-`DOCBLOCK` exit") and the three-surfaces quotations (lines 83–84), so three branches reach it through quotation and one through the carve-out, as published — and "three-surface edit" is still in paragraph 11 at `8c6539a`, so the referent survives. Only the deictic "this one" no longer resolves to the paragraph the sentence is written in.

- `git ls-files` takes no commit argument, so "the table below is read at `700c599`" cannot be reproduced at that commit from the cells as written; the `cf3a862` block carries the same shape, so this is inherited and not introduced by this diff. All five arguments do return empty, and the whole-tree row returns 0, against the working tree — which I ran.
