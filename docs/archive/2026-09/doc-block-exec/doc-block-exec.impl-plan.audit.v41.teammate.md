## Summary
Every figure the brief listed as do-not-disturb reproduces at `7982c18`: four screens 49 / 49 / 0 / 0 with 19 folded paragraphs and byte-identical path-qualified populations, `SKILL.md:[0-9]` 0, all 13 sibling locators at exactly one hit (9 design / 2 plan / 2 spec, and `git rev-parse --show-toplevel` is the *only* one with an off-target sibling hit — I swept all 13 × 3), five tasks / two `wiring`, the design helper matrix at 81 data rows with exactly one naming `SKILL.md`, and the DECISION-N fixture's 2 × 3 grid reproducing 0/0/0 and 1/1/0 byte-identically under `bash` and `zsh`. The recurrence-ledger closure is real at all five named sites (`:335`, `:353`, the deleted `:480` restatement, `:524`, and v1.41's Version History) and both Version History entries are bracket-corrected rather than rewritten, with the v1.41 and v1.42 originals preserved verbatim — but the repair's own explanatory sentence introduced a sixth live site. Three Must-fixes remain, all of them mutation- or grep-verified: one live site the derived-integer sweep missed, and two composite predicates (DECISION O) whose published controls survive deletion of the branch they are supposed to cover.
Evidence: 18 files opened, ~150 greps run.

## Must-fix
- The recurrence ledger's absence claim is falsified by its own sentence, five words later — the same shape as v1.42's control destroying its own zero, and a sixth live site the "sweep for DERIVED integers" pass missed because it is phrased as past-perfect narrative rather than as an ordinal. Measured: the enumeration at `:341`–`:349` has five members and `len(list)` is 5, so the prose integer is currently *correct* and will not drift — but the document states the absence twice as an absolute, and a reader screening for the size in prose gets 1 hit, not 0 (DECISION G: an absence claim is a measurement). Prescription: quote it the way the same clause already quotes the stale `"four"` — `left two saying "four" after the list had reached its fifth member` — or scope both absolute claims to "as a live value; the one dated occurrence is historical".
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `**The count is `len(list)` and this document writes it out in prose nowhere**`
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `left two saying "four" after the list had reached five`
- DECISION O, mutation-verified: AC-6.1's corpus filter ships **three** clauses, the document calls them "Two independent clauses, two independent defects, both measured", and the third — the `archive` clause — is measured by nothing. I deleted `'archive' not in r.parts` from all three published invariants and `grep -v '/archive/'` from the `git ls-files` side, and every one of the four dated integers is unchanged: (i) `p.parts` form **0 → 0**; (ii) rel-rebased dot-excluded **30 → 30** against git side **30 → 30**; (iii) dot clause dropped **35 → 35**. The clause drops zero paths at the freeze sha — `git ls-files -- h-mad handoff | grep '\.md$' | grep -c '/archive/'` is **0**, and the glob's archive-drop set is `[]` — and it appears on *both* sides of invariant (ii)'s equality, so it cancels. A 5d implementer who omits or misspells it gets an identical corpus and AC-6.1 still asserts exactly 1. **Class**: every clause of every multi-clause corpus filter this AC ships. Members: `rel.parts[0] in ('h-mad','handoff')` — controlled (27 h-mad + 3 handoff both contribute); dot-directory — controlled (35 → 30, five named paths); `archive` — **uncontrolled**. **Rule**: publish one reading per clause with that clause removed, not one reading for the composite. **Residual**: a clause that drops 0 on this tree cannot be positively controlled here at all, so the honest form is a stated blind arm — the same disposition `:714` already gives the tab arm.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `dropping any path with `'archive'` in `rel.parts``
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `**Two independent clauses, two independent defects, both measured**`
- The standing control the document names as enforcing the `SKILL.md`-pin class is fold-blind, which is DECISION B one file over from the fold repair this revision just shipped. `h-mad/scripts/h_mad_precheck_doc.py:106` extracts backticked spans with `_CODE = re.compile(r"`([^`\n]{1,200})`")` — the span may not contain a newline — so a path-qualified pin wrapped across the ~95-column fold produces no LINEPIN detail and the control's `assert "SKILL.md:" not in joined` (`h-mad/tests/test_h_mad_precheck_doc.py:310`) passes. Mutation-probed in both directions on `/tmp` copies of this document, the repository untouched: appending `` `h-mad/SKILL.md:1606` `` on one line yields `LINEPIN: L2672 \`h-mad/SKILL.md:1606\`` (**1** hit); appending the identical pin split as `` `h-mad/SKILL.md: `` / `` 1606` `` yields **0**. The document leans on this control twice (`:199`–`:201` and `:711`–`:713`) as the reason it may omit line numbers, and the four folded/line-scoped screens v1.43 published cover `.py:` pins only — nothing covers `SKILL.md:` under a fold. Prescription: either add the folded `SKILL.md: ?[0-9]` screen beside the four `.py:` ones, or state the residual in the two places the control is cited — a wrapped path-qualified pin escapes it, so the control is a screen for unwrapped pins only.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `which asserts no path-qualified `SKILL.md:`*N* pin survives this document's LINEPIN details.`

## Should-fix
- `instance of: the DECISION O class named in Must-fix 2` — the fence-state scan's stated **true negative** is a disjunction reported only as a total. `declined-inside-fence 2` comes from `re.match(r'^[ ]{1,3}(```{3,}|~{3,})', line) or tab.match(line)`; I re-ran the published script at the working tree with the disjuncts split and got **space-disjunct 2, tab-disjunct 0**, with the whole published line reproducing verbatim (`files 30 space-arm 29 in 4 files tab-arm 0 declined-inside-fence 2`). So the 2 is evidence that the fence-state gate declines *space*-indented runs inside a fence and no evidence at all for tab-indented ones. The document already declares the tab arm unpinned at `:714`, which is why this is a Should and not a Must, but the control's total currently reads as covering both. Prescription: publish the split (`2 space / 0 tab`) so the composite is not mistaken for per-branch evidence.
- `instance of: the DECISION O class named in Must-fix 2` — the bare-filename screen's leading alternation `(^|[^/A-Za-z0-9_.-])` has one branch that has never fired in any published reading. Measured: over the before-figure population at `35698f9` (which reproduces exactly — **22** occurrences, **19** lines, **8** distinct files, and the per-file breakdown `h_mad_mutation_harness.py`×9 / `docsections.py`×4 / `test_h_mad_portable_timeout.py`×2 / `test_h_mad_collect_report_docs.py`×2 / `h_mad_assemble_tdd.py`×2 / one each of the remaining three), `grep -cE '^[A-Za-z0-9_]+\.py:[0-9]+'` is **0** — every one of the 22 matched through the character-class branch. The new fixture also exercises only that branch (its needle is preceded by a space). Prescription: add a second fixture line whose pin starts at column 0, or state the `^` branch as a blind arm.
- Unverified, and named rather than passed over: I did not re-run the four figures the document itself registers as inherited-unverified at item (9) of the v1.43 entry — the 2748 / 2486 suite floors, and the plan's 263/76/0 and 268/76/0 heading differentials with their markdown-it-py oracles. They are stamped at `1861157` / `b7d0d77` and transcribed rather than derived here, so "I could not check them this pass" is the accurate statement, not "they are wrong". They have now gone at least two rounds with no surface re-running them.

## Nit
- `the four screens over this document return the same 49 / 49 / 0 / 0` gives four integers with no ordering key. The four screens are named several lines earlier as `{line-scoped, folded} × {bare, path-qualified}`, and the reading is `pathq-line / pathq-fold / bare-line / bare-fold` — but nothing in the sentence says so, and DECISION H's "every count with its unit" has a twin: every count with its identity. All four reproduce; only the mapping is implicit.
- The published fixture command writes to a fixed shared path, `> /tmp/fold-control.txt`, with no note that it clobbers and no cleanup. A reader running the grid twice in different shells is fine; a reader who already has that path is not warned. A `mktemp` form, or one sentence, closes it.

---

### Figures I chose NOT to challenge (this list is contract)

Re-derived and accepted, at `7982c18` unless stated:
- Four screens **49 / 49 / 0 / 0**, **19** folded paragraphs, path-qualified populations byte-identical between the line-scoped and folded forms (`diff` empty, 49 lines each); identical at `cf3a862`, so the fixture's publication added **0**.
- `grep -c 'SKILL.md:[0-9]'` **0**; and the screen is not blind — it reads **1** at `74e126f`, **0** at `335f535`, `35698f9`, `cf3a862`, `7982c18`.
- All **13** sibling locators at exactly **1** hit; split **9** design / **2** plan / **2** spec = 13; the `git rev-parse --show-toplevel` off-target caveat is the only one (I ran all 13 against all three siblings).
- The DECISION-N fixture grid: unrepaired fold **0 / 0 / 0**, `tr -s ' '` fold **1 / 1 / 0**, byte-identical under `bash` and `zsh`.
- The v1.42 Version History bracket's falsification figures at `cf3a862`: body unrepaired **1**, body `tr -s ' '` **2**, whole-file unrepaired **3**.
- Both Version History edits are pure bracketed insertions — the v1.41 and v1.42 originals are preserved verbatim; nothing was rewritten.
- Tree-stamp closure: `git diff --name-only 74e126f 7982c18 -- h-mad handoff` empty, `git diff --name-only 7982c18 -- h-mad handoff` empty.
- `git diff --name-only 8909ec4 cf3a862` empty over this file, over `h-mad handoff`, and over all three siblings; `git diff --name-only 6f0ee85 cf3a862 --` names all three siblings; header pins v1.98 / v1.60 / v1.93 correct at `cf3a862` (one behind at `7982c18` — declared drift, not a finding).
- **Five** tasks, **two** `wiring` (Tasks 1 and 5); `docsections.json` holds **4** rows today, **8** after Task 1; the design matrix at `7982c18` is **81** data rows with exactly **1** naming `SKILL.md` in its mechanism column (`registry-row-removed`), and all 81 design row names occur in this document.
- All **42** distinct path-qualified `.py` pins in the body resolve — file exists, line and range in bounds — and there are **0** bare-filename pins.
- Spot-verified tree pins: `h_mad_mutation_harness.py` `:177`, `:212`, `:482`, `:562`, `:606-607`, `:721`, and `:630-642` (the `continue` at `:642` is the terminator, so the widened span is right); `docsections.py` imports, `:53`, and the two docstrings at `:46-52` / `:60-65` verbatim; `_titled_section` one `def` at `:69` and eight call sites `:301`–`:372`; `_second_surface()` exactly eight call sites `:118 :154 :225 :248 :269 :389 :409 :431`; `^## Run-context ceiling` one hit.
- `h-mad/tests/test_h_mad_precheck_doc.py` is green at the freeze sha — **24 passed**. Not red on main.
- `grep -c 'exception table agrees'`: **1** at `0aac0b7`, `35698f9`, `6f0ee85`; **3** at `cf3a862`; **4** at `7982c18`. Consistent with the document's stated rationale; excluded by the brief and not filed.
- AC-6.1 corpus relation re-derived: **30 / 30 / 35 / 0**, symmetric difference empty, the five dropped paths exactly the five named `.pytest_cache/README.md` files.

Not re-derived at all, and therefore not cleared by this pass: the 2748 / 2486 suite floors; the plan's 263/76/0 and 268/76/0 differentials and their CommonMark oracles; the `25` and `27` addends of `25 + 5 + 24 + 27` individually (I verified the `5`, the `24`, the `81` total, the `80 + 1` split, and `25 + 27 = 52` as a pair); the `__all__` name counts (25 / 29) and the 20-exception hierarchy, which have no landed source to check against; the Task 2–4 AC bodies and the RED/GREEN gate commands, which I read but did not execute; and the eight PLACEHOLDER-shaped hits and the `--allow` list, excluded by the brief.

### Scope of my own reading, stated plainly
I did not read the 708 KB assembled prompt end to end. I read its output contract and role block (lines 1–94) and its section map, then worked against `git show 7982c18:` rather than the inlined copy — and confirmed the two are the same document by locating four load-bearing v1.43 spans inside the prompt (`docsections.pyX` ×3, and one each of the three spans this report quotes). This pass is gating and nothing downstream re-checks it; I have never been scored against a labelled corpus, and I share a model family with the author, so a real codex round on this same tree is still owed before anything I gated is treated as settled.
