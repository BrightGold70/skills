## Summary

v1.94's central claim reproduces exactly: the repaired `[Tt]his [Ss]ession` branch is bounded on both sides and case-folded, all three branches of screen one are named in a list rather than counted, and the v1.93 and v1.94 programs print byte-identical output over the `6f0ee85` body (32 lines, `diff` silent, `awk version 20200816`). What does not hold is the reach of the two new standing decisions: DECISION O was applied to the alternation that produced it and to no other published alternation in the document, and the closure preamble's promise that every sibling-derived figure is re-derived at the freeze is false for four surfaces still stamped `6f0ee85`. Every figure I challenge below I reproduced at the sentence's own scope and sha first; no figure in this document was found arithmetically wrong.

Evidence: 12 files opened, 96 greps run.

## Must-fix

- DECISION O is stated over the axis and swept over one member — three other alternations this document publishes are still controlled at composite level or not at all, which is the exact "healthy sibling covers a sick one" mechanism the decision names. `close the class:` a published alternation is a member of this class the moment it is written, and the sweep driver is the *shape* `(a|b|c)` inside any published program, not which alternation an audit named; residual, which is why the driver cannot be a gate: a regex alternation can also be spelled as two greps in a pipe, and no shape screen sees that form. Three instances, each verified by me at `cf3a862` and `7982c18`: (a) the extractor census `findall.*|split.*|re\.compile.*` — `git grep -cE 'split.*```bash' cf3a862 -- '*.py'` and the `re\.compile` form each return **0**, and both hits come from the `findall` branch alone, so two of three branches have never been shown to fire and the paragraph's "the consumers that would break" claim rests on a pattern that is two-thirds unobserved; its published control (`git grep -l '```' -- '*.py' | wc -l` → 24) is an alternation-level under-matching check and is not evidence about any branch. (b) the companion `path:line` sweep's positive fixture exercises `md` only — over this body at `7982c18` the per-branch readings are `md` **2**, `json` **0**, `sh` **0**, `toml` **0**, so three of four branches are covered by the healthy one. (c) the carve-out stamp driver publishes the count **13** with no control at all, neither positive nor negative nor per-branch, which also breaks this document's own standing rule independently of O; per-branch at `cf3a862` it is python-version **10**, `awk version` **2**, `markdown-it-py` **1**, and the document publishes none of that.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `branch. All three branches now have one, and they are named here so the correspondence is checkable`
  quote: docs/01-plan/features/doc-block-exec.plan.md › `**A checker this document publishes is executed against a positive and a negative control before`
  quote: docs/01-plan/features/doc-block-exec.plan.md › `    | grep -cE '(python3?[.:]? ?[0-9]+\.[0-9]+\.[0-9]+|awk version [0-9]|markdown-it-py [0-9])'`

- The per-branch controls DECISION O ships are themselves boundary-repaired on one side only for two of the three branches, which is DECISION I unapplied to the very controls built to catch one-sided repairs — a fourth half-application, in the revision that diagnosed the mechanism. `[Tt]oday`'s fixture `printf 'measured today\nremeasured todayish\n'` carries a *trailing*-glued negative (`todayish`) and no leading-glued one; `[Mm]easured`'s six-line fixture carries *leading*-glued negatives (`anything unmeasured`, `remeasured today`) and no trailing-glued one; only `[Tt]his [Ss]ession` has both (`xthis session glued` and `in this sessionless mode`). I verified both branches are in fact correct — `printf 'xtoday here\n' | awk '/(^|[^[:alnum:]_])[Tt]oday([^[:alnum:]_]|$)/'` and `printf 'measuredly so\n' | awk '/(^|[^[:alnum:]_])[Mm]easured([^[:alnum:]_]|$)/'` each print nothing — so the regexes are right and the *controls* are the defect: as published they would stay green through a regression on the unexercised side, which is the failure this document has now hit three times. `close the class:` a per-branch fixture carries a glued negative on **each** side plus a case-fold line, and the check is that the fixture has one line per property claimed for the branch.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `` `printf 'measured today\nremeasured todayish\n'` fixture is read against the ``
  quote: docs/01-plan/features/doc-block-exec.plan.md › `The `[Mm]easured` branch's single-branch control — positive and negative, run at `6f0ee85` over one`

- A **fifth** repo-wide `.py` census is still stamped `6f0ee85`, is invisible by construction to the corpus-argument driver v1.94 built for exactly this class, and is absent from the new register. `instance of:` v1.94's MUST 3 (a repo-wide `.py` census the two-root closure never reached). The census is `git grep -l '```' -- '*.py' | wc -l` → **24** and its companion `git grep -l '```bash' -- '*.py' | wc -l` → **4**; its corpus is the whole repository, and at `cf3a862` **5** of the 24 files sit outside `h-mad/` and `handoff/`, so the §Measurements closure does not reach it and the `6f0ee85` stamp rests on nothing. The published driver `grep -oE "grep -r[a-z]* '[^']*' --include='\*\.py' \."` cannot find it because this member is a `git grep … -- '*.py'` form with no `--include`, so the sweep that closed the class missed a member of it. Values re-derived by me and unchanged — 24 at `35698f9`, `6f0ee85`, `cf3a862` and `7982c18`; 4 at `6f0ee85`, `cf3a862` and `7982c18` — so this is provenance and sweep completeness, not arithmetic. Prescription: drive the sweep off the *corpus* (`.` or an unrestricted `-- '*.py'` pathspec) rather than off the command's spelling, and re-stamp both figures at the freeze.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `**24** `.py` files contain **a fence literal of any language** at `6f0ee85`, re-run here and`

- The closure preamble states a completeness claim that this revision did not deliver, and under DECISION G a completeness claim is a measurement. Four sibling- or `docs/`-derived figures still carry only `6f0ee85`, are outside the closure by the preamble's own words, and are named nowhere in the inherited-unverified register: the design's `seven-plus-two-plus` locator at plan line 394; the mutation-target derivation `` grep -c 'the mutation targets `SKILL.md`' `` on the design at plan line 618; the 49 AC anchors plus the "all three premises were re-run at `6f0ee85`" sentence at plan lines 1667 and 1681; and the codex-leg ledger at plan line 1873, whose corpus is `docs/01-plan/features/` and which the closure explicitly excludes. This is the sweep v1.93 ran for `35698f9` and v1.94 did not run for `6f0ee85`. I re-derived all four at `cf3a862` and every value is unchanged — design locator **1**, mutation-target **1**, AC anchors **49**, codex-max **72** — so no conclusion moves; what is false is the sentence asserting they were re-derived. Prescription: either re-stamp the four at the freeze or move them into the register by name.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `` `docs/`: those files did change, so every such figure is re-derived at `cf3a862` on the surface ``

## Should-fix

- The withdrawn-offer sentence publishes a command that does not return its own numbers. `grep -c '74e126f'` as printed returns **29** over the whole file at both `6f0ee85` and `cf3a862`; the stated **27** and **26** are body-scoped and need the `awk '/^## Version History/{exit}{print}'` prefix that the *narrowed* form two lines later does carry. Both figures are correct at the scope the prose names — I reproduced 27 at `6f0ee85` and 26 at `cf3a862`, and 10 / 11 for the narrowed form — so this is legibility, not arithmetic. `instance of:` the unrunnable-cell class v1.94 closed in the carve-out table, where the fix was to put the command's actual argument in the cell rather than three paragraphs away.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `` than corrected: `grep -c '74e126f'` over the body returns **27** at `6f0ee85` and **26** at ``

- `cf3a862` is called "the audited commit" while the round-eight audit's freeze was `8909ec4`, which this document names correctly one paragraph earlier. `git log --oneline -1 cf3a862` reads `docs(doc-block-exec): round eight audit -- six reports at freeze 8909ec4`, and `git diff --stat 8909ec4 cf3a862 -- <plan>` is empty, so the plan body is byte-identical at the two and no figure moves — but a reader reproducing "the audited commit" lands on the commit that *records* the audit rather than the one it *measured*. Same phrase at plan lines 669, 934, 1190, 1277, 1491 and 1877. This is the relational-label class SHOULD 1 of this revision closed for the opener census, unswept at its other five surfaces.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `` between `74e126f` and the audited commit `cf3a862` touches only paths under `docs/`: ``

- The inherited-unverified register says "the three OS probes" while the carve-out table two sections below lists **five** OS- or runtime-determined probes this revision did not re-run: the register omits argparse's `exit_on_error` routing and the AC-5.2 group kill and escape probe, both of which sit in the table and neither of which the `cf3a862..7982c18` delta touches. The register is explicitly declared not-complete, which is why this is not a Must — but the two omissions are visible in the table on the same page, and it is the same "population short by two" shape the table itself was repaired for one revision earlier.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `three OS probes (`rmtree` on a `0o000` directory, the reader-less FIFO, the emptied-group`

- The carve-out driver's residual names only the calendar-date blind spot, but the rule it implements also admits a **platform** stamp, and the driver has no platform branch — `printf 'exempt; stamped darwin\n'` fed to the published `grep -cE` returns **0**. A probe stamped `darwin` or `linux` with no version digits is therefore invisible to the driver for the same structural reason the calendar date is, and only one of the two gaps is stated. This is the drift the document already names for screen one — rule sentence and program maintained as two independent pieces of prose — recurring on the new driver without the residual being carried across.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `platform instead of a sha is a row in this table**, and the sweep is driven by **the stamp**, not by`

## Nit

- Plan lines 1190–1191 split `73` / `88` from their freeze sha across the hard wrap: the line ends `and again at the audited commit` and `` **`cf3a862`** `` sits alone on the next line. That is the exact line-scoping hazard this document states elsewhere as its reason for keeping a number and its sha on one physical line, and the re-stamp v1.94 added here reintroduced it.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `still **73 across 10 files** / **88** at `335f535`, at `35698f9`, and again at the audited commit`

- The `today` branch's single-branch control is described as living "two paragraphs *earlier*" than the list that names it. The relative pointer is an ordinal over paragraphs, which the document elsewhere refuses for exactly the reason that paragraphs get inserted; the probe already has a stable name in the carve-out table (`the awk boundary probe`) and naming it here would close the axis.

- The list naming the three per-branch fixtures is prose in three clauses of one sentence. Since its stated purpose is that the correspondence be "checkable without counting fences", three bullets — one per branch, each carrying its branch pattern and its fixture — would make the check a scan rather than a parse, at no cost to the argument.

---

**Figures I chose NOT to challenge, each reproduced by me at its own scope and sha before I let it stand:** the 32/32 byte-identical screen-one readings over the `6f0ee85` body with `diff` silent, on `awk version 20200816`; the v1.93 program's two substring false positives and its declined `This Session capitalised`, and the repaired branch's two members on the same fixture; the live-impact pair — 2 lines for `[Tt]his session` alone over the `cf3a862` body and 0 with the `measured`/`today` exclusion appended; the `9` / `32` / `23` widening; the carve-out driver's **13** at `cf3a862`; the carve-out table at **8** rows with every `git ls-files` argument returning empty except the wrapper's two paths; the corpus-argument driver's **3** with `.` and **2** with `h-mad handoff` at `cf3a862`; the `git grep` cross-checks **2** and **6** at `cf3a862`; **411** at `6f0ee85` and `cf3a862`; both closure commands (nothing, and `docs` alone) at `cf3a862` *and* at `7982c18`; the opener census at three shas — 9 openers / 5 tokens at `35698f9`, 20 / 11 at `6f0ee85`, 21 / 11 at `cf3a862` with `sed` ×2, `awk` ×1 at all three; `grep -cE '^  \$ awk '` → 1 on the spec and 0 on the design and impl-plan at both shas; `grep -c 'Residual on the enumeration itself'` → 1 at both; the body-scoped **27**/**26** and **10**/**11**; the fence census **73 across 10 files** with control **88** and the tracked/glob pair **30**/**35** at the freeze; `git diff-tree --diff-filter=A 6db8e50 -- h-mad/agents` listing exactly five paths against `git show --stat 6db8e50`'s six rows; the sibling `6db8e50` counts 3 / 2 / 0 with `git show --stat 6db8e50` at 0 on all three, and the body-scoped 2 / 1 split inside this document; **49** unique AC anchors with the duplicate check silent; the **24** / **4** values themselves (challenged only for provenance); and `8909ec4` resolving to `round seven -- design v1.98 / plan v1.93 / spec v1.60 / impl-plan v1.42`.

**Named rather than passed over — what I could not check.** The three OS probes (`rmtree` on `0o000`, the reader-less FIFO, the emptied `killpg` group), the markdown-it-py 14-case corpus, the Setext differential, the `doc-auditor.md` fence-toggle 8/4 readings, the pytest collect counts 2809/2547 and the 2748 floor, and the live `hmad-dispatch run --timeout` probe are all `unverified` by me this pass — I did not run them, and my silence about them is not evidence in either direction. The membership of the five triage categories summing to 32 is likewise `unverified` here; I re-ran only the total.
