## Summary
Second gating leg for design cycle 95 (`3f70eb3`, blob `eaa7fca`), dispatched as a re-dispatch on
the premise that no v95 artifact existed. **That premise is now false**: the first leg's report
landed at `docs/02-design/features/doc-block-exec.design.audit.v95.teammate.md` at 11:34 while I
was reading, so I wrote here instead of clobbering it — treat the two as one union, not as a
duplicate. I re-derived ~35 published figures and nearly all reproduce exactly: the 63/61 raised
pair at `700c599`/`8c6539a`/`b3be433`/`00b961f`/working, all three hops and the `700c599`→working
hunk, the six-blob candidate series, all four sha-description arms and the widened arm, the
refusal arm, the compliance walks 106/62 with the fence-aware 2/60/55 split, the eleven-key
`DETAIL_KEYS` arm at seven with all seven sites opened, the adjective count, the spec's `pgid`
needles, the ten byte-identical `python` fences, and `git diff 00b961f dfae038` empty over the two
feature dirs. Three musts below; all sit in the v1.107 §Version History entry. **One gating
surface — codex is exhausted until 2026-09-07 11:28, this is not a two-surface clean and no exit
gate is claimed. I share a model family with the author of v1.107.**
Evidence: 5 files opened (design, impl-plan, spec, the first leg's report, the v1.106→v1.107 diff)
plus 8 distinct git blobs of the design read through `git show`. ~70 greps run, 9 `git diff`/`diff`
differentials, 2 Python fence-state walks, 1 exception-table row count.

## Must-fix
- The v1.107 appendix's stamp census gives the wrong flat count and the wrong under-report, measured against the document's **own published needle**. The census screen this document ships is `awk … | grep -oE 'after the v1\.[0-9]+ entry'` (§Test Strategy, in the fence that defines the entry-naming screen). Folded over the head it returns **18** (the entry is right); line-scoped it returns **16**, not 17, because **two** hits are wrap-split — `after the v1.106\nentry` at head line 1035 and `after\nthe v1.106 entry` at head line 2406 — so a flat grep under-reports by **two**. The first leg recorded this as `unverified` on the ground that the needle is not published; it is published, at the screen's own definition, and the looser needle `v1.106 entry` that does give 17 is not a needle this document ships. This is a wrong cardinal inside the appendix v1.107 wrote to record a wrong cardinal (its own MUST 3).
  quote: docs/02-design/features/doc-block-exec.design.md › `still returns 18 hits over the head, all of them reading v1.106, and 17 are flat while ONE is split by the hard wrap -- which is why that census is taken folded and why a flat grep for it under-reports by one`
  quote: docs/02-design/features/doc-block-exec.design.md › `| grep -oE 'after the v1\.[0-9]+ entry' | sort | uniq -c`

- The appendix names the `-F` needle as an **awk** idiom; it is the **`tr`** idiom, and the published pair is only reachable with the `tr` one. The needle this document defines is `git show 35698f9:$D | grep -cF "tr '\n' ' '"` (§Implementation Order, fourth-blind-form paragraph), and it returns **3 whole-file / 2 over the head** at `00b961f` and on the working file — exactly the pair published. The newline-collapsing *awk* idiom is `awk 'BEGIN { RS = "" } { gsub(/\n/, " "); print }`; `grep -cF` on `gsub(/\n/, " ")`, on `BEGIN { RS = "" }` and on `RS = ""` each returns **5 whole-file / 5 over the head**, and no awk fixed string I could construct returns 3/2. So the sentence carries a correct measurement inside a false description — this document's own named signature class — in the one paragraph written to prevent exactly that ("naming a check one did not perform is the failure this appendix exists to prevent"). Fix: say `tr`, or quote the fixed string.
  quote: docs/02-design/features/doc-block-exec.design.md › `The needle is the fixed-string count of the newline-collapsing awk idiom, and it returns three whole-file and two over the head at 00b961f and the same pair on the working file.`
  quote: docs/02-design/features/doc-block-exec.design.md › `(`git show 35698f9:$D | grep -cF "tr '\\n' ' '"` → `0`, against `1` at `6f0ee85`)`

- The v1.107 entry states a rule over sha comparisons that the same entry and the body violate three sentences later, and that §Scanning's own stamping rule contradicts head-on. The freeze-sha paragraph forbids the working tree as an endpoint; the entry's own MUST 1 then *prescribes* a working-tree right-hand side ("re-taken AFTER this document's own entry is written"), its appendix publishes three of them ("700c599 to working 60,61c60,61; 8c6539a to working 61c61; b3be433 to working 61c61"), and §Scanning publishes the same as the live derivation `diff <(git show 700c599:"$D" …) <(grep -E "$RAISE" "$D")`. §Scanning also blesses the construct explicitly: a document-self figure is stamped "the working file by the entry it was run after". An author applying the entry's rule as written would have to delete the document's headline hop differential. **The one reading under which this is not a contradiction, named so it can be adjudicated rather than re-derived**: if "sha comparison" is meant narrowly as `git diff <ref> <ref>` and excludes a `diff <(git show …) <(grep … "$D")`, the rule and the hops do not overlap — but the sentence says "every sha comparison published in this document" and the hops are published as sha comparisons, so the narrow reading has to be written down to hold. Fix: scope the rule to *named refs that move* (`HEAD`, branch names, `..HEAD`) and exempt the working file stamped by its entry, which is what the document actually does — I found no `..HEAD`, `HEAD~`, `origin/` or `@{` endpoint anywhere in the head, so the rule's real target is already absent and only its over-reach is live.
  quote: docs/02-design/features/doc-block-exec.design.md › `THE RULE OVER THE CLASS: every sha comparison published in this document names TWO fixed endpoints; HEAD, the working tree and the current branch are not endpoints, they are whatever the reader's tree happens to be.`
  quote: docs/02-design/features/doc-block-exec.design.md › `a hop differential whose right-hand side is the working file is re-taken after this`

## Should-fix
- Independently confirmed, and corroborating the first leg's headline must: the appendix's `95 added and 35 removed` reproduces at no corpus. `git diff --numstat` on this file gives `102 41` for both `00b961f`→`3f70eb3` and `dfae038`→`3f70eb3`, and `96 36` for both `00b961f`→`59cc2ad` and `dfae038`→`59cc2ad`; `59cc2ad`→`3f70eb3` is `7 6`. I raise it here only so the union carries two independent derivations of it.
  quote: docs/02-design/features/doc-block-exec.design.md › `the 95 added and 35 removed lines of this revision's own diff were run against every needle this document ships`

- The hop paragraph's per-hop *attribution* is correct but is not derivable from what it publishes, because its third hop spans two revisions. `b3be433`→working covers v1.106 and v1.107 together. I split it: `b3be433`→`00b961f` prints `61c61` and `00b961f`→working prints nothing, so "v1.106 reworded one" holds — but only because I ran a fourth diff the paragraph does not publish. The first leg raised the same gap; I am recording that I re-took the split independently and that the attribution survives it.
  quote: docs/02-design/features/doc-block-exec.design.md › `So v1.104 reworded one, v1.105 reworded none, and v1.106 reworded one`

- The `DETAIL_KEYS` residual is adequate as a bound on *today* and is not bounded in *time*, and the "for the same reason" bridge to the spec does not hold. I confirm the tree half: `grep -rn DETAIL_KEYS h-mad/` returns nothing (rc=1) and the tuple is a declaration at `docs/01-plan/features/doc-block-exec.impl-plan.md:2510`, eleven members with a trailing `# 11`, member-for-member and in-order identical to the design's alternation — so "no symbol to test against" is correct now. But the design writes the residual as an unqualified "no test can reach it", while the spec closes the same staleness with AC-4.5's registry walk once 5d/5e build the module, and says so in the same paragraph. The two documents therefore name different closures for the same growth: a reader for the design, the registry walk for the spec. The first leg raised the divergence; the part I add is that the design's claim needs "until 5d/5e", which is the only thing that makes it true and the spec false-sounding rather than the two contradicting.
  quote: docs/02-design/features/doc-block-exec.design.md › `same residual against the same tuple for the same reason, and the check is a reader comparing the`
  quote: docs/01-plan/features/doc-block-exec.spec.md › `without this list growing, which is a staleness the registry walk catches and this grammar does`

## Nit
- The seven-site enumeration of the eleven-key triage arm is correct member-for-member — I opened all seven (`:1270`, `:1325`, `:1753`, `:2291`, `:2294`, `:2297`, `:2301`) and every one is a Python keyword argument — but it calls the `verify=None` hit "the read-back-stream default", which is the reader's only bridge from that phrase to the key `verify`. Naming the key beside the description would make the walk re-takeable without opening the row.

---

**What I did NOT check**, named so its absence is not read as clean:
I did not read the whole document. Read in the shipped bytes: 1–1378 (§Executive Summary through §Execution), 1495–1549, 2270–2314 (§Error Handling and its exception table), 2715–2900, 3400–3503, and the v1.104–v1.107 §Version History entries. **Unread**: the rest of §Execution and §Components Changed (1378–1495), §Implementation Order outside 1495–1549, §Data Model, §API / Interface Changes except the `LaunchFailed` signature line at :1753, §Test Strategy outside 2715–2900, all of §Test Plan, §Invariant Compliance before 3400, and §Version History entries v1.0–v1.103. In the spans I did read past the v1.107 diff I found no further inconsistency: the "29 public names" of the Executive Summary reconciles exactly with the exception table (19 subclasses + `DocBlockError` + 7 `__all__` functions + `Block` + `RunResult`) and with the impl-plan's `__all__ (29 names)`, and the five-outcome post-spawn precedence in §Architecture Overview matches §Execution's stage mapping and the `timeout + 2 * DRAIN_SECONDS` bound.
I did not re-execute the ten `python` fences; I established only that their bodies are byte-identical across `8c6539a`, `b3be433` and the working file (Python fence-state walk, all ten pairs equal).
I did not re-read the 55 prose lines; I verified only the mechanical chain 62 → 2 in-fence → 60 → minus 5 named → 55.
I did not run the repository test suite and did not check any AC against `h-mad/scripts/h_mad_doc_block_exec.py`, which the tree does not hold.
I did not re-derive the first leg's attribution-deictic must (its second), its residual-(iii) fence-parity should-fix, or its "every one returns zero in BOTH directions" should-fix. Those stand on its evidence, not mine. Its `DETAIL_KEYS` divergence I did re-derive independently (Should-fix 3 above).
**Report-path collision.** My dispatch assigned `…audit.v95.teammate.md` on the stated premise that no v95 artifact existed. The first leg's report was already there. I did not overwrite it. Neither of us is a second surface for the other in the sense the exit gate needs: we are the same model family as each other and as the author.
