## Summary
Audited `docs/01-plan/features/doc-block-exec.impl-plan.md` at the freeze sha `35698f9`, delta-first (`74e126f..35698f9`, +173/-38) and then whole. The v1.40 delta's headline claims hold under re-measurement: all **13** sibling locators return exactly **1** hit at `35698f9` (not at `74e126f`, which is what the document stamps — I re-ran every one, including the two that landed in the *same* commit `0aac0b7` as design v1.96/plan v1.91/spec v1.58); the residual-(b) AST sweep prints exactly **7** bodies with every named `path:lineno` and every toggle line correct; the tab-arm stdlib one-liner prints `30 0`; the six-token GNU/BSD sweep returns exactly two lines outside the Version History; every markdown-it-py 2.2.0 CommonMark oracle reproduces byte-for-byte; the AC-6.4 tuple is nine members and `len(tuple)` is the addend; the design's mutation matrix is **81** data rows with exactly **1** naming `SKILL.md`; `grep -c 'SKILL\.md:[0-9]'` on the document is **0** and the standing control `test_control_todays_impl_plan_dropped_the_six_stale_SKILL_pins` passes. **The full suite is green on main at `35698f9`: `2809 passed in 418.77s`, 0 failed** — v1.39's RED is closed. One Must-fix: the fence-opener residual the last revision added carries a tree-derived absence claim that is **false**, and a fixture-coverage claim that is also false, in opposite directions.
Evidence: 26 files opened, 58 greps/sweeps/commands run.

## Must-fix
- The fence-opener residual added at v1.40 (impl-plan `:445`–`:448`) makes two tree claims and **both are wrong at `35698f9`**, in opposite directions — this is a count/absence claim published without the command or the sha that produced it, the exact hazard the same revision's other residuals are careful about. (i) *"no corpus instance exercises either arm"* is **false for the 1–3-space arm**: over the tracked corpus this document defines (`git ls-files -- h-mad handoff`, `*.md`, `archive/` excluded — **30** files), a fence-state-aware scan finds **29 top-level fence openers indented 1–3 spaces**, in **4** files, `h-mad/SKILL.md` among them — which is the very file `_gate_block()` scans via `dbe.extract(SKILL_MD, "## Second surface — the codex leg")`. Command run at `35698f9`, opener regex `^(?P<ind> {0,3})(?P<m>`{3,}|~{3,})(?P<info>[^`]*)$` with a closer-matched fence toggle; first hits `h-mad/SKILL.md:32`, `:153`, `:158`, `:168`, `:174`, `:291`, `:442` (2 spaces), `:2061`. The tab arm is separately **0**. (ii) *"so both are pinned by fixtures and by nothing else"* is **false for the tab arm**: no AC in this document prescribes a tab-indented fence opener. I swept every line of the non-Version-History body containing a `\t` escape — `:403`, `:417`, `:434`, `:435`, `:444`, `:446`, `:844`, `:952`, `:954`, `:956`, `:987`, `:1004`, `:1010` — and each is on the **heading** axis. AC-1.6's indented-fence fixtures are `    ```bash hmad:exec` (four spaces, `test_indented_literal_tag_is_not_a_candidate`) and openers at 1, 2 and 3 spaces (`test_indented_fence_body_is_deindented`); the 4-column-by-tab reject arm is in neither. So the space arm is pinned by a fixture **and** by 29 corpus instances, and the tab arm is pinned by nothing at all — the exact `line.lstrip()` hazard the same revision names and fixes on the heading axis is left open on the fence axis. **Prescription**: add `\t```bash hmad:exec` beside the four-space case in `test_indented_literal_tag_is_not_a_candidate`, state that no mutation row follows (parallel to the heading axis; 81 stands, the axis is the design's to add), and replace the absence sentence with the measured corpus figure carrying its command and the sha it was measured at. **Class, and it is open-ended**: *a tree-derived absence or cardinality claim written without the runnable command and the commit it was measured at.* The rule: every "no corpus instance …" / "no guard … covers any of them" sentence in this document carries the command and the sha, as the AC-1.5/1.7 tab-closing-run residual and the `*.sh` three-backtick sweep already do. **Residual on my own sweep**: I checked this sentence and two siblings (`:221` "no guard in this repository covers any of them" — I probed it and found no test in `h-mad/tests`, `handoff/tests` or `handoff/scripts` reading any of the six bodies' source, so it holds but is unsourced; `:246` `grep -rln '```' --include='*.sh' h-mad handoff` — sourced and reproduces). I did not audit every absence sentence in the 2302 lines.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `and **no corpus instance exercises either arm**, so both are`
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `pinned by fixtures and by nothing else. That predicate is implemented **once**, in `_fence_events`, which reports such a`

- The Conventions recurrence ledger now contradicts itself in three places, and the contradiction was introduced by v1.40 — the governing rule for every sibling citation in the document is the one place a reader cannot be left guessing. `:282` still says the class has recurred **four** times and enumerates exactly four instances (v1.24's two quoting flags, the AC-6.1 restriction flag, the 5f clearance on a drifted `spec :458`, the 5f debt). `:313`, added at v1.40, declares the audit-v37 locator breakage **the FIFTH recurrence of this class**, without extending the list at `:282` or incrementing its count. `:363`, unchanged, still tells the reader that **a fifth recurrence is prevented by the sentence not being written** — i.e. that the fifth has not happened. A 5d reader or the next reviser cannot tell from this document whether the count stands at four or five, which is precisely the "ordinal that has silently drifted" failure this feature has already paid for twice (`_close_stream`'s "sixth", `_final_write`'s stale "fifth"). **Prescription — pick one, not both**: either extend `:282`'s list to five members and move `:363` to "a sixth recurrence", or reword `:313` so it does not count a form-(a) locator breakage as a recurrence of the prose-agreement class, since `:359`–`:363` explicitly separates the two (form (a) *has* a detector; prose agreement has none). Prefer a content predicate over the ordinal in both places, per the rule the document already applies to `_close_stream`.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `the FIFTH recurrence of this class and the first one *inside* the form (a) prescribes as safe).`
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `recurred four times and every recurrence cost a full dual-surface audit cycle: v1.24's two`
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `assertions about it, so a fifth recurrence is prevented by the sentence not being written, not`

## Should-fix
- Residual (b)'s AST sweep is shipped as a class closure with a positive control and **no negative control** — what `:266` labels "the negative" is a demonstrated **false negative**, not a true negative, and the same paragraph says so at `:240`. `_braces_outside_fences` is a genuine member of the class that the sweep fails to print; a screen not printing a known member is evidence of *incompleteness*, not of discrimination. Nothing in the paragraph shows the sweep declining to print a non-member — e.g. a body holding a three-backtick literal and an unrelated variable whose name contains `fenced` (a `fenced_output`, a `fenced=True` kwarg). The count itself is not wrong: I re-ran the published sweep at `35698f9`, it prints **7**, and I opened all seven and confirmed each is a genuine fence-state scanner with the stated toggle lines (`h_mad_assemble_tdd.py:114`/`:118`, `h_mad_precheck_doc.py:301`/`:304`, `h_mad_version_history.py:94`/`:98`, `test_h_mad_context_budget_docs.py:48`/`:51`, `test_h_mad_hook_wiring.py:293`/`:296`, `test_h_mad_pane_visible_dispatch_docs.py:50`/`:53`, plus `docsections.py:31`). It is the **control claim** that is wrong, not the number. Prescription: rename `_braces_outside_fences` and `_fenced_blocks` "known false negatives" (which is how `:240` already describes them) and either run a true negative control or say plainly that the reject direction of this screen is unmeasured.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `the one in-scope body — and does not print `_braces_outside_fences`, the negative named above.)`
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `Both are absent from the 7 and both are genuine members of the class — which is why residual (b)`

- The AC-1.5/1.7 bullet's closing sentence is now orphaned by the v1.40 insertion. The bullet used to end its residual with the corpus measurement immediately followed by "…so no live document or fixture outside this test depends on the tab arm — shipping it space-only would be a silent divergence…". v1.40 inserted the whole GNU-vs-BSD toolchain paragraph between the measurement and that conclusion, and the conclusion now begins a new sentence at the end of a paragraph about `grep -P`, where "the tab arm" has no nearby antecedent. Nothing is factually wrong (I re-ran the replacement one-liner: it prints `30 0` at `35698f9`, and `/usr/bin/grep -cP` does exit 2 with `grep: invalid option -- P`, measured). Prescription: move the conclusion back adjacent to the `30 0` measurement, or re-name its subject.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `No live document or fixture outside this test depends on the tab arm — shipping it space-only would be a silent divergence from the renderer the scanner grammar was oracled against`

## Nit
- `:298` lists `test_h_mad_collect_report_docs.py:270` among the tree pins that "stay admissible" because "each carries the **symbol name** beside it", and at that site it carries none — the symbol list that follows (`_TIMEOUT_CMD`, `_SCANNED`, `_ABSENCE_CLAIMS`, `_titled_section`, `_section`, `section_text`, `_second_surface`) covers the other two pins in the same sentence but not this one. `:270` is `    blocks = re.findall(r"```bash\n(.*?)```", section, re.S)` — a line inside a body, not a definition. Give it the enclosing symbol (`_gate_bash_block`, defined at `h-mad/tests/test_h_mad_collect_report_docs.py:267`) or drop it from the example list.
- `:139` scopes the single-source invariant to "the two files this feature owns — `h-mad/scripts/h_mad_doc_block_exec.py` and `h-mad/tests/docsections.py`". Task 1 itself describes `docsections.py` as "(modified)", not new; "owns" reads as authorship where "touches" is meant. One word.
- The document states several tree-derived counts as English words (`six` pre-existing bodies at `:220`, `three of them are production code` at `:221`, `two` lines from the GNU/BSD sweep at `:954`) beside others as digits. Every one of them re-derives correctly at `35698f9`, but a digits-only staleness sweep would skip the word forms. Preferring digits for tree-derived counts costs nothing.
