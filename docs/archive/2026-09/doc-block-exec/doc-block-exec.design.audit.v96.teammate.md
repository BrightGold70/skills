## Summary

Gating pass over `docs/02-design/features/doc-block-exec.design.md` at v1.108, freeze `09e9307` (the working file is byte-identical to the `09e9307` blob `0d0062f`). I re-ran rather than read every figure the orchestrator flagged plus most others the document publishes — the five-hop series, the candidate-sweep blob ladder, the stamp census, the round-deictic census and its 12/4/1/10 partition, the numstat, the value sweep over the 63/11 diff, the moving-ref control and its 13-token `HEAD` split, both span-ordinal arms at three corpora, all three sha-description arms, both line-pin screens, the seam-ordinal screen stripped and unstripped, the compliance walks, the eleven-key triage arm, the seam-set enumeration across all three documents, the tracked-corpus heading differential, the Setext census with all three controls, the `SKILL.md` span and block ordinals, and the cross-document `pgid=` / set-equality / AC-identifier readings — and **every published figure I ran reproduced exactly; I found no wrong number anywhere in this document.** The two must-fixes are both rule-versus-application defects in v1.108's own new paragraphs: an exemption invoked over roughly three times the sites its stated condition licenses, and a new cross-document claim carrying neither of the two readings this document requires of exactly that class.

Not checked, named so their absence is not read as a clean: `test_suite_floor_holds`'s `2748` baseline at `e8eaf6f` (I did not run `pytest --collect-only`); the eleven-shape markdown-it-py proxy render and the four-backtick `titled_section` differential; the `umask`/`mkdtemp`, `getpgid`-versus-`killpg`, five-kinds `os.open`, `argparse` `exit_on_error` and `_field` per-member probes (I did verify the ten `python` fences in the head are byte-identical at `8c6539a`, `b3be433` and the shipped file, which is the ground the document gives for carrying those readings, but I did not re-execute them); the plan's §Measurements beyond the two `grep -cF` locators and two `grep -c` counts this document pins; and the impl-plan's body outside its `DETAIL_KEYS` tuple. I am one surface sharing a model family with this document's author and have never been scored against a labelled corpus; codex owes a round on the same tree before anything here is treated as settled.

Evidence: 21 files opened (plus the 30-file tracked corpus read twice by the shipped differential and census harnesses, and ~16 design blobs read through `git show`), 96 greps/commands run.

## Must-fix

- The entry-bump exemption is invoked over **18 stamp sites while its own stated condition reaches at most 5**, and the "one pass" clause offered as the general justification is false for the other 11 — so the rule v1.108 amended "at its site" in order to answer round fifteen's MUST 3 reproduces that must one level down: a rule published and then applied wider than it licenses, with no reconciliation at the site that states it. Measured on the shipped file: the head carries 16 lines bearing the stamp phrase (18 folded, two wrap-split, all reading v1.106). The condition names exactly two cases — a stamp on a line another paragraph's differential attributes to a named revision, and a stamp in a paragraph whose round-deictics resolve to an earlier revision than the stamp would name. Walking the 16 against this document's own two registers (the `700c599`→shipped-file differential's two changed lines, and the ten attributional deictics the v1.108 entry enumerates), with paragraphs cut on blank lines: **5 are covered** — three in §Test Strategy's fold-ladder paragraph, one in its needle-literal paragraph, one on the `docsections.json` differential line in §Test Plan — and **11 are not**: head lines 922, 988, 1036, 1082, 1533, 1546, 2395, 2752, 2856, 2939 and 3486, each sitting in a paragraph whose only round-deictic is the *self-resolving* form, which by the register's own definition "resolves at every revision" and therefore never resolves to an earlier one. For those 11, re-stamping to v1.108 alone falsifies nothing published anywhere in the document — the appendix already states every one of the 18 figures was re-run and reproduces — so the clause that is the only bridge from the condition to the claim does not hold of them. Prescription: either widen the condition to name a third case (a stamp atomically bound to a blocked member, with the binding stated), or narrow the "same eighteen sites" claim to the members the condition reaches and re-stamp the other 11 to v1.108 in this pass.
  quote: docs/02-design/features/doc-block-exec.design.md › `a stamp may lag the shipped version when re-stamping it *in isolation* would`
  quote: docs/02-design/features/doc-block-exec.design.md › `because moving any one alone is what makes the others wrong.`
  quote: docs/02-design/features/doc-block-exec.design.md › `The v1.107 and v1.108 entries both take this exemption over the same eighteen sites and both`

- §API / Interface Changes' new `DETAIL_KEYS` bound sentence asserts a **present-tense claim about the spec's bytes with neither a blob reading nor a working-file re-check**, at the site where this document states that exact rule and three sentences above the paragraph that names the class. This is the same shape as the debt v1.106 filed and v1.108 discharges in the very next paragraph — that one carries both readings (`git show 8c6539a:"$P" | grep -c …` → 1, working plan → 1, both of which I reproduced); this one carries none, so nothing in the document distinguishes it from the `b3be433`-stamped claim the same paragraph says "was already false the moment it was filed". I verified the claim is currently **true** — the working spec's `DETAIL_KEYS` paragraph does state the bound ("it becomes a *runnable* authority only when Phases 5d/5e build that module, and until then AC-4.5's walk is a specified test, not a passing one") — so no figure is wrong; the defect is that the reading is unrecorded and therefore not re-derivable, which is precisely what the rule exists to prevent. Prescription: attach the pair this document already uses elsewhere — a blob reading at a named sha and a working-spec re-check, both taken after this revision's entry lands.
  quote: docs/02-design/features/doc-block-exec.design.md › `this agree with the spec rather than contradict it**: the spec states the same residual against the`
  quote: docs/02-design/features/doc-block-exec.design.md › `document makes about a sibling's bytes; the rule is that each carries a blob reading **and** a`

## Should-fix

- One of the ten attributional-deictic register members is stamped at a sha where that deictic attributed a **different act**, and acting on it in the promised one-pass rewrite would produce a sentence contradicting both its own first clause and a sibling site. I re-derived first-present over every commit touching the file for all ten: nine reproduce exactly (zero-decomposition → `f91a74b`; provenance-probe vacuity label → `1cbddb7`; ensure_ascii repair → `8c6539a`; mutation-corollary → `f91a74b`; fold-ladder-gap → `00b961f`; struck-in → `00b961f`; what-changes → `f91a74b`; run-after-the-edits → `7982c18`; shape-struck → `00b961f`). The tenth does not: the clause *about the provenance probe* — "the provenance probe carried a command and a sha and no label until this revision" — is first present at `1cbddb7` (v1.102), not `06ef40f`. At `06ef40f` the sentence read "The `$STRIP` bound is the seventh: it carried its reason in prose and no label until this revision", so the deictic fragment is indeed at `06ef40f` but its subject there was `$STRIP` — which the same sentence today already hexes as v1.100. Rewriting the probe clause to v1.100 would yield "the `$STRIP` bound … until v1.100; …; the provenance probe … until v1.100", contradicting the `35698f9`-provenance-probe site four hundred lines earlier, which labels that same act v1.102. The entry's residual ("each version above is the version the phrase was WRITTEN at") describes the bound but does not name this live consequence. Prescription: restamp that member `1cbddb7` (v1.102), or state at the register that a member whose fragment predates its current subject carries the fragment sha and the subject sha as a pair.
  quote: docs/02-design/features/doc-block-exec.design.md › `Scanning's no-label-until sentence on the same probe at 06ef40f (v1.100)`

- The v1.108 appendix's value sweep is **stated as universal over this document's needles and is not**: it enumerates thirteen and omits the seam-ordinal screen, which is the document's flagship `# expect 0` detector and the one whose two published readings the body treats as load-bearing. I ran the enumerated thirteen over the 63 added and 11 removed lines and reproduce the published result exactly (nothing outside §Version History in either direction; 2 added and 1 removed entry line inside it), and separately ran the seam-ordinal screen over the shipped head: stripped `0`, unstripped `6`, both as published. So no figure is wrong; what is wrong is a universal quantifier followed by a list that does not satisfy it, with the legitimate reason for the omission nowhere stated — the screen is paragraph-scoped through `$STRIP`/`$FOLD` and cannot coherently be run over a line-diff at all. That reason is worth stating rather than the omission being repaired, because a reader who tries to satisfy the sentence as written will attempt an incoherent run. Prescription: scope the claim to line-scoped needles and name the paragraph-scoped screens as re-run separately, which they in fact are for the stamp and round-deictic censuses.
  quote: docs/02-design/features/doc-block-exec.design.md › `every needle this document ships returns nothing in BOTH directions OUTSIDE Version History`

- Instance of the class in Must-fix 2: the v1.108 entry's OWED-ELSEWHERE says the spec "currently says the registry walk without the bound" while the body says the spec "closes it at the far end of the bound" — both unstamped, both about the same sibling paragraph, and read together they present as a contradiction inside one revision. Reading the working spec resolves it (the paragraph as a whole carries the bound; the single residual sentence naming the registry walk does not), but that resolution is available only by opening the spec, which is the cost the two-reading rule exists to remove. Prescription: name which sentence of the spec each statement is about, and give each the blob-plus-working pair.
  quote: docs/02-design/features/doc-block-exec.design.md › `the spec is the document that should say which check closes the staleness AFTER 5e, and it currently says the registry walk without the bound`

## Nit

- §Scanning's GRAMMAR exemption locates the colliding shell variable in "§Test Strategy's mutation-range demonstration", and it is not there. Walking the 13 `HEAD` tokens in the head confirms the 9/4 split the entry publishes, but the four variable tokens sit at head lines 2484 and 2487 (the six-gram carry screen's fence, carrying `$BASE..$HEAD` and `"$HEAD"`) and at 2519 and 2647 (that screen's two invocations, `HEAD=6f0ee85`); the mutation-range demonstration is the separate `$MB`/`$MH` fence and table beginning at head line 2555 and contains no `HEAD` token at all. The exemption itself holds — the `..` range at 2484 is spelled against the variable and not the ref, exactly as the entry says — only the locator is one fence off.
  quote: docs/02-design/features/doc-block-exec.design.md › `the variable is bound to a fixed sha in §Test Strategy's mutation-range demonstration`
