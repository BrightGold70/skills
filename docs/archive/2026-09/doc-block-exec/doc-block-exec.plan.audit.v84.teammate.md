## Summary

What I read: the assembled prompt's output contract, role block, rubric, Axis C instructions and the second half of `invariants.base.md` in full; the plan body in full plus Version History v1.87–v1.98 (I did not read VH v1.0–v1.86); the spec by FR heading plus 22 targeted AC greps and four AC bodies read in full; the design and impl-plan by targeted locator. I diffed the inlined plan against the working tree: it is byte-identical to `docs/01-plan/features/doc-block-exec.plan.md` at HEAD `700c599` (plan v1.98), so the prompt's subject premise holds. I then re-ran against the real tree every provenance figure v1.98 added or moved — both interval closures, the `70`/`60` self-counts, the `32`/`21`/`11` *the freeze* triple, all nine members-table cells and the two exception cells, the ten-member `docs/`-scoped closure list (re-run at `1cbddb7` **and** again at HEAD `700c599`, where all ten still hold), the codex `72`/teammate `83` ledger, the eight carve-out rows and the landing-commit derivation — and **every one reproduced exactly as published**; the `h-mad`-scoped figures likewise (block census `7`/gate `[4]`/exec-codex `[2]`, fences `73`/`88`, importers `3`, `_gate_bash_block()` `def`+`3`, `returncode` `0`, `13`/`88`/`48`, `37` scripts, `2` parametrisations, `49` AC anchors, `81` design matrix rows, `29` `__all__` names, collect `2809`). The two Must-fixes are one class: a behavioural premise about the language runtime asserted rather than executed — and the first is materially **false**, propagated from the design, whose published "measured:" evidence for it is a composite control that cannot support its own per-member conclusion.

Axis C — FR reconciliation (plan audit, FR granularity):

| FR | spec heading | classification |
|---|---|---|
| FR-1 | Address a block by document, heading, and explicit tag | `implemented-as-written` |
| FR-2 | Substitute an explicit map, and refuse a substitution that would not apply | `implemented-as-written` |
| FR-3 | Execute in a disposable cwd under a declared shell mode | `implemented-as-written` |
| FR-4 | Verdict-token CLI following the established gate contract | `implemented-as-written` |
| FR-5 | Bounded execution without an external time-bounder | `implemented-as-written` |
| FR-6 | Migrate the existing inline harness onto the helper | `implemented-as-written` |

`grep -n '^### FR-' docs/01-plan/features/doc-block-exec.spec.md` returns exactly these six headings and the plan's §Requirements restates each word for word; each is named in §Goals and mapped in §Deliverables. Every one of the 22 distinct `AC-N.M` identifiers the plan body cites is defined in the spec — `comm -23` of the two sorted lists is empty — so nothing is `restated` or `absent` and no Must-fix is raised on this axis.

Evidence: 11 files opened, 159 greps run.

## Must-fix

- The `_field` escaper's stated rationale is FALSE for most of the set it names, and the design's published measurement for it is a composite control that cannot see the failure. The plan attaches the conjunction "which `json.dumps` leaves literal **and** `splitlines()` breaks on" to the whole list (DEL, the C1 range with U+0085, U+2028/U+2029). Executed on `python3.11` 3.11.8 over all 35 remaining `Cc`/`Zl`/`Zp` code points: `json.dumps(ch, ensure_ascii=False)` leaves **35 of 35** literal, but `('a'+ch+'b')` after `json.dumps` splits into more than one line for only **3 of the 35** — U+0085, U+2028, U+2029. DEL splits into **1** line; of the 32 C1 code points U+0080–U+009F, **1** (NEL) breaks and 31 do not. **This is shared with the design and the design owns the origin**, so fixing the plan alone leaves the defect standing: `docs/02-design/features/doc-block-exec.design.md` writes the same per-member claim and cites a measurement for it — "leaves DEL, the C1 range (U+0085 NEL included) and U+2028/U+2029 literal, every one of which `str.splitlines()` treats as a line boundary (measured: a heading carrying NEL, LS, PS and DEL splits into four lines after `json.dumps` alone — design audit v73)". I re-ran that exact fixture: it **does** return **4** lines, so the measurement is real — but four lines is what *three* breaking characters produce, and DEL contributes nothing to it. The composite fixture is consistent with DEL and 31 of 32 C1 members being inert, which is the healthy-branch-covers-a-sick-one failure DECISION O exists for, committed inside a `measured:` citation. Prescription, both documents: split the set — U+0085/U+2028/U+2029 are the members `splitlines()` breaks on and are what the "one *physical* line" transport invariant (AC-4.3) turns on; DEL and the rest of C1 are escaped because they are unrenderable inside a verdict line, a different and separately-stated reason — and replace the composite fixture with a per-member run, `json.dumps('a'+ch+'b', ensure_ascii=False).splitlines()` one code point at a time, published with its output. The guard itself is unaffected: `test_unicode_line_separators_cannot_split_a_verdict_line` drives LS and PS as well as DEL, so `c1-escape-removed` is still killed.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `remaining `Cc`/`Zl`/`Zp` character — DEL, the C1 range with U+0085, U+2028/U+2029 — which`
  quote: docs/01-plan/features/doc-block-exec.plan.md › ``json.dumps` leaves literal and `splitlines()` breaks on; everything else verbatim`
  quote: docs/02-design/features/doc-block-exec.design.md › `every one of which `str.splitlines()` treats as a line boundary`

- The `titled_section` end-of-match premise — the sole basis for dropping the leading newline, and hence for the whole "no existing test pins the old behaviour" argument — is stated as fact with a *locator* command and no *behaviour* command or output. `grep -n 'P<marks>' h-mad/tests/docsections.py` → **1** proves the regex is there; it says nothing about where `\s*$` ends its match, which is what the paragraph turns on. Base invariant "Behavioural premises carry their command" ("X currently does Y" MUST carry the exact command that produced it, inline and runnable, beside the observed output) is not satisfied by a locator. **The conclusion survives and the fix is provenance only — one edit, not a cycle**: I executed it, and `re.search(r"(?m)^(?P<marks>#+) H\s*$", …)` gives `match.end()=4` on `"## H\nBody\n"` (remainder `'\nBody\n'` — the match stops before the heading line's newline) and `match.end()=5` on `"## H\n\nBody\n"` (the two selectors agree), which is exactly what the sentence claims. Prescription: publish that three-line probe with its output, the way §Measurements publishes the fence-body de-indentation probe that closed the identical gap in v1.95.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `ends its match *before* the heading line's`
  quote: docs/01-plan/features/doc-block-exec.plan.md › `newline whenever a non-blank line follows immediately (with a blank line after the heading the two`
  CLOSE THE CLASS, do not repair these two members. The class is *a premise about language-, library- or OS-level behaviour, written in the body as settled fact, carrying no executed command and no recorded output*. The document already discharges it for every premise whose subject is a named probe — argparse `exit_on_error`, `killpg` on a zombie group, the reader-less FIFO's `ENXIO`, `rmtree` on `0o000`, the markdown-it-py corpus, the fence-body de-indentation case — each with a fenced command, its printed output and a carve-out row. What it does not reach is the premise whose subject is a stdlib call *inside a prose sentence*: the two above, and these further members I read but did not execute and therefore name only as the class's residual rather than as findings — "a buffered `TextIOWrapper` may defer the OS write until `flush()`/`close()`", "`(st_dev, st_ino)` of the two reserved handles compared … so a hard link is caught as well as a symlink", and "unpacking the call directly raises `TypeError` on absence". The rule that closes it: **a sentence naming a stdlib or OS call and asserting what it does is a probe, and takes a probe's treatment — a fenced command, its output, and its version-or-sha stamp — or it is written as `unexecuted`.** Residual, stated because no screen reaches it: the carve-out sweep is driven off *stamps*, so a premise carrying no stamp at all is invisible to it for the same structural reason the calendar-date member was; a fourth driver branch keyed on stdlib symbol names would help, and the honest position is that this class is swept by reading.

## Should-fix

- The sentence that proves the `_run_recipe` hoist "leaves no unbound name" misdescribes the two names it turns on. `collector` and `gate` are **not** locals of the nested `run_recipe`; they are computed in the enclosing test at `h-mad/tests/test_h_mad_collect_report_docs.py:306` and `:307` (`collector = SCRIPT_DIR / "h_mad_collect_report.py"`, `gate = SCRIPT_DIR / "h_mad_audit_gate.py"`, inside `def test_documented_gate_recipe_halts_instead_of_gating_an_empty_path`, which begins at `:294`) and `run_recipe` (defined at `:309`) closes over them. Being *closure* variables rather than locals is precisely why hoisting to module level would leave them unbound, so the sentence understates its own argument. The prescription is right either way; only the description is wrong. Read with `grep -n '^def ' h-mad/tests/test_h_mad_collect_report_docs.py` against `sed -n '294,330p'` on the same file.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `the locals the nested `run_recipe` computes`

- The quantified assertion-shape claim is a hand reading and is not labelled as one, in a document that labels its other hand readings. "**every** `titled_section`/`section_from` assertion in `h-mad/tests/test_docsections.py` is `in`, `not in` or `pytest.raises`" is supported only by two *locator* counts (`grep -c '^def test_'` → 6 and `grep -c 'titled_section(\|section_from('` → 6); neither reads an assertion. I walked all six functions and the claim **holds** — `test_a_fenced_comment_does_not_end_the_section` (`in`, `in`), `test_a_section_owns_its_subsections` (`in`), `test_the_section_stops_at_the_next_same_level_heading` (`not in`), `test_a_missing_heading_fails_loudly` (`pytest.raises`), `test_section_from_bounds_an_offset_anchored_pin` (`in`/`in`, `not in`), `test_the_live_phase5_section_extends_past_its_fenced_blocks` (`in`, `in`) — so this is provenance, not correctness. Prescription: say "hand reading of six functions", the way the members table's row-2 column-2 cell says "a hand reading of that body's eight-row table and no `grep` at all".
  quote: docs/01-plan/features/doc-block-exec.plan.md › `or `pytest.raises`, and none compares exact bytes. The claim is quantified over *all* of them`

- The `--help` carve-out's "three surfaces" claim is a population claim over prose with no published screen, in the same paragraph that prescribes sweeping it by claim. I ran the sweep by hand — `awk '/^## Version History/{exit}{print NR": "$0}' <doc> | grep -nE 'exactly one .*verdict|one \*physical\* line|prints exactly one|verdict line for'` — and it returns the three surfaces named and nothing else, and each of the three does carry the carve-out, so the claim **holds at this body**. But a fourth surface added in a later revision is reachable by neither of this document's two screens, which is the shape §Measurements elsewhere writes a rule against. Prescription: publish that grep beside the claim as a starting-point screen with its own residual, exactly as §Measurements does for the `.py:NNN` shape screen.
  quote: docs/01-plan/features/doc-block-exec.plan.md › `**The carve-out has three surfaces in this plan, and the sweep is by claim rather than by phrase**`

## Nit

- The plan reproduces `titled_section`'s regex as `(?m)^(?P<marks>#+) {heading}\s*$` where the source at `h-mad/tests/docsections.py:53` reads `rf"(?m)^(?P<marks>#+) {re.escape(heading)}\s*$"`. The dropped `re.escape` changes nothing in the argument, but this document reproduces source spans verbatim everywhere else and the omission is the one thing that makes the span unfindable by a literal grep.
