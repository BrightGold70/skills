## Summary

Delta review (ADVISORY, not a gate) of `git show 00b961f -- docs/01-plan/features/doc-block-exec.impl-plan.md`, v1.49 → v1.50, read hunk by hunk against the working tree at HEAD `dfae038` (verified byte-identical to `00b961f` for the four documents). I re-executed the readings the diff touches rather than reading them: marker screen (body 5 / file 5 / five per-marker lines each 1), restated-cardinal screen on both `b3be433` and the tree (0/0, 2 lines / 3 occurrences), the four `.py:` screens (49/49/0/0 over 20 folded paragraphs), both `SKILL` screens (0/0), the self-reference screen (0), the pre-dispatch sibling sweep in both its four- and five-needle forms (26/26) with its full 19/5/2 classification re-derived line by line, the prose-line-pin plural sweep (1, the `declines 2` confound), the sha-stamp screen (**118** readings over **17** shas, every per-sha count matching the published table), and **all fourteen sibling locators run against all three siblings at `b3be433`** — every one reproduced its published value, including the 10 design / 2 plan / 2 spec split and both one-hit caveats. Three findings survive, and all three are prose whose referent is wrong rather than an integer: one is fix-introduced by this very diff, one is the class the diff claims to have closed surviving inside the sentence that closes it, and one is a universal the diff restated without re-deriving.

Evidence: 9 files opened, 68 greps run.

## Must-fix

- **Fix-introduced by this diff: the new `_SCANNED` glob sentence at `:2407` publishes a wrong count — four of the eight sources are globs, not three.** `h-mad/tests/test_h_mad_portable_timeout.py:153`–`:162` builds `_SCANNED` from eight entries, of which `:158` `*sorted((SKILL / "references").glob("*.md"))`, `:159` `*sorted((SKILL / "scripts").glob("*.sh"))`, `:160` `*sorted((SKILL / "scripts").glob("*.py"))` and `:161` `*sorted((SKILL / "hooks").glob("*.sh"))` are all globs. The document's own list one line above (`:2403`–`:2404`) names those same four glob forms, so the sentence contradicts the list it sits beside. It is the first wrong published figure in a round whose thesis is that the unexecuted-number class is drained, and it under-states the exposure the paragraph exists to state — the example given reaches only two of the four glob sources. **Close the class, not the instance**: the repair is not `three` → `four` but a rule over the set — *every `_SCANNED` source spelled with `.glob(` is a tree read, so membership is re-derived at 5d and no count of them is published here* — because the next `.glob(` entry a reviser adds moves the cardinal again and nothing here detects it. The same wrong figure is repeated verbatim in the v1.50 Version History entry (`:3315`, item `(6) SHOULD`, "Three of the eight sources are globs"); this revision bracket-corrects v1.49's entry in two places for exactly this reason, so the same treatment is owed to its own.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `(impl-plan audit v45): three of the eight sources are globs, and a later `references/*.md` or`

- **The class the third must claims to close survives inside the sentence that closes it: `§Scanning` at `:1770` is a sibling location written as prose, and it is false both on the shipping tree and at the revision it cites.** The repair removed `line 43` and kept `§Scanning`, then declared at `:1771`–`:1773` that "The design's location for that spelling is written as a LOCATOR and never as a number" — a section name is a location and is not a locator. Measured at both ends: on the shipping tree `grep -Fn` for `sys.path.insert(0, str(Path(__file__).resolve().parents[1] / "scripts"))` in `docs/02-design/features/doc-block-exec.design.md` returns exactly `64:`, and that file's headings are `## Overview` at `:11`, `## Architecture Overview` at `:77`, `### Scanning (`extract`)` at `:184` — so `:64` sits in **Overview**, not in Scanning. At the cited revision it was never in Scanning either: design v1.79 is `dadf546`, where the same needle returns exactly `43:` against headings `## Overview` `:11`, `## Architecture Overview` `:56`, `### Scanning (`extract`)` `:163`, and `sed -n '163,304p'` of that blob contains **0** occurrences of `sys.path.insert`. So the `§Scanning` half is not stale drift — it was wrong when written and is wrong now, and it is invisible to the fifth needle the same repair adds (`grep -cE 'line [0-9]'` over the body is **0**, and `§Scanning` matches nothing in that needle set). **The class**: a sibling location written as a *section reference* is the same defect as one written as a line number, and the residual at `:1778`–`:1780` enumerates only *at line N of* and *the Nth line* as the further synonyms — it does not name the section-reference form, which has a live member in the sentence the residual is attached to. The rule that closes it: a sibling location is admissible only as a needle that returns exactly one hit in its stated target at the shipping commit; a `§`-reference is admissible only if it is *also* re-derived at that commit, and here it is not.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `# ^ the design's spelling verbatim (design v1.79 §Scanning) and the same idiom every`

- **The rewritten member-(vii) disposition at `:478` publishes a false universal: `plan.md:` is not present in the body it counts, at any sha in the chain.** `awk '/^## Version History/{exit}{print}' <this file> | grep -c 'plan\.md:'` returns **0** at `700c599`, at `8c6539a`, at `b3be433` and on the shipped tree, against **1** each for `design\.md:` and `spec\.md:` at all four. The widest reading does not rescue it: whole-file `grep -c 'plan\.md:'` is **1**, and that single hit is inside the Version History, which the sweep's own bullet instructs the reviser to read outside — so the needle is absent from the counted scope under either reading. The document contradicts itself two paragraphs later, where the classification of the 26 hits allots exactly "**2** write a sibling-filename-plus-colon needle", and the v1.50 Version History entry publishes the split as "`design.md:` 1 and `spec.md:` 1" — three sibling-filename forms cannot all be present when only two hits exist across two distinct forms. This is not fix-introduced (the four-needle wording carried the same universal since v1.48), but the hunk **rewrote this exact clause** to widen it from four needles to five and restated "every one of them present" without re-deriving it — which is the class the previous two rounds' musts were both filed under, and the class this round writes its own rule over at `:530`–`:533` ("a bin's defining predicate is re-derived by running the walk's own signatures against each enumerated member"). Prescription: state presence per needle from a run (`design.md:` 1, `spec.md:` 1, `owed` 24 lines, `plan.md:` 0, the character class 0 by construction), or drop the universal; a presence/absence claim is a measurement under decision G exactly as a count is.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `class v1.50 adds — the first four are every one of them literal and every one of them present`

## Should-fix

- The new Task 3 comment carries an **unstamped present-tense sibling-content claim** at `:1775`–`:1776`. The `b3be433` stamp at `:1773` attaches to the design clause; the plan clause has none, and the Conventions rule at `:1031`–`:1032` forbids "a claim in the **present tense** about what a sibling now contains". The reading itself is correct — I ran it, `git show b3be433:docs/01-plan/features/doc-block-exec.plan.md | grep -Fc` on the insert needle is **1**, and **1** on the working tree too — so the repair is to stamp the clause, not to withdraw it.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `#   carries the same line once, so the one-hit property holds under the stated target file — the`

- **The new glob sentence at `:2407`–`:2409` frames the exposure as prospective when this feature itself lands a live member of the glob it names.** Task 1 creates `h-mad/scripts/h_mad_doc_block_exec.py`, which is a `scripts/*.py` file and therefore a `_SCANNED` member the moment it lands — the design says so explicitly at `docs/02-design/features/doc-block-exec.design.md:3008`–`:3010`, naming the two node IDs it collects. The obligation is discharged elsewhere in this document, at `:2764` ("Task 1 therefore inherits two guards it did not have before"), so this is a framing defect and not a gap: the residual's example is "a **later** `references/*.md` or `scripts/*.py` file", which reads as *no current member*, in a paragraph whose preceding sentence says `h-mad/SKILL.md` is the only member the feature touches. Prescription: name the live member and point at `:2764`, so the glob residual and the guard obligation are readable from one place.
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `**Production file**: `h-mad/scripts/h_mad_doc_block_exec.py` (new) and `h-mad/tests/docsections.py` (modified)`

## Nit

- `:1354`'s repaired placement sentence reads "at both of the sites that USE them — here, in Task 1's wire description, and in the §Verification (Phase 5f) paragraph". The list is correct — I verified the four integers occur in the body only at `:1348`/`:1351` (inside `## Task 1`, whose heading is `:1143`) and at `:3213`, and that the 5f note at `:3216` names the same two sites — but "here, in Task 1's wire description" reads as two items on first pass, which makes a two-site claim look like three.

- `:2405`'s "the feature adds nothing under `references/`, `hooks/` or `scripts/*.sh`" mixes a directory form and a glob form in one list, and the omission of `scripts/*.py` from that list is load-bearing rather than stylistic (see the should-fix above); spelling the three exclusions uniformly and naming the one inclusion in the same breath would stop a reader taking the list as exhaustive.
