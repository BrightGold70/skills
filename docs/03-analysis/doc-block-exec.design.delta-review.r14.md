## Summary
Delta review of `git show 8c6539a -- docs/02-design/features/doc-block-exec.design.md` (v1.103 → v1.104), read as a diff and then re-executed against the tree at `8c6539a`, which is byte-identical to the working file for all three moved documents (`git diff --stat 8c6539a --` on the three paths is empty). **This pass is ADVISORY: it is not a gate, it stamps nothing, and a real codex round on this tree is still owed.** Every premise the orchestrator handed me held on re-derivation and I say so plainly, because two of the last three rounds refuted one: `git rev-list --reverse cf3a862..700c599 -- "$D"` returns exactly `7982c18 06ef40f f91a74b 1cbddb7 700c599`; the sweep re-derives to **36/42/50/53/61/63** at `cf3a862/7982c18/06ef40f/f91a74b/1cbddb7/700c599`, matching the shipped list character for character; design and plan **agree** on the `_field` partition (67 / 35 literal / 3 breakers / 32 for the second reason — design §Verdict lines vs plan §Scope L56–65 and §Measurements L2386–2418), with no divergence between the two independently-written repairs; all six output-bearing `python` fences in the head reproduce (bounder, `umask`, `argparse`, `_field` byte-identical; `getpgid` and the reaping control differing only in PIDs the text already calls output), which is exactly the `6 / 4 / 2` split the revision claims; the setsid content-predicate resolves (`grep -c 'os.setsid() descendant'` on the plan → `2` at `700c599` and `2` working, both quoted output lines `grep -cF` → `1`/`1`); the sha-description screen calibrates as stated (2 lines and 1 line at `700c599`, nothing on the working file); Invariant Compliance's two walks return `104` and `60` with `2` inside a fence and `58` prose; and the nine re-stamped document-self figures all hold (entry-naming screen → `9 after the v1.104 entry`; partition screen `36/12/8/0/16` with `grep -c parametrize` the only 2-anchor; stripped head `0`, unstripped head `6`, tail `8`; `^ {4,}` `0`, `~{3,}` `0`; `-F` needle `3`/`2`; `$P` head 14 tail 12, `$W` head 6 tail 6, all branches at 1 and both true negatives at 0; the ten-sha unstripped fold `2/1/1/2/2/3/3/6/6/6`). Two defects survive, both fix-introduced at the very site MUST 1 repaired, and both of the same shape MUST 1 named: a true measurement carried inside a description that the shipped bytes falsify.
Evidence: 8 files opened, 41 greps/scripts/probes run (including 8 python fences executed and 14 `git show`-scoped sweeps).

## Must-fix
- **The widened cardinality arm's published reading does not reproduce on any corpus, and two of its hits are this revision's own disposal prose.** Run verbatim: `awk '/^## Version History$/{exit} {print NR": "$0}' "$D" | grep -iE '\b(the|at|over|across|of) ([a-z]+|[0-9]+) (shas|commits|blobs)\b'` returns **four** lines on the working file — `941` and `942` (this paragraph quoting `"at ten shas"` and `"at two blobs"`), `2449` (the real "at ten shas") and `2475` (the real "at two blobs") — not three. On the `700c599` blob it returns three (`882`, `2245`, `2270`), but the third is `882`'s `` the four shas later than the stamped one ``, which *counts* shas; **no line on either corpus "names later shas as a set rather than counting them"**, so the clause disposing of the third hit has no referent at either candidate reading of "this file". This is DECISION Q on the document's own new text — a stated property of a screen read off what the screen is *for* rather than executed on the bytes shipped — and the mechanism is the one the v1.103 NIT rule already closed for literal needles: the disposal sentence writes the needle's own matches into the scope the needle counts, so naming the two real cardinalities doubled them. It matters because this arm is the stated *residual* of a screen MUST 1 shipped: a residual whose census is wrong understates what the shipped screen misses. **Instance of: the class of definite descriptions whose numbers are true and whose descriptions are not** — the same class MUST 1 closed for role-labels and cardinalities over shas, recursing onto the paragraph that closed it. Prescription, either arm: re-run on the shipped bytes and publish `4` with `941`/`942` named as the disposal prose's own self-matches (which also documents why an un-shipped arm cannot be held at *print nothing*), or de-literalise the two quoted cardinalities so the count returns to `2`; strike the "names later shas as a set" clause in both cases, since it describes nothing.
  quote: docs/02-design/features/doc-block-exec.design.md › `three lines of which one names later shas as a set rather than counting them — a description, not`
- **"this revision writes more labels again" is false on the bytes this revision ships, and it is the whole reason the working-file value is withheld.** Re-derived with the paragraph's own needle: `RAISE='[`]0[`]|expect[ ]0'; git show 700c599:"$D" | grep -cE "$RAISE"` → **63**, and `grep -cE "$RAISE" "$D"` → **63**; `diff` of the two raised-line sets shows a **single** changed line (`stands at the audited commit` → `per the sha-description rule under §Scanning`) and **zero** added or removed lines. So v1.104 wrote no new candidate lines at all: the withheld working-file value is `63`, identical to the last published one. The clause was true for v1.103 (`61` at `1cbddb7` → `63` at `700c599`) and was carried verbatim into a paragraph v1.104 rewrote — which is the mechanism the document's own rule at §Test Strategy forbids ("when a revision edits the *input* to a claim … the claim is re-run **after** the edit lands, never restated from the run that preceded it") and which the entry-bump re-stamping rule makes mandatory here, since eight neighbouring figures were re-stamped to v1.104 and this one was not re-run. It also makes the justification circular in the wrong direction: `63` does not match `` [`]0[`]|expect[ ]0 ``, so publishing the number could not have moved it. **Instance of: the same class as the Must above** — a correct measurement (the six blob readings, all of which reproduce) inside a description the bytes falsify. Prescription: publish `**63** on the working file, after the v1.104 entry`, beside the blob series, and keep blob-stamping as the standing rule stated on its own terms rather than on a per-revision claim that goes stale every round it is not re-run.
  quote: docs/02-design/features/doc-block-exec.design.md › `figure is stamped at a blob and deliberately not given for the working file: this revision writes`

## Should-fix
- The v1.104 entry's `OWED ELSEWHERE` says the plan owes the `_field` split, and the plan discharged it **in this same commit**. `git show --stat 8c6539a` shows `docs/01-plan/features/doc-block-exec.plan.md` moving in the same commit, and plan `v1.99` publishes the per-code-point run under §Measurements (L2386–2418, `Cc/Zl/Zp total 67 | left literal by json.dumps 35 | set-equal to DEL+C1+LS+PS: True`) with §Scope L56–65 stating the two reasons apart — I re-executed both plan fences and both reproduce byte-identically. The two repairs *do* agree numerically, which is the thing this round was most at risk of; what is stale is the debt notice. The plan's mirror sentence ("the design must split the same set by reason…") is stale in the same way and is the plan author's to fix, not this document's. Version History is a dated record here, so this is a Should and not a Must — but a reader of `OWED ELSEWHERE` at HEAD is told to open a debt that does not exist.
- The v1.104 entry ships **no DECISION K sweep**, where v1.102 and v1.103 each published one, and the entry's substitute ("every python fence in the head was extracted … and executed") covers only the 10 python fences — it does not cover the shell screens whose stamps this revision moved. That gap is exactly where the two Must-fixes above live: both are shell-scoped figures at re-stamped sites. I re-ran the sweep this entry omits and every member of it holds (values in the Summary), so the omission cost two findings and no more — but the omission is the reason they shipped.
- `**Both print nothing on the working file this revision ships**` for the sha-description screen carries no entry stamp, while the nine neighbouring document-self figures all carry `after the v1.104 entry`. It reproduces (`rc=1` on both arms, run after the entry), so this is a stamping-consistency gap and not a wrong figure; the entry-naming screen cannot see it because the phrase is absent, which is the residual that screen already states.

## Nit
- The prose disposing of the widened arm quotes its two matches with straight double quotes inside a paragraph the same arm is run over; if the Must-fix above is taken by de-literalising rather than by republishing `4`, the rule to state is the v1.103 NIT rule verbatim ("no screen's needle is written literally anywhere in the scope that screen counts") applied to *matches* as well as to needles — a quoted match is a needle for the purposes of that rule.
- `handoff/tests/test_skill_candidates_census.py` is now named by path and the "two tests under `handoff/tests/`" derivation reproduces (`grep -oE '[A-Za-z0-9_/.-]*test_[A-Za-z0-9_]+\.py'` over the head → `handoff/tests/test_skill_candidates_census.py` ×1 and `handoff/tests/test_mutation_specs_clean.py` ×2, both files present in `git ls-files`). Two further tests are still cited bare in the head — `test_h_mad_review_evidence.py` and `test_h_mad_wire_registry.py`, both resolving to `h-mad/tests/` — so the should-fix's own rule ("name the file by path") has two live members left inside the same document.
