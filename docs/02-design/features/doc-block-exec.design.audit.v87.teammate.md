## Summary

Read the `74e126f..35698f9` delta in full, then swept the whole document, and re-ran every command it publishes at the freeze sha `35698f9` rather than reading its stated outputs. Most of the delta holds up under re-derivation — the scoped sha-equality diff, the Setext census and both its controls, the heading differential (byte-exact), the eleven-shape ATX proxy, the `_second_surface` ast set (8), the block census (`lines 159 blocks 7 gating 1`, gate ordinal 4 / exec-codex 2 in both spans), the tagging simulation (7/1 → 6/0), the line-pin detector and its blind-form sweep, the three `parametrize` standing counts (2/0/0), the 81/8 mutation-row split with 1 naming `SKILL.md`, `__all__` at 29 names / 19 subclasses against the impl-plan literal, and all eight impl-plan needles at 1 hit each. Four findings do not: a carried unscoped-diff figure, a class-closure detector with two demonstrated unstated blind forms (one of which hid an ordinal this very revision struck), a self-contradicting entry count written as a word, and a three-member enumeration of a class that has at least eleven members. Both commits after `74e126f` touch only `docs/`, so every corpus and scoped-diff figure is provably identical at `74e126f` and at `35698f9`.
Evidence: 19 files opened (plus the 35 `.md` the published census reads programmatically), 96 greps run.

## Must-fix

- The unscoped-diff figure published as the reason the scoped trip-wire is needed is the `a8e0372..335f535` measurement carried into a sentence about `a8e0372..74e126f`. Re-derived at `35698f9`: `git diff --name-only a8e0372 74e126f` names **18** files, **16** of them `.md` — not 13/11. (13/11 is exactly right for `a8e0372 335f535`, which is why the v1.95 bracketed note two lines below is **correct and must not be "fixed"**.) At the audited sha the same command names 25 files, 23 `.md`. The conclusion survives — `git diff --name-only a8e0372 <sha> | grep '\.md$' | grep -vc '^docs/'` is `0` at both `74e126f` and `35698f9` — but this document's own rule is that a figure travels with the sha it was measured at, and this one does not. It is also self-refuting in place: the paragraph exists to teach a reader to re-run the command, and the reader who does gets a different number. Two sites carry it, the body and the v1.96 entry. Prescription: replace the bare pair with the `grep -vc '^docs/'` → `0` form, which is the invariant actually being argued and is stable across revisions of `docs/`.
  quote: docs/02-design/features/doc-block-exec.design.md › `it names **13** files across these two shas, **11 of them `.md`**`
  quote: docs/02-design/features/doc-block-exec.design.md › `unscoped, git diff --name-only a8e0372 74e126f names 13 files, 11 of them .md`

- The seam-ordinal detector is published as proof the class is closed, with a residual stated as "exactly two items", and it has two further blind forms — both demonstrated, and one of them not hypothetical. (a) **Line wrap.** The document hard-wraps at ~95 columns and `grep` is line-scoped, so an ordinal separated from its noun by a newline is invisible. v1.95's own `**fifth named` / `injection**` was exactly that shape: at `74e126f` the head contains it, and the published pattern returns `0` on that line. The "3 on v1.95 → 0 on v1.96" pair therefore saw **3 of the 4** ordinals actually present in v1.95's head — it establishes the detector fires, not that a `0` means none remain. (b) **A dotted token between ordinal and noun.** The gap is `[^.]{0,60}`, and seven of the eight seams are dotted module paths, so `the fifth \`os.unlink\` seam` — the natural phrasing for this set — scores `0` (control run, versus `1` for `the fifth named seam`). The same wrap blindness is in the line-pin detector on the facing page (control: `foo/bar.py:` + newline + `186` → `0`), so this is one class across two published checks, not two nits. Live state at `35698f9` is clean: a folded sweep of the head finds `0` real wrapped ordinal addresses (its 2 hits are the two published pattern literals themselves), and `last|final|penultimate|tenth` × seam and the reverse noun-then-ordinal order are each `0` — so the finding is that the *proof* is weaker than stated, not that a member is loose today. Prescription: state both forms in the residual and fold paragraphs before matching, excluding fenced code (see Should-fix). `instance of: a checker shipped as class closure whose negative half was never run against the shapes it cannot see.`
  quote: docs/02-design/features/doc-block-exec.design.md › `It returned `3` on v1.95 of this document and returns `0` on v1.96, so it discriminates`
  quote: docs/02-design/features/doc-block-exec.design.md › `**Residual, exactly two items, both stated so the next author neither strikes a`
  quote: docs/02-design/features/doc-block-exec.design.md › `with no judgement call left to the reader.`

- Two sites added by this same revision contradict each other on how many Version History entries quote an ordinal, and the wrong one is written as a word so a digits-only sweep misses it. The Invariant Compliance prose says five entries, one of them a strike annotation; the Test Strategy residual says the number is derived and returns `7`. I re-ran the published tail command at `35698f9`: it returns **7**, on seven distinct entry lines (v1.12, v1.23, v1.48, v1.49, v1.69, v1.95, v1.96), and **three** of those (v1.49, v1.95, v1.96) are strike annotations quoting the ordinal they struck. "Five, one of them a strike annotation" is the v1.95-bracket enumeration (v1.49 plus four standing) carried into a paragraph written by the revision that added two more — and it was already `6` on the v1.95 blob, so it was never right at `74e126f` either. It matters because the sentence *reasons* from the number to justify scoping the prohibition, and because the facing residual explicitly forbids writing this count as a list precisely because it grows. Prescription: delete the figure at the prose site and point it at the derived command, per the rule the document already states one section away.
  quote: docs/02-design/features/doc-block-exec.design.md › `five entries there quote an ordinal as part of a dated`
  quote: docs/02-design/features/doc-block-exec.design.md › `It returns `7` at v1.96.`

- The Single-source contract compliance claim is scoped by a closed enumeration of "three further hand-rolled `##`-slicers" invisible to the `from docsections import` census, and the class has at least **eleven** members at `35698f9`. Only three files in the two roots mention `docsections` at all (`test_docsections.py`, `test_h_mad_review_evidence.py`, `test_h_mad_wire_registry.py`), so everything else is invisible to that census by construction. Beyond the three named, I opened and read each of these definitions: `h-mad/tests/test_h_mad_install_check_docs.py::_section`, `h-mad/tests/test_h_mad_audit_cycle_docs.py::_section`, `h-mad/tests/test_h_mad_preflight_docs.py::_section` (all three the identical `text.index(start)` / `text.index(end, …)` body as the `_section` the document does name), `h-mad/tests/test_h_mad_analysis_versioning_docs.py::section`, `handoff/tests/test_handoff_carry_forward.py::_section`, `h-mad/tests/test_h_mad_skill_telemetry_docs.py::telemetry_section`, `h-mad/tests/test_h_mad_context_budget_docs.py::_section`, and `h-mad/tests/test_h_mad_pane_visible_dispatch_docs.py::_section`. A mechanical predicate agrees: eight locally-defined functions in the two roots are called with a `"## …"` string literal, none in a `docsections`-importing file. The supporting property does not extend either — the last two of those are deliberately **fence-aware** (one tracks fences because its section contains a bash block with `#` comments), so "all three are fence-blind" is true of the three named and false of the class. The compliance *conclusion* may well still hold for all eleven (they all read as test-local assertion helpers), but it has been argued over three of them and the other eight were never examined against it, and this document's standard everywhere else is an exact residual. Prescription: state the class by predicate with its count re-derived at a named sha, or scope the sentence to "three examples" and say what the residual over the rest is.
  quote: docs/02-design/features/doc-block-exec.design.md › `the tree holds three further hand-rolled `##`-slicers that the`
  quote: docs/02-design/features/doc-block-exec.design.md › `all three are fence-blind`

## Should-fix

- The Setext census's fence tracker closes a fence on any line whose *marker character* matches, ignoring run length, so a `` ``` `` line inside a `` ```` `` enclosing fence closes it and everything after is scanned as prose. Constructed fixture: a four-backtick fence containing a three-backtick line followed by `Fake Heading` / `============`, run through the shipped `census()` verbatim — returns `1` where CommonMark says `0`. The published negative control covers a thematic break, a fenced underline, a list-item underline and a table delimiter row, and does not cover this shape — which is the exact shape AC-1.6 exists for and the same shape this document cites as the reason a differential against `_fence_aware_end` is not achievable. The direction is safe (it can only invent a Setext heading, never hide one), so `setext_headings 0` at `35698f9` still means zero and no figure moves; what is owed is the control and a stated residual, or a one-token fix (`len(m.group(1)) >= len(fence)` with the opener's run recorded).
  quote: docs/02-design/features/doc-block-exec.design.md › `a fixture holding a thematic`

- If the fix for Must-fix 2 folds paragraphs before matching, it must exclude fenced code or it self-matches: I ran the folded form of the ordinal check over the head at `35698f9` and got 2 hits, both of which are the published alternation literal inside the document's own `bash` fences, not addresses. Worth stating in the prescription so the next revision does not read `2` as two live ordinals and strike its own detector.

- The two block-census ordinals still dated `335f535` (the only two of the five `335f535` dates not spot-checked at `74e126f`) reproduce at the freeze sha: gate `4`, exec-codex `2`, over both the 7-block named-anchor span and the 4-block AC-1.5 span, and `lines 159 blocks 7 gating 1`. Not a defect — recorded because the document says these two were not re-run, and a future reader should not have to.

## Nit

- Every figure in the document is dated `74e126f`, two commits behind the audited tree. Both intervening commits (`0aac0b7`, `35698f9`) touch only paths under `docs/`, so `git diff --name-only 74e126f 35698f9 -- h-mad handoff` is empty and every corpus and scoped-diff figure is identical at the two shas — the same one-sentence closure the document already gives the `335f535` figures. One sentence would spare the next reader re-running all of them.
