## Summary

Gating leg for design cycle 95, one surface only (codex exhausted; no two-surface clean and no exit gate is claimed here). **Freeze `3f70eb3`**, design blob `eaa7fca` — confirmed as `HEAD:docs/02-design/features/doc-block-exec.design.md` with the feature directories clean. The dispatch originally froze `59cc2ad` and two commits landed on the document while I was reading it (`7b182b0`, `3f70eb3`); I snapshotted the file at the start and re-hashed at the end, and the body — doc lines 1–3611 — is **byte-identical** across the assembled prompt, the staged blob `8ea8ca8` and the shipped blob `eaa7fca`, so every head-scoped reading below is of the shipped bytes. Only the v1.107 §Version History entry moved between those points, and I read the shipped entry in full (14,000 characters, one line) rather than the prompt's. I re-derived roughly 60 published figures and the great majority reproduce exactly — the 63/61 raised pair at all four corpora, all three hop differentials and the `700c599`→working endpoint, the six-blob candidate series, all five sha-description arms, the refusal arm, the walks at 106/62 with the fence-aware 2/60/55 split, the eleven-key `DETAIL_KEYS` arm at seven with all seven sites walked and confirmed as Python keyword arguments, the adjective counts, the `-F` needle, both line-pin screens with a constructed positive control, the provenance probe and `PRECHECK: PASS issues=0`. All four musts below sit in the §Version History entry v1.107 itself wrote, or in a rule that entry states or declines to apply — the same fix-introduced-prose population this round predicted, with the fourth landing inside the same line that states the rule it breaks.

Evidence: 28 files and git blobs opened, ~165 greps run.

Also run: 12 `git diff` differentials, 2 script executions (`h_mad_precheck_doc.py`, a fence-state Python walk), and 4 constructed positive controls. Every zero I report was taken in the same invocation as a non-zero control, or with a hand-built positive, because `ugrep` shadows `grep` in this shell and returned `error: exceeds complexity limits` on me once; all counts below were re-taken with `/usr/bin/grep` (BSD grep 2.6.0-FreeBSD) or with Python `re`.

## Must-fix

- The v1.107 §Version History appendix publishes `95 added and 35 removed` as the corpus of the value sweep that certifies no screen moved, and that figure reproduces at no corpus. `git diff --numstat 00b961f 3f70eb3 -- docs/02-design/features/doc-block-exec.design.md` prints `102	41` on the shipped file, and the same command against `dfae038` — the freeze this very entry names — prints `102	41` too; at `59cc2ad`, the commit where the appendix landed, it prints `96	36`, and with §Version History entry lines removed, `94` added / `35` removed. No scoping I could construct yields 95. The appendix opens by promising every reading below it was "re-taken on the file that includes it", so the sweep's own stated corpus is 7 added and 6 removed lines short of the file it certifies — the exact hazard the entry's own MUST 1 rule was written to close, recurring in the paragraph that states it. The conclusion survives and should be said to: I ran the shipped needles over all 102 added and 41 removed lines and the only non-zero results are three hits inside the two added §Version History entry lines, which every one of those screens excludes by head scope. Fix by re-taking the count at the merge commit and naming the two fixed endpoints, as the same entry's own rule requires.
  quote: docs/02-design/features/doc-block-exec.design.md › `the 95 added and 35 removed lines of this revision's own diff were run against every needle this document ships`

- The appendix states the attribution-deictic residual as exactly two members and names them as complete; at least **eight further live attribution deictics stand unnamed in the head**, and one of them is in a paragraph this revision did open. Folded sweep over the head returns 29 occurrences of `this revision`; 12 are the stable "the working file / the file this revision ships" form (L927, L963, L1011, L1035, L1508, L1521, L2365, L2406, L2650, L2678, L2805, L3435), 5 are quotations of the phrase as a described defect (L2538, L2540, L2561, L2726, L2806) and 1 is v1.107's own and correct (L2135), leaving 11 attributional uses, of which the entry names 2. Traced through the blob series (`cf3a862`/`700c599`/`8c6539a`/`b3be433`/`00b961f`/working), the unnamed live members are: §Scanning L645 "which it did not until this revision", L821 "labelled there by this revision", L895 "no label until this revision", §Test Strategy L2569 "the corollary this revision had to add", L2959 "Run after the edits this revision ships" — all five present at `700c599`, so they mean v1.103 or earlier; §API / Interface Changes L2141 "was a defect this revision repairs", first present at `8c6539a`, so it means v1.104; §Test Strategy L2662 "the ladder's end and this revision" and §Test Plan L3101 "the shape §Scanning struck this revision", both first present at `00b961f`, so they mean v1.106. L3101 is the sharper one: it is near-verbatim the phrase the entry names ("struck in this revision"), so a reader chasing the named member finds two sites and is told about one, and it is in §Test Plan, not §Test Strategy. Separately, §Invariant Compliance L3451 "re-derived fence-aware after this revision's edits" is an attribution deictic left standing in a paragraph this revision did open — it happens to resolve correctly to v1.107, which re-ran the split, but it falsifies the "in paragraphs this revision did NOT open" characterisation. Fix by naming the class and its measured membership rather than two instances, or by stamping all of them.
  quote: docs/02-design/features/doc-block-exec.design.md › `The ones left standing are in paragraphs this revision did NOT open -- Test Strategy's 'struck in this revision' and 'what this revision changes' -- and they are named here so their absence is not read as their non-existence.`

- The document contradicts itself across two sites on whether an entry bump obliges a re-stamp. §Test Strategy states the rule in the strongest available terms and calls the version the corpus identifier; the v1.107 entry then declines to apply it to the 18 stamps and books the omission as a residual. I re-derived the census: over the folded head the phrase `v1.106 entry` returns **18**, line-scoped **17** — so 17 flat and 1 split by the wrapper, as the entry says — and all 18 name v1.106 on a file that ships v1.107, so 18 document-self figures now carry a corpus identifier one revision behind their corpus. The entry's disclosure of the omission is adequate as disclosure — it states the count, the reason, the fact that all 18 reproduce, and exactly what the phrase no longer certifies — and I am not filing it as hidden. What is not adequate is that the §Test Strategy rule is left standing unamended: a reader who reaches it is told the version *is* the corpus identifier and that a bump obliges a re-stamp, with nothing at that site saying the current revision took a documented exemption. Either the rule is amended to admit a lag when re-stamping would falsify neighbouring attributions, or the 18 are re-stamped; leaving both is a rule the document publishes and then declines with no reconciliation at the site that states it.
  quote: docs/02-design/features/doc-block-exec.design.md › `that names an entry goes stale on the very next bump, so an entry bump is itself the trigger`
  quote: docs/02-design/features/doc-block-exec.design.md › `Re-stamping all 18 was NOT done here because it would make several neighbouring attribution deictics false in the same stroke.`


- The endpoint rule the v1.107 entry introduces as a class repair is falsified by the shipped document at seven live sites, three of them inside the same §Version History line that states it. The rule forbids the working tree as an endpoint; forty-odd sentences later in that same line the appendix publishes `Hops, each its own diff: 700c599 to working 60,61c60,61; 8c6539a to working 61c61; b3be433 to working 61c61`. In the head the same shape is load-bearing and deliberate: §Scanning publishes `b3be433`→working as one of three hops, publishes the differential `diff <(git show 700c599:"$D" | grep -E "$RAISE") <(grep -E "$RAISE" "$D")` whose right-hand operand *is* the working file, and five further readings are stamped "and on the working file"; §Execution's cross-document discharge is stamped "on the working spec". The document already has the correct convention for this — a working-file endpoint is admissible when it is stamped by the §Version History entry it was taken after — and the new rule wipes it out by omission rather than carving it out. The residual the entry offers does not reach this: it names only the wrong-pair hazard (`a fixed pair can still be stamped at the wrong pair`), which is a different failure from a rule that outlaws the document's own dominant comparison shape. Prescription: state the rule as "two fixed endpoints, or one fixed endpoint and the shipped file identified by the entry it was taken after; HEAD and the current branch are never endpoints" — which is what the body already does and what the repaired `00b961f..dfae038` control actually demonstrates.
  quote: docs/02-design/features/doc-block-exec.design.md › `THE RULE OVER THE CLASS: every sha comparison published in this document names TWO fixed endpoints; HEAD, the working tree and the current branch are not endpoints, they are whatever the reader's tree happens to be.`
  quote: docs/02-design/features/doc-block-exec.design.md › `Hops, each its own diff: 700c599 to working 60,61c60,61; 8c6539a to working 61c61; b3be433 to working 61c61`

## Should-fix

- The hop series is defined over three hops but attributed over three revisions, and its third hop spans two of them, so the published derivation cannot support the published attribution. The third hop's right-hand side is the working file, which carries v1.106 **and** v1.107. I split it: `b3be433`→`00b961f` prints `61c61` (one changed line) and `00b961f`→working prints nothing, so the attribution is right — but nothing published in the paragraph establishes that, and had v1.107 reworded a raised line instead of v1.106 the same three hop shapes would have printed and the sentence would have been false. This is the SCOPE class the round already paid for once. Prescription: publish the fourth hop `00b961f`→working as empty, or re-define the third hop as `b3be433`→`00b961f` plus `00b961f`→working. The totals themselves are not in dispute: I re-derived 63 whole-file and 61 over the head at `700c599`, `8c6539a`, `b3be433`, `00b961f`, the prompt's bytes and the shipped bytes alike.
  quote: docs/02-design/features/doc-block-exec.design.md › `So v1.104 reworded one, v1.105 reworded none, and v1.106 reworded one`

- The appendix's "every one returns zero in BOTH directions" is false as literally written and true only under a scope the sentence does not state. Run over the 102 added lines of `00b961f`→`3f70eb3`, the shipped sha-cardinality arm returns 1, the widened cardinality arm 1 and the eleven-key detail-field arm 3; over the 41 removed lines all needles return 0. Excluding the two added §Version History entry lines every arm returns 0 in both directions, which is the reading the sentence means. A reader who re-runs it as written gets non-zero and cannot tell a scope difference from a defect. Prescription: say "over the added and removed lines outside §Version History", which is the corpus every one of those screens is defined over anyway.
  quote: docs/02-design/features/doc-block-exec.design.md › `and every one returns zero in BOTH directions, which is why the figures above reproduce rather than needing republication`

- §Invariant Compliance residual (iii) carries four re-execution figures on a stated ground narrower than the figures need. Three of the four — that 6 fences carry an adjacent output block, that 4 reproduce byte-identically and that 2 differ only in PIDs — are readings about the **adjacent output blocks**, and the ground offered covers only the ten `python` fence *bodies*. The claim does hold: I parsed every fence in the head at `8c6539a`, `b3be433`, `00b961f` and the working file and compared them as (info, body) pairs — 51 fences at `8c6539a` and `b3be433`, 52 at `00b961f` and working, with the single difference being the one `bash` fence v1.106 added; all 15 bare output fences and all 10 `python` fences are byte-identical across all four. Prescription: state the ground over every fence in the head, which is what was actually true and is the same one command to re-take.
  quote: docs/02-design/features/doc-block-exec.design.md › ``8c6539a`, at `b3be433` and on the working file, and the ten bodies are byte-identical across all`

- Cross-document: the design says the spec states the same `DETAIL_KEYS` residual "for the same reason", but the two documents name opposite closure statuses for it. The design says no test can reach the staleness and that the check is a reader comparing two lists; the spec says the same growth is a staleness the registry walk catches. Both cannot be the ground a reader relies on, and the design is the document asserting the equivalence. The tuple itself agrees across the pair — I read it in the impl-plan at `docs/01-plan/features/doc-block-exec.impl-plan.md:2510`, eleven members with a trailing `# 11`, member-for-member identical to the design's alternation and to the spec's enumeration, and `grep -rn DETAIL_KEYS h-mad/` returns nothing (rc=1), so the "not yet a symbol in the tree" half is correct. Prescription: state which check actually closes it and make the two documents agree, or drop "for the same reason".
  quote: docs/02-design/features/doc-block-exec.design.md › `same residual against the same tuple for the same reason, and the check is a reader comparing the`
  quote: docs/01-plan/features/doc-block-exec.spec.md › `without this list growing, which is a staleness the registry walk catches and this grammar does`

- **unverified** — the stamp census reproduces under one plausible needle and not the other, and the entry does not say which it used. For the needle `v1.106 entry` the head gives 18 folded and 17 line-scoped, exactly matching "17 are flat while ONE is split by the hard wrap"; for `after the v1.106 entry` it gives 18 folded and 16 line-scoped, an under-report of two rather than one. I could not determine the intended needle from the document, so I am recording this as a needle that is not published rather than as a wrong figure. Prescription: publish the needle beside the census, as this document does everywhere else.

## Nit

- §Invariant Compliance stamps its two walks "re-run after the v1.106 entry below was written", but both walks are head-scoped (`awk '/^## Version History$/{exit}'`) and therefore cannot be moved by any §Version History entry at all. The stamp is harmless but tells a reader the wrong thing about what identifies the corpus for that figure; the blob hex is the identifier that matters there. Measured: 104/60 at `8c6539a` and `b3be433`, 106/62 at `00b961f`, at the prompt's bytes and at the shipped bytes.

- The `LaunchFailed` signature is written twice with identical text — once in §API / Interface Changes (head line 1753) and once in the exception table under §Error Handling (head line 2301) — and the seven-site enumeration disposes of them as "the same signature ... twice". Both are correct and both are Python keyword arguments, so this is not the GRAMMAR defect the arm exists to bound; it is only that the duplication makes the arm's count two higher than the number of distinct claims a reader has to check, and the enumeration could say so.

---

**What I did not check**, named so its absence is not read as a clean finding:

  · I did not read the head in full. I read, in the shipped bytes: §Detailed Design and §Scanning (184–1133), §Implementation Order 1476–1532, §API / Interface Changes 2070–2160, §Test Strategy 2596–2734, §Test Plan 3078–3117, §Invariant Compliance 3400–3502, plus the Executive Summary / Overview / Architecture Overview as inlined at the top of the prompt. I did **not** read §Substitution (1133–1170), §Execution (1170–1401), §Components Changed (1401–1476), §Data Model (1630–1651), most of §API (1651–2070), §Error Handling (2274–2310), §Test Strategy 2310–2596 and 2734–2987, §Test Plan 2987–3078 and 3117–3320, or §Invariant Compliance 3320–3400. Figures in those spans were re-derived only where a screen I ran covers them.
  · In §Version History I read only the v1.104–v1.107 entries. The other 100-odd entries are unread.
  · I did not re-execute the ten `python` fences. I established only that their bodies — and every other fence in the head — are byte-identical across `8c6539a`, `b3be433`, `00b961f` and the shipped file, which is the ground the document offers, not a re-run.
  · The 55-line disposition is a reading and I did not re-read the 55 prose lines. I verified only the mechanical chain 62 → 2 in-fence → 60 → minus 5 named → 55.
  · I did not run the repository test suite, and I did not check any AC against `h-mad/scripts/h_mad_doc_block_exec.py`, which does not exist in the tree by design.
  · I read the plan and impl-plan only through targeted greps and the `DETAIL_KEYS` block; I did not audit them.
  · **One gating surface.** codex is exhausted until 2026-09-07 11:28, so nothing here is a two-surface clean and no exit gate is claimed. I share a model family with the author of v1.107, so my blind spots and theirs correlate by construction; a real codex round on `3f70eb3` is still owed before anything I certified here is treated as settled.
