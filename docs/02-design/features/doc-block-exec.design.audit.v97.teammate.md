## Summary
GATING pass on `docs/02-design/features/doc-block-exec.design.md` v1.109 at freeze `cb4fe99`. I re-ran rather than read every figure I could reach: the 30/35 corpus, the two-guard differential (`both 292 old_only 82 new_only 1`), the eleven-shape ATX proxy, the Setext census and its committed probe, the `raised 36 / sites 12 / noclaim 8 / both 0 / neither 16` partition, the whole blob series `36/42/50/53/61/63` with its head/whole split at `1cbddb7`, the two-walk `126`/`70`, the argparse five-shape table, the substitution intersect probe, the 85-row matrix count, the `_second_surface` 50-vs-159 span and the line-pin sweep — all reproduce byte-exactly. Two Must-fixes remain: one published `*.py`-derived figure is wrong at the freeze and this revision's own entry asserts it is not, and the exception signature this design specifies contradicts the impl-plan committed in the same commit.
Evidence: 11 files opened, 84 greps run.

## Must-fix
- The v1.109 entry's `*.py` null is FALSE at the audited freeze, and the figure it exonerates is wrong there — §Invariant Compliance publishes the `rglob('*.py')` slicer sweep as **22** stamped `35698f9`, and the sweep returns **23** at `af19d53`, at `fbc2ea0` and at the freeze `cb4fe99`. Re-derived by running the shipped fence at the freeze (23) and reconstructing it from blobs at each sha (`35698f9` 22, `af19d53` 23, `fbc2ea0` 23, `cb4fe99` 23); the single arriving member is `h-mad/scripts/h_mad_assemble_audit.py _trim_version_history`, which is **one of the two assembler files the entry names as harmless** and which sits inside the sweep's own declared `h-mad/scripts` root. Blob-pinning does not save it: the number is presented as the sweep's output and a reader re-running the fence at the freeze gets a different one. **Prescription**: restamp the figure at the freeze as 23, name `_trim_version_history` as the arrival, and withdraw the `*.py` half of the C10 null — both halves of C10 were wrong, not one. **The class, and the reason no screen here can catch it**: this document's scoped-diff trip-wire trips only on `.md` (`if *it* ever names an `.md``), while `git diff --name-only a8e0372 cb4fe99 -- h-mad handoff` names exactly two `.py` and no `.md`. Every `.py`-derived figure in this document is therefore outside every screen it ships. The rule that closes it is that the trip-wire's extension must be the extension of the corpus the figure was taken over, not `.md` for all of them; the residual is a figure whose corpus is neither `.md` nor `.py` (a `.json` mutation spec, a `SKILL.md` fence census), which needs its own arm.
  quote: docs/02-design/features/doc-block-exec.design.md › `It prints 22 lines at `35698f9`, and that is *its* output, not a count of slicers.`
  quote: docs/02-design/features/doc-block-exec.design.md › `so af19d53 moving that corpus by two assembler files touches no published figure`
- `OverlappingSubstitution`'s signature contradicts the impl-plan committed in the same commit, and each document claims the decision is its own — this design specifies ONE argument `pairs` carrying both refusal kinds tagged, while `docs/01-plan/features/doc-block-exec.impl-plan.md` at the same freeze specifies TWO fields and its AC-2.7 intersection test asserts `pairs == []` on the very case this design says `pairs` must carry. An implementer following this design fails the impl-plan's named test by construction; an implementer following the impl-plan leaves this design's error table wrong. The word `intersections` occurs **0** times in this design and **4** times in the impl-plan. **Prescription**: pick one owner. If the two-field form wins, §Error Handling's row becomes `OverlappingSubstitution(pairs, intersections=())` and §API's raise list follows; if the tagged-single-field form wins, the impl-plan's `__init__`, both AC-2.7 test bodies and its "this document's own decision" sentence all move. **The class**: every field this feature adds to a shared exception. The rule is that an exception's field list has exactly one owning document and every other cites it by locator; the residual is `StreamPathUnwritable.leftover` and `StreamWriteFailed.verify`, which the impl-plan also introduces as its own decisions on the same stated ground and which no round has checked for the same split. `instance of: shared-exception field ownership`.
  quote: docs/02-design/features/doc-block-exec.design.md › `` `pairs` carries both kinds, each tagged with the kind that raised it ``
  quote: docs/02-design/features/doc-block-exec.design.md › `OverlappingSubstitution(pairs) carries BOTH refusal kinds in the one argument, each pair tagged with the kind that raised it`
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `def __init__(self, pairs, intersections=())`
  quote: docs/01-plan/features/doc-block-exec.impl-plan.md › `` `intersections == [("ab", "bc", 1)]` ``

## Should-fix
- The v1.109 OWED ELSEWHERE list is written present-tense against `fbc2ea0` and four of its seven items are already discharged by the sibling bytes in the commit this design ships in — (1) `os.lstat` in the canonical taxonomy is present in both siblings at the freeze (spec names it "the ninth and newest", impl-plan "the ninth module-level seam"); (2) the twelfth `DETAIL_KEYS` member is present, the tuple reading `("missing_key:", "overlap:", "intersect:", …)` with a trailing `# 12`; (6) `cleanup-chain-selection-flipped` occurs 4 times in the impl-plan; (7) the connective-free `intersect: "<a>" "<b>" "<offset>"` spelling is in both siblings. Only (5), the exception signature, is genuinely outstanding, and it is outstanding as a contradiction rather than a gap. This is the document's own OWED-ELSEWHERE-is-a-claim-about-a-moving-tree rule failing on the list that states it: the readings were taken at the parent commit and never re-taken after the batch landed. **Prescription**: re-take each of the seven at the landing sha and mark the discharged ones, per the rule the same entry states for item (4).
  quote: docs/02-design/features/doc-block-exec.design.md › `(2) The impl-plan owes a twelfth DETAIL_KEYS member, intersect:`
- The plan's mutation-matrix figure diverges from this design's at the same commit and the owed-elsewhere list does not name it — this design's matrix is 85 rows (84 helper + 1 `SKILL.md`), derived by its own `awk` over the table, which I ran and which returns 85; `docs/01-plan/features/doc-block-exec.plan.md` still states 81 (80 + 1) in its deliverables table, outside its Version History. This revision is what moved the count from 81 to 85, so the sibling debt is one this revision created, and it is the only such debt absent from the seven-item OWED ELSEWHERE list. **Prescription**: add it as an eighth item naming the plan's deliverables-table row and both addends.
  quote: docs/02-design/features/doc-block-exec.design.md › `Eighty-five rows, eighty-five mutations`
  quote: docs/01-plan/features/doc-block-exec.plan.md › `` 81 mutations with a full-node-ID `test` binding each ``
- Five acceptance criteria the spec defines are never named anywhere in this design — AC-2.3, AC-3.4, AC-3.5, AC-4.4 and AC-6.3 return 0 hits; the spec defines 49 distinct ACs and this design references 42 (AC-1.2 and AC-3.2 appear only inside the slash forms `AC-1.1/1.2` and `AC-3.1/3.2`, which a naive count misses). The behaviour behind at least AC-2.3 and AC-4.4 IS specified here (map-insertion order for `missing_key:` lines with its pinning test; `AMBIGUOUS blocks=<n>` in the verdict table), so this is a labelling gap and not a coverage hole — but it means no reader can check AC coverage by label, and the three shell-mode and wire ACs (3.4, 3.5, 6.3) I did not trace to prose, so their coverage is `unverified`. **Prescription**: either label the five where their behaviour is already stated, or state once that this design's AC references are illustrative rather than exhaustive.
- The line-pin exemption's own arm is `.md`-blind in the same way Must-fix 1 describes — the sweep's alternation is `\.(py|md|json|sh):[0-9]+`, so a pin written against a path with no extension, or against a `.txt`/`.toml`/extension-less artifact, is unreached. It returns 0 today, which is correct, but the zero is a property of which extensions the document happens to cite. `unverified` whether any such pin exists; I found none, and I did not construct a positive control for the arm.
  quote: docs/02-design/features/doc-block-exec.design.md › `grep -nE '[A-Za-z0-9_./-]+\.(py|md|json|sh):[0-9]+`

## Nit
- `git ls-files | grep -c grammar_corpus` is written as though it took a sha (`→ `0` at `cf3a862``); `git ls-files` reads the index, not a commit, so that reading is only reproducible by checking the sha out. The neighbouring Setext paragraph already uses the correct form, `git ls-tree -r --name-only <sha>`, which I ran and which returns 0 at `cf3a862`, 1 at `fbc2ea0` and 1 at the freeze.
- The Executive Summary's parenthesis "the seven functions in `__all__`, beside `Block`, `RunResult` and the exception hierarchy — 29 public names" states the total without its addends, and the addends are only recoverable from §API 800 lines later. The exception table does carry exactly 19 rows, so 7 + 2 + 1 + 19 = 29 checks out; one clause naming the four addends would make the summary self-checking.
