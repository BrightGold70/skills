## Summary
v1.100's executed evidence largely holds: I re-ran the carry screen in both forms on a clean and a
dirtied tree and reproduced `8/3`, `15/8`, `1/1` and `49/17` exactly; all sixteen span-noun
branches, the flag branch and the true negative; the head `14` at `cf3a862` and the tail `12`; both
tilde control arms; the six `$N` branch scores on the v1.95 blob and the six fixtures; the four
`NUM` tokeniser cases; `7` of `49` stamped and unstamped; the candidate sweep `36`/`42`; the
`finder`/`bounder` differential `30/292/82/1`; and the docs-invariant `0` at eight shas including
the freeze sha. Four property claims in the new text were **not** executed and are false as
written — the falsification's stated mutation, the completeness of the seven-site denominator, the
tilde run's reach to `$STRIP`, and the span-ordinal screen's coverage of "any shape this document
writes one" (which has two live counter-instances in the tail it screens); two more are wrong
figures of the same class. **The experiment's result: 6 unexecuted property claims out of ~25
checked in the delta.** This pass is gating and it is not clean.
Evidence: 18 files/blobs opened, 40 greps run.

## Must-fix
- The falsification's stated mutation is not the mutation the shipped command runs, and running the stated one gives different numbers — the headline instance of decision Q re-committed inside its own repair. The fence appends `git diff 35698f9 6f0ee85 -- "$D"`, i.e. the **v1.97** revision's added body lines (design is v1.96 at `35698f9`, v1.97 at `6f0ee85`); "this revision" is v1.100 = `4e4a00c..06ef40f`. Reproduced in a `git clone --local --shared` of this repo at `06ef40f` (the audited tree was never touched): appending `35698f9..6f0ee85` gives the published `BODY: 1 runs, 1 of them figure-bearing` / `VERSION HISTORY: 49 runs, 17 of them figure-bearing` under the `open()` form and `8/3`, `15/8` unchanged under the shipped `git show` form; appending `4e4a00c..06ef40f` — the prose read literally — gives `BODY: 13 runs, 7 of them figure-bearing` / `VERSION HISTORY: 15 runs, 8 of them figure-bearing`. The demonstration itself is sound; the sentence describing it is a property claim about the run that was not run, and it also violates this document's own settled rule that `this revision` stops resolving. Prescription: "the measured revision's own added body lines (`35698f9..6f0ee85`)". The same wording is repeated in the v1.100 entry.
  quote: docs/02-design/features/doc-block-exec.design.md › `Appending this revision's own added **body** lines to the working `$R` and re-running both forms:`
- The absence-site denominator is larger, not complete: an eighth site sits inside the sweep's own needle at `cf3a862`, is an absence claim about a corpus rather than about this document, and carries no label. I enumerated all 36 candidate lines at `cf3a862`; line 610 there (line 624 on the working file) is the Setext census's corpus zero, and no label from the three-label vocabulary appears anywhere in lines 596–645 of the working file (`grep -niE 'vacuous|incidental|load-bearing'` over that range → nothing). The rule requires every absence sentence to carry the runnable command, the sha and one of the three labels; this one carries the first two and not the third. The offered disposal of `headings shipped 0` does not reach it — the two arms are about `census()`'s fence tracker mis-scanning, not about whether the corpus contains a Setext heading, and this site is ~100 lines earlier than the harness — and v1.100's own new *inside the needle* clause is precisely the rule that raises it. Prescription: label it (the shape is absent, so *vacuous*) and publish the denominator as eight, or state that the seven counts only sites the rule's scope carve-out does not exclude and say which carve-out excludes this one.
  quote: docs/02-design/features/doc-block-exec.design.md › `rather than a scan that finds nothing. It was `0` at `1861157` too, over the 25 files tracked then.`
- The tilde run does not reach `$STRIP`, but the paragraph names `$STRIP` among the branches it moves, so one of the three screens it claims to control is still a branch no control and no corpus has ever moved (decision O, the decision the sentence itself invokes). The run is inside the `census()` harness, whose only fence regexes are `SHIPPED` and `ANYIND`; `$STRIP` is a separate awk defined at `docs/02-design/features/doc-block-exec.design.md:1815` in the seam-ordinal pipeline. Executed: extracting the shipped harness and re-running with `B = '~' * 3` does print `control arm1 shipped 1 repaired 0` / `control arm2 shipped 1 repaired 0` (that part holds), but `grep -cE '^ {0,3}~{3,}'` over this document returns **0** on the working file and **0** on its head, and no control anywhere in the document feeds a tilde fence to `$STRIP` — so after this revision `$STRIP`'s `~{3,}` alternative is exactly as unexercised as before. Prescription: run a two-line fixture whose ordinal-plus-noun sits inside a `~~~` fence through the shipped `$STRIP`/`$FOLD` pipeline and show the hit disappear, or drop `$STRIP` from the sentence and state its tilde branch as an uncovered residual.
  quote: docs/02-design/features/doc-block-exec.design.md › `alike would be a branch that no control and no corpus had ever moved, with the healthy backtick`
- The replacement span-ordinal screen is blind to a shape this document writes six times inside the very region it screens, and two of those six are genuine unbased span ordinals — so the conclusion it now carries is false, not merely unsupported. The screen matches only a span noun beside a **digit**; a word-ordinal beside a span noun scores `0`. Run over the tail of the working file, `grep -oiE "\b(first|second|…|penultimate)\b [a-z]{0,12} ?\**($S)\b"` returns six hits — `sixth row` ×2, `seventh row`, `eighth row`, `third column`, `third matching line`. Four are cardinalities or harness columns; two are addresses into an ordered span with no base named, both into `h-mad/tests/mutation-specs/docsections.json`, whose array today holds **4** rows (`fence-tracking-removed`, `section-no-longer-owns-its-subsections`, `offset-anchored-bound-runs-to-end-of-file`, `missing-heading-returns-empty-instead-of-failing`) — so neither ordinal even resolves yet, which is the hazard rule 1 exists for, and rule 1's scope is stated to include §Version History. Prescription: either add a word-ordinal arm (`\b$O\b *\**($S)\b`) with its own per-branch firing and re-dispose of the hits, or name word-ordinals-over-a-span as a stated residual and withdraw "no §Version History entry states a span ordinal without its base".
  quote: docs/02-design/features/doc-block-exec.design.md › `names the sixth row's cross-file key`
  quote: docs/02-design/features/doc-block-exec.design.md › `docsections.json seventh row docsections-heading-lookup-reverted`

## Should-fix
- The v1.100 entry calls `42 at 7982c18` "the working-file value", and it is not: `7982c18` is the v1.99 blob, and the file this revision ships raises **50**. Executed with the shipped needle over the head: `cf3a862` → `36`, `7982c18` → `42`, `4e4a00c` → `42`, working file at `06ef40f` → `50`. The body text is careful and correct ("a reader re-running the sweep at a later sha gets a larger number, **42** at `7982c18`"); only the entry misdescribes it, which also means NIT 3's own claim to have named the working-file count is not discharged. §Version History is exempt for line pins, never for factual claims.
  quote: docs/02-design/features/doc-block-exec.design.md › `the working-file value is named for the first time (42 at 7982c18)`
- The `NUM` residual is labelled "named exactly" and omits `zero`, which is the uncovered numeral this document is most likely to write. Executed against the shipped tokeniser and regex: `zero runs` → tokens `['zero','runs']`, figure-bearing `False`; `zero` is not in `WORDS` (`one two three four five six seven eight nine ten eleven twelve thirteen twenty thirty`). The four cases the document does publish all reproduce exactly, including `twenty-two` as one token.
  quote: docs/02-design/features/doc-block-exec.design.md › `Uncovered members named exactly: `fourteen` through `nineteen`, `forty` through `ninety`, `hundred`,`

## Nit
- The tilde paragraph adds a new *vacuous*-labelled absence claim to the working file, so the file now carries at least eight labelled absence sites while the rule paragraph publishes seven. The seven is legitimately stamped at `cf3a862` and no contradiction follows, but a half-clause saying "this revision adds one more, labelled where it is stated" would stop the next reader re-deriving the discrepancy and filing it.
- The falsification fence writes to a tracked file in the repository under audit (`>> "$R"`, restored by `cp`), so a reviewer bound by the read-only audit contract cannot run it as published. I reproduced it in a `git clone --local --shared` at `06ef40f` and got the published numbers; naming that (or a scratch clone) in the fence's comment would make the demonstration runnable by the audience it is written for.
