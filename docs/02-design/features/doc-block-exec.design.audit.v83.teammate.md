## Summary

I re-probed the three v1.91/v1.92 edits first: the `exit_on_error` five-shape table reproduces exactly on python 3.11.8 (5/5 rows, `--help` still exit 0), and the 2748/2486 floor reproduces at HEAD `d6aa0e6` — both stand. Two must-fixes are elsewhere: Task 5's split rationale is measured false and contradicts the paired plan one revision after the plan corrected it, and the `*.md` corpus behind the heading differential, the Setext census and AC-6.1's cardinality sweep silently includes five untracked, gitignored `.pytest_cache/README.md` files, on which the design's "zero instances" softening claim is false. Axis C: all 49 spec ACs are `implemented-as-written` (the seven the design does not name by identifier — AC-1.2, 2.3, 3.2, 3.4, 3.5, 4.4, 6.3 — are each covered by content in the verdict table or the Test Plan); the one borderline, AC-6.1's `*.md` scope, is raised as a Should-fix rather than a `restated` must-fix because the spec reaches that scope by reference to the plan's census.
Evidence: 16 files opened, 26 greps run.

## Must-fix

- Task 5's rationale for not splitting the tag from the migration is measured FALSE, and the paired plan already says so — tagging the gate fence leaves `:270`'s `re.findall` matching **3** blocks, not zero. Measured at `d6aa0e6` by slicing `h-mad/SKILL.md` between `## Second surface — the codex leg` and `## Helper scripts` and tagging the gating opener in place: `BEFORE tag: findall blocks=4 gating=1 execcodex=1` / `AFTER tag: findall blocks=3 gating=0 execcodex=1`. The plan corrected this exact claim at v1.84 and states the consequence the design now loses: what empties is the `h_mad_audit_gate.py` filter, so the loud failure is `_gate_bash_block`'s `assert gating`, not an empty `findall`. An implementer who splits the tasks and looks for the symptom the design names will not find it, and will read the RED as unexplained. Prescription: replace the sentence with the plan's measured form (4 blocks before, 3 after, the gating filter goes to zero, `assert gating` is the loud failure).
  quote: docs/02-design/features/doc-block-exec.design.md › `Tag and migration cannot be split: tagging the gate fence makes`
  quote: docs/01-plan/features/doc-block-exec.plan.md › `match **3 of the section's 4 blocks instead of 4**`

- The 30-file `*.md` corpus every heading measurement in this design cites is 25 tracked files plus 5 untracked, gitignored `.pytest_cache/README.md` artifacts, and the softening accounting is false on it — breaching base §"Guard narrowing" ("account for **every** input whose verdict softened") and base §"Assumption verification" ("a cited output is checkable by a reviewer"). Measured: the plan's own glob and part-filters over `h-mad/`+`handoff/` excluding `archive/` return 30 files here, while `git ls-files` over the same roots returns 25; the five extras are `h-mad/.pytest_cache/README.md`, `h-mad/scripts/.pytest_cache/README.md`, `h-mad/tests/.pytest_cache/README.md`, `handoff/.pytest_cache/README.md`, `handoff/tests/.pytest_cache/README.md`, each confirmed ignored by `git check-ignore -v` and each carrying `# pytest cache directory #` on line 1 — five instances of the closing-hash-run shape the design says has none, and a shape the new normalized selector newly makes reachable by the request `pytest cache directory` where the old `^(?P<marks>#+) …\s*$` regex could not. Over the 25 tracked files the count is 0, so the claim is true only for a corpus the documents never define. This makes `files=30`, `old_only=76` and `setext_headings=0` reproducible only on a tree where pytest has already run — a clean clone gives 25 — which is precisely the staleness-detection the plan says the citation exists to provide. **This is a class, not the one instance**: the same corpus definition is what AC-6.1's cardinality-1 sweep is bound to, so any generated `.md` dropped under those two roots enters that count too, and the design's residual paragraph names non-`.md` files, `.md` outside the roots and `archive/` but not untracked/generated `.md` inside them. Prescription: define the corpus as tracked files (or exclude dot-directories), re-report the numbers over it, and add the untracked-`.md` residual to AC-6.1.
  quote: docs/02-design/features/doc-block-exec.design.md › `theoretical softenings `##\tx` and `## x ##` have zero instances`
  quote: docs/02-design/features/doc-block-exec.design.md › `(30 files, `archive/` excluded)`

## Should-fix

- The Single-source resolution claims "all surfaces call" the authoritative bounder, but three further hand-rolled `##`-section slicers remain in `h-mad/tests/` and none is named in the design, the plan or the spec — and §"Invariant Compliance" omits the Single-source contract entirely, though it is the base invariant that drives AC-1.8, the largest decision in the document. The consumer census that backs the claim is a grep for `from docsections import` (three files), which by construction cannot see a local re-implementation. Opened and read: `h-mad/tests/test_h_mad_context_budget_docs.py:69` `def _titled_section(text: str, title: str) -> str:` with its bound at `:82` `nxt = text.find("\n## ", start + 1)` — fence-blind and level-blind, used nine times against the live `h-mad/SKILL.md`; `h-mad/tests/test_h_mad_batch_doc_rules.py:26` `def section_text(path: Path, name: str) -> str:` — exact-heading match then `lines[end].startswith("## ")`, fence-blind; `h-mad/tests/test_h_mad_collect_report_docs.py:40` `def _section(text: str, start: str, end: str) -> str:` — two-marker `text.find`, fence-blind, and the very function `_second_surface()` uses today. I am not asserting these must migrate — `_titled_section` anchors on a substring rather than a heading match and is not a drop-in (measured: `docsections.titled_section` cannot find its target `"Run-context ceiling"` at all, because the real heading is `## Run-context ceiling — halt the run at 80%`). What is owed is that the design says which branch of the Single-source contract covers them and why, rather than leaving a compliance claim broader than the tree supports.
  quote: docs/02-design/features/doc-block-exec.design.md › `Resolution: satisfy the invariant's FIRST branch — one authoritative implementation all surfaces`

- AC-6.1's `*.md` scope is now called load-bearing in the design but spec AC-6.1's own words do not carry it — the spec reaches that scope only by reference to "the plan's fence census". Under Axis C this is the borderline between `implemented-as-written` and `restated`; because the reference does resolve to an `*.md`-globbed census I have not filed it as a must-fix, but the design's v1.92 note calls the restriction load-bearing and says the AC "could not pass at Task 5 GREEN" without it, which is a strong enough dependency to belong in the spec's own sentence rather than in a pointer.
  quote: docs/01-plan/features/doc-block-exec.spec.md › `across `h-mad/` and `handoff/` (excluding `archive/`, the same sweep as the plan's fence`

- The floor number travels with two different commits across the document set: the design (and plan and spec) pin 2748 at `e8eaf6f`, the impl-plan pins the same 2748 at `b7d0d77`. Both are correct in value — I measured 2748 from the repository root and 2486 from `h-mad/` at HEAD `d6aa0e6`, and `b7d0d77` is the child of `e8eaf6f` — so this is not a wrong number. It is a violation of the rule the same paragraph states, that the count travels with the commit it was measured at, and it gives the 5c re-measurement two provenances to reconcile.
  quote: docs/02-design/features/doc-block-exec.design.md › `re-measured at `e8eaf6f``

- The convention deliverable has no stated home, and any live example of it under the two sweep roots would fail AC-6.1. The plan lists "`hmad:exec` fence info-string tag convention" as a Deliverable, but the design's Components table gives `h-mad/SKILL.md` exactly two changes — the registry entry and the one tagged fence — so the grammar is documented only in the design itself. The design's own scanner rules supply the escape (a four-space-indented literal is never an opener), but nothing says an author documenting the convention in an `.md` under `h-mad/`/`handoff/` must use it. Worth one sentence at the AC-6.1 residual.

## Nit

- The AC-6.1–6.6 Test Plan row names `run_recipe` on the post-migration executing path (`no ... left on the **executing** path (`_gate_bash_block` and `run_recipe`)`), but Task 5 hoists it to `_run_recipe`; the Implementation Order and the wire table already use the new name.

- The same row's `` `re.findall(r"```bash` `` is a one-backtick code span containing a three-backtick run, so it renders as an unterminated-looking literal. It is a consistent house idiom (the plan and impl-plan spell it identically), so this is cosmetic only.

- Spec FR-5 lists AC-5.6 before AC-5.5; every other FR is in ascending order. Not a design defect, noted because the design's Test Plan reads the two in the spec's order.
