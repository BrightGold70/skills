{
 "_why": "Each mutation is a way a hand-rolled anchored substitution already fails. The first two are the original silent no-op the helper was filed against: an anchor assert that stops refusing writes into the wrong place or not at all. The rest strip one refusal each. The accept case is NOT tested here -- a guard that refuses everything kills every mutant -- it is pinned by TestAccepts in the suite, which these mutants must leave passing. Each mutation names the ONE test it is aimed at, so a kill by an unrelated assertion, a crash or a timeout is reported as a survivor with the actual catcher named rather than counted as ALL_CAUGHT.",
 "root": "/Users/kimhawk/orca/skills/h-mad",
 "command": [
  "python3.11",
  "-m",
  "pytest",
  "tests/test_h_mad_version_history.py",
  "-q"
 ],
 "target_command": [
  "python3.11",
  "-m",
  "pytest",
  "-q"
 ],
 "mutations": [
  {
   "name": "missing-anchor-becomes-a-silent-noop",
   "_mechanism": "The original defect: a drifted anchor writes nothing and reports success. Must be caught by test_a_missing_section_refuses, NOT merely by a crash.",
   "file": "scripts/h_mad_version_history.py",
   "find": "    if not hits:\n        raise Refusal(\"anchor_missing\")",
   "replace": "    if not hits:\n        return len(lines)",
   "test": "tests/test_h_mad_version_history.py::TestAnchorRefusals::test_a_missing_section_refuses"
  },
  {
   "name": "two-anchors-take-the-first",
   "_mechanism": "What `.replace()` does natively: silently picks match 1. Caught by test_two_headers_refuse_rather_than_taking_the_first.",
   "file": "scripts/h_mad_version_history.py",
   "find": "    if len(hits) > 1:\n        raise Refusal(\"anchor_ambiguous\", f\"matches={len(hits)}\")",
   "replace": "    if False:\n        raise Refusal(\"anchor_ambiguous\", f\"matches={len(hits)}\")",
   "test": "tests/test_h_mad_version_history.py::TestAnchorRefusals::test_two_headers_refuse_rather_than_taking_the_first"
  },
  {
   "name": "loosen-the-anchor-to-ignore-indentation",
   "_mechanism": "A stripped-line anchor finds the 7 fenced template examples in references/inline-protocols.md and refuses a doc that is not ambiguous. This is the exact mismatch that a stripped corpus measurement hid.",
   "file": "scripts/h_mad_version_history.py",
   "find": "ANCHOR = re.compile(r\"^##[ \\t]*Version History[ \\t]*$\", re.IGNORECASE)",
   "replace": "ANCHOR = re.compile(r\"^[ \\t]*##[ \\t]*Version History[ \\t]*$\", re.IGNORECASE)",
   "test": "tests/test_h_mad_version_history.py::TestAnchorRefusals::test_indented_template_headers_are_not_live_sections"
  },
  {
   "name": "reformat-tables-instead-of-refusing",
   "_mechanism": "Mangles a 140-section shape into a bullet list. Caught by test_a_table_section_refuses_rather_than_being_reformatted.",
   "file": "scripts/h_mad_version_history.py",
   "find": "    if TABLE_ROW.match(first):\n        raise Refusal(\"table_shape\")",
   "replace": "    if TABLE_ROW.match(first):\n        return \"bullet\"",
   "test": "tests/test_h_mad_version_history.py::TestShapeRefusals::test_a_table_section_refuses_rather_than_being_reformatted"
  },
  {
   "name": "guess-placement-on-an-unsorted-section",
   "_mechanism": "The 22%-wrong case. Appending blind to a 1.0/1.2/1.1 section is wrong in a way no assertion on the write itself sees.",
   "file": "scripts/h_mad_version_history.py",
   "find": "    raise Refusal(\"mixed_order\")",
   "replace": "    return \"ascending\"",
   "test": "tests/test_h_mad_version_history.py::TestOrderRefusals::test_the_real_unsorted_section_refuses"
  },
  {
   "name": "descending-sections-get-appended-at-the-end",
   "_mechanism": "Placement derived but then ignored -- the entry lands at the bottom of a newest-first log. Must be caught by test_descending_section_gets_the_entry_at_the_top, not by any refusal test.",
   "file": "scripts/h_mad_version_history.py",
   "find": "    elif order == \"descending\":\n        insert_at = indices[0]\n        placement = \"prepend\"",
   "replace": "    elif False:\n        insert_at = indices[0]\n        placement = \"prepend\"",
   "test": "tests/test_h_mad_version_history.py::TestAccepts::test_descending_section_gets_the_entry_at_the_top"
  },
  {
   "name": "allow-the-same-version-twice",
   "_mechanism": "27 bumps per session means re-runs happen; the anchor still matches on the second run, so a substitution appends a duplicate.",
   "file": "scripts/h_mad_version_history.py",
   "find": "        raise Refusal(\"duplicate_version\", f\"version={version}\")",
   "replace": "        pass",
   "test": "tests/test_h_mad_version_history.py::TestDuplicateRefusal::test_the_same_version_twice_refuses"
  },
  {
   "name": "compare-versions-as-strings",
   "_mechanism": "v1.10 < v1.9 lexically, so a correctly ascending section reads as unsorted and is refused. Caught by test_ten_sorts_after_nine_not_before -- a false-POSITIVE guard, the direction mutation testing usually misses.",
   "file": "scripts/h_mad_version_history.py",
   "find": "            out.append((int(m.group(1)), int(m.group(2))))",
   "replace": "            out.append((m.group(1), m.group(2)))",
   "test": "tests/test_h_mad_version_history.py::TestOrderRefusals::test_ten_sorts_after_nine_not_before"
  },
  {
   "name": "accept-a-multiline-entry-body",
   "_mechanism": "A newline in --text smuggles a second list item past every other check.",
   "file": "scripts/h_mad_version_history.py",
   "find": "    if \"\\n\" in entry_text:\n        raise Refusal(\"multiline_text\")",
   "replace": "    if False:\n        raise Refusal(\"multiline_text\")",
   "test": "tests/test_h_mad_version_history.py::TestArgumentRefusals::test_multiline_text_refuses"
  },
  {
   "name": "stop-at-headers-but-not-at-rules",
   "_mechanism": "20 of 713 sections end at `---`. Without the rule terminator the append lands below it, in the next section's prose.",
   "file": "scripts/h_mad_version_history.py",
   "find": "        elif not fenced and (HEADER.match(line) or RULE.match(line)):",
   "replace": "        elif not fenced and HEADER.match(line):",
   "test": "tests/test_h_mad_version_history.py::TestAccepts::test_a_rule_terminated_section_appends_above_the_rule"
  },
  {
   "name": "refusal-line-carries-a-line-number",
   "_mechanism": "Makes a cannot-write readable as a write that landed -- the cannot-judge-carries-no-count invariant.",
   "file": "scripts/h_mad_version_history.py",
   "find": "            print(f\"{TOKEN}: REFUSED path={path} reason={exc.reason}{detail}\")",
   "replace": "            print(f\"{TOKEN}: REFUSED path={path} reason={exc.reason}{detail} line=0\")",
   "test": "tests/test_h_mad_version_history.py::TestCli::test_a_refusal_carries_no_line_number"
  },
  {
   "name": "refusal-exits-zero",
   "_mechanism": "An `&&`-chained audit loop would proceed past a doc that never got bumped.",
   "file": "scripts/h_mad_version_history.py",
   "find": "            print(f\"{TOKEN}: REFUSED path={path} reason={exc.reason}{detail}\")\n        return 2",
   "replace": "            print(f\"{TOKEN}: REFUSED path={path} reason={exc.reason}{detail}\")\n        return 0",
   "test": "tests/test_h_mad_version_history.py::TestCli::test_a_refusal_exits_two_and_names_the_reason"
  },
  {
   "name": "dry-run-writes-anyway",
   "_mechanism": "A preflight that mutates the tree it is previewing.",
   "file": "scripts/h_mad_version_history.py",
   "find": "    if not dry_run:\n        path.write_bytes(new_raw)",
   "replace": "    path.write_bytes(new_raw)",
   "test": "tests/test_h_mad_version_history.py::TestAccepts::test_dry_run_verifies_without_writing"
  },
  {
   "name": "splice-self-check-never-fires",
   "_mechanism": "The insertion-only assert is the last line of defence against a slice that ate a neighbour. Expected to be caught by the untouched-document test rather than by a dedicated one.",
   "file": "scripts/h_mad_version_history.py",
   "find": "    new_lines = lines[:insert_at] + [new_line] + lines[insert_at:]",
   "replace": "    new_lines = lines[:insert_at] + [new_line] + lines[insert_at + 1:]",
   "test": "tests/test_h_mad_version_history.py::TestAccepts::test_the_rest_of_the_document_is_untouched"
  },
  {
   "name": "count-indented-sub-bullets-as-entries",
   "_mechanism": "Found by an adversarial review, then reproduced live: a `  - v1.4 ...` nested under a v1.1 entry made a correctly ascending real impl-plan read as unsorted and be REFUSED; 2 corpus docs were affected. BOTH regexes must be loosened, because either one staying strict is sufficient protection -- entry lines come from BULLET_ANY and versions from BULLET_VERSION, so a single-regex mutant is EQUIVALENT and reports as a survivor. The original defect had both loose.",
   "file": "scripts/h_mad_version_history.py",
   "find": "BULLET_VERSION = re.compile(r\"^[-*][ \\t]+\\**v(\\d+)\\.(\\d+)\")\nBULLET_ANY = re.compile(r\"^[-*][ \\t]+\")",
   "replace": "BULLET_VERSION = re.compile(r\"^[ \\t]*[-*][ \\t]+\\**v(\\d+)\\.(\\d+)\")\nBULLET_ANY = re.compile(r\"^[ \\t]*[-*][ \\t]+\")",
   "test": "tests/test_h_mad_version_history.py::TestEntryBoundaries::test_an_indented_sub_bullet_is_not_an_entry"
  },
  {
   "name": "append-between-an-entry-and-its-continuation",
   "_mechanism": "43 corpus sections wrap their last entry. Inserting after the last BULLET line divorces the continuation from its owner, and nothing about the diff looks wrong.",
   "file": "scripts/h_mad_version_history.py",
   "find": "        while insert_at < end and lines[insert_at].strip() and not BULLET_ANY.match(lines[insert_at]):\n            insert_at += 1",
   "replace": "        pass",
   "test": "tests/test_h_mad_version_history.py::TestEntryBoundaries::test_continuation_prose_keeps_its_entry"
  },
  {
   "name": "a-fenced-heading-ends-the-section",
   "_mechanism": "A ``` block inside the section can hold a `# heading`; treating it as the boundary splices the new entry into the middle of the code block.",
   "file": "scripts/h_mad_version_history.py",
   "find": "        if line.lstrip().startswith(\"```\"):\n            fenced = not fenced",
   "replace": "        if False:\n            fenced = not fenced",
   "test": "tests/test_h_mad_version_history.py::TestEntryBoundaries::test_a_heading_inside_a_fenced_block_is_not_the_section_end"
  },
  {
   "name": "normalise-line-endings-on-write",
   "_mechanism": "`read_text()`+`write_text()` rewrites EVERY line ending while every assertion still reports one inserted line, because they all run on already-normalised text. The self-check cannot see a change it is handed in normalised form.",
   "file": "scripts/h_mad_version_history.py",
   "find": "    newline = \"\\r\\n\" if b\"\\r\\n\" in raw else \"\\n\"",
   "replace": "    newline = \"\\n\"",
   "test": "tests/test_h_mad_version_history.py::TestEntryBoundaries::test_crlf_line_endings_survive_the_write"
  }
 ]
}
