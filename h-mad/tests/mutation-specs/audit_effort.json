{
 "root": "../..",
 "command": ["python3.11", "-m", "pytest", "tests/test_h_mad_audit_cycle.py", "tests/test_h_mad_review_evidence.py", "-q"],
 "mutations": [
  {"name": "thinking-counts-active-steps-too", "file": "scripts/h_mad_review_evidence.py",
   "find": "        if step.get(\"step_type\") == \"agent_response\":\n            if state == \"DONE\":",
   "replace": "        if step.get(\"step_type\") == \"agent_response\":\n            if state in (\"DONE\", \"ACTIVE\"):"},
  {"name": "null-thinking-crashes-the-scan", "file": "scripts/h_mad_review_evidence.py",
   "find": "                    if isinstance(value, (int, float)) and not isinstance(value, bool):\n                        thinking += int(value)",
   "replace": "                    thinking += int(value)"},
  {"name": "thinking-omitted-from-the-line", "file": "scripts/h_mad_review_evidence.py",
   "find": "        f\"ok={counts['ok']} failed={counts['failed']} \"\n        f\"thinking={counts['thinking']}\"",
   "replace": "        f\"ok={counts['ok']} failed={counts['failed']}\""},
  {"name": "effort-decides-the-verdict", "file": "scripts/h_mad_review_evidence.py",
   "find": "    verdict = \"PASS\" if counts[\"ok\"] >= 1 else \"NONE\"",
   "replace": "    verdict = \"PASS\" if counts[\"ok\"] >= 1 and counts[\"thinking\"] > 1000 else \"NONE\""},
  {"name": "unreadable-log-renders-as-zeros", "file": "scripts/h_mad_audit_cycle.py",
   "find": "    except OSError:\n        return {\"readable\": False, \"shape\": \"missing\"}",
   "replace": "    except OSError:\n        return {\"readable\": True, \"tools\": 0, \"ok\": 0, \"failed\": 0, \"thinking\": 0}"},
  {"name": "empty-log-renders-as-zeros", "file": "scripts/h_mad_audit_cycle.py",
   "find": "        return {\"readable\": False, \"shape\": \"empty\"}",
   "replace": "    if not text.strip():\n        return {\"readable\": True, \"tools\": 0, \"ok\": 0, \"failed\": 0, \"thinking\": 0}"},
  {"name": "delivery-floor-off-by-one", "file": "scripts/h_mad_audit_cycle.py",
   "find": "DELIVERY_FLOOR = 2", "replace": "DELIVERY_FLOOR = 1"},
  {"name": "low-evidence-marker-dropped", "file": "scripts/h_mad_audit_cycle.py",
   "find": "        if effort[\"ok\"] <= DELIVERY_FLOOR:", "replace": "        if False:"},
  {"name": "effort-leaks-into-the-contract-line", "file": "scripts/h_mad_audit_cycle.py",
   "find": "    lines = [\" \".join(fields)]\n    # Beside the verdict",
   "replace": "    fields.extend(f\"p{r.index}tools={(r.effort or {}).get('tools', 0)}\" for r in results)\n    lines = [\" \".join(fields)]\n    # Beside the verdict"},
  {"name": "four-field-caller-rejected", "file": "scripts/h_mad_audit_cycle.py",
   "find": "    if len(parts) not in (4, 5):", "replace": "    if len(parts) != 5:"},
  {"name": "log-field-not-last-so-colons-split", "file": "scripts/h_mad_audit_cycle.py",
   "find": "    parts = value.split(\":\", 4)", "replace": "    parts = value.split(\":\")"},
  {"name": "empty-effort-block-still-printed", "file": "scripts/h_mad_audit_cycle.py",
   "find": "    if effort:\n        lines.append(\"Effort:\")", "replace": "    if True:\n        lines.append(\"Effort:\")"}
 ]
}
