{
 "root": "../..",
 "command": ["python3.11", "-m", "pytest", "tests/test_h_mad_hook_wiring.py", "-q"],
 "mutations": [
  {"name": "no-settings-reads-as-unwired", "file": "scripts/h_mad_hook_wiring.py",
   "find": "    if not read_anything:\n        return [], False",
   "replace": "    pass"},
  {"name": "referenced-counts-as-wired", "file": "scripts/h_mad_hook_wiring.py",
   "find": "        uncovered = [t for t in tools\n                     if not any(_matcher_fires(m, t) for m, _ in hits)]",
   "replace": "        uncovered = []"},
  {"name": "match-all-not-special-cased", "file": "scripts/h_mad_hook_wiring.py",
   "find": "    if matcher in MATCH_ALL:\n        return True",
   "replace": "    pass"},
  {"name": "invalid-regex-passes", "file": "scripts/h_mad_hook_wiring.py",
   "find": "    except re.error:\n        # An invalid pattern is not a match; it is also not this check's job to\n        # explain regex. Reported as a wrong matcher, which is what it behaves as.\n        return False",
   "replace": "    except re.error:\n        return True"},
  {"name": "no-env-expansion", "file": "scripts/h_mad_hook_wiring.py",
   "find": "    return os.path.expanduser(os.path.expandvars(text))",
   "replace": "    return text"},
  {"name": "exact-command-match-only", "file": "scripts/h_mad_hook_wiring.py",
   "find": "        hits = [(m, c) for m, c in entries[event] if basename in c]",
   "replace": "        hits = [(m, c) for m, c in entries[event] if basename == c]"},
  {"name": "no-ancestor-walk", "file": "scripts/h_mad_hook_wiring.py",
   "find": "        for d in (project_root, *project_root.parents):",
   "replace": "        for d in (project_root,):"},
  {"name": "ignore-claude-config-dir", "file": "scripts/h_mad_hook_wiring.py",
   "find": "    config = os.environ.get(\"CLAUDE_CONFIG_DIR\", \"\").strip()",
   "replace": "    config = \"\""},
  {"name": "stale-path-unreported", "file": "scripts/h_mad_hook_wiring.py",
   "find": "            if target is not None and not Path(target).exists():",
   "replace": "            if False:"},
  {"name": "unknown-exits-0", "file": "scripts/h_mad_hook_wiring.py",
   "find": "        print(\"WIRING: UNKNOWN reason=no_settings\")\n        return 2",
   "replace": "        print(\"WIRING: UNKNOWN reason=no_settings\")\n        return 0"},
  {"name": "docs-lose-the-no-halt-rule", "file": "SKILL.md",
   "find": "and it must never halt bootstrap", "replace": "and it may halt bootstrap"},
  {"name": "docs-lose-a-detail-line", "file": "SKILL.md",
   "find": "| `HOOK_WIRED_STALE_PATH:<hook> -> <path>` |", "replace": "| `HOOK_PATH_ODD` |"}
 ]
}
