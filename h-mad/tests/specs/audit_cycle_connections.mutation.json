{
 "root": "../..",
 "command": [
  "/opt/anaconda3/bin/python3.11",
  "-m",
  "pytest",
  "tests/test_hmad_dispatch_audit_cycle.py",
  "tests/test_h_mad_audit_cycle.py",
  "-q"
 ],
 "mutations": [
  {
   "name": "verb-assemble-drop",
   "file": "scripts/hmad-dispatch.sh",
   "find": "    if python3 \"$here/h_mad_assemble_audit.py\" --feature \"$feature\" --phase \"$phase\" \\\n         --cycle \"$cycle\" --project-root \"$root\" \\\n         --report-file \"${report[$i]}\" --out \"${prompt[$i]}\" >\"${asm[$i]}\"",
   "replace": "    if : >\"${asm[$i]}\"",
   "failure_test": "test_verb_assemble_halt_no_dispatch"
  },
  {
   "name": "verb-assemble-result-guard",
   "file": "scripts/hmad-dispatch.sh",
   "find": "    [ -n \"${tok[$i]}\" ] || exit 4",
   "replace": "    :",
   "failure_test": "test_verb_assemble_no_token_is_operational_error"
  },
  {
   "name": "verb-exec-drop-p2",
   "file": "scripts/hmad-dispatch.sh",
   "find": "  i=1\n  while [ \"$i\" -le \"$passes\" ]; do\n    (\n      _cmd_exec \"${agent[$i]}\" \"${prompt[$i]}\" --cd \"$root\" \\",
   "replace": "  i=1\n  while [ \"$i\" -lt \"$passes\" ]; do\n    (\n      _cmd_exec \"${agent[$i]}\" \"${prompt[$i]}\" --cd \"$root\" \\",
   "failure_test": "test_verb_two_distinct_dispatches"
  },
  {
   "name": "verb-exec-force-2",
   "file": "scripts/hmad-dispatch.sh",
   "find": "  i=1\n  while [ \"$i\" -le \"$passes\" ]; do\n    (\n      _cmd_exec \"${agent[$i]}\" \"${prompt[$i]}\" --cd \"$root\" \\\n        --out \"${out[$i]}\" --log \"${log[$i]}\" --timeout \"$timeout\" >/dev/null\n    ) &\n    pids[$i]=$!\n    i=$((i + 1))\n  done\n\n  i=1\n  while [ \"$i\" -le \"$passes\" ]; do\n    if wait \"${pids[$i]}\"; then rc[$i]=0; else rc[$i]=$?; fi\n    # 5th field: the pass's NDJSON log, so the combiner can report reasoning\n    # effort beside the verdict (J49 — a hollow pass that read nothing is\n    # indistinguishable from a real clean pass at the AUDITCYCLE line). Optional\n    # in the combiner; reported, never gated.\n    pass_args+=(--pass \"${i}:${report[$i]}:${out[$i]}:${rc[$i]}:${log[$i]}\")",
   "replace": "  i=1\n  while [ \"$i\" -le 2 ]; do\n    (\n      _cmd_exec \"${agent[$i]}\" \"${prompt[$i]}\" --cd \"$root\" \\\n        --out \"${out[$i]}\" --log \"${log[$i]}\" --timeout \"$timeout\" >/dev/null\n    ) &\n    pids[$i]=$!\n    i=$((i + 1))\n  done\n\n  i=1\n  while [ \"$i\" -le 2 ]; do\n    if wait \"${pids[$i]}\"; then rc[$i]=0; else rc[$i]=$?; fi\n    # 5th field: the pass's NDJSON log, so the combiner can report reasoning\n    # effort beside the verdict (J49 — a hollow pass that read nothing is\n    # indistinguishable from a real clean pass at the AUDITCYCLE line). Optional\n    # in the combiner; reported, never gated.\n    pass_args+=(--pass \"${i}:${report[$i]}:${out[$i]}:${rc[$i]}:${log[$i]}\")",
   "failure_test": "test_verb_passes_one"
  },
  {
   "name": "helper-report-wait-drop",
   "file": "scripts/h_mad_audit_cycle.py",
   "find": "    if _run_report_wait(spec.report_path, grace):\n        return \"report-file\", _copy_collected_report(\n            spec.report_path, collected_path, overwrite=overwrite\n        )\n",
   "replace": "    if False and _run_report_wait(spec.report_path, grace):\n        return \"report-file\", _copy_collected_report(\n            spec.report_path, collected_path, overwrite=overwrite\n        )\n",
   "failure_test": "test_collect_delayed_report"
  },
  {
   "name": "helper-report-wait-force",
   "file": "scripts/h_mad_audit_cycle.py",
   "find": "    if _has_complete_report(spec.report_path):\n        return \"report-file\", _copy_collected_report(\n            spec.report_path, collected_path, overwrite=overwrite\n        )\n",
   "replace": "    if False and _has_complete_report(spec.report_path):\n        return \"report-file\", _copy_collected_report(\n            spec.report_path, collected_path, overwrite=overwrite\n        )\n",
   "failure_test": "test_collect_report_file_present",
   "_mechanism": null
  },
  {
   "name": "helper-extract-drop",
   "file": "scripts/h_mad_audit_cycle.py",
   "find": "        report_text = _run_extract_report(\n            spec.out_path, feature=feature, phase=phase, cycle=cycle\n        )\n",
   "replace": "        report_text = \"\"\n",
   "failure_test": "test_collect_falls_back_to_out"
  },
  {
   "name": "helper-extract-force",
   "file": "scripts/h_mad_audit_cycle.py",
   "find": "    if _run_report_wait(spec.report_path, grace):\n        return \"report-file\", _copy_collected_report(\n            spec.report_path, collected_path, overwrite=overwrite\n        )\n\n    if spec.out_path is not None:\n        report_text = _run_extract_report(\n            spec.out_path, feature=feature, phase=phase, cycle=cycle\n        )\n",
   "replace": "    if _run_report_wait(spec.report_path, grace):\n        pass\n\n    if spec.out_path is not None:\n        report_text = _run_extract_report(\n            spec.out_path, feature=feature, phase=phase, cycle=cycle\n        )\n",
   "failure_test": "test_collect_delayed_report"
  },
  {
   "name": "helper-gate-drop-p2",
   "file": "scripts/h_mad_audit_cycle.py",
   "find": "                    verdict, must, should, findings = gate(\n                        collected_path,\n                        ack_file=args.ack_file,\n                    )",
   "replace": "                    if spec.index == 2:\n                        verdict, must, should, findings = \"PASS\", 0, 0, []\n                    else:\n                        verdict, must, should, findings = gate(\n                            collected_path,\n                            ack_file=args.ack_file,\n                        )",
   "failure_test": "test_fail_in_either_pass_fails_cycle"
  },
  {
   "name": "helper-gate-force-none",
   "file": "scripts/h_mad_audit_cycle.py",
   "find": "                if delivered != \"none\":\n                    if collected_path is None:\n                        raise OperationalError(\n                            f\"missing collected path after delivered output for p{spec.index}\"",
   "replace": "                if True:\n                    if collected_path is None:\n                        raise OperationalError(\n                            f\"missing collected path after delivered output for p{spec.index}\"",
   "failure_test": "test_main_delivered_none_is_unverified"
  },
  {
   "name": "verb-helper-drop",
   "file": "scripts/hmad-dispatch.sh",
   "find": "  python3 \"$here/h_mad_audit_cycle.py\" \"${helper_args[@]}\"\n  # Propagate the helper rc either way; explicit exit prevents main() fall-through.",
   "replace": "  :\n  # Propagate the helper rc either way; explicit exit prevents main() fall-through.",
   "failure_test": "test_completed_cycle_emits_token"
  },
  {
   "name": "verb-helper-force",
   "file": "scripts/hmad-dispatch.sh",
   "find": "  if [ -n \"$halt_pass\" ]; then\n    python3 \"$here/h_mad_audit_cycle.py\" \\\n      --feature \"$feature\" --phase \"$phase\" --cycle \"$cycle\" \\\n      --project-root \"$root\" --passes \"$passes\" \\\n      --size-status \"$size_status\" \\\n      --halt-reason \"assemble_halt:p${halt_pass}\"\n    exit $?\n  fi\n",
   "replace": "",
   "failure_test": "test_verb_assemble_halt_no_dispatch"
  }
 ]
}
